/**
 * 단일 정예 관문의 **표현** 규칙.
 *
 * 정예는 원정 지도의 정예 노드와 같은 문법이다 — 여럿 대신 하나가 서고 그 하나가 더 무겁다.
 * 다만 **무거워지는 몫은 이 표에 없다.** 세기는 언제나 `StageEnemyDef.ferocityLevel` 하나가
 * 말하고 화면은 그 값을 `LV.10` 옆의 작고 붉은 `+20`으로 이미 그린다. 여기에 능력치 배율을
 * 두면 관문을 조일 때 움직일 수가 둘이 되고, 화면에 선 수치와 실제로 맞는 수치가 갈린다
 * (`CLAUDE.md`의 "스테이지 전용 배율이나 숨은 보정을 만들지 않는다").
 *
 * 그래서 이 표가 여는 것은 **눈에 보이는 것뿐**이다 — 몸집과 표식.
 */
export const STAGE_ELITE = {
  /**
   * 혼자 서는 만큼 몸이 크다.
   *
   * 전투 계산에는 들어가지 않는다(`Fighter.bodyScale`은 그리는 크기와 이펙트 배율만 정한다).
   * 원정 정예 노드의 1.1보다 조금 더 큰 이유는 스토리 전장에는 비교할 다른 적이 하나도 서
   * 있지 않아, 같은 배율로는 "혼자라서 커 보이는 것"과 구별되지 않기 때문이다.
   */
  bodyScale: 1.18,
} as const;
