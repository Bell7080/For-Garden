# 버전 관리

현재 버전: **v0.42.4**

`VERSION.md`와 `package.json`의 `version`은 항상 같은 값을 쓰고, 타이틀(로딩) 화면 좌측
하단 표기는 그 값을 그대로 읽는다. 화면에 손으로 적어 두지 않는다.

## 번호 규칙

`주.부.패치` 형식이며, 변경에 필요한 **가장 낮은 자리만** 올린다.

- **패치(0.0.x)** — 버그 수정, 수치 조정, 연출 손질, 리팩터링, 문서, 에셋 교체
- **부(0.x.0)** — 새 시스템·화면·콘텐츠 묶음. 예: 룬 세공, 새 재화, 새 서버 경계
- **주(x.0.0)** — **정식 런칭 시에만 올린다.** `v1.0.0`은 출시 단계를 뜻하는 번호라
  개발 중에 임의로 올리지 않는다.

여러 성격이 섞이면 가장 큰 변경을 기준으로 고른다.

출시 전까지 주 자리는 계속 `0`이며, 부 자리는 `0.11` → `0.12` … 처럼 얼마든지 늘어난다.
저장 데이터(`SaveData`)나 서버 DTO의 **비호환 변경도 부 자리**를 올리고, 그 항목에
마이그레이션 방법과 예전 저장을 어떻게 처리하는지를 반드시 함께 적는다 — 출시 전에는
저장이 깨지는 일이 잦으므로 그때마다 주 자리를 올리면 `v1.0.0`이 런칭 전에 소진된다.

## 기록 규칙

- 이력은 **이 문서에만** 쌓는다. `CLAUDE.md`에는 지금 지켜야 하는 규칙만 남긴다.
- 항목은 **무엇을 왜** 바꿨는지 쓴다. 무엇만 적으면 6개월 뒤 되돌릴 수 없다.
- **되돌린 변경은 되돌린 이유를 반드시 남긴다.** 같은 시도를 두 번 하지 않기 위한 기록이다.
- 푸시할 때마다 그 버전 항목을 남기고, `package.json`과 이 문서가 같은 값인지 확인한다.
- 아래 이력은 현재 저장소 히스토리(2026-08-22 이후)를 기준으로 정리한 것이라,
  그 이전의 초기 프로토타입 단계는 `v0.1.0` 항목 하나로 묶었다.

## 변경 이력

## v0.42.4 — 2026-09-01

**세 편성 화면의 드래그 표현을 하나로 합쳤다.**

- 파티에만 있던 감광·확대 추적·교체 고스트·깎인 드롭 칸·취소 복구를 공용 Phaser 표현
  컨트롤러로 옮겼다. 미리보기와 확정이 갈리지 않도록 배열 결과는 계속 `moveFormationSlot`만 쓴다.
- 원정과 발굴은 Puppet이 컨테이너 변환을 물려받지 않는 제약을 유지하며, 각 화면의 기존 화면 좌표
  배치 콜백을 공용 표현기에 주입한다.
- 모바일 회귀 검증에 원정·발굴의 드래그 중 대상 칸과 반투명 교체 미리보기 상태를 추가했다.

## v0.42.3 — 2026-09-01

- **불사 폰토스의 HP 센티널을 제거했다.** 전투 생존은 기존 `SkirmishState.boss`와
  `Fighter.immortal` 계약만 사용하고, 지도·전투·상세창은 같은 유한한 고정 스탯을 유지한다.
- **폰토스 정보 표기를 1등급 LV.20으로 통일했다.** 표기 레벨로 스탯을 다시 계산하지 않아 기존의
  높은 보스 수치는 보존하며, 전투 중 상세창도 런타임 HP 대신 지도 표시 스냅샷을 사용한다.

## v0.42.2 — 2026-09-01

- **관찰 일지 스쿼드 엠블럼을 기존 104px의 정확한 130%인 135.2px로 키웠다.** 확대율을 별도
  상수로 남기고, 복제 그림자의 성장·아래쪽 오프셋까지 포함한 실제 외곽으로 x·y·이름 위치와
  메타데이터 폭을 다시 계산했다. 덕분에 커진 표식이 팝업 안쪽 여백이나 제목 영역을 침범하지 않는다.
- **엠블럼 원화의 가로세로 비율을 보존한다.** 긴 변만 지정 크기에 맞추므로 가로형·세로형 표식도
  정사각형으로 찌그러지지 않으며, 대표 비율 1:2·1:1·2:1에서 그림자와 이름/본문 안전 영역을
  순수 레이아웃 테스트로 고정했다.

## v0.42.1 — 2026-08-31

- **도디의 동경 1순위를 주인공으로 못 박았다.** 시그널 아이를 올려다보는 것도 결국 "대장님 옆에서
  더 쓸모 있어지려고"라서, 두 마음의 순서가 흐려지면 안 된다. `squadNote`에 **연구원의 1호 팬을
  자처한다**는 문장을 넣고(일지 첫 장부터 그 이름이 적혀 있다), 일지가 그리는 스쿼드 줄의 라벨도
  `동경` → `스쿼드 동경`으로 바꿨다 — 그냥 "동경"이라고만 적으면 본문이 말한 순서와 어긋난
  두 문장이 나란히 선다.
- **타이틀 로고를 30% 키웠다**(680 → 884px).
- **로고 자리를 그림 상자가 아니라 보이는 글자 기준으로 다시 잡았다.** 로고타입 파일은 사방에
  넓은 투명 여백이 있어(글자가 세로로 31.4%~69.6%만 차지한다), 상자 기준으로 두면 30% 확대와
  함께 부제가 190px 아래로 밀려 **배경 원화의 얼굴을 덮었다.** `titlename.webp`의 alpha 경계를
  실측해 `TITLE_LOGOTYPE_CONTENT` 표로 두고, 부제 자리와 섬광 폭을 모두 거기서 계산한다.
  덕분에 로고는 커지고 부제는 글자 바로 아래에 붙으면서도 얼굴을 가리지 않는다.
- **관찰 일지의 스쿼드 이름을 두껍게 세웠다.** 설명이 아니라 이름표라 `emphasis`(20px)에서
  `display`(24px)로 올렸다. 뒤에 판이나 복제 그림자는 두지 않는다 — 엠블럼이 이미 제 어둠을
  데리고 있어 겹치면 표식이 두 겹으로 보인다. 대신 엠블럼과 같은 강조색·같은 중심선에 맞춰
  한 덩어리로 읽히게 했다.

## v0.42.0 — 2026-08-31

소속 스쿼드를 배정표 속 한 칸에서 **화면에 보이는 설정**으로 끌어냈다. 관찰 일지에 엠블럼이
찍히고, 그 무리에서 무엇을 하고 누구를 어떻게 부르는지가 함께 읽힌다.

- **자치 스쿼드를 코드가 아는 값으로 만들었다.** `docs/factions.md`의 배정표만 있고 코드에는
  아무것도 없어서, 소속은 문서를 열어야만 알 수 있는 정보였다. `SquadId`는 코어 모델 타입으로,
  이름·라틴 표기·역할·**연구원 호칭**은 `src/data/factions.ts` 한 표로 두고, 12종 전원의
  `RelicDef.squad`를 배정표 그대로 채웠다. 코어가 데이터 표를 알 필요는 없으므로 id만 코어에 둔다.
- **관찰 일지 우상단에 소속 엠블럼을 찍었다.** 판 **안쪽**에 앉힌다 — 밖으로 내밀면 홀로그램
  판의 실루엣이 깨져 붙인 스티커처럼 보인다. 일지 원화 클립은 좌상단·우하단만 깎으므로 우상단은
  직각이라 표식이 모서리에 바짝 붙어도 사선에 잘리지 않는다. 엠블럼만으로는 이름을 말하지
  못하므로 바로 아래에 스쿼드 이름을 짧게 붙였다.
- **엠블럼 양식을 `src/ui/FactionMark.ts` 하나로 통일했다.** 스쿼드가 늘어도 같은 모양으로
  따라오게 하려는 것이다. 판때기를 받치지 않고 **같은 그림을 검게 세 겹 복제해** 크기를 조금씩
  키우며 겹쳐 어둠이 번지게 만든다 — 도형으로 그림자를 그리면 엠블럼과 무관한 네모가 뒤에
  남고, 캔버스 흐리기(`shadowBlur`)는 팝업을 열 때마다 CPU가 다시 계산해 프레임이 튄다.
  엠블럼 그림이 아직 없는 스쿼드는 **아무것도 세우지 않는다** — 빈 네모나 물음표를 두면 그것이
  무슨 표식인지 되묻게 만든다.
- **도디에게 쁘띠 로그의 성격을 입혔다.** 막내 기록병이라는 자리, 일지 절반이 주운 반짝이
  목록이라는 사고뭉치 결, 주인공을 `대장님`으로 부르는 호칭까지 `squadNote`·`researcherTitle`에
  남겼다. **시그널 아이를 동경한다**는 설정도 `admiredSquad`로 넣어, 관제탑에서 다 내려다보는
  "다 아는 어른들"을 우러러보는 관계가 일지에 한 줄로 뜬다. 배정표에도 같은 근거를 적었다.
- **소속 이야기가 있는 개체는 일지 판이 150px 더 길어진다.** 같은 높이에 밀어 넣으면 기록
  본문이 인터뷰 줄과 겹쳐 두 문단이 한 덩어리로 읽힌다. 문단 자리는 눈대중이 아니라 실제로
  그려진 기록 본문의 높이에서 잰다 — 그러려고 `KeywordManager.layout`이 채운 줄 수를 컨테이너
  크기로 남기게 했다(컨테이너는 자식이 늘어도 크기를 스스로 알지 못한다).
- 규칙은 `tests/unit/factions.test.ts`가 고정한다(스쿼드 표의 이름·호칭 중복 없음, 엠블럼 목록과
  키 규칙, 전원 배정, 동경 스쿼드가 자기 소속과 다른 실재 스쿼드인지, 개체가 쓰는 호칭이 소속
  스쿼드가 실제로 쓰는 말인지).

## v0.41.0 — 2026-08-31

타이틀 진입과 편성 조작을 손봤다. 판때기로 가리던 것을 **복제 그림자**로 바꾸고, 편성에서
자리를 끌어 옮기는 동안 무슨 일이 일어나는지를 그림으로 보여 준다.

- **타이틀의 제목·부제 뒤 판때기를 걷어 내고 복제 그림자를 넣었다.** 검은 판 두 장이 화면
  위쪽을 통째로 어두운 상자로 만들어 배경 원화가 잘려 보였다. 같은 그림·같은 글자를 검게 한 겹
  복제해 깔고, 글자 복제본에는 획 둘레로 번지는 검은 띠(글자 크기의 0.26배)를 둘렀다. 띠가 실제
  배경이 되므로 대비가 배경 원화와 무관해진다 — 부제 자리(노란 후드)는 밝은 곳 휘도가 0.758까지
  올라 강조색이 **1.45:1**, 설명 줄이 1.85:1까지 떨어지지만 검은 띠 위에서는 각각 **10.8:1**·
  7.9:1로 AA(4.5:1)를 넉넉히 넘는다(`background_011.webp` 부제 띠 실측 평균 0.132·최대 0.758).
- **제목과 부제를 바짝 붙였다.** 판이 없어진 만큼 사이가 벌어지면 한 덩어리가 아니라 서로 다른
  두 정보처럼 보인다. 로고 아래 40px·90px이던 두 줄을 6px·54px로 당겼다.
- **최소 리소스가 도착하기 전에는 검은 화면만 둔다.** 예전에는 배경이 없는 동안 회색 판 위로
  부제·버전·마름모가 먼저 떠 화면이 조립되는 과정이 그대로 보였다. 이제 배경과 로고가 준비된
  순간 한꺼번에 들어온다. 파일 하나가 없어도 검은 화면에 갇히지 않도록 `loaderror`도 같은
  진입을 부르고, 진입은 정확히 한 번만 통과한다.
- **제목이 가운데에서 좌우로 열린다.** 로고를 가로로 늘였다 줄이면 그림이 찌그러지므로 잘라내는
  창을 넓혀 연다 — 비율은 처음부터 끝까지 그대로다. 벌어지는 틈을 따라 흰 섬광이 함께 퍼졌다
  사라지고, 다 열린 뒤에는 마스크를 폐기해 이후 프레임 비용으로 남기지 않는다.
- **오프닝 회상 버튼을 좌상단에 작게 올렸다.** 이 화면의 주된 조작이 아니라 이미 본 사람을 위한
  곁길이라 가운데를 비웠다(360×96 → 220×64).
- **화면을 누른 자리를 푸른빛으로 바꿨다**(`EFFECT_TAP_COLOR`). 금색은 재화·보상·강조를 뜻하므로
  아무 데나 눌러도 뜨는 반응에 같은 색을 쓰면 "눌렀다"와 "받았다"가 섞여 읽힌다.
- **편성 자리를 끌어 옮길 때 무슨 일이 일어나는지 보여 준다.** 그동안 드래그 판정은 있었지만
  화면에는 아무 표시도 없어서 놓기 전까지 결과를 알 수 없었다. 이제 판이 어두워지고 **든
  캐릭터 자체가 1.25배로 커져 손끝에 떠오르며**, 자리를 내주는 캐릭터가 바뀔 자리로 반투명하게
  미리 옮겨 선다. 미리보기는 확정 규칙(`moveFormationSlot`)을 그대로 통과시켜 보여 준 것과
  놓은 결과가 갈리지 않는다. 이름·자리 글자는 확정된 편성을 말하므로 끄는 동안 눌러 둔다.
- **놓을 자리를 네모칸으로 알린다.** 섬광 한 번은 어디까지가 그 자리인지 말해 주지 못한다.
  네 모서리를 어긋나게 깎은 칸을 끄는 동안 계속 띄우고, 지금 가리키는 칸만 진해지고 1.06배
  커진다. 배경이 비칠 만큼만(평소 0.14, 가리킨 칸 0.3) 채워 판이 세 조각으로 갈라져 보이지
  않게 했다.
- 규칙은 `tests/unit/formationDragVisual.test.ts`가 고정한다(미리보기와 확정 규칙의 일치, 빈
  자리 교환, 가리킨 칸과 나머지 칸의 차이, 진하기 상한).

## v0.40.1 — 2026-08-31

- **편성 제스처를 입력 의도만 전달하는 공용 Phaser 컨트롤러로 좁혔다.** 탭·드래그 시작·이동·드롭·취소만 콜백으로 전달하고, 스토리·원정·발굴의 배열과 저장 경계는 기존 소유자가 유지한다.
- **장기 누름·이동 거리·경계 드롭 판정을 순수 함수와 단일 상수로 통합했다.** 슬롯 밖 종료, 씬 종료, 팝업 소유자 종료, 두 번째 포인터를 명시적으로 취소하고 숫자키 슬롯 선택 뒤 방향키 이동도 같은 드롭 경로를 쓴다.
- **탭·긴 탭·드래그·포함 경계·취소 사유를 단위 테스트로 고정했다.** Phaser 렌더링 없이 임계값 회귀를 검출한다.
- **타이틀 로고 판과 부제 판을 분리했다.** 로고 판은 로고 그림만 딱 감싸는 자체 레이어로 alpha를
  따로 계산했다(흰 로고가 그 자리 배경 위에서 AA 4.5:1을 넘기는 최소값 0.0933 → 0.1). 부제·설명
  두 줄은 별도의 얕은(낮은) 레이어로 두고 기존 계산값(0.46)을 그대로 쓴다.
- **로딩 마름모를 화면 완전 최하단으로 내렸다**(0.7 → 0.94). TAP TO ENTER 문구보다도 아래로
  내려가 로딩 표시가 화면에서 가장 낮은 자리에 선다.

## v0.39.4 — 2026-08-31

- **타이틀 유리판 alpha를 눈대중이 아니라 실측으로 정했다.** 부제·설명 줄 자리의 배경 원화
  픽셀 평균을 재서 COLOR.void와 섞었을 때 가장 옅은 글자색(inkDim)이 WCAG AA 일반 텍스트
  기준(4.5:1)을 넘기는 최소 alpha가 0.4508임을 계산했고, 0.46으로 살짝 여유를 둬 적용했다.

## v0.39.3 — 2026-08-31

- **타이틀 로고를 훨씬 키우고 뒤에 옅은 유리판 + 검은 그림자를 받쳤다.** 로고 그림 하나만
  배경 원화 위에 얹으면 밝은 부분에 묻혀서, `drawLayer`로 기울어진 유리판(alpha 0.4)을
  로고·부제 뒤에 깔아 대비를 만들었다(그림자는 `HOLO.shadow` 기본값). 로딩 마름모와
  "TAP TO ENTER"·오프닝 회상 버튼은 캐릭터 얼굴에서 더 떨어지도록 화면 하단으로 밀었고,
  탭 문구는 배경과 겹칠 수 있어 검은 그림자를 얹었다.

## v0.39.2 — 2026-08-31

- **타이틀(로딩) 화면이 텍스트 제목 대신 로고 그림과 캐릭터 원화 배경을 쓴다.** "For - Garden"
  텍스트를 `sprites/ui/titlename.webp` 로고 이미지로 바꾸고, 단색 배경 대신
  `sprites/background/background_011.webp` 캐릭터 원화를 `drawVignette`로 눌러 깐다. 이 화면
  자체가 로딩 화면이라 두 자산은 `scenes/loadingSteps.ts`의 공용 로딩 단계를 기다리지 않고
  `TitleScene`이 진입 직후 곧바로 읽는다. 로딩 마름모 칸도 얼굴과 겹치지 않도록 조금 더
  아래로 내렸다.
- **5대 자치 스쿼드 엠블럼 원화를 구워 `sprites/factions/{fang,gear,eye,rune,rogue}.webp`로
  정리했다.** `docs/factions.md`의 1~5번 순서(앱솔루트 팽·나이트 기어·시그널 아이·사일런트
  룬·쁘띠 로그)에 맞춰 매칭했다. 원본은 저장소에 남기지 않고
  `scripts/prepare_title.py`가 굽는다.

## v0.40.0 — 2026-08-31

- **스토리·원정·발굴의 상단 SD 슬롯에 공용 장기 누름 드래그 편성을 추가했다.** 원본 Puppet 대신
  확대 번호 고스트와 얇은 강조면만 움직이며, 드롭 뒤 각 화면의 기존 비동기 SD 렌더 경로로
  재구성해 컨테이너 좌표 상속 제약을 피한다.
- **슬롯 이동 규칙을 Phaser 없는 순수 함수로 분리했다.** 채운 칸 교환과 빈 칸 이동을 같은 교환
  규칙으로 처리하고, 동일·범위 밖 입력에서도 원본을 보존한 새 배열을 반환한다.
- **발굴 순서 이동은 편집 사본에서만 허용한다.** 완료 전에는 서버 확정 편성과 Session을 바꾸지
  않아 취소·닫기가 기존 생산 편성을 안전하게 유지한다.

## v0.39.1 — 2026-08-31

- **스토리·원정·발굴 편성 슬롯을 직접 탭해 해당 자리를 해제할 수 있게 했다.** SD 로딩과 분리된
  투명 입력면이 기존 확대 피드백과 드래그 판정을 소유하며, 자리 인덱스로 제거해 보이는 칸과
  결과가 어긋나지 않는다.

## v0.39.0 — 2026-08-31

- **스토리와 원정 준비 화면이 마지막으로 확정한 콘텐츠별 편성을 복원한다.** 원정 저장 v27은
  미보유·중복 렐릭을 마이그레이션에서 제거하며, 기존 스토리 파티와 독립 발굴 배치를 서로
  복사하지 않아 콘텐츠를 오갈 때 사용자의 선택이 덮어써지지 않는다.

## v0.38.0 — 2026-08-31

- **전투 프로필 위에 활성 버프 액자 행을 추가했다.** 코어 셀렉터가 확정한 효과만 프리팹에
  전달하고, 프리팹이 안정 키 기반 재사용·입력 차단·스킬 아트 폴백까지 소유해 HUD 갱신 때
  기존 아이콘이 흔들리거나 버프 탭이 궁극기 입력으로 이어지지 않게 했다.
- **1080×1920 프로필 배치표에 버프 슬롯 규격과 실제 외곽을 포함했다.** 한 개와 최대 네 개의
  액자가 화면 밖으로 나가지 않고 카드·체력·야성 게이지와 겹치지 않는 계약을 정적 테스트로 고정했다.

## v0.37.0 — 2026-08-31

- **전투 UI 움직임 강도를 `기본`·`감소`·`끔`으로 선택하는 저장 설정을 추가했다.** 머리 위 체력 바,
  하단 프로필 체력 게이지와 EffectManager 및 궁극기의 카메라 흔들림이 같은 강도 배율을 소비한다.
  `끔`에서도 체력 색, 붉은 피해 잔상과 피해 숫자는 유지해 접근성 선택이 전투 정보를 지우지 않는다.
- **저장 규격을 v26으로 올렸다.** v25 이하와 버전 없는 저장에는 기존 연출과 같은 `default`를
  부여하며, 손상되거나 알 수 없는 선택값도 같은 값으로 정규화한다.

## v0.36.2 — 2026-08-31

전투 화면이 이펙트와 수치에 가려 정작 캐릭터가 안 보인다는 지적을 반영해 **표시를 전부 뒤로
물렸다.** 판단 기준 하나를 세웠다 — **예쁜 캐릭터를 가리는 순간 이펙트와 수치는 타격감이
아니라 방해다.**

- **피해 수치를 절반 가까이 줄이고 반투명하게 깔았다.** 등급별 크기를 34~92px에서 22~52px로
  낮추고, 캐릭터 위에 머무는 동안은 진하기 0.3으로 거의 비쳐 보이게 뒀다. 위로 떠오르며
  또렷해졌다가(`risingAlpha`) 끝에서 사라진다. 떠오르는 거리도 SD 키(210px)를 넘기도록
  170~280px로 늘려, 얼굴 위에 머물며 가리는 구간 자체를 없앴다. 진하기는 tween이 아니라 상승
  tween의 `onUpdate`에서 계산한다 — 프레임당 곱셈 몇 번이라 tween을 하나 더 거는 것보다 싸다.
- **속성 상성 화살표(▲·▼)를 걷어 냈다.** 유리하면 숫자 자체가 커지므로 같은 말을 두 번 하면서
  글자 폭만 넓혔다. 코어 `attack` 사건의 `effectiveness` 필드와 `elementEffectiveness`도 함께
  지웠다 — 쓰는 곳이 없는 필드는 남겨 두면 썩는다.
- **"주는 피해"와 "받는 피해"를 색으로 갈랐다.** 아군이 받은 피해는 물리·마법·고정·치명타를
  가리지 않고 **붉은 계열 하나로** 묶이고, 세기에 따라 옅은 살구빛에서 진한 다홍으로 짙어진다
  (`INCOMING_DAMAGE_TONE`). 여섯이 뒤엉킨 난전에서 "지금 누가 크게 맞았나"를 숫자를 읽지 않고도
  알 수 있다. 적에게 입힌 피해만 종류별 색을 갖는다 — 보통 피해(물리·마법)는 흰색, 고정 피해는
  보랏빛, 치명타는 노란색이다. 회복(연두)·보호막(푸른빛)·디버프는 받는 쪽에서도 제 색을
  지킨다: 그 셋은 "맞았다"가 아니라 다른 사건이기 때문이다.
- **경감은 종류를 가리지 않고 회색 하나로 통일했다.** "덜 들어갔다"는 무엇으로 맞았는지보다
  먼저 읽혀야 하는 정보라 색이 갈리면 오히려 흐려진다. 색 우선순위를 **경감 → 아군 피격 →
  치명타 → 궁극기 → 종류**로 고정했다.
- **디버프 색을 `DEBUFF_TONE` 표로 분리했다.** 출혈은 다크체리, 중독은 어두운 보랏빛이고 전부
  어둡게 눌러 둔다 — 지속 피해는 잔타로 자주 뜨므로 밝게 두면 큰 한 방과 같은 무게로 읽힌다.
  **새 디버프는 그 표와 `DebuffId`에만 더하면 되도록** 화면에서 색을 고르지 않게 했다(중독은
  전투 규칙보다 색표가 먼저 준비된 상태다).
- **광역 공격의 범위를 바닥에 그린다.** 코어에 `areaImpact` 사건을 더해 실제로 터진 자리와
  반경을 싣고(지정 원형·주위 광역·폭주 광역), 씬은 `groundArea`로 **눌린 마름모**를 SD보다
  뒤에 깐다. 정원을 그리면 바닥에 누운 것이 아니라 캐릭터 앞에 세워 둔 고리처럼 보인다.
  숫자만 셋이 한꺼번에 뜨면 왜 함께 맞았는지 읽히지 않는다.
- **폭주를 발광에서 필터로 바꿨다.** 몸 안팎에 두르던 흰 발광 두 겹(`FEVER_GLOW_TEXTURE`)을
  통째로 걷어 냈다 — 밝은 배경 원화 위에서 하얗게 떠 정작 봐야 할 캐릭터가 그 속에 묻혔다.
  대신 일러스트 자체에 그 개체의 스킬 아이콘 색을 입히고, 섞는 비율이 0.28~0.62를 오가며
  울그락불그락 끓는다. 매 프레임 칠하면 Puppet 조각 전부에 tint를 다시 먹여 그만큼이 프레임
  비용이 되므로 **10단계로 끊어** 단계가 바뀐 프레임에만 칠하고, 피격 섬광이 도는 동안에는
  아예 칠하지 않는다(`isHitFlashing`을 새로 노출했다).
- **섬광 진하기 상한을 0.6으로 낮추고 잦은 것일수록 더 옅게 했다**(일반 공격 0.5→0.3,
  궁극기 0.72→0.55). 실제 전장 캡처에서 일반 공격 섬광이 SD 위에 하얀 덩어리로 뭉쳐 보였다.
- 규칙은 `tests/unit/damageNumbers.test.ts`(20개)와 `tests/unit/effectPresets.test.ts`가
  고정한다 — 받는 피해의 붉은 계열 통일, 회복·보호막·디버프의 색 유지, 경감 회색 통일,
  화살표 부재, 진하기 곡선, 크기 상한, 바닥 범위의 눌림·진하기까지 포함한다.

## v0.36.1 — 2026-08-31

- **전투 입력을 명시적 판별 DTO로 통일하고 Phaser 비의존 정규화 경계를 추가했다.** 원정 뒤 스토리
  출격에서 직전 `runId`·노드·편성·증강이 재사용될 수 있던 경로를 새 `{ mode: "stage" }` 객체로
  차단했다. 전투 상단 문구도 모드별로 나눠 원정 중 선택된 스토리 이름이 보이지 않게 했다.
- **일반 원정·보스에서 스토리로, 스토리에서 원정으로 전환하는 순서 회귀 테스트를 추가했다.**
  스토리 입력에 원정 전용 필드가 남지 않는지와 모드별 헤더 문구까지 순수 단위 테스트로 고정했다.


## v0.36.0 — 2026-08-31

- **이펙트 매니저(`src/managers/EffectManager.ts`)를 세워 화면에 터지는 모든 것의 소유자를 하나로 모았다.**
  폭주·패시브·일반 공격·궁극기와 회복·보호막·사망·화면 조작이 전부 이 경계를 지난다. 씬은
  "무슨 일이 일어났는지"만 넘기고 파편 수·색·크기를 직접 고르지 않는다 — 씬마다 값을 정하면
  같은 기술이 화면마다 다른 무게로 터진다. `BattleScene`의 사망 별(`add.star`)과 손으로 만든
  피해 글자도 이 경계로 옮겼다.
- **동그란 파티클을 마름모 파편으로 바꾸고 중력을 걷어 냈다.** 참고한 데모는 원형 파티클이
  중력 0.15로 발밑에 쏟아지는 불꽃놀이였는데, 위에서 내려다보는 난전에서는 "터졌다"가 아니라
  "흘렸다"로 보인다. 파편은 좌우 꼭짓점 높이를 어긋나게 깎은 **마름모**이고 대부분의 종류가
  **위로** 뜬다(`gravity` 음수). 파문도 원이 아니라 납작한 마름모다.
- **렉이 걸리지 않는 방식으로 다시 짰다.** 데모의 `shadowBlur` 빛 번짐은 파티클 하나마다
  CPU가 그림자를 다시 계산해 난전에서 곧바로 프레임이 떨어진다. 대신 ① 흰 그림 두 장
  (`fx-shard`·`fx-glow`)을 한 번만 굽고 `tint`로 색만 갈아 끼우며(`src/ui/effectTextures.ts`),
  ② 종류마다 emitter 하나를 재사용하고 파문·수치 글자는 풀에서 꺼내 쓰고, ③ 한 프레임에 여는
  수(3)와 살아 있는 파문(14)·글자(26) 상한을 두어 넘치면 조용히 버린다. 폭주 발광도 같은
  `fx-glow` 한 장을 쓰도록 합쳐, 같은 발광이 두 곳에서 따로 구워지지 않게 했다.
- **인게임과 메뉴의 조작 이펙트를 다른 결로 나눴다.** 메뉴는 근미래 홀로그램 장비를 누른
  손맛(얇은 마름모 파문 한 겹), 전장은 단순한 SD가 뚜따시하는 화면에 맞춘 작은 조각 셋이다.
  씬마다 붙이면 화면이 늘 때마다 빠뜨리므로 모든 씬 위에 늘 떠 있는
  `EffectOverlayScene` 한 겹이 포인터를 듣는다(부트가 `launch`로 한 번 띄운다).
- **피해 수치를 세기에 따라 커지게 했다**(`src/ui/damageNumbers.ts`). 등급은 절대 수치가 아니라
  **대상 최대 체력 대비 비율**로 매긴다 — 절대값으로 가르면 레벨이 오를수록 모든 타격이 최대
  등급에 붙어 크기가 뜻을 잃는다. 잔타 34px에서 한 방 92px까지 다섯 등급이고, 등급이 오를수록
  외곽선·튀는 배율·머무는 시간이 함께 커진다(예전에는 30/40px 두 가지에 750ms 고정이라 너무
  빨리 사라졌다). 최고 등급의 큰 한 방만 화면을 흔들고, 조작이 어긋나지 않도록 아군이 맞을
  때는 흔들지 않는다.
- **피해 종류를 색으로 갈랐다.** 물리(흰빛)·마법(하늘빛)·고정(보랏빛)·출혈(다크체리)·회복(연두)·
  보호막(에너지 푸른빛)·무효(흐린 잉크)이며, 궁극기는 종류색보다 강조색이 앞선다. 치명타·
  궁극기는 한 등급 크고 숫자 뒤로 파편이 튀며, 경감된 타격은 한 등급 작다. 속성 상성은 문장이
  아니라 방향 표식(▲·▼) 하나로만 붙인다.
- **씬이 def를 다시 읽어 색을 되짚지 않도록 코어가 표시 정보를 함께 싣는다.** `SkirmishEvent`의
  `attack`에 `damageType`·`effectiveness`·`mitigated`를 더했다. 피해 전이는 확정 값을 그대로
  옮기므로 `"true"`(고정 피해)다. 상성 방향은 `src/core/element.ts`의 `elementEffectiveness`
  한 곳에서만 나온다.
- 규칙은 `tests/unit/damageNumbers.test.ts`·`tests/unit/effectPresets.test.ts`가 고정한다
  (등급 단조 증가, 종류별 색 분리, 섬광 진하기 상한, 프레임 예산, 파편 수 상한, 중력 상한).

## v0.35.1 — 2026-08-30

- **렐릭 카드에서 렉시아만 얼굴이 작아 보이던 문제를 고쳤다.** 카드 배율은 `content` **폭**으로
  정해지는데, 렉시아는 낫 무기가 좌우로 크게 뻗어 그 폭이 캔버스를 거의 다 차지한다
  (1023 / 1054). 몸은 그만큼 넓지 않으므로 혼자 축소됐다 — 두 눈 사이 거리로 재면 전신 원화
  여섯 종 중앙값의 **72%**로, 다음으로 작은 루카(94%)와도 큰 차이였다. 게다가 배율을 정한 그
  낫은 **정작 잘라내기에서 버려진다**: 카드 크롭은 머리 관절 기준 327~899이고 낫은 15~327에
  있어 화면에 나오지도 않는다. 화면 밖으로 나가는 만큼만 되돌리는 `cardZoom: 1.38`을 붙여
  100%로 맞췄다(값은 눈대중이 아니라 중앙값 49.0 ÷ 측정값 35.4로 계산했다).
  `content` 자체는 ZIP 안 WebP를 alpha > 16으로 다시 측정해 기존 값이 정확함을 확인했다 —
  경계가 틀린 게 아니라 폭 기준 배율이 무기까지 세고 있었던 것이다.
- **"작아 보인다"를 검사 가능한 계약으로 바꿨다.** `tests/unit/puppetAnchors.test.ts`에
  "실제 원화의 카드 얼굴 크기"를 더해, 모든 전신 원화의 얼굴이 중앙값의 0.8~1.35배 안에
  들어오는지 검사한다. 크기 대리 지표로 **두 눈 관절 사이 거리**를 쓴다 — 실루엣 폭은 무기·
  소매·들어 올린 손에 휘둘리지만 눈 간격은 얼굴 크기만 따라간다. 렉시아의 `cardZoom`을 떼면
  실패하는 테스트도 함께 두어, 보정이 사라지면 바로 드러나게 했다.
- **남겨 둔 것:** 도디(129%)·메테(120%)는 한계 위쪽에 붙어 있다. 둘은 원화 자체가 머리가 큰
  디자인이라 측정 오차가 아니라 의도로 보고 이번에는 손대지 않았다. 아트 방향이 더 촘촘한
  정렬을 원하면 그 둘의 `cardZoom`을 낮추면서 테스트의 상한도 함께 좁힌다.

## v0.35.0 — 2026-08-30

- **플레이어 정보창에 애착 렐릭·결투장 티어·최대 클리어·원정 역대 최고 기록을 같은 위계의 2×2 기록 영역으로 추가했다.** 애착은 공개 렐릭/초상 ID를 함께 전달해 공용 `PortraitCard`를 사용하고, 최대 스테이지는 저장 Set 순서가 아닌 정적 스테이지 순서를 따르는 순수 규칙으로 결정한다. 원정은 주간 기록과 혼동하지 않도록 `allTimeBestScore`와 “역대 최고” 라벨을 한 쌍으로 고정했다.
- **결투장 프로필 조회용 manager/API 경계를 먼저 마련했다.** 서버 확정 `seasonTierId`가 없는 현재 FakeServer 및 미배치 계정에서는 티어를 만들어 내지 않고 기록 칩 자체를 숨기며, 티어 표식은 공용 glyph 로더에서 그린다.

## v0.34.5 — 2026-08-30

- **렐릭 탭 그리드에서 토리카·도디의 정수리가 수평으로 잘리던 버그를 고쳤다.** 여러 번 손댔는데도
  낫지 않았던 이유까지 함께 남긴다 — 원인은 카드가 아니라 `src/puppets/anchors.ts`의
  `MAX_HEAD_DROP_RATIO`(0.34)였다. 이 한계가 걸리면 자르기 시작점이 `content.top`보다 **아래로**
  내려가 원화의 맨 윗부분이 통째로 사라지고, 그 단면이 수평선으로 남는다. 원래는 들어 올린 손처럼
  머리가 아닌 부위가 머리보다 높이 솟은 포즈만 걸리라고 둔 값인데, 등신이 낮아 머리가 큰
  토리카(머리 위 여백 ÷ 자르기 높이 = 0.386)·도디(0.383)가 함께 걸렸다. 실제 원화 여섯 종을
  전부 재서 가장 큰 값이 0.39 언저리인 것을 확인하고 한계를 **0.46**으로 올렸다 — 카드 절반(0.5)
  보다는 아래라 진짜 들어 올린 손 포즈에서는 여전히 얼굴을 상단 절반 안에 붙잡아 둔다.
- **되돌린 변경:** 토리카의 `cardHeadDropY: 14`를 지웠다. 이미 잘린 그림을 아래로 옮기는 보정이라
  잘린 단면을 되돌리지 못했고, 잘림이 사라진 지금은 공통 상단 여백(`HEAD_TIP_MARGIN_RATIO`)만으로
  정수리 위에 충분한 틈이 남는다. 원인을 덮는 개체별 보정 손잡이가 남아 있으면 다음에도 같은
  자리를 다시 덮게 되므로 필드 자체를 없앴다.
- **회귀 테스트를 지어낸 값이 아니라 실제 에셋으로 바꿨다.** 지금까지의 카드 잘라내기 테스트는
  전부 손으로 만든 프레임을 써서, 실제 원화가 잘리는 동안에도 한 번도 실패하지 않았다.
  `tests/unit/puppetAnchors.test.ts`에 전신 원화 일곱 종의 실제 메타데이터와 ZIP `머리1` 관절로
  "정수리를 자르지 않는다 / 정수리 위에 틈이 남는다 / 얼굴이 위쪽 절반 안에 선다"를 검사하는
  회귀 테스트를 더했다. 그러려면 메타데이터가 Phaser 없이 읽혀야 해서, 토리카·렉시아·스피나·
  루카·타페자라의 크기·알파 경계도 도디·메테·폰토스처럼 `src/puppets/assetMetadata.ts`로 옮기고
  `assets.ts`는 URL만 붙이게 했다(경계값은 ZIP 안 WebP를 alpha > 16으로 다시 측정해 확인했다).
- **그리드 왼쪽의 `/` 깎임이 머리 옆을 세로로 베던 문제를 구조로 풀었다.** 머리가 빠져나오는 홈이
  위아래 같은 폭의 직사각형이라, 잘린 모서리를 피하느라 좁아진 아랫폭이 정수리까지 그대로 따라
  올라가 머리 옆을 잘랐다. 홈을 **위로 갈수록 벌어지는 사다리꼴**로 바꿨다(`chipPoints`의
  `openTopWidth`·`openTopOffsetX`). 모서리를 피해야 하는 것은 홈이 칩 윗변과 만나는 한 줄뿐이고
  그보다 위에는 칩이 없어 아무것도 침범하지 않기 때문이다. 아울러 `portraitCardHeadWindow`를
  모든 카드가 쓰게 해, 깊게 깎인 왼쪽에 맞춰 오른쪽까지 함께 좁히던 예전 대칭 공식
  (`portraitCardNotchWidth`, 삭제)을 대신하게 했다. 도감 카드 기준으로 홈이 180 → 203(칩 윗변)·
  271(꼭대기)로 넓어졌고, 캐릭터별 `cardHeadEscape` 실측 없이도 대부분 해결된다 — 그 필드는
  그래도 모자란 원화를 위한 미세 조정으로 남긴다.
- **`Session`에 `playerResearch`가 들어오며 깨져 있던 단위 테스트 세 개의 세션 픽스처를 고쳤다.**
  `collectionManager`·`fakeServer`·`relicProgression` 테스트가 손으로 만든 세션 리터럴을 쓰는데
  새 필수 필드가 빠져 `npm run typecheck`가 실패하고 있었다. `createInitialPlayerResearchProgress()`를
  넣어 되살렸다 — 이 변경 자체와는 무관하지만 커밋 전 품질 게이트를 막고 있었다.

## v0.34.4 — 2026-08-30

- **`public/` 바로 아래에 흩어져 있던 콘텐츠 원화 11장을 `public/sprites/content/`로 옮겼다.**
  `Content1~5_001*.webp`·`journal_001.webp`가 배경(`sprites/background`)·재화
  (`sprites/currency`) 등 다른 모든 원화와 달리 `public/` 루트에 그대로 남아 있어 정리
  기준이 어긋나 있었다. `src/ui/backgrounds.ts`·`src/scenes/loadingSteps.ts`의 로딩 경로와
  `scripts/prepare_backgrounds.py`의 굽는 대상(콘텐츠 원화는 이제 `sprites/content`로 굽는다)을
  함께 옮겨 `public/` 루트에는 `fonts`·`puppets`·`sprites`만 남는다.

## v0.34.3 — 2026-08-30

- **환경설정 "지원" 탭(계정 정보·계정 탈퇴 등)이 위쪽으로 뭉쳐 겹쳐 보이던 버그를 고쳤다.**
  `SettingsScene.buildRows`의 `section()` 클로저는 자기 스코프의 `y`만 갱신하는데,
  `buildSupportRows(y, section)`는 그 갱신을 반영하지 못하는 별도의 매개변수 `y`를 계속
  들고 있어 — 패널 제목은 올바른 위치에 그려지지만 그 아래 행들은 제목이 그려지기 **전**
  위치(패널 시작점 바로 아래)에서부터 다시 쌓여 제목·본문이 겹쳤다. `section()`이 갱신된
  `y`를 반환하도록 고치고 `buildSupportRows`가 그 값을 이어받게 해 각 섹션의 제목과 본문이
  올바른 순서로 쌓이게 했다. 겹침이 사라진 김에 두 패널의 높이(420→330, 742→660)도 실제
  내용 길이에 맞춰 줄여 아래쪽 빈 공백을 없앴다.
- **병합 과정에서 남은 `InventoryPopup`의 죽은 `onWalletChanged` 호출을 지웠다.**
  `InventoryManager.useConsumable`이 이미 managerEvents로 지갑 갱신을 발행하고
  TopBar가 이를 구독하므로(v0.34.2 이전에 들어온 "매니저 이벤트로 UI 동기화" 변경), 더는
  존재하지 않는 생성자 콜백을 여기서 다시 부를 필요가 없다.

## v0.34.2 — 2026-08-30

- **퍼블릭에 있었지만 어디에도 연결되지 않았던 재화·Puppet 원화를 정리했다.**
  `sprites/currency/dna.webp`는 이미 구워져 있었는데도 `currency-dna` 키가 계속
  `heart.webp`를 임시로 빌려 쓰고 있었다 — `src/ui/currencyIcons.ts`가 제 파일을 가리키게
  고쳤다. `puppets/char_007.zip`·`charSD_007.zip`(가칭 "타페자라", 프테라 모티브 신규
  캐릭터)과 `puppets/enemy_004.zip`·`enemySD_004.zip`(puppet.json 원제 "비늘 후드의 신비한
  꼬마 탐험가")은 렐릭 데이터·전투 수치·소속 팩션이 아직 정해지지 않아 `src/puppets/assets.ts`에
  측정된 `PuppetAsset`(`TAPEJARA_ASSET`/`TAPEJARA_SD_ASSET`, `EXPLORER_ASSET`/
  `EXPLORER_SD_ASSET`)로만 등록해 두고, 화면이 실제로 쓰는 표(`PORTRAIT_ASSETS`·
  `ENEMY_SD_ASSETS`)에는 아직 넣지 않았다. 콘텐츠가 정해지면 이 네 상수를 그 표에 연결하기만
  하면 된다.

## v0.34.1 — 2026-08-30

- **퍼블릭에 원본 그대로 남아 있던 콘텐츠 배경 4장을 구웠다.** `Content2~5_001background.png`
  (장당 9MB 안팎)를 `scripts/prepare_backgrounds.py`로 WebP(장당 500KB 이하)로 굽고 원본은
  저장소에서 지웠다. `Content2`(원정)는 이미 원정 화면이 지도·필드 배경을 따로 갖고 있어,
  대신 원정 순위 팝업(`ExpeditionRankingPopup`)의 전용 배경으로 붙였다. `Content3`(케이크
  대작전)·`Content4`(현상수배)·`Content5`(레이드)는 각 콘텐츠 진입 화면(`SortiePreviewScene`)이
  지금까지 임시로 빌려 쓰던 스테이지 지도 배경 대신 자기 배경으로 갈아입는다. 새 키 네 개
  (`expeditionRanking`·`sortieCake`·`sortieBounty`·`sortieRaid`)는 `src/ui/backgrounds.ts`
  한 곳에서만 소유하며 `BACKGROUND_ASSETS`를 통해 타이틀 로딩 단계에서 함께 적재된다.

## v0.34.0 — 2026-08-30

- **전투 기여도 그래프 팝업을 "공격 · 방어 · 회복" 버튼과 짝지었다.** 결과 팝업에서 그래프를
  열면 그 버튼은 사라지고, 그래프 판 우측(원래 닫기 X가 서던 자리)에 작은 "돌아가기" 라벨
  버튼이 대신 선다. 그래프를 닫으면 숨겼던 버튼이 다시 보인다
  (`StageCompletePopup.onOpenContribution`이 `onClosed` 콜백을 받고,
  `BattleContributionPopup.open`이 그 콜백을 실제 팝업 종료 시점에 정확히 한 번 불러 준다).
- **기여도 그래프의 이름 목록에 얼굴 액자를 더했다.** 이름만으로는 다섯 줄에서 누구인지 한눈에
  읽히지 않아, 재화 액자·룬 조각과 같은 "액자" 예외(불투명 사각 + 사방 테두리 + 안쪽 비네팅)를
  얼굴에도 적용하는 `FaceFrame`을 새로 만들어 이름 앞에 붙였다. 카드(`PortraitCard`)와 달리
  머리가 빠져나오는 홈을 두지 않고 사각 안에 얼굴을 그대로 채운다.
- **스토리 승리 팝업의 편성이 그리드 카드 대신 SD가 idle로 선 모습으로 바뀌었다.** Puppet은
  컨테이너 변환을 물려받지 않으므로 팝업 본문 밖의 전용 레이어(화면 좌표)에 세우고, 이름표와
  "MVP!"만 팝업 본문에 얹는다. "MVP!"는 승리 표제처럼 튀어 오르며 살짝 기울어 작아도 먼저
  눈에 든다.
- **토리카의 그리드 카드 뿔 잘림을 마저 고쳤다.** 모든 카드가 공유하는 상단 여백
  (`HEAD_TIP_MARGIN_RATIO`, v0.33.0)만으로는 뭉툭한 뿔이 여전히 살짝 넘쳐 보였다. 카드
  전용(`portraitOffsetY`와 달리 전신 정보창에는 영향을 주지 않는) 새 보정값
  `PuppetAsset.cardHeadDropY`를 토리카에만 얹어 그림을 카드 안에서 살짝 더 내렸다.

## v0.33.0 — 2026-08-30

- **머리 카드 잘라내기에 공통 상단 여백을 더했다(`computeHeadCardFrame`).** `headroom: 0`은
  내용 상자 맨 위에 자르기를 딱 붙여, 뾰족하거나 갈래진 장식(깃털·머리카락)은 자연스럽게
  가늘어지며 사라져 눈에 띄지 않았지만 토리카의 뿔처럼 끝이 뭉툭·넓은 장식은 잘린 단면이
  수평선으로 그대로 남아 그리드 허용 범위 밖으로 넘친 것처럼 보였다. 캐릭터별 예외 없이
  `HEAD_TIP_MARGIN_RATIO`(카드 높이의 3.5%) 하나만 자르기 시작점 위에 더해 모든 캐릭터의
  정수리가 살짝 낮아지고 숨 쉴 틈이 생기게 했다.
- **일반 노드 클리어 보상도 주간 누적 점수(랭킹 팝업의 단계 보상 트랙)에 더한다.** 순위
  기준(`bossWeek.bestScore`)은 여전히 불사 보스에게 입힌 피해량만 반영하지만(v0.32.0 참고),
  누적 점수는 보스전만이 아니라 노드를 도는 노력도 조금씩 보태 주간 보상 단계에 기여하게
  했다(`expeditionNodeRewardScore`, `completeExpeditionNode`). 활성 원정 화면의 "획득 전리품"
  판 아래에는 이번 런이 보탠 몫을 판 없이 흰 글자+검은 테두리로만 보여준다.
- **원정 "포기하기" 버튼을 작게 줄이고 화면 우상단 구석으로 옮겼다.** 전리품 판이 점수 한
  줄만큼 커지며 버튼과 가까워졌던 것을, 겹치지 않는 화면 위쪽 빈 자리로 올려 실수로 누르기
  어렵게 했다.
- **원정 랭킹 팝업에서 기여도 보상 목록을 지웠다.** "랭킹"과 "주간 보상" 두 팝업이 같은 누적
  보상 단계 목록을 중복해서 보여주고 있었다 — 이제 보상 수령은 "주간 보상"
  팝업(`ExpeditionRewardPopup`)의 몫으로 단일화하고, 랭킹 팝업은 순위표만 크고 또렷하게
  보여준다(그래서 이제 쓰이지 않는 `src/ui/expeditionRewardStage.ts`는 지웠다). 아직 다인원
  서버가 없어 순위표에 항상 내 기록 하나만 뜨던 것도, 친구 화면의 임시 인물(`PREVIEW_FRIENDS`)
  셋을 빌려 순위표가 늘 여럿 있는 것처럼 채웠다 — 실제 서버가 다수 이용자 기록을 돌려주면
  이 보정은 지운다.

## v0.32.0 — 2026-08-30

- **주간 랭킹 반영 대상을 바로잡았다.** 불사 보스는 애초에 처치가 불가능해 "완료"라는 결과가
  없다 — 성과는 승패가 아니라 그 안에서 입힌 피해량 자체다. v0.31.0에서 일반 노드 진행 점수를
  런 종료 시 함께 반영하도록 고쳤던 것을 되돌리고, 대신 `submitExpeditionBossScore`(승패와
  무관하게 항상 제출된다)만이 주간 랭킹·누적 보상·소탕이 참조하는 역대 최고점을 갱신하도록
  단일화했다.
- **스토리 스테이지 승리 결과를 `StageCompletePopup`(보상 팝업의 연장선)으로 다시 만들었다.**
  "확인 및 저장" 버튼을 눌러야 저장되던 계약을 버리고, 승리 즉시 `completeStage`로 확정한 뒤
  이미 지급이 끝난 결과만 보여준다 — 그래서 보상 팝업과 같은 계약대로 화면 아무 곳이나 눌러도
  다음 화면(지도)으로 넘어간다. 위에서부터 귀여운 별 장식의 "Victory!" 표제, 분류별 몫의 합으로
  고른 MVP를 가운데 크게·나머지 둘을 옆에 작게 세운 편성, 공격·방어·회복 기여도 그래프를 여는
  버튼, 보상 액자 순으로 쌓는다. MVP 판정(`battleContributionMvp`)은 원시 수치 대신 분류별
  몫(ratio)의 합을 쓰므로, 공격을 몰아 받은 딜러와 회복을 몰아 받은 힐러가 규모 차이 없이
  공정하게 경쟁한다. 패배 결과 화면은 기존 그대로 둔다.

## v0.31.0 — 2026-08-30

- **주간 원정 실제 점수를 랭킹에 연동.** 지금까지 주간 랭킹(`getExpeditionWeeklyBest`/
  `getExpeditionLeaderboard`)은 20층 보스를 잡아 점수를 제출한 런만 반영했고, 일반 진행만 하고
  포기하거나 전멸한 런의 실제 도달 점수는 어디에도 남지 않았다. `settleExpeditionRun`이 결과와
  무관하게 `run.bestScore`(실제 도달 점수)를 같은 주간 누적/최고 집계에 합류시키도록 고쳤다 —
  단, 그 런이 이미 보스 점수를 제출했다면(그 값이 `run.bestScore`에도 흡수돼 있으므로) 중복
  반영하지 않는다.
- **원정 기회를 주 2회로 제한.** `playsThisWeek`는 있었지만 실제로 막는 곳이 없어 무제한이었다.
  `EXPEDITION_WEEKLY_POLICY.maxPlaysPerWeek`(2)를 두고 `ExpeditionManager.start`에서 강제한다.
- **소탕 추가.** 원정 기회를 하나 소비해 직접 플레이하지 않고도 지금까지 달성한 역대 최고
  점수(`allTimeBestScore`, 새 저장 필드)의 80%를 주간 기록에, 노드 클리어 전리품 상한의 50%를
  즉시 지급한다(`GameApi.sweepExpedition`). 주간 달성도(누적 점수 단계 보상)는 문턱을 처음
  넘는 순간에만 지급되므로 같은 주에 반복해도 이미 넘은 문턱에는 의미가 없지만, 다음 문턱을
  향해 누적치를 계속 쌓을 수는 있어 막지 않았다. 출격 버튼 옆에 나란히 두었다.
- **원정 첫 화면(주간 기록)을 다시 짰다.** 폰토스 전신을 화면 가득 채우도록 크게, 얼굴이 제목
  아래에서 시작하도록 높이 세웠다(이전에는 애매한 높이로 잘려 작아 보였다). 상시 펼쳐 두던
  큰 순위표 판을 없애고, 화면 중하단에 내 주간 최고/순위/누적을 보여주는 간결한 점수 패널
  하나만 남겼다. 순위표는 "랭킹" 버튼을 눌러야 여는 팝업(`ExpeditionRankingPopup`, 이미
  있었지만 보스 결과 화면에서만 쓰이던 것을 재사용)으로 옮겼고, "주간 보상" 버튼도 같은 줄에
  단순한 크기로 추가해 기록 보상 팝업을 바로 연다.

## v0.30.5 — 2026-08-30

- 토리카(`TORIKA_ASSET.cardZoom`)가 0.62로 지나치게 확대를 줄여, 도감·편성·발굴 등 모든
  카드 그리드에서 혼자만 작고 왼쪽으로 치우쳐 보였다. 0.82로 다시 맞춰 다른 카드와 비슷한
  크기로 채운다.
- 방치 발굴 팝업의 "보유 렐릭" 편집 그리드(`IdleExcavationPopup`의 `GRID_VIEW`) 카드
  비율(215×235, 세로/가로 1.09)이 도감 그리드(300×400, 1.55)와 달라, 같은 캐릭터라도
  머리 관절 기준 잘라내기(`computeHeadCardFrame`)가 카드 가로세로비를 그대로 잘라내는
  비율로 쓰는 탓에 두 화면에서 서로 다른 구도로 잘려 보였다(도디의 모자·책 노출 범위가
  달랐던 사례). 카드 높이를 268로 늘려 도감과 같은 비율로 맞췄다 — 그리드는 이미
  스크롤 가능하게 설계돼 있어 안전하게 늘어난다.

## v0.30.4 — 2026-08-29

- 편성 화면 상성 화살표를 흰 테두리로 두른 홑겹 삼각형 대신, 사방 테두리 없이 그림자 ·
  같은 색 발광 · 뾰족한 끝 두 변에만 거는 밝은 강조선으로 이루어진 V자 홈 화살촉으로 다시
  그렸다(`AffinityDirection`). 기존 모양은 이 화면의 "테두리를 두르지 않는다" 홀로그램
  규칙을 어기고 있었다.
- 자동 배치 버튼을 화면 위쪽(제목 옆)에서 보유 렐릭 그리드 위 우측으로 옮겼다
  (`PartyScene`). 그리드와 같은 배치표(`ROSTER_GRID`)를 공유해 그리드 오른쪽 경계에
  맞추고, 셋째 아군 자리 이름표와 겹치지 않도록 버튼 폭을 좁혔다.
- `refresh()`가 매번 자동 배치 버튼 좌표를 `{ x: 180, y: 190 }`으로 하드코딩해 덮어써
  E2E가 실제 이동한 위치를 읽지 못하던 버그를 고쳤다 — 이 때문에 위치를 옮긴 뒤에도
  자동 배치 버튼을 실제로 누르는 테스트가 옛 좌표만 눌러 왔다.
- 자동 배치로 이미 셋을 고른 상태에서 새 카드를 누르면 아무 반응이 없어 "편성을 고칠 수
  없다"로 보이던 것을 고쳤다 — 가득 찬 상태에서 새 카드를 고르면 마지막 자리를 그 카드로
  바로 바꾼다(`PartyScene.toggle`).

## v0.30.3 — 2026-08-29

- 루카의 관찰 프로필에 벨로키랍토르 기반 성체의 복원 이력과 신장 1.62m·체중 59kg을 추가하고,
  도감에도 같은 수치와 단거리 질주에 적합한 하체 근육·가벼운 골격을 명시해 두 화면의 설정이
  어긋나지 않게 했다. 해금 기록은 유일한 연구원인 주인공의 1인칭 관찰로 고쳐, 집과 휴식을
  좋아하는 단거리 선수라는 정체성과 육식 렐릭들과의 관계, 연구소 소파 및 치즈케이크 습관을
  체중 조롱이나 의학적 단정 없이 담았다.

## v0.30.2 — 2026-08-29

- 스피나의 `stealthLeap`가 이동 속도에 따른 도약처럼 읽히던 설명을 `순간이동` 규칙 키워드로
  바로잡았다. 저장·전투 호환성을 위해 기존 내부 식별자는 유지하되, 실제 전투처럼 경로와 이동
  시간을 거치지 않고 같은 발동 프레임에 착지 좌표가 확정된다는 계약을 타입 주석과 단위 테스트에
  명시했다.

## v0.30.1 — 2026-08-29

- 렉시아(`LEXIA_ASSET`, char_002.zip)의 `content`(alpha 실제 경계)가 1번 토리카
  (char_001.zip) 값을 그대로 옮겨 온 것이었다. 렉시아가 왼쪽으로 크게 뻗어 쥔 낫 무기가
  이 잘못된 경계 밖으로 잘려 있어, 카드·정보창 배율이 무기 없는 좁은 폭을 기준으로 계산되며
  실제보다 확대되어 도감 그리드에서 다른 카드보다 얼굴이 눈에 띄게 크고 상반신이 덜 보였다.
  ZIP 안 WebP의 alpha > 16 실제 경계로 다시 측정해 `{ left: 15, top: 43, right: 1038,
  bottom: 1455 }`로 고쳤다. 스크린샷으로 스밀라·케찰·토리카와 같은 비율의 상반신이 보이는지
  확인했다.

## v0.29.11 — 2026-08-29

- v0.29.10의 대각선-홈 겹침 수정 이후에도 스피나(뒷머리 오른쪽)·메테(후드 왼쪽)·도디(머리
  깃털)의 머리 장식이 한쪽으로 쏠려 대칭 홈의 그 쪽 대각선 모서리 안쪽에서 여전히 애매하게
  잘려 보였다. 관절은 항상 카드 가운데를 기준으로 잡지만 장식은 포즈에 따라 한쪽으로 쏠려
  그려지기 때문이다. `portraitGrid.ts`에 `portraitCardHeadWindow`를 추가해 `PuppetAsset`의
  새 필드 `cardHeadEscape: { left?, right? }`(칩 폭 대비 비율)로 그 쪽 홈만 넓히도록 했다 —
  대각선 안쪽 안전 여유(v0.29.10에서 고친 자기 교차 문제)는 항상 지킨다. 세 캐릭터의 실측값을
  채우고 스크린샷으로 확인했다.
- 루카처럼 들어 올린 손·망토가 얼굴보다 높이 솟은 포즈는 `computeHeadCardFrame`의
  `headroom: 0`이 "내용 상자 맨 위"에서 그대로 자르기를 시작해, 얼굴이 카드 아래로 밀려나거나
  잘려 나갔다(발굴·도감 카드에서 루카 얼굴이 거의 보이지 않던 문제). `MAX_HEAD_DROP_RATIO`
  안전장치를 추가해 머리 관절이 항상 카드 상단 34% 안에 들어오도록 자르기 시작점의 하한을
  잡았다 — 새 캐릭터를 추가해도 자동으로 적용되는 일반 안전망이라 손볼 필요가 없다. 다만
  루카의 들어 올린 손 자체(머리 폭 기준 잘라내기 범위 밖으로 나가는 부위)는 이 두 안전장치로
  담을 수 있는 범위를 넘어서 카드에 여전히 나오지 않는다 — 카드 잘라내기 폭 자체를 늘리는
  시도(`cardZoom` 계열 배율 조정)가 루카 원화에서 일관되지 않게 동작해(비율에 따라 캐릭터가
  아예 화면 밖으로 밀려나는 등) 이번에는 되돌리고 남겨 둔다. 같은 시도를 반복하지 않기 위한
  기록이다 — 다음에는 실제 렌더링된 Puppet 텍스처의 실측 크기부터 확인해야 한다.
- 회귀 테스트: `portraitCardHeadWindow`의 비대칭 확장·안전 클램프(`tests/unit/portraitGrid.test.ts`),
  `computeHeadCardFrame`의 상단 안전장치(`tests/unit/puppetAnchors.test.ts`).

## v0.30.0 — 2026-08-29

- 런타임 전투원 ID별 공격·방어·회복 기여도 시스템을 추가했다. 공격은 경감과 과잉 피해를
  제외한 실제 HP 손실만 공격력/주문력으로 나누고, 방어는 방어력/저항력 경감과 보호막 제공자
  흡수를 나누며, 회복은 시전자가 만든 실제 HP 증가만 기록한다. 결과 UI와 향후 서버 검증기가
  같은 순수 누적 정책과 불변 스냅샷을 사용해 중복 렐릭 편성과 프레임 분할에도 안정적으로
  재현할 수 있게 했다.

## v0.29.10 — 2026-08-29

- 모든 캐릭터 카드(`PortraitCard`)의 좌측 상단 대각선 깎임이 머리가 빠져나오는 윗변 홈과
  겹쳐, 튀어나온 머리·모자·장식이 그 `/` 대각선에 애매하게 잘려 보이던 문제를 고쳤다.
  출격 팝업의 SD 자리(`ExpeditionEntryButton.sdMask`)가 이미 같은 문제를 "띠를 깎인
  깊이만큼 내린다"는 방법으로 풀어 둔 걸 그대로 가져와, 홈 폭 자체를 위쪽 두 모서리의
  깎인 깊이만큼 줄이는 순수 함수 `portraitCardNotchWidth`로 만들었다(`portraitGrid.ts`).
  도감·발굴·원정 등 이 카드를 쓰는 모든 화면에 한 번에 적용된다. 실제 화면 스크린샷으로
  전/후 차이(도디 모자 장식이 인접 카드 배경에 걸쳐 잘리던 부분)를 직접 확인했다.

## v0.29.9 — 2026-08-29

- 발굴 편성 그리드의 하단 그라데이션(비네트)을 지웠다. 스크롤 없이도 항상 떠 있어서
  다 스크롤한 마지막 줄까지 가려 버렸다 — 마스크로 자연스러운 컷을 만드는 대신, 도감
  그리드(RelicsScene)와 같은 28px 여유를 스크롤 하한에 더해 마지막 줄 밑변이 마스크
  경계에 정확히 겹쳐 앤티에일리어싱으로 깎이지 않게 했다.
- 원정 편성판(보유 렐릭 그리드)이 보유 렐릭 3줄부터 출격 버튼과 겹치던 문제를 고쳤다.
  그리드가 고정 좌표에 그대로 쌓이기만 하고 스크롤·마스크가 아예 없었다 — 도감·발굴과
  같은 스크롤 그리드 패턴(뷰포트 마스크, 드래그·휠, 카드별 syncMask 틱커)을 새로 달아
  편성판 아래·힌트/출격 버튼 위 사이에서만 스크롤하게 했다. 실제 화면 스크린샷으로
  겹침이 사라졌는지 확인했다.
- 스피나 기본 공격 설명에서 상단 라벨과 겹치는 "공격력의 80%"를 지우고 토리카 일반
  공격과 같은 "적 한 명에게 물리 피해를 준다" 형태로 줄였다. 궁극기 설명의 "피해를
  준다고 기절시킨다"는 인용형 어미("~라고")로 읽히는 오타였다 — 어간에 연결어미를
  붙인 "주고 기절시킨다"로 고쳤다.

## v0.29.8 — 2026-08-29

- 발굴 편성 그리드에서 둘째 줄부터 카드가 뷰포트 경계에서 갑자기 잘려 취소·배치 버튼을
  침범하는 것처럼 보이던 문제를 고쳤다. 스크린샷으로 직접 확인해 원인을 잡았다 — 뷰포트가
  한 줄 반 정도만 담을 높이라 스크롤 없이도 둘째 줄이 이름표 없이 얼굴만 걸친 채 끊겨
  버튼 바로 위에서 뜬금없이 잘려 보였다. 다른 화면 가장자리 바와 같은 `drawGlassFade`
  검정 그라데이션과 얇은 경계선을 마스크 밖(항상 보이는 층)에 얹어, 스크롤할 카드가 있을
  때만 카드가 어둠 속으로 자연스럽게 사라지고 버튼 자리와 분명히 갈라지게 했다.

## v0.29.7 — 2026-08-29

- 룬 세공을 한 번 성공/실패한 뒤 버튼이 꺼진 채로 남아 바로 이어서 누를 수 없던 버그를
  고쳤다. `render()`가 요청 완료 시 세공 버튼을 새로 만드는데, 그 시점에 `pending` 플래그를
  `finally`에서야 늦게 풀어 방금 만든 새 버튼도 꺼진 채로 나왔다 — `render()` 호출 전에
  먼저 풀도록 순서를 바꿨다. 이름 변경 뒤에도 같은 이유로 세공 버튼이 꺼져 있던 문제를
  같은 방식으로 고쳤다.
- 스피나(전신 원화) 카드가 실제보다 작고 왼쪽으로 치우쳐 보이던 문제를 고쳤다. `content`
  경계가 잘리지 않은 원본 캔버스 그대로였던 탓이며, ZIP 안 WebP의 alpha > 16 실제 경계로
  다시 측정해 넣었다. 카드 확대 배율은 이 경계의 폭에 반비례하므로 자동으로 커지고, 머리
  관절 중심 크롭도 더 좁아진 창으로 다시 잡혀 자연히 중앙으로 당겨온다.

## v0.29.6 — 2026-08-29

- 발굴 보관량 게이지가 경과 시간이 아니라 실제 쌓인 재화량을 보관 한도(시간당 생산량 ×
  보관 한도 초)와 비교해 채운 비율을 보여주도록 고쳤다. 기존 경과 시간 기준 계산은 조회
  (`getIdleExcavation`)마다 서버가 정산을 다시 돌려 기준 시각(`lastSettledAt`)이 지금으로
  밀리므로, 창을 열 때마다 게이지가 늘 0%로 보이는 버그가 있었다. `excavationStorageFillRatio`
  시그니처를 실제 재화량 기반으로 바꾸고 `excavationStorageLimitSeconds`를 분리했다.
- 발굴 편집 그리드를 두 줄 이상 스크롤하면 둘째 줄부터 캐릭터 원화가 카드를 따라 내려오지
  않고 예전 자리에 남아 그리드와 어긋나던 현상을 고쳤다. `PortraitCard`의 원화 자체 기하
  마스크는 부모 컨테이너의 이동을 상속하지 않아 스크롤마다 `card.syncMask()`를 다시 불러야
  하는데(도감 그리드가 이미 쓰는 패턴), 발굴 그리드는 뷰포트 마스크만 갱신하고 카드별 마스크
  갱신을 빠뜨렸다.
- 1~3번 칸에 이미 배치된 렐릭 카드에 원정 편성판과 같은 어두운 반투명 오버레이를 추가했다.
  칩 밖으로 튀어나온 머리 몫까지 원화 알파 그대로 검게 겹치는 기존 `selectedOverlayAlpha`
  옵션을 그대로 켰을 뿐이라 별도 마스크 계산은 새로 만들지 않았다.

## v0.29.5 — 2026-08-29

- 원정 최종층 보스의 영문 스펠링을 `pontus`에서 `pontos`로, 한글 표기를 "폰투스"에서
  "폰토스"로 바로잡았다. 렐릭 id·스킬 id·Puppet 에셋 상수·타입·경로·테스트를 전부 함께
  고쳐 이름이 코드 전역에서 하나로 일관되게 했다(Puppet ZIP `enemy_Pontos.zip`·
  `enemySD_Pontos.zip`, 스킬 아트 `public/sprites/skills/pontos/` 포함). 저장 데이터에는
  등장하지 않는 보스 정의라 마이그레이션은 필요 없다.

## v0.29.4 — 2026-08-29

- 원정 최종층 보스 폰투스의 전용 스킬 일러스트를 구워 연결했다. 보스는 번호 대신 렐릭 id를
  원본 이름 머리말로 그대로 쓰므로 `prepare_skill_icons.py`의 원본 표에 `pontus: pontus`를
  추가했다. 흰 실루엣 WebP 네 장을 `public/sprites/skills/pontus/`에 구웠고, `skillArt.ts`의
  `RELICS_WITH_ART`에 `pontus`를 더해 정보창이 전용 그림을 쓰게 했다. 원본 PNG 4장은 굽고 나서
  저장소에서 지웠다.

## v0.29.3 — 2026-08-29

- 도디(5번)·메테(6번) 전용 스킬 일러스트를 구워 연결했다. `prepare_skill_icons.py`의 원본 표에
  `char005: dodo`, `char006: mette`를 추가해 흰 실루엣 WebP 네 장씩(패시브·일반 공격·궁극기·
  폭주)을 `public/sprites/skills/{dodo,mette}/`에 구웠고, `skillArt.ts`의 `RELICS_WITH_ART`에
  두 id를 더해 정보창 스킬 아이콘이 공용 대체 아이콘 대신 전용 그림을 쓰게 했다. 원본 PNG
  8장은 굽고 나서 저장소에서 지웠다.

## v0.29.2 — 2026-08-29

- `CLAUDE.md`에 야성·패시브·일반 공격·궁극기 설명문의 공통 규칙을 문서화했다("스킬 설명문
  규칙" 절). 고정 % → 실제 능력치 환산 태그, 상단과 겹치는 수치 생략, 반복되는 같은 값은 한
  번만 말하기, 정의된 규칙어는 태그로만 연결, `%p` 금지(회귀 테스트로 고정)를 캐릭터마다
  다시 판단하지 않도록 한곳에 모았다.
- 폰투스 패시브 "심해의 압력"이 "매초 주문력이 상승한다"처럼 막연하게만 말하던 것을 실제
  수치("12 상승")로 명시하도록 `passiveDescription()`에 `abyssalPressure` 분기를 추가했다.
  키워드 사전에 `ap`(주문력)를 추가해 atk·def와 같은 방식으로 태그를 걸 수 있게 했다.

## v0.29.1 — 2026-08-29

- 메테 궁극기(순수 회복형)를 정보창에서 누르면 스킬 팝업이 열리지 않던 버그를 고쳤다. `previewSkillDamage`는
  damageType/power가 없는 스킬에 호출하면 예외를 던지는데, 궁극기는 패시브와 달리 이 조건 없이
  항상 미리보기를 시도했다. 판별을 `canPreviewSkillDamage()`로 뽑아 궁극기도 패시브와 같은 기준으로
  가드한다.
- 설명문에서 스킬마다 다시 적던 % 수치를 실제 능력치로 환산해 태그로 보여 주는 쪽으로 정리했다.
  메테 크레센도(스타카토 추가타), 아다지오의 무게(보호막), 도디 궁극기(아군 회복)가 각각
  `[[damage-value]]`, `[[shield-value]]`, `[[heal-value]]` 태그로 실제 수치를 보여 준다.
  전투 중 계속 바뀌는 수치(스피나 궁극기의 "현재 공격 속도"처럼)는 여전히 %로 남긴다 — 고정값이
  아니라서 미리 환산해 둘 수 없다.
- 메테 기본 공격 설명에서 "0.1초간 경직"의 "0.1초" 중복을 지웠다. 경직 키워드 자체가 이미 약
  0.1초라고 설명하므로 스킬 문구에서는 `[[stagger|경직]]` 태그만 남긴다.
- 크레센도·아다지오의 무게 설명을 각각 "50%의 피해량을 가진 스타카토(태그) 추가 발동", "아군이
  군중제어(태그)에 걸리면 정화" 형태로 줄였다. "군중제어"·"마법 피해" 키워드를 새로 추가했다.
- 렉시아 패시브 설명을 "전투 시작 시, 네 능력이 각각 25%, 25%, 25%, 25%"에서 "전투 시작 시,
  네 능력이 모두 25% 오른다"로 줄였다 — 네 수치가 항상 같은 값이라 한 번만 말해도 충분하다.
- 도디 야성 발현("인비저블 썸띵?") 설명에서 "(속도 ×2)" 괄호를 지웠다. 배속 환산은 개발 참고용
  주석에만 남긴다.
- (참고) "%p"(퍼센트포인트) 표기는 이전에 한 번 시도했다가 플레이어에게 낯선 단위라 되돌리고
  회귀 테스트(`사용자 노출 퍼센트 표시 계약`)까지 남긴 결정이라, 이번에도 치명타 확률류 수치는
  일반 `%`로 유지했다.

## v0.29.0 — 2026-08-29

- 발굴 현황의 SD 발밑과 누적 액자 사이에 보관 한도가 찬 비율을 임무 연구도와 같은 HoloBar 게이지로 보여 준다. 정산 로직과 같은 확장 배율을 공유하는 순수 함수 `excavationStorageFillRatio`를 추가했다.
- 설정 > 고객지원·데이터 탭에 "모든 캐릭터 획득" 임시 디버그 진입점을 추가했다. 스타터 렐릭 추가처럼 저장 마이그레이션이 옛 계정까지 소급하지 않는 변경을 재설치 없이 확인할 수 있다. (참고: 기존 "저장 데이터 초기화" 역시 로컬 저장을 지우고 재부팅해 최신 스타터 구성을 다시 받는 방법으로 이미 동작한다.)

## v0.28.1 — 2026-08-28

- 도디와 메테의 전신·SD를 각각 5번과 6번 Puppet 묶음에 연결하고, 실제 알파 경계로 카드 확대와 전투 바닥선을 맞췄다. 전용 원화가 연결됨에 따라 토리카를 재사용하던 임시 메테 계약을 제거했다.
- 도디 스킬명을 `인비저블 썸띵?`, `연구원님, 이것 좀 보세요!`, `깃펜 톡톡`, `세기의 대발견... 맞죠?!`로 확정하고 일반 공격 계수를 기획안의 주문력 70%로 바로잡았다.

## v0.28.0 — 2026-08-28

- 저장 호환 ID `dodo`를 유지하면서 표시 이름을 도디로 확정하고, 토리카와 같은 일러스트 비율의 신장 1.08m 및 가벼운 조류형 체중 20kg을 관찰·도감·해금 기록에 통일했다.
- 도디의 폭주를 공격 속도 +100%(속도 x2, 공격 간격 50%)로 교체하고, 생존 중 아군 방어력·저항력 +15% 및 적 받는 회복량 -30% 오라를 최댓값 비중첩 규칙으로 추가했다.
- 도디 일반 공격에 실제 감소 HP 기반 최저 현재 HP 아군 회복을 추가하고 자신 포함·편성 순서 동률 규칙을 확정했다. 궁극기는 250 게이지로 지정 원 안 적에게 주문력 200% 마법 피해, 생존 아군에게 주문력 200% 회복을 함께 적용한다.
- 도디의 임시 전신과 SD를 같은 토리카 기반 에셋으로 명시했으며, 폰투스 정보창 전신은 조금 키우고 위로 올려 하단 치우침을 완화했다.

## v0.27.10 — 2026-08-28

- 스피나 궁극기 `범람의 포식자`의 현재 공격 속도 계수를 300%에서 150%로 낮춰 공격 속도 누적의 피해 기여도를 조정했다. 공격력 200% 부분과 3초 기절, 궁극기 비용 300은 그대로 유지한다.
- 레벨 1의 공격력 124와 기본 공격 속도 122에서 복합 원피해가 431이고, 기본 공격 실제 적중마다 공격 속도 +3에 의해 다음 궁극기 원피해가 4.5씩 증가하는지 고정했다. 방어 적용 뒤 정수 반올림되는 실제 피해는 원피해와 분리해 검증한다.

## v0.27.9 — 2026-08-28

- 플레이어에게 노출되는 스피나 관찰 기록을 유일한 연구원인 주인공의 1인칭 기록으로 고쳤다. 별도의 `연구원 한 명`이 있는 듯한 표현을 없애면서 수중경비대 근무, 정확한 순찰·수문 점검, 숨겨 둔 어항 취미, 주인공의 조용한 청소와 다음 날 책상에 놓인 조개는 유지했다.
- 스피나 기록에 제3의 연구원이 다시 등장하지 않고 주인공의 직접 관찰과 책상 위 답례가 유지되는지 정적 데이터 테스트로 고정했다.

## v0.27.8 — 2026-08-28

- 렉시아 전용 관찰 질문에 체격 경쟁, 강자 도전, 작은 렐릭을 대하는 예절 상황을 추가했다. 선택별 직접 답변과 눈에 보이는 습성을 분리해, 예의를 갖춘 경쟁심과 약자를 위압하지 않는 태도가 연구 기록에 남는다.
- 관찰 질문 순환 테스트를 렐릭 ID 기준으로 일반화하고 렉시아 전용 질문의 등장·격리·고유 저장을 검증한다. 완료 뒤 연구 일지의 인터뷰 기록 글자도 공용 본문 크기로 키워 모바일 가독성을 높였다.

- 기록 보상 길을 **얇은 실선**으로 다시 그렸다. 굵은 막대 대신 한 줄짜리 선이 위로 뻗고, 지나온 구간은 **흰 선분** 한 겹으로 덧그어 표시한다. 마디는 선 위에 찍히는 작은 마름모(지나온 마디만 속이 참)이고 가지도 같은 굵기의 실선이다. 현재 누적은 흰 선이 끝나는 자리에 붙는 짧은 칩이 말한다.
- 순위 화면의 보스 전신을 더 크게(1000 → 1150) 세우고 더 위로 올려(발끝 1300 → 1400) 얼굴과 상반신이 판 위에서 넉넉히 보이게 했다.
- 편성 카드의 선택 표시가 **머리 픽셀만** 누른다. 칩 몸통은 도형으로 덮되 윗변 밖 머리는 원화를 검게 복제해 홈 마스크 안에서만 겹친다 — 홈을 도형으로 칠하던 직전 방식은 머리 옆의 투명한 빈자리까지 네모나게 어둡게 만들었다.
## v0.27.7 — 2026-08-28

- 스킬 팝업의 요약 줄에만 전달되던 문맥형 키워드 사전을 본문에도 연결했다. 이제 토리카 `다들 그만해!` 본문의 추가 피해 수치에도 밑줄과 입력 영역이 생기며, 누르면 방어력 15%에서 계산했다는 상세 팝업이 열린다.
- 요약과 본문이 같은 레이아웃 옵션 생성기를 사용하게 해, 이후 캐릭터의 동적 피해 수치가 어느 위치에 놓여도 링크 동작이 빠지지 않도록 회귀 테스트로 고정했다.

## v0.27.6 — 2026-08-28

- 토리카 폭주 설명에서 `방어력의 15% (19)`처럼 산식과 결과를 한꺼번에 보여 주던 문장을 **피해 수치 19**만 보이도록 정리했다. 수치를 누르면 현재 방어력에서 15%를 받은 값이라는 근거가 추가 팝업에 열린다.
- 일반 공격과 궁극기도 같은 문맥형 피해 수치 정의를 사용한다. 렉시아를 비롯해 렐릭 정의가 늘어나도 공격력·주문력·방어력과 배율을 공용 양식으로 설명하며, 캐릭터별 팝업을 복제하지 않는다.
- 플레이어에게 보이는 야성 설명과 렉시아 기록에서 `피버`를 실제 버프 명칭인 **폭주**로 통일했다.

## v0.27.5 — 2026-08-28

- 기록 보상을 게이지 세 줄에서 **위로 뻗는 길 한 줄**로 바꿨다. 마디마다 임계값이 서고 보상은 우 → 좌를 번갈아 가지처럼 내민다. 지나온 길만 강조색으로 차고 현재 누적은 그 끝에 표식으로 붙는다. 길이 창보다 길면 판을 키우지 않고 그 안에서 드래그·휠로 흐른다. 자리 계산은 순수 모듈 `expeditionRewardTrack.ts`가 맡고 Vitest가 마디 간격·좌우 번갈이·채움 보간을 고정한다.
- 원정 편성 화면의 "빠른 원정 확인 중…", "기준 점수를 먼저 기록하세요", "빠른 원정을 불러오지 못했습니다" 같은 상태 문구를 없앴다. 서버가 슬롯을 열어 두었을 때만 버튼이 생기고, 그 버튼이 보상과 남은 횟수를 직접 말한다. 지급 결과처럼 행동을 바꾸는 짧은 알림만 편성 안내 줄에 남는다.
- 규칙(`CLAUDE.md`)에도 조회 중·준비 안 됨·실패 상태를 화면에 쓰지 않고 그 자리를 비운다는 조항을 더했다.

## v0.27.4 — 2026-08-28

- 원정 기록 화면의 순위판을 **불투명하게**(0.98) 바꾸고 위로 200px 더 길게 늘렸다(680 → 880). 반투명한 판 위에서는 보스의 옷자락과 순위 글자가 섞여 읽혔다.
- 보스 전신도 한 뼘 키우고(900 → 1000) 그만큼 위로 올려 판이 길어져도 얼굴과 상반신이 그대로 보인다.
- 판 안의 기둥과 목록 자리는 이제 판 윗변에서 재므로, 판 높이를 바꾸면 함께 따라온다. 순위 줄도 여섯에서 여덟까지 선다.

## v0.27.3 — 2026-08-28

**원정의 첫 화면을 주간 기록으로 바꾸고 편성을 그 다음 단계로 내렸다.**

- 원정에 들어가면 편성판이 아니라 **기록 화면**이 먼저 뜬다. 위에는 20층 보스 폰투스가 서고 순위판이 그 허리 아래를 덮으며 내려온다. 왼쪽 기둥은 내 최고 순위·최고 점수·누적 점수와 그 아래 **기록 보상** 입구이고, 오른쪽은 주간 순위 목록이다.
- 하단 **출격** 버튼이 편성 단계를 연다(`scene.restart({ stage })`). 편성에서 뒤로가기는 로비가 아니라 한 단계 앞인 기록 화면으로 돌아간다. 준비 화면의 옛 "주간 기록" 버튼은 첫 화면과 중복이라 없앴다.
- 누적 보상은 순위와 분리해 **기록 보상 팝업**(`ExpeditionRewardPopup`)이 맡는다. 단계 수만큼만 판을 열고 바깥을 눌러 닫는다. 단계 한 줄을 그리는 규칙은 결과 화면의 기록판과 `expeditionRewardStage.ts` 하나를 공유한다.
- 진행 중 런이 있으면 지금처럼 곧바로 지도로 들어간다. 기록 화면은 새 원정을 시작할 때만 거친다.
- E2E: 저장 검증이 실제로 저장되지 않는 파생값(`expedition.active`)을 보고 있어 늘 통과할 수 없었다. 진행 중 런(`expedition.run.relics`)을 보도록 고쳤다.

## v0.26.13 — 2026-08-28

- **궁극기 충전의 시계 방향 가림막이 다시 보인다.** 카드를 통째로 `BitmapMask`로 쓰면 카드 안의 기하 마스크(원화·배경)와 겹쳐 가림막이 통째로 사라졌다. 카드 몸통(윗변이 닫힌 칩) 기하 마스크(`PortraitCard.createBodyMask`)로 바꿔 같은 결과를 안전하게 얻는다.
- **궁극기 연출이 전투 배속을 20%만 받는다**(`ultimatePresentationSpeed`, 3배속에서 1.4배). 컷인 기본 길이도 읽을 수 있게 늘렸다(진입 190·퇴장 140ms, 머무름 380~440ms). 결정타 동작도 고정 2배속 대신 같은 몫을 쓴다.
- 전투 조작 칩을 전장 위쪽에서 **우하단 프로필 줄 바로 위**로 옮기고, 궁극기 **연출 on/off 칩**을 자동 궁극기 위에 새로 세웠다. 연출 칩은 환경설정의 `ultimateCutIn`과 같은 값을 켜고 끈다.
- 카드 선택 반투명 표시가 칩 몸통만 덮어 빠져나온 머리만 밝게 남던 것을 고쳤다. 머리 홈까지 같은 실루엣으로 덮는다(원정 준비 화면의 선택 표시).
- 출격 선택판 개편으로 어긋났던 E2E 진입 좌표(스토리·원정·전투 조작 칩)를 새 자리로 맞췄다.
## v0.27.2 — 2026-08-28

- 궁극기 결정타의 공격·사망·종료 사건은 원래 순서대로 즉시 재생하되, 종료 사건 뒤에는 공격 모션과 확대 복귀를 기다리지 않아 결과 UI와 다음 장면 진행이 바로 이어진다.
- 사망 SD의 760ms 비행은 결과 정산과 분리된 배경 시각 효과로 유지하고, 궁극기 취소는 현재 시전자의 확대·공격만 정리해 사망 연출이나 정산을 중복 실행하지 않는다.

## v0.27.1 — 2026-08-28

- 궁극기 컷인·확대·공격 Puppet·복귀를 하나의 연출 시간축으로 묶었다. 1배속도 빠르게 시작하되 2·3배속은 관절 보간이 순간이동처럼 보이지 않는 상한을 공유한다.
- 공격 전 확대와 Puppet 공격을 동시에 시작하고 복귀는 더 짧게 만들어, 확대를 별도 턴처럼 기다리거나 공격 완료가 늦어지지 않게 했다.
- 스킵은 컷인과 확대만 생략하고 코어의 발사와 사건 소비는 기존 단일 경계를 그대로 거친다. 연출 배율은 코어 시간에 재적용하지 않는다.

## v0.27.0 — 2026-08-28

- 궁극기 연출을 전투 중 즉시 건너뛰는 세 번째 HUD 칩을 추가했다. 배속·자동 궁극기와 같은 홀로그램 테마와 저장 경계를 사용하며, 스킵해도 코어가 만든 공격·피해·사망·종료 사건은 빠짐없이 재생한다.
- 저장 모델의 `presentation.ultimateCutIn`을 제거하고 전투 조작인 `game.skipUltimatePresentation`으로 옮겼다. 과거 저장에서 컷인을 꺼 둔 경우 새 스킵 값을 켜며, 새 필드가 이미 있으면 그 값을 우선하는 마이그레이션으로 하위 호환한다.

## v0.26.13 — 2026-08-28

- 스피노사우루스 렐릭의 표시 이름을 **세이라**에서 **스피나**로 바꾸고, 전투 검증 문구와 E2E 기대값, 에셋 설명 및 관련 문서도 같은 이름으로 통일했다. 저장 호환성을 위해 내부 렐릭 ID(`spino`)와 에셋 키(`seira`)는 그대로 유지한다.

## v0.26.12 — 2026-08-28

- 출격 칸의 **검은 테두리 잔상**을 없앴다. 같은 도형을 조금씩 줄여 여러 번 두르는 옛 비네트(`drawInnerVignette`)는 가로로 긴 판에서 좌우가 세로보다 훨씬 많이 줄어 검은 줄이 여러 겹 어긋나 남는다. 네 변에서 안쪽으로 빠지는 그라데이션 네 장(`drawFrameVignette`)으로 바꿔 어느 비율에서도 고르게 어두워지고 선이 남지 않는다. 원화와 같은 칩 마스크를 나눠 쓴다.
- 케이크 대작전과 현상수배는 맞닿는 대신 16px 여백으로 갈랐다. 마주 보는 변을 곧게 두는 규칙은 그대로라 여전히 한 판을 나눈 것처럼 읽힌다.
- 스토리 칸의 SD를 10% 줄였다(`sdScale`). 개체마다 원화 크기가 달라 혼자 커 보이는 SD만 이 배율로 맞춘다.

## v0.26.11 — 2026-08-28

- 출격 칸의 SD가 캐릭터 카드와 같은 규칙으로 선다. 몸은 칸 안에 갇히고 머리만 윗변 밖으로 빠져나오며(`sdMask`), 발끝을 아래 변 밖에 두어 **상반신 위주**로 보인다. 크기도 1.5배 이상(칸 높이의 1.68배)으로 키웠다.
- SD마다 **복제 그림자**를 한 겹 깔았다. 칸 가운데를 향해 밀고 본체와 같은 동작을 함께 재생한다 — 한쪽만 움직이면 그림자가 딴 자세로 남는다.
- 칸 사이를 76px로 띄우고 전체를 위로 올려 판 상단의 빈 공백을 줄였다.
- 제목을 25% 키우고(46 → 58, 일일 던전 30 → 38), 부제목은 잘린 오른쪽 아래 모서리만큼 안쪽으로 밀어 빗변 밖으로 삐져나오지 않게 했다.
- 두 일일 던전의 SD는 뺐다. 칸이 좁아 SD가 이름 위로 올라와 "케이크 대작전"이 가려졌다 — 지정된 스토리(적1)와 원정(폰투스)만 세운다.
- 머리 띠가 윗변 아래로 깎인 모서리 깊이만큼 더 내려간다. 그러지 않으면 `/`로 잘린 모서리가 머리 옆을 대각선으로 베어 낸다.
- 케이크 대작전과 현상수배는 한 판을 둘로 나눈 것처럼 맞닿는다. 마주 보는 변은 깎지 않고 곧게 두고(`split`), 그 사이만 곧은 선 하나(`|`)가 끊는다.

## v0.26.10 — 2026-08-28

- 출격 선택판을 870×1250 → 980×1240으로 넓히고 다섯 콘텐츠 칸도 함께 키웠다(스토리 800×220, 일일 던전 390×200, 레이드 800×200, 원정 800×230).
- 칸의 글자를 이름 46 · 상태 22로 키우고 **진한 그림자**를 깔아 원화 위에서도 먼저 읽히게 했다. 자리는 SD 반대쪽 아래 구석이다 — 스토리·던전·레이드는 우하단, 원정만 좌하단이다.
- 칸마다 SD가 버튼 윗변 밖으로 빠져나와 선다(카드 그리드의 머리와 같은 결). 스토리는 적1, 케이크 대작전은 적2, 현상수배는 적3, 원정은 오른쪽에 폰투스다. 준비 중인 레이드에는 세우지 않는다. 자리 계산은 `sortieEntrySdSpot` 하나가 맡는다.
- 세워 둔 SD가 2.6초마다 한 기씩만 드문드문 움직인다. 다섯이 동시에 뛰면 무엇을 고르는 화면인지 흐려진다.
- Puppet은 컨테이너 변환을 물려받지 않으므로 SD는 원점에 선 전용 레이어에 화면 좌표로 세우고, 판이 닫힐 때 타이머·Puppet·돌아가기를 함께 거둔다.
- 출격 판의 돌아가기를 판 안쪽 우하단에서 **다른 팝업과 같은 화면 우하단 슬롯**(`BACK_SLOT`)으로 옮겼다.

## v0.26.9 — 2026-08-28

**대부분의 카드 그리드에서 맨 위 머리가 잘리던 원인 세 가지를 모두 고쳤다.** 카드가 머리 모양이 아니라 사각형 구멍에 맞춰 잘리고 있었다.

- 칩 윗변의 머리 구멍이 칩 폭의 0.58뿐이라 뿔·모자·묶은 머리가 구멍의 좌우 변에 깎여 네모난 덩어리로 보였다. 폭을 0.8로 넓히고 높이도 카드 높이의 0.22(최대 54) → 0.26(최대 64)으로 키웠다.
- 잘라내기 시작점(`headroom`)이 0.04이라 원화의 정수리를 늘 몇 px씩 잘라 냈다. 카드에서는 머리 끝이 곧 목록의 윗선이라 0으로 두고 한 줄도 자르지 않는다.
- 전신 정보창의 세로 보정(`portraitOffsetY`, 루카 -34)을 카드에서도 그대로 올려 정수리가 구멍 위로 넘어가 잘렸다. 카드는 위로 올리는 보정을 받지 않는다.
- 직전 버전에서 넣었던 `headOverhang` 잘라내기 보정은 되돌렸다. 머리 관절 위 원화가 구멍보다 크다는 이유로 시작점을 내려, 오히려 머리를 절반 가까이 잘라 냈다.
- 마스크가 있는 목록(도감·발굴)은 첫 줄과 마스크 사이에 `PORTRAIT_GRID_MASK_GAP`(8px)을 둔다. 정확히 맞닿으면 가장자리 한 줄이 깎인다.

## v0.26.8 — 2026-08-28

- 치즈케이크 대작전·현상수배 원본 PNG(각 2MB대)를 `scripts/prepare_backgrounds.py`로 구워 WebP(130KB·217KB)만 남기고 원본을 지웠다. 스크립트가 배경(`background_00N`)뿐 아니라 콘텐츠 원화(`ContentN_00M`)도 함께 굽는다.
- 출격 선택판의 두 일일 던전이 각자의 전용 원화를 칩 실루엣에 클리핑해 세운다. 같은 그림을 나눠 쓰면 나란히 선 두 버튼이 한 콘텐츠의 두 갈래처럼 읽힌다.
- 카드 머리가 칩 구멍의 좌우 변까지 잘려 네모난 덩어리로 보이던 것을 고쳤다. 구멍을 칩 폭의 0.58 → 0.8로 넓히고, 구멍보다 큰 머리는 `headCardFrame`의 `headOverhang`이 잘라내기 시작점을 내려 구멍 안에 담는다. 머리 구멍 자체도 카드 높이의 0.22(최대 54) → 0.26(최대 64)으로 키워 뿔·모자가 들어간다. 그리드 안전 영역은 이 값에서 유도되므로 배치도 함께 따라간다.

## v0.26.7 — 2026-08-28

- 캐릭터 카드 그리드의 **머리 안전 영역**을 공용 순수 규칙으로 세웠다(`src/ui/portraitGrid.ts`). 머리는 칩 윗변 밖으로 빠져나오므로 첫 줄을 마스크나 판 윗변에 붙이면 정수리가 잘린다. 첫 줄 자리와 스크롤 길이는 이제 화면이 눈대중으로 정하지 않고 `portraitGridFirstRowY`·`portraitGridContentHeight`만 쓴다.
- 도감(렐릭 탭)의 첫 줄이 상단 마스크에 4px 물려 머리가 잘리던 것을 고쳤다. 카드 규격도 한 곳으로 모아 미보유 구역과 같은 값을 읽는다.
- 발굴 배치 그리드가 현황의 누적 액자 판과 **같은 윗변**에서 시작한다. 첫 줄은 그보다 머리 돌출만큼 내려 서므로, 눈에 보이는 그리드 윗선(머리 끝)이 액자 판 윗선과 맞아 두 화면이 한 자리를 공유한다.

## v0.26.6 — 2026-08-28

- 발굴 팝업의 `/ 발굴` 머리글이 배경 원화 뒤로 숨던 문제를 고쳤다. Phaser Container는 자식의 depth로 순서를 바꾸지 않고 넣은 순서대로만 그리므로, `build` 뒤에 원화를 얹는 판은 머리글을 다시 올려야 한다. `PopupLayer.raiseChrome`을 공용 경계로 두고 발굴이 본문·원화를 갈아 끼울 때마다 부른다.
- 임무 카드의 달성도 게이지가 흐리게 읽히던 것을 고쳤다. 레이어 순서 문제는 아니었고(카드 면을 먼저 넣어 게이지가 그 위에 있다) 옅은 홈과 채움만으로는 카드 면에서 떨어져 나오지 않는 문제였다. 연구도 게이지와 같이 빈 자리를 짙은 검정으로 누르고 최대치를 흰 선으로 둘렀다.
- 게이지 규칙(`CLAUDE.md`)도 함께 고쳤다. 읽어야 하는 진행도는 판때기 위에서도 검은 홈과 흰 외곽을 켜고, 설정 슬라이더처럼 조작 자체가 값을 말하는 줄만 켜지 않는다.

## v0.26.5 — 2026-08-28

- 발굴 배치가 새 화면을 열지 않는다. 슬롯을 누르면 팝업이 통째로 갈리는 대신 **아래 칸만** 현황 요약(누적 액자·광고·수확)에서 보유 렐릭 그리드로 바뀌고, 위 칸의 슬롯과 SD는 그대로 서 있다. 수확이 서 있던 자리를 그대로 **배치**가 물려받고 **취소**는 배치 중에만 그 왼쪽에 나타났다 사라진다.
- 한 칸을 채우면 선택이 저절로 다음 빈 칸으로 넘어간다(`nextExcavationSlot`). 세 번의 배치가 카드→칸→카드로 끊기지 않는다. 방금 비운 칸에서는 그대로 머문다.
- 슬롯 줄을 배경 원화의 발굴 벽면 가운데로 조금 내리고, SD가 서는 바닥도 슬롯 자리에서 유도해 둘이 따로 놀지 않게 했다.
- 발굴 SD가 땅으로 꺼져 보이던 모션을 통통 뛰는 도약으로 바꿨다. 원인은 두 가지였다 — Puppet(Mesh)의 원점은 이미지 한가운데인데 발끝 좌표(`groundY`)로 y를 움직여 제 키의 절반만큼 내려갔고, 같은 이징을 되감는 yoyo라 내려올 때도 감속했다. 이제 실제로 선 자리를 기준으로 포물선을 그린다.
- 편성이 바뀔 때 이전 SD를 치우지 않아 옛 자리에 SD가 남던 문제도 함께 고쳤다.

## v0.26.4 — 2026-08-28

- 원정 지도에서 연 적 정보창의 전신 원화가 스킬 아이콘·수치 판 앞으로 튀어나오던 문제를 고쳤다. 원화 깊이를 스테이지와 같은 1001로 낮춰 정보창의 판 아래에 서게 했다(3001은 정보창의 chrome·스킬 층보다 위였다).
- 증강 선택 팝업이 아군 얼굴을 다시 그리지 않는다. 지도 하단의 생존 HUD가 곧 대상 선택지이므로 판을 그 위로 올리고, 고르는 동안만 HUD를 암전 위로 띄워 입력을 연다. 같은 아군이 한 화면에 두 벌로 보이던 중복과, 두 곳에서 따로 늙던 카드 규격을 함께 없앴다.
- 증강 후보 세 장의 글자를 키우고 범위·효과를 두 줄로 갈라 각각 제 색과 본문 밝기로 읽히게 했다. 판도 조금 더 불투명하다.
- 협업 규칙: E2E는 기본으로 돌리지 않고 필요한 한 편만 지정해 돌리며, 전체 E2E는 사람에게 먼저 물어본 뒤에 시작한다는 규칙을 `CLAUDE.md`에 명시했다. 한 편이 1~2분이라 확인 없이 돌리면 다른 작업이 그만큼 멈춘다.
- 원정 지도의 생존 HUD를 인게임 전투와 같은 크기·가로 기준선으로 맞추고, 생존을 알리던 노란 발광을 뺐다 — 그 발광은 전투에서 "궁극기가 찼다"는 뜻이라 지도에서 같은 색을 쓰면 두 뜻이 섞인다. 배치표(`EXPEDITION_LAYOUT`)의 `relics` 구역과 증강 요약 칩 자리도 실제 bounds에 맞춰 옮기고, 쓰이지 않던 `sortie` 구역은 하단 행동선으로 합쳤다. 저장 마이그레이션은 필요 없다.

### v0.26.3 — 2026-08-28

**개발 빌드에서 실제 원정 보스 진입 흐름을 빠르게 검수한다.**

- 준비 화면에 개발 전용 20층 바로가기를 추가했다. 바로가기는 실제 지도 노드의 적 미리보기와
  출격 버튼을 거쳐 기존 보스 전투 DTO로만 진입한다.
- 매니저가 유효한 지도·3기 편성·19층 도달점·보스 멱등 ID를 한 저장으로 준비하며 production
  경계에서는 호출 자체를 거부한다. 정식 주간 점수 제출과 정산 규칙은 그대로 둔다.

## v0.26.2 — 2026-08-28

- 전투 프로필 생성이 `Cannot read properties of undefined (reading 'syncMask')`로 씬 진입을 통째로 막던 문제를 고쳤다. Phaser Container 생성자가 내부에서 `setPosition`을 부르는데, 그 시점에는 재정의한 `syncMask`가 참조하는 `card`가 아직 만들어지기 전이었다. 카드가 없으면 조용히 넘어가도록 해 생성자 안의 첫 호출을 무해하게 만든다.

## v0.26.1 — 2026-08-28

- 전투·원정 지도·20층 보스의 카드, 체력, 야성, 활성·사망 표현을 공용 전투 프로필 프리팹으로 통합해 화면마다 달랐던 글자 기준선과 게이지 간격을 하나로 고정했다.
- 원정 지도는 프리팹 내부 치수를 재정의하지 않고 전체 배율만 조절하며, 세 칸의 실제 bounds와 하단 출격 버튼 비겹침을 순수 배치 테스트로 보호한다. 저장 마이그레이션은 필요 없다.

## v0.26.0 — 2026-08-27

**원정 20층에 전용 보스 폰투스를 배치한다.**

- 리바이어던 멜빌레이 모티브의 폰투스 정적 정의와 네 스킬 표시 정보를 추가하고, 플레이어 획득 풀과는 분리했다.
- 전신·SD ZIP의 원본 크기와 알파 경계를 측정해 전용 레지스트리 및 중앙 프리로드에 연결했다.
- 20층 boss만 폰투스 단독 편성을 반환하며 다른 boss 층은 기존 3인 fallback을 유지한다. 저장 마이그레이션은 필요 없다.

## v0.25.0 — 2026-08-27

**원정 불사 보스를 단일 실시간 난전과 서버 재현 점수 흐름으로 통합.**

- 보스도 공용 `skirmish`의 이동·피격·사망·궁극기·배속 흐름을 사용하며, 옵션 상태가 생존 시간, 누적 실제 피해, 단계와 리미트를 함께 계산한다. 적 HP 소진은 승리가 아니고 아군 전멸만 종료다.
- 별도 보스 씬 진행기를 제거하고 공용 SD/HUD 위에 실시간 점수와 서버 최고 기록만 고정했다.
- 제출에는 피해량 대신 실제 전투에서 발생한 동작 시각·종류만 담아 서버가 편성과 성장 상태로 점수를 다시 계산하며, 요청 ID 멱등 처리를 유지한다. 저장 마이그레이션은 필요 없다.

## v0.24.5 — 2026-08-27

- 관찰 일지의 표본 설명·일기 내용·문답·선택 버튼 사이 여백을 넓히고 팝업과 원화 클립을 함께 늘려, 확장된 세로 흐름에서도 배경 모서리가 튀어나오지 않게 했다.
- 문답 버튼을 두껍게 만들고 답변을 가운데 정렬했으며 글자 크기와 강조 두께를 높여 선택지를 빠르게 비교할 수 있게 했다.

## v0.24.4 — 2026-08-27

- 토리카의 지각 붕괴 방어력 계수를 180%에서 300%로 상향하고 전투 회귀 기대값도 같은 단일 스킬 정의를 기준으로 갱신했다.
- 원정 지도 하단 편성을 인게임 프로필 카드와 체력·야성 게이지 문법으로 통일했다. 체력 문구는 성장 후 최대 체력과 저장된 잔여 비율을 결합한 실제 수치이며, 전투 전 야성은 0으로 표시한다.
- 노드 정보창을 스토리 적 편성 판처럼 간결하게 바꾸고 적 카드를 공용 적 상태창에 연결했다. 출격은 정보창에서 분리해 캐릭터 그리드 아래, 돌아가기와 같은 하단 행동선 중앙에 고정했다.

## v0.24.3 — 2026-08-27

- 토리카 폭주의 2초 기절을 제거하고 기절 저항과 별개인 0.1초 경직으로 교체했다. 3명을 맞혀 궁극기보다 효율이 높아지던 방어력 추가 피해는 60%에서 15%로 낮추고 공격 속도 20% 증가를 추가했다.
- 폭주 설명은 현재 방어력을 15% 피해로 환산한 값을 동적으로 보여 준다. 일반 공격·궁극기의 피해량 요약도 기본 능력치가 아니라 레벨·돌파·장착 룬이 반영된 최종 능력치로 계산하도록 수정했다.

## v0.24.2 — 2026-08-27

- 토리카의 폭주 기본 공격에 방어력 60% 물리 추가 피해를 적용하고, 주 대상과 주변 적 모두에게 2초 경직을 적용하도록 전투 규칙을 구체화했다. 방어력 계수는 방어형 성장을 보상하되 방어력 180% 계수인 궁극기보다 효율을 낮게 유지하려고 60%로 책정했다.
- 모든 폭주 설명을 특성의 구조화된 수치에서 동적으로 생성해, 밸런스 수치를 바꾸어도 도감 문구와 실제 전투가 함께 갱신되게 했다.

## v0.24.1 — 2026-08-27

- 관찰 질문을 공용 풀과 렐릭별 풀로 분리하고, 토리카의 식욕·몸무게·보호 행동을 설정에 맞춘 전용 질문과 반응으로 추가했다.
- 렐릭과 UTC 날짜를 함께 쓰는 단일 결정 함수로 화면 표시와 저장 검증을 통일했다. 기존 기록은 원문·답변 스냅샷과 공용 질문 ID를 그대로 유지하므로 저장 마이그레이션은 추가하지 않았다.

## v0.24.0 — 2026-08-27

- 토리카를 유치원생 또래의 트리케라톱스 해츨링으로 구체화하고, 기원 연대·복원 연차·성장 단계·키·몸무게와 성격 관찰 기록을 일지에 추가했다.
- 폭주를 다른 스킬과 같은 아이콘 중심 상세창으로 통일하고, 야성·경직처럼 별도 규칙이 있는 말은 눌러 설명을 열도록 했다.
- 토리카의 패시브·일반 공격·궁극기 명칭과 설명을 새 설정에 맞추고, 도감 기준 실제 피해량과 산출 능력치를 함께 표시하도록 피해 미리보기를 확장했다.

## v0.23.0 — 2026-08-26

- 원정 노드별 적 ID·레벨·속성 정의를 정보창과 실제 전투가 함께 읽도록 연결해 임시 스토리 편성 의존을 제거했다. 새 적 데이터는 중앙 편성표만 교체하면 두 화면에 동시에 반영된다.
- 원정 진입 일러스트의 마스크를 팝업 내부 월드 좌표에 동기화하고, 스토리 입구에는 Content1, 원정 편성에는 Content2 필드 원화를 적용했다.
- 증강 후보의 선택 연출을 단일 선택으로 정리하고 전체 증강도 하단 확정 전에는 저장되지 않도록 통일했다.
- 원정 승리·패배·포기·보스 완료의 서버 확정 전리품을 공용 보상 팝업으로 표시하며, 이전 원정 DTO가 스토리 전투에 재사용되지 않도록 전투 진입 입력을 초기화했다.

## v0.22.1 — 2026-08-26

- 임무 연구도 게이지의 중심/왼쪽 기준 혼용을 바로잡아 팝업 좌측 돌출을 막고, 기울어진 발굴 버튼의 알림점에 같은 좌표 변환을 적용했다.
- 원정 증강 후보·대상 안내·초상·확정 조작을 독립 세로 구역으로 재배치해 세 번째 후보와 대상 UI가 겹치지 않게 했다. 이후 UI 작업은 공용 프리팹과 다른 화면의 안전 영역을 함께 분석하고 실제 bounds와 모바일 캡처로 검수하도록 협업 규칙을 보강했다.

## v0.22.0 — 2026-08-26

- 20층 보스 노드를 일반 난전과 분리된 불사 보스 모드에 연결했다. 런의 현재 HP와 전체 증강을 시작 스냅샷에 적용하고, 단계별 광역 피해로 전멸할 때까지 클라이언트에는 행동만 기록한다.
- 서버가 런 편성·성장·증강·현재 HP로 행동열을 재현해 피해 점수를 확정한 뒤, 같은 점수를 노드에 반영하고 `completed` 정산까지 직렬화한다. 제출 ID와 정산 ID는 전투 진입 전에 저장해 중간 종료 재시도에도 중복 점수와 지급을 막았다.
- 결과 영수증에 이번 점수, 주간 최고 갱신, 누적 점수, 순위 변화와 실제 지급된 런 재화를 함께 표시한다. v23 저장은 보스 멱등 키를 빈 값으로 보충해 v24로 마이그레이션한다.

## v0.21.0 — 2026-08-26

- 원정 노드 완료를 서버 API 경계로 옮기고, 노드 종류·런 누적량·주입 난수로만 보상을 계산해 클라이언트 재화 조작과 재요청 중복 지급을 막았다.
- 전투 배율과 보석 보장 보물 표를 분리하고, HUD에 방금 증가분과 런 상한을 다른 색·문구로 표시해 경로 선택 결과를 즉시 읽게 했다.

## v0.20.2 — 2026-08-26

- 원정 증강 선택을 승리 보상에서 전투 진입 준비로 옮기고, 저장된 후보가 남아 있으면 지도 입력보다 먼저 복원해 후보 재추첨과 건너뛰기를 막았다.
- 증강 후보에 이름·등급·전체/개인 범위·효과 수치를 함께 표시하고, 개인 증강은 서버가 지정한 세 렐릭 중 PortraitCard 발광으로 대상을 고른 뒤 매니저에서 확정하도록 바꿨다. 무리 전투의 두 번째 선택은 같은 작업판으로 이어진다.
- 하단 축약 증강 행을 누르면 전체 확정 목록과 개인 대상을 펼쳐 볼 수 있는 상세 쪽지를 추가했다.

## v0.20.1 — 2026-08-26

- 원정 경로와 입력면을 같은 스크롤 지도 프리팹으로 분리하고, 큰 층 간격·드래그/휠 경계·현재 도달 층 자동 포커스를 순수 배치 계산과 함께 추가했다. 노드 상태는 테두리 대신 크기·발광·명도로 구분해 홀로그램 테마를 유지한다.

## v0.20.0 — 2026-08-25

- 원정 노드 보상을 런 임시 보상으로 보관하고, 정상 종료와 포기를 동일한 서버 원자 정산으로 지갑 상한까지 이전하도록 바꿨다. 런 ID와 정산 ID를 함께 저장해 재전송과 다른 ID를 이용한 중복 지급을 막는다.
- 기존 광고 검증 슬롯에 빠른 원정을 추가했다. 서버가 현재 또는 직전 주 최고 점수의 25%만 계산하며 기준 점수가 없거나 광고가 실패하면 지급하지 않고, UTC 일일 2회·주간 5회로 제한한다.
- 저장 식별자 도입 전 원정 런은 맵 시드 기반 런 ID와 빈 정산 ID로 마이그레이션한다.


### v0.25.0 — 2026-08-27

**원정 불사 보스를 단일 실시간 난전과 서버 재현 점수 흐름으로 통합.**

- 보스도 공용 `skirmish`의 이동·피격·사망·궁극기·배속 흐름을 사용하며, 옵션 상태가 생존 시간,
  누적 실제 피해, 단계와 리미트를 함께 계산한다. 적 HP 소진은 승리가 아니고 아군 전멸만 종료다.
- 별도 보스 씬 진행기를 제거하고 공용 SD/HUD 위에 실시간 점수와 서버 최고 기록만 고정했다.
- 제출에는 피해량 대신 실제 전투에서 발생한 동작 시각·종류만 담아 서버가 편성과 성장 상태로
  점수를 다시 계산하며, 요청 ID 멱등 처리를 유지한다. 저장 마이그레이션은 필요 없다.

### v0.19.0 — 2026-08-25

**불사 원정 보스의 검증 가능한 주간 피해 경쟁과 누적 보상을 추가한다.**

- 보스는 HP 승리 없이 받은 피해를 점수로 누적하고, 시간 단계별 광역 공격과 종말 단계로 반드시 아군 전멸 시 종료하도록 순수 규칙과 정적 밸런스를 분리했다.
- 클라이언트는 피해 숫자 대신 공격 동작열만 제출하며 API가 서버 편성 능력치로 재현해 전멸 순간 점수, 최고 기록, 주간 누적을 확정한다.
- 월요일 00:00 UTC 초기화, 동점 최초 달성 우선, 비정상 점수 거부 정책과 최고 점수·제출·누적 보상·리더보드 계약을 추가했다.
- 누적 보상은 단일 단계 표와 서버 수령 ID로 중복 지급을 막고, 가방 팝업에 붙어 있던 중복 돌아가기를 없애 로비 우하단 버튼 하나로 닫기 수명을 연결했다.

### v0.18.0 — 2026-08-25

**전투 종류별 원정 증강 제안과 재접속 고정 선택을 추가한다.**

- 증강의 ID·이름·등급·개인/전체 범위·효과 수치·중첩 상한을 정적 데이터로 모아 보상 규칙과 전투 효과가 같은 정의를 읽게 했다.
- 일반은 일반 풀 1회, 무리는 일반 풀 2회 연속, 정예는 고급 풀 1회로 제안하며 휴식·보물은 증강을 포기하는 경로 고점 손실을 그대로 유지한다.
- 제안 안의 중복, 적용 불가능한 개인 대상, 중첩 상한을 순수 규칙에서 제거하고 개인 효과는 생존/부활 가능 렐릭을 명시적으로 고르게 했다.
- 제안 seed·후보·선택 결과·개인 대상을 런 저장에 함께 보존해 재실행 재추첨을 막고, 원정 화면 하단에는 선택 결과를 비대칭 칩과 `+N` 축약으로 표시한다.

### v0.17.0 — 2026-08-25

**원정 전투의 가변 편성, 지속 체력과 범위형 증강 규칙을 추가한다.**

- 난전 생성기를 팀별 1~5기로 일반화하면서 기존 3대3 시작 좌표는 보존하고, 원정 저장 HP·생존 상태를 시작값으로 받아 종료 결과 DTO로 되돌리도록 했다.
- 일반 3기·강화된 대형 정예 1기·무리 5기의 정적 밸런스와 휴식 회복/조건부 부활 규칙을 데이터에 모았다.
- 전체/지정 렐릭 공격력 및 매 공격 출혈을 순수 증강 모델로 분리하고 기존 피해 공식과 단일 출혈 슬롯에 연결해 계산과 상태이상을 복제하지 않았다.
- 전멸 시 전투와 런을 종료하며 배치, 지속 HP, 전멸, 효과 범위, 출혈 중첩 및 휴식 부활 회귀 테스트를 보강했다.

### v0.16.0 — 2026-08-25

**저장 seed로 복원 가능한 20층 원정 갈림길 생성 규칙을 추가한다.**

- Phaser 비의존 원정 맵 생성기가 외부 RNG를 받아 1~19층의 여러 갈래와 모든 경로가 합류하는
  20층 단일 보스를 만들며, 각 노드는 고유 ID·층·열·선행·후행 ID를 함께 보존한다.
- 전투·휴식·보물 가중치, 연속 제한과 모든 경로의 최소 전투 수를 보장하는 필수 전투 층을
  `src/data/expedition.ts` 한 표로 모아 이후 밸런스 조정이 생성 규칙에 흩어지지 않게 했다.
- 도달 불가, 끊긴 경로, 역행·건너뛴 층과 연결 역참조를 찾는 검증 함수 및 고정 seed 회귀 테스트를
  추가해 앱 재실행 복원과 20층 보스 불변식을 보호한다.

### v0.15.0 — 2026-08-25

**출격 선택판과 주간 원정 준비·이어하기 저장 경계를 추가한다.**

- 로비의 기존 원정 버튼을 추후 PvP용 결투 입구로 바꾸고, 출격을 누르면 로비가 은은하게 남는
  선택판에서 스토리와 원정을 고르도록 분리했다.
- 신규 원정 화면에서 보유 렐릭 정확히 3기를 선택하며, 선택 검증과 진행 중 스냅샷 저장은
  `ExpeditionManager` 한 곳만 수행한다. 진행 중 원정은 새 편성으로 덮지 않고 이어하기로 연다.
- 저장 버전을 v22로 올렸다. v21 이하 저장은 원정 주차·횟수·최고 점수·진행을 빈 상태로 보충하고,
  첫 조회에서 현재 UTC 주차로 정규화한다.
- 로비 원정 항목에는 주간 플레이 횟수, 진행 여부, 최고 점수, 빠른 원정 가능 여부만 짧게 표시한다.

### v0.14.6 — 2026-08-25

**임무 진입점을 오른쪽 편의 레일에서 왼쪽 콘텐츠 가장자리로 분리한다.**

- 임무 버튼을 프로필·홍보와 하단 교류·발굴 사이의 왼쪽 가장자리로 옮겨 중앙 캐릭터 무대와
  오른쪽 무역·우편·친구·가방 레일의 역할을 분리했다.
- 임무 전용 각진 체크리스트 클립보드 글리프를 추가해 가방의 두루마리 표식과 구분했다.
- 임무·원정·출격·하단 내비게이션 입력 bounds와 최소 간격을 단위 테스트로 고정하고 E2E 클릭
  좌표를 새 진입점에 맞췄다.

### v0.14.5 — 2026-08-25

**임무 팝업의 탭·연구도·목록 영역을 충돌 없는 한 레이아웃으로 정리한다.**

- 흩어진 Y 좌표를 영역별 상수로 모으고 연구도 액자와 게이지를 탭 아래로 내려 일정한 안전 여백을
  확보했다. 첫 임무 카드도 연구도 라벨 아래에서 시작하도록 맞췄다.
- 어두운 팝업에서도 게이지 최대 범위가 읽히도록 기존 HoloBar의 얇은 흰 외곽선을 적용했다.
- 보상 액자 외곽선의 반지름까지 포함한 탭 충돌 단위 테스트와 일일·주간 탭별 E2E 스크린샷을 추가했다.

### v0.14.4 — 2026-08-25

**도감의 보유·미보유 카드 전체를 경계가 있는 관성 스크롤로 묶는다.**

- 카드·구분선·미보유 제목을 하나의 콘텐츠 컨테이너로 옮기고 제목/정렬 아래부터 BottomNav 위까지만
  보이는 고정 기하 마스크를 적용해 목록이 조작 영역을 침범하지 않게 했다.
- 포인터 드래그·휠·모바일 관성을 하나의 높이 기반 clamp 경로로 통합하고, 정렬/씬 종료 시 위치와
  입력 핸들러를 초기화한다. 부모 이동을 상속하지 않는 PortraitCard 내부 마스크도 계속 동기화한다.
- 상단에는 기존 홀로그램 유리 페이드만 추가하고 하단은 BottomNav 페이드를 그대로 사용해 중복 판을
  만들지 않았다. E2E는 상·하단 제한과 드래그/휠, 탭 입력 분리를 검증한다.

### v0.14.3 — 2026-08-25

**적 1~3번 정보창 일러스트를 중심 관절 기준으로 다시 정렬한다.**

- ZIP 안 WebP 헤더를 직접 확인해 1번은 2000×2828, 2번은 1086×1449, 3번은 2000×2833으로
  실제 픽셀 크기를 바로잡고 기존 알파 경계도 같은 좌표계로 환산했다.
- 잘못된 캔버스 크기 때문에 1번이 과도하게 확대되고 2번이 우측 하단으로 밀리던 원인을 제거했다.
  개체별 관절 배율이나 임의 축소값을 덧대지 않고 다른 캐릭터와 동일하게 `중심1`을 기준으로 둔다.

### v0.14.2 — 2026-08-25

**도감 즐겨찾기를 유대 순서로 모으고 카드에 전용 표식을 더한다.**

- 보유 즐겨찾기를 선택 정렬보다 앞에 합성하고, 여러 즐겨찾기는 유대 레벨·경험치 내림차순으로
  정렬하되 동률에는 현재 도감 기준을 유지하는 순수 비교 함수를 추가했다.
- 미보유 구역은 즐겨찾기 대상에서 제외해 기존 하단 구역과 선택 정렬을 보존했다.
- 희귀도 로마자 아래에 각진 그림자의 즐겨찾기 전용 별표를 추가하고, 정보창을 닫을 때 카드 표식과
  위치를 함께 다시 조립하도록 바꿨다.
- 즐겨찾기 우선·유대 순서·동률 fallback·해제 복귀를 단위 테스트로 고정했다.

### v0.14.1 — 2026-08-25

**핵심 콘텐츠에서 설정을 열고 원래 위치로 안전하게 돌아간다.**

- 고고학·렐릭·연구소·상점 TopBar에 설정 진입을 연결하고, 콜백 없는 설정 아이콘은 입력면을
  만들지 않도록 바꿨다.
- 설정 반환 씬을 허용 목록으로 검증하며 상점 반환 데이터는 등록된 섹션만 복사한다. 설정 탭을
  바꿔 씬이 재시작되어도 검증된 반환 경로를 유지한다.
- 네 핵심 콘텐츠의 설정 왕복과 상점 섹션 복원을 모바일 E2E로 고정했다.

### v0.14.0 — 2026-08-25

**알림 판정과 붉은 점 프리팹을 공용 계층으로 모은다.**

- 임무 보상·발굴 상한·친구 요청·신규 이벤트·우편 키와 순수 표시 조건을 추가했다.
- API 상태 합성과 변경 이벤트를 `NotificationManager` 한곳에 두어 씬의 중복 판정을 없앴다.
- 숫자 배지를 붉은 원·흰 외곽선·짙은 그림자의 공용 점으로 교체하고 씬 종료 가드를 뒀다.
- 받은 편지함 데이터가 아직 없는 항목은 임의로 켜지 않고 0을 반환하는 API 계약부터 마련했다.

### v0.13.0 — 2026-08-25

**룬·지갑·중첩 아이템을 한 가방에서 보는 인벤토리 시스템을 추가한다.**

- 정적 아이템 카탈로그와 판별 가능한 소비 효과를 도입하고, 룬 인스턴스·지갑·중첩 스택의
  저장 소유권은 분리한 채 표시 모델에서만 합성했다.
- 저장 버전을 v20으로 올렸다. v19 이하 저장은 기존 지갑과 룬을 그대로 보존하며 빈 중첩
  인벤토리를 보충하고, 신규 스택은 ID·양·최대 중첩을 검증한다.
- 소비품 사용은 FakeServer가 보유량·양의 정수 수량·스테미나 상한을 검증하고 효과 적용·차감·
  저장을 한 처리로 확정하도록 했다.
- 로비 오른쪽 레일에 가방을 추가하고 기존 홀로그램 팝업 위에 네 카테고리 탭과 실제 행 수 기반
  마스크 세로 스크롤 격자를 배치했다. 룬은 기존 정보 쪽지, 재화는 읽기 전용 상세, 소비품은
  확인과 서버 결과로 이어진다.

### v0.12.6 — 2026-08-25

**개발 메모처럼 읽히는 화면 문구를 걷어 내고 설정의 좁은 화면 배치를 보완한다.**

- 무역·상점 제목 아래의 기능 풀이 부제와 연구소의 재화 희소도·복원 방식 설명을 제거했다.
  픽업 대상, 비용, 확률처럼 플레이어의 선택에 필요한 정보는 그대로 유지한다.
- 기능명 반복, BM·운영 의도, 구현 상태 같은 개발 설명은 인게임이 아니라 주석과 기획 문서에만
  두도록 UI 문구 검수 원칙을 `CLAUDE.md`에 명시했다.
- 환경 설정의 마지막 탭 이름을 짧게 다듬어 텍스트 확대 시 탭끼리 겹치지 않게 하고, 지원 화면의
  계정 패널과 고객지원·데이터 패널 사이에 안전 여백을 보장했다.

### v0.12.5 — 2026-08-25

**획득 확인창과 발굴 현황을 한 양식으로 묶고, 보이지 않던 발굴 SD를 화면에 세운다.**

- 공용 보상창의 암전을 짙은 검정으로 올려 뒤 발굴 현황과 확실히 갈랐다. 얕은 암전은 두 화면이
  한 겹으로 읽혀 무엇을 확인하는 창인지 흐려졌다.
- 확인 안내를 팝업 안이 아니라 **화면 하단**의 반투명 굵은 글자로 빼고 바깥 누름으로도 닫히게
  했다. 닫을 수 있는 곳이 화면 전체라는 사실은 팝업 밖에서 말해야 읽힌다. 같은 이유로 오른쪽
  위 X를 없앴다(중복 조작).
- 액자 수량에서 `+`를 빼고 검은 테두리를 둘렀다. 증가량인 것은 제목이 이미 말하고, 테두리가
  없으면 수가 액자 선에 붙어 읽힌다.
- 발굴 현황의 재화 칸(`ExcavationCurrencyFrame`)을 보상창 액자와 같은 실루엣·외곽선·비네트로
  다시 만들고 누적량을 액자 오른쪽 아래에 같은 양식으로 얹었다. 시간당 생산량은 `1.2K/H`처럼
  시간 단위를 약자로만 적고, 시간당 1을 못 채우는 재화만 `/D`로 바꿔 0으로 죽지 않게 했다.
- **발굴 SD가 화면에 나오지 않던 버그를 고쳤다.** Puppet은 자기 화면 좌표로 직접 그리는 GPU
  개체라 컨테이너의 이동·배율을 물려받지 않는다. 팝업 본문 안에 넣으면 국소 좌표 그대로 화면
  바깥에 그려진다. SD만 원점에 선 전용 레이어(팝업 판 바로 위 depth)로 옮기고 좌표도 화면
  좌표로 넘긴다. 함께 들어가 있던 표시 크기·가시성 재검증은 원인이 아니었으므로 텍스처 확인만
  남겼다.
- 발굴 뒤로가기를 좌하단 예외에서 공용 우하단 자리(`BACK_SLOT`)로 되돌렸다. 화면을 벗어나는
  버튼의 자리는 화면마다 다르지 않아야 한다.
- 배경 원화를 까는 팝업이 제목을 덮던 문제를 `PopupLayer`에서 고쳤다 — 내용을 채운 뒤 머리글을
  한 번 더 맨 위로 올린다.

### v0.12.4 — 2026-08-25

**발굴 슬롯 입력과 SD 비동기 fallback을 모바일에서 안전하게 분리한다.**

- 세 슬롯에 카드·Puppet과 독립된 210×245 최상단 입력면을 두어 현황과 편집이 같은 누름·확대·
  드래그 취소 규칙을 사용하고, 발굴 닫기는 팝업 X 대신 화면 좌하단 뒤로가기 아이콘으로 옮겼다.
- SD는 새 render마다 독립 generation을 발급하고 실제 루트·텍스처·표시 크기를 검증한 뒤에만
  fallback 카드를 숨긴다. 실패·늦은 완료·닫힘 직후 완료는 카드를 유지하며 Puppet을 한 번만 폐기한다.
- 주입 가능한 로더 경계의 성공·실패·지연 수명 단위 테스트와, SD 완료 뒤 세 슬롯 선택 및
  1.15 텍스트 확대 모바일 안전 영역을 확인하는 E2E를 추가했다.

### v0.12.3 — 2026-08-25

**발굴 수확 보상을 모바일 한 줄 결과창으로 또렷하게 확인한다.**

- 보상 액자를 넓고 낮은 한 줄로 바꾸고 1~4개는 가운데 정렬, 그 이상은 가로 드래그하도록 해
  보상 수가 달라도 화면 안전 여백과 아이콘 크기를 함께 지킨다.
- 수량을 액자 오른쪽 아래 안으로 옮기고 비네트와 짧은 그림자를 더해 아이콘을 가리지 않으면서
  밝은 원화 위에서도 읽히게 했다.
- 공용 팝업에 제목 크기와 암전 강도를 추가하고 발굴 결과에는 큰 제목과 0.18의 얕은 암전을 써,
  뒤 현황의 맥락은 남기되 확인 중 입력은 차단한다.
- 모바일 E2E가 보상창 열림, 뒤 로비 입력 차단, 본문 확인 뒤 발굴 현황 복귀를 연달아 검증한다.

### v0.12.2 — 2026-08-25

**발굴 수확 확인창이 현황 연출을 가리던 병합 회귀를 바로잡는다.**

- 획득 재화는 작은 액자와 수량만 남기도록 확인창 높이와 여백을 줄였다. 발굴 현황창 위에
  둘째 암전을 깔지 않아 발굴장 배경과 확정 편성 SD가 수확 확인 중에도 그대로 보인다.
- 현황 UI 재배치에서 수확 버튼이 하단 중앙으로 옮겨졌지만 과거 좌표를 누르던 E2E를 실제 배치와
  맞춰, 병합 후 확인창 회귀 검증이 다른 영역을 누르지 않게 했다.

### v0.12.1 — 2026-08-25

**연구소 획득 연출과 배치형 자원 발굴의 이름을 분리한다.**

- 연구소 전용 상태 기계와 첫 단계를 `researchPresentation`·`research`로 바꾸고 연구소 균열음의
  의미 키도 같은 계열로 맞췄다. 저장 데이터와 서버 DTO에는 이 연출 이름이 없어 마이그레이션은
  필요하지 않으며, 직렬화 계약인 `idleExcavation`은 배치형 자원 발굴 이름으로 그대로 유지한다.
- 확률 안내의 과거 각성 문구를 개체별 렐릭 파편, 파편을 소비하는 한계 돌파, 별 V 이후 공용
  DNA 조각 전환이라는 현재 규칙으로 고쳤다.

### v0.12.0 — 2026-08-24

**로비를 유지하는 방치 발굴 현황 팝업을 연결한다.**

- `PopupLayer` 한 장에서 서버 정산 중·실패 재시도·누적 자원·시간당 생산·보관 한도 시간을
  보여 주며, 닫을 때 로비와 애착 렐릭을 다시 만들지 않는다.
- 배치 슬롯의 SD Puppet과 실제 누적 비율로 걷히는 자원 더미 마스크를 연결하고, 저사양 및
  모션 감소 설정에서는 SD를 정지시킨다.
- 좁은 모바일에서 팝업 안전 영역, 중복 열기 방지, 뒤 로비 입력 차단을 E2E로 고정했다.

### v0.11.0 — 2026-08-24

**버전 문서를 도입하고 로딩 화면에 버전을 표기.**

- 개발 로그를 `VERSION.md` 하나로 모았다. 그동안 "무엇을 했다"가 커밋 제목에만 남아 있어
  왜 그렇게 바꿨는지가 남지 않았다 — 규칙은 `CLAUDE.md`, 이력은 이 문서로 갈랐다.
- 타이틀(로딩) 화면 좌측 하단에 `v0.11.0`을 표기한다. 로딩 화면은 어느 빌드에서나 반드시
  지나는 화면이라, 스크린샷 한 장으로 어느 빌드인지 알 수 있다.
- 버전 문자열의 단일 출처는 `package.json`이다. 화면은 그 값을 읽기만 한다.
- `v1.0.0`을 **정식 런칭 번호로 예약**했다. 개발 중에는 저장 비호환 변경이라도 부 자리만
  올린다 — 그때마다 주 자리를 올리면 정작 런칭에서 쓸 번호가 남지 않는다.

### v0.10.0 — 2026-08-24

**전투 화면을 읽히게 다시 짠다 — 게이지 둘, 카드가 말하는 궁극기.**

- 전투 프로필 게이지를 체력·폭주 둘로 줄였다. 궁극기 충전은 바 대신 **카드 그림 자체**가
  12시부터 시계 방향으로 밝아지며 말한다. 바가 셋이면 어느 것이 급한지 읽히지 않았다.
  가림막은 도형이 아니라 BitmapMask다 — 도형으로 자르면 칩 밖으로 나온 머리까지 검게 찬다.
- 게이지와 수치를 스르륵 따라오게 했다(`stepMeters`). 깎이는 순간이 보이지 않으면 얼마나
  아팠는지 알 수 없고, 궁극기 연출이 끝나는 순간 값이 점프해 보였다.
- 머리 위 체력 바를 `UnitHealthBar` 한 곳으로 모았다. 밝은 배경 원화 위에서 사각형 두 장은
  묻혀서, 그림자·깎인 몸통·칸 구분선·왼쪽 끝 빗금 네 겹을 함께 쓴다.
- 체력은 능력치와 같은 연두, 야성은 붉은 계열로 묶고 게이지 옆 수치도 같은 색을 쓴다.
  어느 수가 무엇인지 색으로 먼저 읽히게 하려는 것이다.
- 게이지에 최대치 흰 테두리와 빗금 칸을 넣었다(`HoloBar`의 `outline`·`ticks`). 채움만으로는
  "어디까지가 이 게이지인가"가 배경 원화 위에서 보이지 않았다. 판때기 위 게이지는 켜지 않는다.
- 배경 원화 7·8·9를 WebP로 굽고 원본 PNG는 저장소에서 뺐다. 한 장이 10MB에 가까워 그대로
  두면 첫 로딩이 통째로 그만큼 늘어난다.
- 적 셋의 스킬 일러스트를 아군과 같은 파이프라인으로 구워 넣었다.

### v0.9.0 — 2026-08-23 ~ 2026-08-24

**등급·별·전투력 표기를 정리 — 별은 돌파, 등급은 바탕색.**

- 별 다섯 칸을 **로마자 한 글자**(`I`~`V`)로 바꿨다. 칸을 세어야 알 수 있었고, 카드마다
  다섯 개가 반짝여 얼굴보다 먼저 읽혔다. 희귀도는 대신 **카드 바탕색**이 맡는다.
- 별의 뜻을 성급에서 **한계 돌파 단계**로 바꾸고 중복 획득에 파편을 도입했다. 중복을
  "각성"과 "돌파" 두 축으로 나누면 무엇을 키우는 중인지 읽히지 않아 하나로 합쳤다.
  돌파 버튼은 레벨 칸이 아니라 별 옆에 세우고, 드는 파편을 버튼이 직접 말한다.
- 룬을 **자리별 조각**으로 갈랐다(`RuneInstance.part`). 아무 룬이나 아무 칸에 끼우면 세 칸이
  사실상 한 칸이라, 어느 자리를 파밍할지가 목표가 되지 않았다. 자리 불변식은 서버가 지킨다.
- 전투력을 별도 칸에서 빼내 **오각형 면 안에 눕혔다**. 전투력이 곧 그 면의 넓이이므로 판 위에
  숫자 칸을 따로 세울 이유가 없다. 축마다 실제 수치를 축 이름과 같은 색으로 쌓았다.
- 도감을 보유·미보유로 가르고 정렬을 버튼 하나(개체번호→희귀도→전투력)로 돌게 했다.
  기준마다 버튼을 세우면 지금 어느 기준인지 색으로 읽어야 한다.
- 세공은 고른 줄이 계속 고른 채로 남게 했다. 한 줄에 세 번씩 반복하는 일이라 누를 때마다
  다시 고르게 하면 그 손이 그대로 낭비다. 각인만 남으면 선택을 푼다 — 되돌릴 수 없는
  한 번의 선택이라 사람이 다시 고르게 한다.
- 칸 제목과 팝업 머리글을 `SectionTitle` 한 모양으로 모았다. 화면마다 제 나름의 제목을
  만들면 같은 위계가 어디서는 판, 어디서는 맨 글자가 된다.
- 재화 수를 세로로만 늘려 굵게 키우고 아이콘 쪽으로 붙였다. 가로까지 키우면 자릿수가
  늘 때 칸이 함께 넓어지고, 멀리 떼면 그림과 숫자가 두 정보로 읽힌다.

### v0.8.0 — 2026-08-23

**운영·플랫폼 경계를 코드 밖으로 밀어낸다.**

- 계정/인증과 원격 저장 메타데이터를 `AccountApi`가 소유하게 했다. 인증 토큰은 `Session`·
  `SaveData`·`localStorage`에 절대 넣지 않고, SDK가 없는 빌드는 성공을 흉내 내지 않고
  `unsupported`를 돌려준다. 로컬/서버 해시가 다르면 자동으로 덮지 않고 고르게 한다.
- 광고 보상을 서버 검증형으로 만들고 슬롯 구성 계약과 정책 문서를 함께 뒀다. 클라이언트가
  보상을 확정하면 어떤 검증도 의미가 없다.
- 연구 후원 패스(월정액) 권리 정의를 추가했다.
- 오디오 버스를 `AudioManager` 한 곳으로 모으고, 진동·공유 같은 플랫폼 기능은 어댑터 뒤로
  숨겼다. 씬이 플랫폼 API를 직접 부르면 웹·앱 빌드가 갈라진다.
- 설정 저장 모델(`GameSettings`)과 탭형 모바일 설정 화면을 추가했다.

### v0.7.0 — 2026-08-23

**스킬 일러스트 파이프라인 — 흰 실루엣 한 장에 속성·직군 색을 입힌다.**

- 스킬 아이콘을 흰 실루엣 WebP 한 장으로 굽고(`prepare_skill_icons.py`), 색은 파일에 굽지
  않고 `skillArtTint`가 속성·직군을 섞어 정한다. 색이 개체에서 나오기 때문이다.
- 그림 한 장을 담는 칸에만 **액자 예외**를 뒀다(불투명 채움 + 사방 외곽선 + 안쪽 비네트).
  배경 원화 위에서 어디까지가 그림인지 또렷해야 한다. 판때기에는 쓰지 않는다.
- 아트가 없는 개체는 공용 효과 아이콘으로 되돌아가되 색판은 그대로 깔아, 아군 창과 같은
  체계로 남게 했다. 적도 같은 파이프라인을 쓴다.
- 로비 왼쪽에 발굴 입구를 세우고, 고고학(장기 탐사)과 발굴이 서로 다른 콘텐츠임을 문구에서
  갈라 놓았다.

### v0.6.0 — 2026-08-23

**전투 연출과 적 표시를 정리.**

- 궁극기 컷인을 직렬화하고 방향·배율·머무는 시간·흔들림을 `ultimatePresentations.ts` 한 표로
  뺐다. 전투 중 여러 번 반복되는 연출이라 길이가 곧 기다림이다 — 포효를 따로 기다리지 않는다.
- 궁극기 트윈이 중간에 끊겨도 입력 잠금이 풀리게 했다. 씬 전환과 겹치면 전투가 굳었다.
- **적도 아군과 같은 정보창을 쓴다.** 전용 화면을 따로 세우면 정보창이 좋아질 때 그 화면만
  옛 모습으로 남는다. 적은 문맥으로 현재 전투 줄(체력·궁극·야성·상태이상)만 덧붙인다.
- 파티 미리보기의 상성을 힌트 문구 대신 방향 화살표로 바꿨다.
- 난전에서 적이 확실히 눌리도록 히트영역을 고쳤다.
- 개체 2·3·4와 트리케라톱스 스킬셋 데이터를 채웠다.

### v0.5.0 — 2026-08-23

**룬(하트 젬) 시스템 — 순수 규칙 + 원자적 서버 경계.**

- 룬 도메인 타입과 강화 규칙을 Phaser 없는 순수 모듈로 만들고 난수를 인자로 주입했다.
- 룬 인벤토리 DTO와 원자적 API를 추가하고, 보유 목록을 ID 나열에서 **인스턴스**로 옮겼다.
  같은 등급이라도 옵션이 다른 물건이라 ID만으로는 표현되지 않는다.
- 룬 파생 능력치를 전투 계산에 일관되게 반영하고, 세공 팝업을 공용 프리팹 하나로 모았다.
- 세공 화면을 가운데로 옮겨 주·보조 옵션을 갈라 놓고, 가방을 목록에서 격자로 바꿨다.
- 확률을 막대 두 개가 아니라 한 줄을 성공·실패가 나눠 갖게 하고 결과를 별로 박았다.
- 룬을 누르면 곧바로 세공이 열리지 않고 간소한 쪽지가 먼저 열리게 했다. 골드를 쓰고
  되돌릴 수 없는 조작이라 손이 한 번 더 들어가야 한다.

### v0.4.0 — 2026-08-22

**야성을 벌에서 상으로 뒤집고, 전투 코어를 하나로 남긴다.**

- 야성을 억제 대상에서 **피버 보상**으로 바꿨다. 최대치에 닿으면 폭주가 시작되고 게이지는
  스스로 줄어든다 — 별도 진압 조작은 없고, 피버 중에도 궁극기를 쓰고 적만 공격한다.
- 개체별 발현(`ferocityTrait`)을 난전에 적용하되 공식은 공용 야성 규칙 하나로 뒀다.
- 레거시 턴제 전투 코어를 제거했다. 실시간 난전(`skirmish.ts`)과 둘이 남아 있으면 어느 쪽이
  진짜 규칙인지 알 수 없다.
- 경제 재화를 잡초에서 **치즈케이크**로 개명하고 상단 재화 줄에 렐릭 문맥을 연결했다.
- 발굴을 연구소에서 분리하면서 저장 불변식을 함께 지켰다.
- 루카(랩터)와 적 1~3의 전신·SD 에셋을 붙이고, 룬 아이콘을 하트 원화 세 조각으로 갈라
  등급색 넷을 정했다.
- 파티 진입 가드와 모바일 탭 오작동을 고쳤다.

### v0.3.0 — 2026-08-22

**서버 경계를 넓힌다 — 가챠·상점·경제·이벤트.**

- 가챠의 천장과 픽업 보장을 갈라 놓았다. 한 카운터가 둘을 겸하면 어느 쪽이 찼는지 말할 수 없다.
- 공용 상품 카탈로그 기반 상점, 화석 경제와 DNA 교환, 서버 시각 기준 이벤트 정의,
  일일 관찰 인터뷰, 일일·주간 미션 진행도를 각각 정적 데이터 + API 경계로 추가했다.
- 서버 권위 비동기 아레나를 설계 문서로 먼저 고정하고(`docs/asynchronous-arena.md`),
  로비에서 친구 흐름을 연결했다.
- 프로젝트 가이드 문서를 현재 플레이에 맞춰 정렬했다.

### v0.2.0 — 2026-08-22

**아이콘·재화 표기를 굽기 파이프라인으로 통일.**

- 속성·직군·재화 아이콘을 `prepare_icons.py`가 굽는 WebP로 옮기고 원본은 저장소에서 뺐다.
  속성·직군은 단색 실루엣이라 **색도 그 스크립트 표에서 정한다** — 화면이 tint로 다시
  칠하지 않는다.
- 상단 재화를 보석·골드·스테미나로 바꾸고 모집 화면에는 다이아·화석·호박석을 세웠다.
  화면마다 다른 조합을 `TopBar`의 표 하나에 뒀다.
- 재화 칸만 기울이지 않고 둥근 면으로 바꿨다. 여러 칸이 나란히 설 때 칸마다 기울면
  숫자와 아이콘까지 비뚤어 보여 줄이 어수선해진다.
- 화석 색은 한 번 채도를 올렸다가 **되돌렸다** — 호박석과 구분은 됐지만 돌빛이라는 인상이
  사라져, 채도를 덜어 회색 쪽으로 맞추는 편이 재료로 읽혔다.
- 카드 위 속성·직군이 원화에 묻히지 않게 그늘을 넓혔다.

### v0.1.0 — 2026-08-22

**기록 시작 — 정보창과 성장 흐름의 기본형.**

- 정보창에 감상 모드와 좌우 넘김을 넣고, 상세를 여는 돋보기를 판 오른쪽 끝 안쪽에 작고
  흐리게 붙였다. 화면에 늘 떠 있어야 하는 것은 균형이고 숫자는 물어볼 때다.
- 돌파가 레벨 상한을 열도록 하고, 성장을 레벨업 버튼이 아니라 **급여**(치즈케이크)로 짰다.
- 일일·주간 미션 진행도를 추가했다.
- 이 항목 이전의 프로토타입 단계(씬 골격·저장 모델·홀로그램 UI 토큰 도입)는 별도 이력을
  남기지 않았다.
