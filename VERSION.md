# 버전 관리

현재 버전: **v0.66.1**

`VERSION.md`와 `package.json`의 `version`은 항상 같은 값을 쓰고, 타이틀(로딩) 화면 좌측
하단 표기는 그 값을 그대로 읽는다. 화면에 손으로 적어 두지 않는다.

## 번호 규칙

`주.부.패치` 형식이며, 변경에 필요한 **가장 낮은 자리만** 올린다.

- **패치(0.0.x)** — 버그 수정, 수치 조정, 연출 손질, 리팩터링, 문서, 에셋 교체
- **부(0.x.0)** — 새 시스템·화면·콘텐츠 묶음. 예: 룬 세공, 새 재화, 새 서버 경계
- **주(x.0.0)** — **정식 런칭 시에만 올린다.** `v1.0.0`은 출시 단계를 뜻하는 번호라
  개발 중에 임의로 올리지 않는다.

여러 성격이 섞이면 가장 큰 변경을 기준으로 고른다.

출시 전까지 주 자리는 계속 `0`이며, 부 자리는 `0.11` → `0.12` … 처럼 얼마든지 늘어난다.
저장 데이터(`SaveData`)나 서버 DTO의 **비호환 변경도 부 자리**를 올리고, 그 항목에
마이그레이션 방법과 예전 저장을 어떻게 처리하는지를 반드시 함께 적는다 — 출시 전에는
저장이 깨지는 일이 잦으므로 그때마다 주 자리를 올리면 `v1.0.0`이 런칭 전에 소진된다.

## 기록 규칙

- 이력은 **이 문서에만** 쌓는다. `CLAUDE.md`에는 지금 지켜야 하는 규칙만 남긴다.
- 항목은 **무엇을 왜** 바꿨는지 쓴다. 무엇만 적으면 6개월 뒤 되돌릴 수 없다.
- **되돌린 변경은 되돌린 이유를 반드시 남긴다.** 같은 시도를 두 번 하지 않기 위한 기록이다.
- 푸시할 때마다 그 버전 항목을 남기고, `package.json`과 이 문서가 같은 값인지 확인한다.
- 아래 이력은 현재 저장소 히스토리(2026-08-22 이후)를 기준으로 정리한 것이라,
  그 이전의 초기 프로토타입 단계는 `v0.1.0` 항목 하나로 묶었다.

## 변경 이력

## v0.66.1 — 2026-09-05

**토리카를 제어형 탱커로 세우고, 그 과정에서 드러난 보스의 기절 저항 공백을 메웠다.**

- 들이받기를 **위력 100 → 70 + 「세 개의 뿔」 세 타마다 0.5초 기절**로, 지각 붕괴를
  **기절 2 → 3.5초 · 게이지 120 → 100**으로 바꿨다. 한 방이 아프지 않은 대신 상대의 시간을
  빼앗는 개체가 되어, 방어형 탱커가 "혼자 튼튼하기만 한" 자리에서 벗어난다. 처음에는 **매 타격
  1초**로 잡았다가 공격 간격이 1.92초라 상대의 시간을 절반이나 빼앗는 것이 과해 세 타마다
  0.5초로 낮췄다 — 실효 점유가 52%에서 9%가 된다.
- **뿔 셋은 파치의 배트와 같은 자리에 칩으로 뜬다.** 주기 타수 칩은 이미 `statusEffectEvery`를
  읽어 범용으로 붙고 있었으므로 새로 만든 것은 없고, 쌓이는 것이 제 이름을 가진 개체만 칩
  이름을 따로 세우도록 `statusEffectStackName` 하나를 열었다(적지 않은 파치는 예전처럼 스킬
  이름을 쓴다). 주기를 숨기지 않는 이유는 다음 한 방이 언제인지 플레이어가 보고 셀 수 있어야
  하기 때문이다.
- **그 과정에서 원정 최종 보스가 제어에 무방비였다는 것이 드러났다.** 기절은 공격 쿨다운까지
  멈추는데, 매 타격 1초로 잡았던 시안에서는 폰토스가 시간의 절반을 빼앗겨 첫 해일이
  **24.7초 → 72.7초**로 밀렸고 40초를 돌려도 해일이 한 번도 나오지 않았다. 원인은
  `stunResistancePercent` 필드가 처음부터 있었는데도 **어떤 개체도 값을 적지 않아 보스가 잡몹과
  똑같이 기절했다**는 것이다 — 제어기를 가진 개체가 없어 여태 드러나지 않았다.
- **폰토스에만 기절 저항 50%를 줬다.** 최종안(세 타마다 0.5초)은 저항이 0이어도 26.3초·35.2초로
  원래 구간(24.7초·33.5초) 안이라 당장은 없어도 된다. 그래도 남긴 이유는 그 취약점이 개체가
  아니라 **보스 쪽 성질**이기 때문이다 — 다음에 더 센 제어기가 들어오면 같은 자리가 다시
  무너진다. 50%는 어떤 제어든 절반으로 줄여 잠금을 막으면서도 제어를 무의미하게 만들지 않는다
  (한때 검토한 85%는 0.5초를 0.075초로 만들어 반대로 제어를 지웠다).
  **저항을 적지 않은 일반 적에게는 기절이 그대로 다 들어간다.**
- 토리카를 도구로 쓰던 테스트 셋을 고쳤다. 케리스의 저주 전이 편은 적으로 세운 토리카가 케리스를
  기절시켜 전이 대신 기절을 재고 있었다(매 프레임 기절만 푼다). 궁극기 기절 초는 손으로 적던 것을 데이터에서 읽게 바꿔, 앞으로 수치를
  조정해도 검사가 깨지지 않는다.

## v0.66.0 — 2026-09-04

**13번 델로피를 넣었다 — 중독을 처음 쓰는 개체이고, 그 하나를 위해 전투 규칙 네 가지가 늘었다.**

### 델로피 (SR · 풀 · 암살자 · 나이트 기어)

- 딜로포사우루스 화석에서 복원한 중학생 또래의 광대·마술사 콘셉트다. 잠입 중에도 손장난을
  멈추지 못하는 나이트 기어의 막내로, 조용한 선배들 틈에서 혼자 말이 많은 신입으로 잡았다.
- **루카의 속성을 풀에서 바람으로 옮겼다.** 델로피가 들어오면 등급·직업·속성·소속이 넷 다
  같은 개체가 둘이 되어, 루카를 뽑을 이유와 델로피를 뽑을 이유가 서로를 지운다.
- 태생은 SR 띠 안(전투력 2,249)이고 **유일한 혼합형**이다. 카드 한 장이 손끝 힘과 발라 둔
  독을 함께 쓰므로 공격력 120과 주문력 108을 비슷하게 들며, 그래서 둘 중 어느 쪽도 실전에
  쓰이지 않는 남는 수치가 아니다. 대신 몸은 가장 얇다(체력 820 · 방어 50).

### 중독 — 색만 예약돼 있던 자리를 처음 채웠다

- `DEBUFF_TONE`에 `poison`(어두운 보랏빛)이 오래전부터 있었지만 실제로 거는 개체가 없었다.
  이제 `CombatStatusEffect`의 `poison`이 그 자리를 쓴다.
- **출혈과 다른 축이다.** 출혈은 맞은 쪽의 최대 체력에서 깎지만 중독은 **바른 쪽의 공격력·
  주문력**에서 뽑는다(각 15%). 그래서 같은 독이라도 누가 발랐는지에 따라 세기가 갈리고,
  독을 바른 개체가 성장하면 그만큼 더 아프다.
- 이름이 마법 피해인 만큼 **대상의 저항력과 속성 상성도 그대로 거친다.** 다만 그 계산은
  바르는 순간 한 번만 하고 값을 굳힌다 — 매 틱 다시 계산하면 아래 청산이 몰아 주는 값과
  그냥 두었을 때의 총합이 반올림만큼 갈린다.
- **청산**(`liquidate`)은 남은 지속 피해를 한 번에 몰아 주고 상태를 지운다. 앞으로 실제로
  터졌을 틱 수만 세므로, 이미 한 틱 아팠던 적이 그 몫을 또 맞지 않는다 — 총량은 그대로 두고
  시점만 당긴다. 테스트가 "그냥 두었을 때의 남은 총합"과 같은 값인지 직접 비교한다.

### 늘어난 전투 규칙 넷

- **위력을 두 능력치가 나눠 갖는 스킬**(`AttackSkill.secondaryScaling`). 트릭 카드는 공격력
  50% + 주문력 50%다. 스피나 궁극기의 공속 복합 계수와 다른 축이라 — 그쪽은 공격 속도를
  공격력 배율로 환산해 더하지만 이쪽은 서로 다른 두 능력치에서 각각 뽑아 더한다.
- **때리지 않는 궁극기**(`SetupSkill.selfSetup` + `targeting: "self"`). 그랜드 피날레는 은신
  3초와 최저 체력 적으로의 순간이동, 그리고 다음 일반 공격 한 번의 강화만 남긴다. **위력을
  적지 않는다** — 그 피해는 이어질 트릭 카드의 몫이라, 여기에 숫자를 두면 평타 위력을 조정한
  뒤 이 한 방만 옛 값으로 남는다.
- **다음 한 방을 강화하는 상태**(`Fighter.empoweredBasic`). 확정 치명타 + 방어 무시이고,
  실제로 나간 일반 공격 한 번을 쓰고 사라진다. 방어를 지나친 그 한 방은 사건에서도 종류가
  아니라 `true`(고정 피해)로 뜬다.
- **전투 시작 은신 패시브**(`openingVanish`). 첫 프레임부터 걸려 있어야 "숨어서 시작했다"가
  되므로 `createFighter`가 세운다 — 한 박자 뒤에 걸면 이미 표적이 잡힌 뒤다.

### 폭주 — 초절정 도파민 중독

- 폭주 중 일반 공격이 **바르거나 터뜨리거나 둘 중 하나만** 한다. 그 적에게 자기 중독이 없으면
  평소대로 바르고, 이미 있으면 청산한다. 번갈아 하도록 토글을 들지 않고 **매 타격마다 지금
  상태를 본다** — 공속이 빨라지거나 중독이 먼저 꺼져도 스스로 맞는 동작으로 돌아온다.
- 함께 공격 속도가 30% 오른다. 바르고 터뜨리기를 번갈아 하는 손이라 속도가 곧 그 주기다.

### 에셋과 표시

- `char_013.zip`·`charSD_013.zip`을 `public/puppets`로 옮기고 alpha 경계와 관절을 실측해
  `assetMetadata.ts`에 적었다. 로비 배율은 키 1.45 m에서 계산한 `lobbyZoom: 0.95`다.
- **카드는 `cardZoom: 1.2`로 되돌렸다.** 양옆으로 뿌린 카드가 캔버스를 거의 다 채워(1020/1086)
  얼굴이 다른 카드의 70%로 앉았다 — 렉시아의 낫과 같은 이유다. 1.25를 넘으면 머리 위 여백이
  `MAX_HEAD_DROP_RATIO`에 바짝 붙고 1.35에서는 정수리가 홈 윗변에 닿으므로, 한계값 자체를
  건드리지 않고 얼굴이 중앙값의 84%가 되는 자리에 세웠다.
- 설명문은 전부 구조화 필드에서 짓는다. 중독은 **시간만 본문이 적고 매초 계수는 태그가**
  가진다(쓰는 개체가 하나뿐이라 출혈과 반대다). 새 규칙어로 `poison`·`liquidate`·`fixed-damage`를
  등록했고, 머리 위 상태 칩과 그 쪽지도 중독을 함께 그린다.

## v0.65.0 — 2026-09-04

**E2E가 병렬로 돌면서 멀쩡한 화면을 무더기로 실패시키던 것을 고쳤다.**

- pixel-7 전체를 돌리면 **31편이 실패**했는데, 원인이 전부 같았다 —
  `Timeout 5000ms exceeded while waiting on the predicate`. 예외가 아니라 화면이 기대한 상태로
  넘어가지 못해 폴링이 시간을 넘긴 것이다.
- **같은 커밋을 `--workers=1`로 다시 돌리자 flow.spec이 16실패 → 3실패가 됐다.** 이 게임은 매
  프레임 WebGL로 전장을 다시 그리는데, 4코어에서 브라우저 넷이 동시에 돌면 프레임이 굶어 그
  시간 안에 상태가 도달하지 못한다. `playwright.config.ts`에 `workers: 1`을 박았다 — 느려지는
  대신 **실패가 실패를 뜻하게** 한다.
- 전투 조작 칩 좌표를 스펙이 손으로 적고 있었다(1360·1268). v0.62.0에서 칩 줄을 1288로 올린
  뒤로 그 스펙만 옛 자리를 두드렸다. `BATTLE_CONTROLS`를 내보내 **화면이 소유한 표를 읽게** 했다.
- 정보창 **급여 버튼의 자리를 화면이 알린다**(`__PF_DEBUG.feedButton`). 레벨 칸은 경험치 줄·상한
  표기가 함께 자라 버튼 자리가 달라지는데, 좌표를 스펙에 적어 두면 버튼 위가 아닌 곳을 눌러
  실패가 "성장 팝업이 안 뜬다"로만 보인다. **열 때마다** 알린다 — 만들 때 한 번만 알리면 씬을
  오갈 때 새 창이 알린 값을 옛 창이 사라지며 지운다.
- 룬 스펙이 룬을 끼우는 대상을 **도감 첫 카드에 서는 렐릭**으로 바로잡았다. 애착 렐릭에 끼우고
  있었는데 목록의 첫 카드는 즐겨찾기 → 개체번호 순으로 정해져 둘이 다를 수 있다. 화면과 같은
  순수 규칙(`compareBookmarkedOwnedRelics`)을 그대로 통과시킨다.
- `playwright.config.ts`가 `PW_CHROMIUM_PATH`를 읽는다(v0.64.0에서 넣은 줄). 브라우저가 이미
  깔린 컨테이너·CI에서 그 실행 파일을 쓰기 위한 것이고, 없으면 예전 동작 그대로다.

## v0.64.1 — 2026-09-04

- `main`에서 빨갛게 남아 있던 원정 증강 테스트를 고쳤다(PR #282가 병합될 때부터 실패 중이었다).
  상태 위력이 **증강 출혈의 지속시간**에 적용되는지 재는 검사인데 시전자를 렉시아로 두었다 —
  렉시아의 기본 공격은 제 출혈 3초를 함께 걸고 그 3초에도 같은 위력이 곱해져(3.75초) 증강의
  2.5초를 덮는다. 제 출혈이 없는 토리카로 바꿔, 이 검사가 증강의 지속시간만 재게 했다.

## v0.64.0 — 2026-09-04

**교류를 두 곳에서 다시 짰다 — 층의 원화와 파견대 편성.**

### 층은 원화가 전체를 덮고 양 끝에서 녹는다

- 원화를 왼쪽 한 칸에만 눕히니 나머지가 빈 띠로 남아, 도시마다 다른 곳이라는 것이 이름으로만
  읽혔다. 이제 **원화가 층 전체를 덮고 양 끝으로 갈수록 판 색에 녹는다.** 그 덕에 기울어진
  좌우 변도 이미 투명한 자리라 **마스크가 필요 없다** — 이 목록은 세로로 흐르고 기하 마스크는
  컨테이너 이동을 물려받지 않아, 마스크를 쓰면 스크롤하는 순간 원화만 제자리에 남는다.
- 원화가 판을 가득 채우면 어디까지가 한 층인지 흐려지므로 **`drawFrameVignette`을 한 겹** 얹어
  네 변을 고르게 눌렀다. 같은 도형을 줄여 가며 두르는 `drawInnerVignette`은 가로로 긴 판에서
  좌우가 더 많이 줄어 검은 줄이 여러 겹 어긋난 잔상으로 남는다.
- `artWidth`는 이제 그림의 폭이 아니라 **글이 판 왼쪽 변에서 얼마나 들어와 시작하는지**라
  `textInset`으로 이름을 바꿨다. 층이 화면보다 넓어 왼쪽 여백이 화면 밖에 있기 때문이다.

### 파견대는 발굴 배치와 같은 구조가 됐다

- 예전에는 칸 하나를 누를 때마다 목록이 아래에서 **올라와 덮었다.** 세 번을 고르는 동안 목록이
  세 번 열리고 닫혔고, 칸이 보이지 않는 순간에는 지금 누구를 세웠는지도 사라졌다. 게다가 그
  목록은 5열 150×182짜리로 도감·발굴 어느 그리드와도 다른 규격이었다.
- 이제 **세 칸과 목록이 한 화면에 함께 선다.** 칸을 누르는 것은 화면을 여는 일이 아니라 어느
  자리에 세울지 고르는 일이고, 고른 칸은 발광으로 알린다. 카드는 **발굴 배치와 같은 215×268**,
  3열이다 — 같은 일을 하는 두 화면이 다른 크기의 카드를 쓰면 얼굴이 화면마다 다르게 잘린다.
- **누르는 것만으로 빼고 맞바꾼다.** 선 렐릭을 누르면 그 자리가 비고, 이미 다른 자리에 선
  렐릭을 목록에서 누르면 두 자리가 맞바뀐다. 그래서 이 파견대에 선 렐릭도 목록에서 지우지
  않는다(다른 도시에 나가 있는 렐릭만 아예 보여 주지 않는다).
- **자동 배치**를 넣었다. 교류에는 발굴의 생산 특화 같은 개체별 기준이 없어 비교 가능한 값이
  전투력뿐이라, 발굴처럼 기준을 돌려 고르는 화살표는 두지 않는다. 동률은 ID 순으로 끊어 같은
  편성이 늘 같은 결과를 낸다 — 누를 때마다 순서가 바뀌면 자동이 아니라 난수다.
- 목록은 그 창 안에서만 흐른다(드래그·휠). 카드의 기하 마스크는 컨테이너 이동을 물려받지
  않으므로 스크롤할 때마다 `syncMask`로 다시 맞춘다.

### 곁들여

- `playwright.config.ts`가 `PW_CHROMIUM_PATH` 환경 변수를 읽는다. 브라우저가 이미 깔린
  컨테이너·CI에서 그 실행 파일을 그대로 쓰기 위한 것이고, 변수가 없으면 예전처럼 Playwright가
  제 캐시에서 찾으므로 개발자 기계의 동작은 바뀌지 않는다.
## v0.63.1 — 2026-09-04

**전투 후 회복 증강을 원정 HP 정산에 연결했다.**

- 선택한 전체 대상 회복 증강을 단순 합산하되 한 전투당 50%로 제한하고, 생존자만 100%까지
  회복한다. 전멸한 파티와 개별 사망자는 회복으로 부활하지 않는다.
- 유효한 완료 결과라면 승패와 무관하게 회복하므로 생존자가 남은 전투 실패와 보스 시간 종료에도
  적용한다. 회복된 HP와 노드 방문 표식을 한 번에 저장해 같은 결과 재전송이 중복 회복하지 못한다.

## v0.63.0 — 2026-09-04

**원정 증강을 실제 전투 능력치와 전술 카테고리까지 확장했다.**

- 공격력 하나뿐이던 누적기를 최대 체력·방어·저항·주문력·공속·시작 보호막·상태 위력까지
  능력치별로 합산하는 순수 규칙으로 일반화했다. 전투 진입 때 정적 렐릭 정의를 복제한 스냅샷에
  한 번만 적용하며, 최대 체력이 바뀌어도 저장된 현재 HP 비율은 그대로 유지한다.
- SR/SSR 후보를 공격·주문·생존·보호막·회복·상태·조건부 일곱 카테고리로 확장하고 각 수치의
  예산 근거를 데이터 주석에 남겼다. 기존 피해·방어·저항·보호막·출혈 경로를 그대로 재사용한다.
- 증강 칩과 선택 팝업이 새 카테고리를 문양과 한글 태그로 구분하며, 순수 합산과 범위, 전투
  초기값, 정적 원본 불변성을 단위 테스트로 고정했다.
**설정 스위치를 면(레이어) 스타일 토글로 다시 만들었다.**

- 예전에는 같은 자리의 **동그란 점** 하나가 조금 커지고 노래질 뿐이었다. 그 줄만 봐서는 지금
  어느 쪽인지 알 수 없어 눌러 봐야 알았고, 모양도 화면 전체의 규칙(동그라미 대신 살짝 기운 면)과
  어긋났다.
- 이제 **자리가 상태를 말한다.** 홈 안에서 손잡이가 미끄러지고, 손잡이가 **비운 쪽**에 그 상태의
  글자가 선다 — 꺼져 있으면 손잡이가 왼쪽에 "OFF"가 오른쪽에, 켜지면 반대다. 같은 쪽에 겹치면
  글자가 손잡이에 가려 읽을 수 없다.
- 홈과 손잡이 모두 `slantedRect` + `drawLayer`다. 사방 외곽선 없이 윗변 강조선과 그림자로만
  깊이를 만들고, 누르면 커진다(1.06배) — 화면 전체의 같은 규칙이다.
- 처음 그릴 때는 즉시 앉히고 이후에만 미끄러진다(140ms). 화면을 열 때 스위치들이 한꺼번에
  움직이면 무엇이 바뀐 것처럼 보인다.
- 자리는 Phaser 없는 `src/ui/settingsToggleLayout.ts`가 소유하고, 손잡이가 홈을 넘지 않는지와
  줄 구분선 안쪽에 머무는지(화면 985)를 테스트가 지킨다.

## v0.62.1 — 2026-09-04

**Codex PR #277을 지금 남은 진입점으로 옮겨 병합했다.**

- 그 PR은 `TradePopup.refresh()`가 storefront 고르기를 `tradePopupModel`에 맡기는지 검사하는
  회귀 테스트였는데, 먼저 병합된 #278이 `TradePopup`과 `tradePopupModel`을 **삭제**해 그대로는
  typecheck와 단위 테스트가 함께 깨졌다.
- 지키려던 규칙은 여전히 살아 있고, **실제로 어겨져 있었다** — `ShopScene.refresh()`가
  `response.products.filter(...)`로 storefront 판정을 다시 쓰고 있었다. 그 자리를
  `shopModel(response.products)` 호출로 바꾸고, 같은 검사를 그 파일에 대해 남겼다.
- `package-lock.json`의 버전이 0.56.0에 멈춰 있던 것도 그 PR의 의도대로 함께 맞췄다.

## v0.62.0 — 2026-09-04

**원정 증강이 이름표 한 줄이 아니라 그 캐릭터에게 붙는다.**

- 예전에는 확정 증강이 화면 위쪽 한 줄에 이름표로만 늘어섰다. "이 캐릭터가 지금 무엇을 받고
  있는가"를 알려면 그 줄을 읽고 아래 HUD의 얼굴과 대조해야 했고, 넷을 넘으면 그마저 `+N`에
  잠겼다. 이제 **전체 증강만 위쪽 줄**에 서고, **개인 증강은 그 캐릭터의 프로필 위에 왼쪽부터**
  쌓인다. 표식 줄은 전투 버프 액자와 **같은 y·같은 왼쪽 끝**을 쓴다 — 두 줄이 다른 자리에 서면
  같은 프로필 위에서 서로 다른 체계로 읽힌다.
- 표식은 전투 버프 칩과 같은 액자 규칙(깎인 네모 + 사방 외곽선 + 안쪽 비네트)을 줄여 쓰고,
  안에 글자를 넣지 않는다 — 40px에 이름을 적으면 뭉개진다. 문양은 **효과 종류 셋**(공격·출혈·
  회복)이 정하고 색은 등급이 정한다.
- **프로필이나 위쪽 줄을 누르면 지금 붙어 있는 것을 한 장에 모아 보여 준다.** 차례는 전체
  적용이 가장 위, 그다음이 편성 순서다(`expeditionAugmentRows`) — 고른 순서로 늘어놓으면 같은
  캐릭터의 증강이 목록 여기저기에 흩어져 창을 열고도 다시 세야 한다. 누구에게 붙었는지는
  이름이 아니라 **기여도 판과 같은 얼굴 액자**(`FaceFrame`)가 말한다. 프로필 꾹 누르기는
  예전처럼 상세 정보창이다.
- 그 갈래와 차례는 Phaser 없는 `src/ui/expeditionAugmentBadges.ts`가 소유한다. 화면과 팝업이
  저마다 정렬하면 위에서 본 차례와 목록의 차례가 갈린다.

**원정 지도에서 얼굴이 반쯤 지워져 보이던 것을 고쳤다.**

- 충전 가림막은 "궁극기가 아직 안 찼다"는 뜻이라 **전투에서만** 뜻이 있다. 궁극기를 쓸 수 없는
  화면(단계·증강 선택)에서는 그저 얼굴이 반쯤 지워진 카드로 보였다. 읽기 전용 프로필은 처음부터
  다 찬 상태로 세운다.

**버프 액자의 겹 수를 읽을 수 있게 했다.**

- 파치의 4타처럼 지금 몇 대째인지는 액자 그림보다 먼저 읽혀야 하는 수인데, 머리 위 칩(22px)의
  값을 그대로 써서 56px 액자에서는 판이 점만큼 작아 숫자가 밖으로 넘쳤고 액자와 같은 색이라
  그림에 묻혔다. 자리·크기를 액자 크기에서 뽑고(`battleBuffStackSpot`) **흰 글자에 검은 테두리**로
  바꿨다. 판이 액자 안에 머무는지는 테스트가 지킨다.

## v0.61.1 — 2026-09-04

- 채찍을 두껍게(뿌리 9 → 15 · 끝 2 → 4), 탄환을 크게(50×18) 키웠다.
- **채찍이 두 자리를 통째로 잇지 않고 닿는 쪽 끝만 그린다**(`lash.startAt` 0.34). 중거리 적과
  원거리 아군이 맞붙으면 화면을 가로지르는 리본이 그 사이의 탄환을 통째로 덮었다. 길 가운데를
  비우면 같은 동작이 남으면서 날아가는 것이 그 위로 보인다 — 뿌리가 두껍고 끝이 가는 형태
  자체가 이미 방향을 말하므로 누가 쳤는지도 흐려지지 않는다. 진하기도 0.75 → 0.66으로 낮췄다.
- **탄환을 채찍보다 한 겹 위에 그린다**(`bullet.depthLift`). 같은 깊이면 나중에 열린 채찍이
  탄환을 덮는다. 탄환 두께가 채찍 뿌리보다 두꺼운지와 이 깊이 차이는 테스트가 지킨다.

## v0.61.0 — 2026-09-04

**멀리서 때리는 타격이 지나가는 길을 그린다.**

사거리를 나눈 뒤로 중·원거리 개체는 떨어진 자리에서 때리는데, 화면에는 맞은 쪽에서만 파편이
터져 **누가 쳤는지도 무엇이 날아갔는지도 보이지 않았다.** 근거리는 몸이 붙어 있어 그 자체가
답이지만, 떨어져 있으면 사이를 잇는 것이 없어 그냥 숫자만 떴다.

- **중거리는 채찍(`lash`)이다.** 날아가는 물체가 아니라 이미 닿아 있는 선이라 자라나지 않고,
  처음부터 끝까지 이어진 채 뻗었다 걷힌다. 곧은 선은 채찍이 아니라 레이저이므로 가운데를
  진행 방향의 수직으로 밀어 한 번 휘게 하고, 뿌리에서 끝으로 갈수록 가늘어져 휘두른 방향이
  선 하나에 남는다. 도형은 Phaser 없는 `src/ui/reachStrikeShape.ts`가 소유한다 — 130ms만
  보이는 그림이라 눈으로는 검수할 수 없어 테스트가 두께와 휨을 직접 잰다.
- **원거리는 탄환(`bullet`)이다.** 실제로 날아가며, 날면서 뒤로 늘어나 지나온 자리가 꼬리로
  남는다(잔상 객체를 따로 만들지 않는 방법이다). 피해는 이미 확정된 뒤라 연출이 길면 숫자가
  먼저 뜨고 총알이 나중에 닿으므로, **거리와 무관하게 시간을 고정**해 먼 적일수록 빨라 보이게 했다.
- 값은 `src/ui/effectPresets.ts`의 `REACH_STRIKE` 한 표에만 있고, 씬은 두 자리와 사거리 단계만
  넘긴다. 탄환은 파문과 같은 이유로 풀에서 꺼내 쓰고 상한(10)을 넘으면 가장 오래된 것을
  회수한다 — 평타마다 객체를 새로 만들면 난전에서 GC가 프레임을 끊는다.
- **전이처럼 실제로 휘두르지 않은 타격(`animate: false`)에는 그리지 않는다.** 케리스의 저주
  전이는 사슬이 이어질 때마다 채찍이 늘어나 한 번의 공격이 여러 번으로 보인다.

## v0.60.0 — 2026-09-04

**표적을 한 번 정하고 끝내지 않고 주기적으로 다시 고른다.**

- 예전에는 고른 표적을 **죽을 때까지** 바꾸지 않았다. 그래서 전투가 시작되는 순간 저마다
  상대를 정하고, 바로 옆으로 적이 걸어와도 처음 정한 상대를 향해 전장을 가로질렀다. 이제
  `SKIRMISH.retargetSeconds`(2초)마다 주위를 다시 잰다.
- 점수는 낮을수록 좋고 세 항으로만 만든다: **거리**(기본) + 이미 붙은 아군 수 ×
  `crowdPenalty` − 지금 노리던 상대의 `targetStickiness`(160) − 이미 사거리 안에 든 상대의
  `inReachBonus`(320). 거리가 여전히 기본 항이라 **주위에 아무도 없으면 먼 적을 고르고**,
  손이 닿는 상대가 있으면 그를 지나쳐 걸어가지 않는다. 고정 보너스가 없으면 거리가
  엎치락뒤치락하는 두 적 사이에서 매번 표적이 뒤집혀 다가가다 돌아서기를 반복한다.
- **표적을 남이 정해 주는 개체는 주기 재평가에서 뺀다** — 무리 사냥(루카)은 대장의 표적을
  따라야 하고 고품격 식재료(마키)는 제 시계로 가장 약한 적을 고른다. 여기서 다시 재면 그
  규칙이 2초마다 조용히 덮인다.

**사거리 단계를 다시 벌렸다 — 중거리 300 → 360, 원거리 430 → 600.**

- 앞의 두 번(250/340 → 300/430)은 **그림만 보고** 고른 값이라, 아군이 한 대 치는 사이에 적이
  이미 붙어 차이가 남지 않았다. 기준을 "먼저 몇 대 치는가"로 바꿔 실제로 재서 정했다 —
  같은 개체의 단계만 바꿔 재면 근거리 0대 · 중거리 1대 · 원거리 2대다.
  `tests/unit/skirmishTargeting.test.ts`가 그 순서를 지킨다.

**머리 위 상태 칩이 체력 바를 가리던 것을 마저 고쳤다.**

- v0.59.0에서 부채꼴을 칩 폴리곤 안에서 자르게 했지만 여전히 바를 덮었다. 원인은 도형이
  아니라 **줄 높이**였다 — 칩 아래 변(중심에서 11)이 바의 끝 빗금 꼭대기(중심에서 7.4)와
  맞닿아 있었다. `chipRowLift`를 18 → 28로 올리고, 두 값이 다시 겹치면 실패하는 테스트를
  함께 남겼다.

## v0.59.0 — 2026-09-04

**12번 케리스(메갈로케로스)와 새 디버프 두 종(저주·광란)을 넣었다.**

- 전신·SD 원화(`char_012.zip` / `charSD_012.zip`)를 `public/puppets`로 옮기고 ZIP 안 WebP의
  alpha > 16 경계를 실측해 `assetMetadata.ts`에 적었다. `발2` 관절이 캔버스 아래(y=1623 /
  높이 1536) 밖에 박혀 있어 바닥선은 관절이 아니라 `content.bottom`이 잡는다. 로비 배율은
  키 1.62 m 기준 `lobbyZoom` 1.079이고, 카드 얼굴 크기는 보정 없이 다른 개체와 같은 띠에 든다.
- **저주(`curse`)** — 겹당 저항 15% 감소, 최대 3겹, 8초 갱신형. 덧칠과 다른 축이다: 덧칠은
  받는 피해에 곱하는 배수라 물리·마법을 가리지 않지만, 저주는 **저항 수치 자체**를 깎아
  마법 피해에만 듣는다. 적용은 `defensiveDefinition` 한 곳이라 오라 보정과 같은 자리에서 든다.
- **광란(`frenzy`)** — 공격 속도 +50%와 함께 **표적의 편을 뒤집는다.** 때릴 자기 편이 없으면
  제자리에서 자신을 공격하므로 적이 혼자인 보스전에서도 무효가 되지 않는다. 광란 중에는
  궁극기를 쓰지 못한다 — 적 궁극기까지 아군에게 꽂히면 한 판이 그 한 번으로 갈린다.
  자기 편을 때린 몫의 기여도는 **광란을 건 쪽**에 붙는다(`frenzy.sourceId`).
- 케리스의 기본 공격은 저주가 **이미 최대인** 적을 때렸을 때 25%를 옆으로 전이하며, 전이된
  적도 최대였다면 같은 방식으로 이어진다. 같은 적을 두 번 고르지 않아 사슬은 살아 있는 적
  수에서 저절로 끝나고, 1 미만이 되는 몫에서도 끊는다. 전이된 타격은 흡혈·야성·패시브
  누적·폭주 광란을 부르지 않는다 — 한 번의 공격이 사슬 길이만큼 값을 벌면 사거리 하나로
  전투가 갈린다.
- 궁극기는 저주에 걸린 적만 치되, 걸린 적이 하나도 없으면 가장 가까운 적에게 먼저 씌우고
  발동한다(`cursedTargetsOnly.seedCurse`). 그러지 않으면 게이지가 찬 자동 궁극기가 대상 없이
  헛돌고 플레이어는 왜 안 나가는지 알 수 없다.
- 패시브 `cursedInsight`는 저주에 걸린 적을 **직접** 때릴 때마다 주문력 2%를 최대 10회까지
  누적한다. 스피나의 공속 누적과 축을 일부러 갈랐다 — 같은 규칙을 둘이 나눠 가지면 전이로
  적중 수가 더 많은 케리스가 스피나의 정체성을 대신 가져간다.
- 소속은 시그널 아이(중앙 도서관 기록 담당), 호칭은 "선배"다. 근거는 `docs/factions.md`
  배정표에 함께 남겼다.

**같은 버전에서 함께 고친 것**

- **사거리 단계를 더 벌렸다** — 중거리 250 → 300, 원거리 340 → 430. 전장(820×760)에서
  근거리와 나란히 서 보면 예전 값은 차이가 눈에 남지 않았다. 단계를 나눈 값은 "뒷줄이
  뒷줄에 남는 그림"이라 보이지 않으면 없는 것과 같다. 원정 보스 회귀의 첫 해일 상한도
  26 → 28초로 함께 옮겼다.
- **전투 조작 칩(배속·자동 궁극기·연출)을 한 칸 위로** 옮겼다(1360 → 1288). 세 번째 카드의
  버프 액자 줄이 1363까지 올라와 그 위에 겹쳐 있었다.
- **머리 위 상태 칩의 시계가 깎인 모서리를 넘지 않게** 했다. 덮는 부채꼴을 사각형 기준으로
  그려 오른쪽 아래 빗변 밖으로 흘러내린 검은 조각이 바로 아래 체력 바를 가렸다. 이제
  `clockWedgeOnShape`가 **칩 폴리곤 자체**를 향해 쏘아 그 안에서만 자른다.
- 정보창 능력치 칸의 사거리 한 줄을 858 → 874로 내렸다. 제목에 너무 붙어 있었다.

## v0.58.2 — 2026-09-04

- 정보창 능력치 칸의 오각형을 18px 내렸다. 판 한가운데에 두면 위쪽 축만 이름 위에 수치를
  한 줄 더 이고 있어 그래프가 판 천장에 붙어 보였다 — 그 한 줄만큼 내려 위아래 여백을 맞췄다.
- `main`의 교류 교환소 진입 경로 정리(PR #278)를 병합했다. `TradePopup`·`tradePopupModel`이
  사라지고 상점 진입이 `ShopScene` 한 곳으로 모인 변경이라, 그 파일들을 검사하던 Codex
  PR #277은 병합할 수 없다(아래 참고).

## v0.58.1 — 2026-09-04

- **능력치 상세(돋보기)를 키우고 화면 가운데로 옮겼다.** 열두 줄이 쌓인 성적표라 돋보기에
  매달아 두면 판이 통째로 아래로 밀려 마지막 줄이 화면 밑변에 붙었다 — 손이 닿기도 읽기도
  어려운 자리다. 관찰 기록판과 같은 이유로 **누른 자리에 붙지 않고 자리를 고정**하고
  (`EXTRA_STATS_POPUP_Y`), 판을 640×1010 → 700×1150으로 키워 줄 간격도 함께 넓혔다.
  자리는 화면 한가운데보다 조금 위다 — 정확히 가운데면 밑변이 하단 조작 줄과 겹쳐 보인다.

## v0.58.0 — 2026-09-04

- **사거리를 근거리·중거리·원거리 세 단계로 나눴다.** 지금까지는 전원이 `SKIRMISH.reach`
  한 값(172px)에서 붙어, 바이올린을 켜는 메테도 깃펜을 든 도디도 적 코앞까지 걸어갔다.
  개체는 `RelicDef.reachTier`로 **단계만** 고르고 실제 거리는 `REACH_TIER`(172 / 250 / 340)
  한 표가 정한다 — 개체마다 숫자를 적으면 새 개체가 들어올 때마다 사거리가 조금씩 늘어나
  앞뒤 줄이 흐려진다. 근거리는 예전 값 그대로라 근접 개체의 전투는 바뀌지 않는다.
  기본값을 두지 않고 **필수 필드**로 둔 이유는, 기본값이 있으면 아무도 고르지 않아 새
  개체가 전부 근거리로 들어오기 때문이다.
- 원거리는 **도망치지 않는다.** 카이팅을 넣으면 난전이 술래잡기가 되어 지금의 결이 무너지므로,
  맞으면 그 자리에서 계속 때린다. 얻는 것은 "뒷줄이 저절로 뒷줄에 남는다"는 그림 하나다.
- 배정: 근거리 = 렉시아·토리카·스피나·루카·티아·파치·마키·토비·아모·폰토스,
  중거리 = 도디·스테라·메론·리파, 원거리 = 메테. 폰토스는 거대한 턱에 리치가 있지만
  **일부러 근거리로 뒀다** — 보스가 한 걸음 물러서면 붙는 데 그만큼이 더 걸려 첫 해일이
  늦어지고 원정 최종층이 통째로 쉬워진다.
- 정보창 **능력치 칸 제목 아래**에 "사거리 · 근거리"를 회색 한 줄로 두고, 돋보기로 여는
  **능력치 상세**에서는 다섯 축 아래에 한 줄을 더해 단계 자체를 색으로 말한다
  (근거리 붉은색·중거리 푸른색·원거리 노란색, `REACH_TONE`). 색과 이름은 `statTones.ts`
  한 표가 갖고 화면이 따로 고르지 않는다.
- 원정 보스 회귀 테스트의 첫 해일 상한을 21초 → 26초로 넓혔다. 도디·메테가 뒷줄에 남아
  보스에게 덜 맞으므로 보스의 충전이 그만큼 늦다 — 뒷줄이 안전해지는 것이 이 기능이 노린
  결과라 수치를 되돌리지 않고 구간을 옮겼다.

## v0.57.3 — 2026-09-04

**교류 도시에서 회수한 일지를 발견하고 다시 읽는 기록 시스템을 추가했다.**

- 도시·제목·발견 순서와 일반/분기 본문을 정적 카탈로그에 두고, 분기 원문은 `DialogueStory`로
  분리했다. 미발견 기록은 개수와 잠금 상태만 보여 원문을 미리 노출하지 않는다.
- 발견/읽음 Set을 저장 버전 31의 배열로 직렬화한다. v30 이하 저장은 두 목록을 빈 배열로
  보충하므로 기존 플레이어 진행을 잃지 않고 새 수집을 시작한다.
- 중복 표본은 아이템을 거듭 지급하지 않고 같은 도시의 다음 미발견 표본으로 바꾸며, 도시를 모두
  모은 뒤에는 추가 지급 없이 소진한다. 이 정책과 최초 발견·읽음·복원·정렬을 단위 테스트로 고정했다.
## v0.57.2 — 2026-09-04

**교류 층을 가운데로 모으고, 발굴 생산량 줄에 실제 재화 아이콘을 세웠다.**

- 층이 **좌우로 번갈아 뻗던 것을 걷었다.** 지그재그는 훑는 눈을 매 줄 좌우로 끌고 다녀 목록이
  정신없이 읽혔다. 모두 같은 x에 서고 양 끝만 화면 밖으로 조금 넘친다. 두께(132 → 176)와
  간격(168 → 214)을 함께 키워 여백을 줬다.
- 층 왼쪽 안에 **도시 원화를 슬쩍 눕혔다.** 글자만 있는 띠로 두면 도시마다 다른 곳이라는 것이
  이름으로만 읽힌다. 오른쪽으로 갈수록 판 색에 녹도록 좌우 그라데이션을 덮는다 — 마스크 끝을
  그대로 두면 붙여 넣은 섬네일처럼 각진 경계가 남는다.
- 잠긴 층의 이름이 **화면 왼쪽으로 잘려 나가던 것**을 고쳤다. 층이 화면보다 넓어 왼쪽 여백은
  화면 밖에 있는데 거기서 글을 시작하고 있었다. 이제 잠기든 아니든 같은 시작선에서 시작한다 —
  같은 시작선이 목록을 목록으로 읽히게 한다.
- 발굴 카드의 생산량 줄이 **실제 재화 그림**을 쓴다(`subStyle: "currency"`). 여러 색으로 그린
  재화 아이콘에 강조색 tint를 먹이면 무슨 재화인지 알 수 없는 한 덩어리가 되므로 tint를 걷고
  알파도 되돌렸다. 글자는 흰색에 검은 테두리를 둘러 밝은 발굴장 원화 위에서도 살아남는다 —
  판을 깔면 카드 안에 상자가 하나 더 생긴다.
## v0.57.0 — 2026-09-04

**교류를 층으로 갈아엎었다. 여러 도시에 함께 보내 두고 나중에 걷는 콘텐츠가 됐다.**

- 도시는 **좌우에서 뻗어 나오는 긴 층**으로 위에서 아래로 쌓인다(`interactionLayerLayout.ts`).
  뻗어 나온 쪽 끝은 늘 화면 밖에 남고 반대쪽 끝만 안으로 들어온다 — 양 끝이 모두 안에서 끝나면
  목록이 아니라 카드 여러 장으로 읽힌다. 홀짝으로 방향이 갈려 훑는 눈이 지그재그로 걸린다.
- **도시마다 교류부와 수뇌부가 따로 열린다.** 교류부는 자주(10분~2시간), 수뇌부는 오래
  (4~24시간) — 같은 도시라도 물자를 받아 오는 실무 창구와 협정을 맺고 오는 윗선은 같은 일이
  아니다. 초반에 10분짜리를 둬 자주 들어오는 것에도 몫이 있게 했다.
- 해금은 플레이어 레벨이 연다. **잠긴 층도 목록에 남긴다** — 지우면 다음에 무엇이 열리는지
  보이지 않아 레벨을 올릴 이유가 화면에서 사라진다.
- **여러 도시에 동시에 파견한다.** 응답 계약이 한 장(`dispatch`)이라 도시가 늘어도 한 곳밖에
  못 보냈다. 목록(`dispatches`)으로 바꾸고, 같은 도시와 이미 나가 있는 렐릭만 서버가 막는다.
  저장의 슬롯 배열 상한도 함께 풀었다(옛 저장 한 장은 그대로 통과한다).
- 층을 누르면 그 도시의 쪽지가 열린다 — **위 원화 · 가운데 설명과 기대 재화 · 아래 파견대 세 칸**.
  칸을 누르면 아직 나가지 않은 렐릭이 아래에서 올라와 덮고, 고르면 다음 빈 칸으로 이어진다.
  이미 나가 있는 렐릭은 **아예 보여 주지 않는다** — 눌러야 막히면 왜 안 되는지 화면이 말하지
  않은 채 손만 헛돈다.
- 나가 있는 층은 검은 반투명을 한 겹 더 쌓아 남은 시간을 그 위에 적고, 다녀온 층을 누르면
  보상 영수증으로 이어진다. 우하단 뒤로가기 자리는 판이 아니라 **그라데이션**으로 풀어 층이
  침범하지 않게 했다.
- FakeServer의 파견 id에 증가 번호를 넣었다. 시각과 난수만으로 만들면 같은 밀리초에 두 곳으로
  나갈 때 id가 겹쳐 **수령이 엉뚱한 파견을 집는다.**

## v0.56.1 — 2026-09-04

**발굴 배치에도 자동 배치를 두고, 기준은 화살표로 고른다.**

- 무엇을 많이 캘지는 지금 모자란 재화에 따라 달라지므로 **하나의 "가장 좋은 배치"를 강요하지
  않는다.** 골고루 · 치즈케이크 · 화석 · 골드 · 젬 다섯 기준을 화살표로 돌려 고른다.
- 규칙은 순수 모듈(`src/core/excavationAutoAssign.ts`)이 갖는다 — 골고루는 서로 다른 재화를 먼저
  한 명씩 세우고, 치우친 기준은 그 재화를 캐는 렐릭을 먼저 세운다. 어느 쪽이든 남는 자리는
  생산량 순으로 메운다(빈 칸은 아무것도 캐지 않으므로 기준을 지키느라 비우는 것이 더 손해다).
- **같은 입력은 늘 같은 배치를 낸다.** 누를 때마다 결과가 바뀌면 자동 배치를 다시 눌러 보는 것이
  곧 뽑기가 된다 — 같은 생산량은 개체 id로 갈라 순서를 고정했다.
- 그 자리에 있던 조작 설명 문구는 걷었다. 설명 대신 **그 조작을 대신해 주는 단추**를 둔다.

## v0.56.0 — 2026-09-04

**스토리 편성 그리드를 창 안에서 흐르게 하고, 자동 배치를 머리 끝 위로 올렸다.**

- 보유 그리드에 **마스크도 스크롤도 없었다.** 렐릭이 늘면 줄이 그대로 아래로 자라 안내 문구와
  전투 시작 버튼을 침범했다. 원정 편성·도감과 같은 방식으로 창(`ROSTER_VIEWPORT`) 안에서만
  흐르게 하고, 첫 줄은 머리가 잘리지 않는 공용 안전 영역만큼 내려 세운다. 끌어 내리다 손을 뗀
  자리의 카드는 편성되지 않는다.
- 자동 배치 버튼은 **카드 윗변이 아니라 머리 끝**을 기준으로 띄운다. 몸체만 피하면 칩 밖으로
  빠져나온 정수리(도디처럼 머리가 큰 원화)와 겹친다 — 그리드의 보이는 윗선은 몸체가 아니다.
## v0.55.29 — 2026-09-04

**프로필 편이 틀린 이유로 통과하고 있었다.**

- 공용 X를 누른다던 편이 실제 X(y 550)보다 **90px 아래**를 누르고 있었다. 그 빈 판을 누른 손이 뒤
  배경까지 내려가 팝업이 닫혔고, 그래서 "X로 닫힌다"가 확인된 적 없이 통과했다. 판이 제 입력을
  삼키게 된 뒤(v0.55.22) 그 구멍이 막히며 드러났다.
- 좌표를 적어 두지 않고 `PLAYER_PROFILE_LAYOUT`과 `POPUP_CLOSE_LAYOUT`에서 구하도록 고쳤다 —
  창 크기가 바뀌어도 따라간다.

## v0.55.28 — 2026-09-04

**적 머리 위 상태 칩의 겹 수 배지가 칩 밖으로 흘러내려 체력 바를 덮던 것을 고쳤다.**

- 칩을 22px로 줄일 때 **배지 값이 예전 큰 칩 기준으로 남았다.** 판 반지름 7이 (8,8)에 앉아 15까지
  뻗는데 칩 반폭은 11이라 오른쪽 아래로 4px 넘쳤고, 그 모서리는 빗변으로 깎여 있어 더 크게
  삐져나와 **바로 아래 체력 바를 덮었다.** 지속시간 오버레이는 칩 중심 기준으로 잘려 있어
  문제가 없었다 — 흘러내린 것은 배지다.
- 배지를 칩 안에 앉혔다(`stackCount` (4,4) · 반지름 4 · 글자 11). 빗변도 `chipBevel` 한 값에서
  읽어 칩 모양과 배지가 함께 움직인다 — 룬 액자의 "표식은 액자 안쪽에 앉힌다"와 같은 규칙이다.
- `tests/unit/battleStatusLayout.test.ts`가 판이 네 변과 깎인 빗변을 넘지 않는지, 숫자도 아래 변을
  넘지 않는지 지킨다. 칩 크기를 다시 줄이면 이 테스트가 먼저 깨진다.

## v0.55.27 — 2026-09-04

**정지 중에도 화면과 관찰값을 갱신한다. v0.55.12에서 들어간 회귀다.**

- 전투를 멈추는 판단(`simulationPaused`)이 `refreshProfiles`·`refreshDebug` **앞에서** 돌아가 버려,
  판이 열리는 순간 프로필과 `__PF_DEBUG.battle`이 **직전 값으로 얼어붙었다.** 기여도 판이 눈앞에
  펼쳐져 있는데도 밖에서는 접혀 있다고 읽혔고, 팝업이 떠 있는 동안 전투 상태를 읽는 곳은 모두
  같은 영향을 받았다. 멈추는 것은 코어 시간뿐이므로 그 둘은 계속 돈다.
- 다중 기절 편은 "준비됐다"를 보고 누르러 가는 사이에 전투가 끝나 이미 지도로 돌아가 있었다.
  손으로 누르는 대신 자동 궁극기에 맡겨 충전이 끝나는 그 프레임에 터지게 했다 — 배속은 프레임이
  촘촘할 때만 실제로 빨라지므로, 느린 환경에서 그 틈은 사람이 메울 수 없다.

## v0.55.26 — 2026-09-04

**원정 편성 관찰값을 되살리고, 원정 두 편의 옛 전제를 고쳤다.**

- **`setDebugExpeditionFormation`은 정의만 남고 부르는 곳이 없었다.** 관찰값이 늘 비어 있어, 편성
  화면이 눈앞에 떠 있는데도 E2E는 열리지 않았다고 읽었다(추적에서 꺼낸 마지막 프레임이 "원정대 3기
  선택"을 그대로 보여 준다). 준비 화면이 편성을 그릴 때마다 인원수와 세 칸 자리를 올리고, 기록·지도
  단계로 돌아가면 비운다.
- 보유 카드로 편성을 되돌리는 자리는 **고정 좌표로 정할 수 없다** — 어느 렐릭이 빠졌는지는 앞선
  드래그가 정하고, 카드는 누를 때마다 뒤집히므로 이미 선 렐릭을 도로 내리기도 했다. 늘지 않으면
  곧바로 되돌리고 다음 카드를 보게 했다.
- 지도를 밀어 편성판을 지우는 편은 **판 위에서 끌기 시작하고 있었다.** 편성판은 지도 위에 떠서 제
  입력을 먹으므로 지도가 한 픽셀도 움직이지 않았고, 몇 번을 밀어도 판이 그대로였다. 판이 덮지 않은
  곳에서 시작하도록 고쳤다.

## v0.55.25 — 2026-09-04

**발굴 편집 편이 옛 해제 경로를 기대하고 있었다. 게임 코드는 바뀌지 않는다.**

- 남은 모바일 세 편은 **v0.55.7(이번 작업 이전)에서도 그대로 실패한다** — 별도 워크트리에 그 시점을
  빌드해 확인했다. 이번 변경이 만든 회귀가 아니라 오래 방치된 실패다.
- 발굴 편집 편은 "카드가 든 칸을 고른 뒤 같은 카드를 눌러 해제한다"를 기대했는데, **지금은 칸을
  누르는 것 자체가 그 자리를 비우고 고른다**(편집 슬롯의 짧은 탭은 draft의 그 자리만 해제한다).
  그래서 카드를 다시 누르면 해제가 아니라 재배치가 되고 선택이 다음 칸으로 넘어간다. 실제 경로를
  확인하도록 고쳤고, 칸 선택과 카드 배치 사이에 확인을 하나 넣어 다음에 빗나가면 **어느 쪽이**
  빗나갔는지 실패가 말하게 했다.

## v0.55.24 — 2026-09-04

**모바일 편의 두 전제를 고쳤다. 게임 코드는 바뀌지 않는다.**

- **자동 배치 화살표 편**은 "화살표가 0개인 상태에서 자동 배치를 누르면 3개가 된다"를 확인하는데,
  준비 화면이 빈 상태로 열리지 않아 시작부터 2개가 서 있었다. 먼저 자리를 비우고 확인한다.
- **원정대 3기 편**은 기록 화면의 판들이 닫힌 뒤에야 드러나는 출격 버튼을 한 번만 눌러 허공을
  쳤다. 편성이 열릴 때까지 다시 누른다.
- 공용 헬퍼 수정(저장 씨앗·팝업 관찰값·판 입력 막)만으로 `mobile.spec` 15편 중 11편이 통과한다.

## v0.55.23 — 2026-09-04

**새로 고침마다 준비된 저장이 게임의 저장을 덮던 것을 고쳤다. 게임 코드는 바뀌지 않는다.**

- `startAfterOpening`의 씨앗 저장은 `addInitScript`로 심기는데 **그것은 새로 고침마다 다시 돈다.**
  그래서 재접속을 확인하려고 `page.reload()`를 하는 순간 방금 게임이 남긴 저장을 준비된 저장이
  도로 덮어썼고, 교류 편은 `interaction`이 비어 있는 채로 이어가다 `completesAt`을 null에 쓰려다
  터졌다. 씨앗은 **저장이 없을 때만** 심는다 — 저장 유지·복원을 확인하는 모든 편에 해당한다.
- 하단 탭 편도 화면이 이름을 바꾼 뒤에 탭의 입력면을 마저 만들므로, 옮겨질 때까지 다시 누른다.

## v0.55.22 — 2026-09-04

**판 안을 눌렀는데 판이 닫히던 것을 고쳤다.**

- 팝업 몸판은 그림(Graphics)이라 입력을 받지 않는데, 그 뒤에는 화면 전체를 덮는 배경 판이
  "바깥을 누르면 닫기"로 깔려 있다. 그래서 **판의 빈 곳이나 머리글을 누른 손이 뒤의 배경까지
  내려가 팝업을 닫았다.** 읽는 중에 손이 스치기만 해도 창이 없어진다.
- 몸판이 제 입력을 삼키도록 투명한 막을 판 바로 위에 깐다. 막은 내용보다 **뒤에** 있으므로 그
  위에 얹는 버튼·목록은 그대로 제 입력을 받고, 화면 아무 곳이나 눌러 닫는 보상 팝업은 제 전용
  입력면을 따로 갖고 있어 그대로 닫힌다.
- 진단 기록: 급여 편은 팝업이 열리지 않는 줄 알았는데, 관찰값을 200ms 간격으로 찍어 보니 **열려
  있었다.** 실패한 것은 그다음 "판 안을 눌러도 닫히지 않는다"는 확인이었다 — 테스트 이름이
  가리키던 그 회귀가 실제로 살아 있었다.

## v0.55.21 — 2026-09-04

**급여가 레벨을 올리는 순간 성장 팝업이 사라지던 것을 고쳤다.**

- Playwright 흔적에서 실패 순간의 화면을 꺼내 보니 렐릭은 **레벨 2**가 되어 있었다 — 급여는
  성사됐는데 팝업만 없었다. 급여가 레벨을 올리면 정보창 판이 다시 그려지고, 그때 급여 버튼에
  `pointerout`이 튄다. 그 사건이 실제 `pointerup`보다 먼저 도착하면 **아직 누르고 있는 제스처를
  취소로 삼켜** 버렸다. 레벨업이 걸리는 회차에만 나타나 회차마다 결과가 갈렸다.
- 열지 말지는 **손을 뗀 자리**로 정한다 — 버튼 위에서 떼면(`pointerup`) 열고, 밖에서 떼면
  (`pointerupoutside`) 취소한다. 벗어남(`pointerout`)은 되풀이 급여만 멈춘다.

## v0.55.20 — 2026-09-04

**열려 있는 팝업을 관찰값이 지우던 것을 고쳤다.**

- 화면을 찍어 보니 **팝업은 내내 열려 있었다** — "한 번에 급여"가 1 레벨·10 레벨 버튼과 함께
  떠 있는데 `__PF_DEBUG.popupTitles`만 비어 있었다. 팝업 층은 한 화면에 여럿인데(씬의 층,
  정보창의 층, 용어 층) 관찰값은 하나뿐이고, 각 층이 **제 스택만** 올려서 **나중에 올린 빈 층이
  실제로 열려 있는 팝업을 지웠다.** 살아 있는 층 전부의 제목을 연 순서대로 모아 올린다.
- 층은 **무언가를 열고 있는 동안에만** 그 목록에 남는다. 팝업 층은 조작할 때마다 새로 만들어지기도
  하므로(전투의 기여도·보상) 만들 때 넣고 두면 씬을 오갈수록 죽은 층이 끝없이 쌓인다.
- 급여 버튼의 `release`에서 `pointerup`과 `pointerout`이 잇달아 오면 **뒤에 온 쪽이 앞선 호출이
  서버 응답을 기다리는 동안 제스처 상태를 지우던** 경합도 함께 고쳤다. 기다리기 전에 소유권을
  먼저 가져오고, 이미 가져간 호출이 있으면 곧바로 물러난다.

## v0.55.19 — 2026-09-04

**룬 쪽지의 "세공" 버튼 자리도 화면이 알린다.**

- 계측해 보니 **세공 화면은 애초에 열린 적이 없었다.** E2E 진입 헬퍼의 마지막 탭이 쪽지의 세공
  버튼이 아니라 **쪽지 바깥**을 눌러 그냥 닫고 있었다(`popupTitles`가 `["룬"]`에서 빈 값으로
  바뀌었다). 이 버전대에서 "이미 장착한 룬은 세공·해제 둘"로 줄을 바꾸며 버튼 폭(140 → 176)과
  x(-122 → -94)가 함께 달라진 결과다.
- 그래서 `__PF_DEBUG.runeNoteCraft`로 그 버튼 중심을 알리고, 헬퍼는 그 좌표를 눌러 **세공 화면이
  실제로 열렸는지 확인한 뒤** 진행한다. v0.55.18에서 더한 연필 좌표는 쪽지가 아니라 세공 화면의
  것이라 이름을 `runeForgeRename`으로 바로잡았다.
- 같은 헬퍼를 쓰는 "골드 부족" 편이 그동안 통과한 것은 그 편이 탭과 캡처만 해서 **세공 화면이
  없어도 통과했기 때문**이다 — 통과가 검증을 뜻하지 않았다.

## v0.55.18 — 2026-09-04

**룬 쪽지의 연필 자리를 화면이 알린다. 그 밖에는 E2E만 바뀐다.**

- **연필은 이름 글자 폭만큼 밀려 선다**(`pencilX`가 이름 너비에서 나온다). 그래서 스펙이 좌표를
  적어 두면 이름이 바뀌는 순간 조용히 빗나가고, 실패는 "입력이 없다"로만 보인다. 보상 팝업의
  확인 버튼과 같은 방식으로 `__PF_DEBUG.runeNoteRename`에 자리만 알린다 — 판이 제자리를 잡은
  다음 프레임에 재야 컨테이너 이동·기울임이 반영된 실제 좌표가 나온다. 쪽지가 닫히면 지운다.
- **짧은 급여 탭 편의 준비가 어긋나 있었다.** 애착 렐릭의 경험치만 경계에 세워 두고 정작 여는
  것은 도감의 **첫 카드**여서, 둘이 다르면 급여가 성사되지 않아 성장 팝업이 뜨지 않았다(같은
  자리를 길게 누르는 편은 통과하므로 좌표 문제가 아니었다). 보유한 전부를 같은 경계에 세운다.

## v0.55.17 — 2026-09-04

**되풀이해 누르면 안 되는 자리에까지 재시도를 쓴 것을 되돌렸다. 게임 코드는 바뀌지 않는다.**

- **`tapUntil`은 한 번 더 눌러도 같은 결과인 자리에만 쓴다.** 화면을 옮기거나 판을 여는 자리가
  그렇다. 급여는 누를 때마다 실제로 먹이고, 안내·쪽지는 배경을 누르면 닫히므로 **늦게 열린 판을
  다음 누름이 도로 닫는다.** 룬 이름 연필은 네이티브 입력을 띄워 더 나빴다 — 그 편이 240초를
  태우고 끝났다. 세 자리는 그려질 틈을 두고 한 번만 누르도록 되돌리고, 규칙은 헬퍼 주석에 남겼다.
  느린 판을 두 번 누르지 않도록 재시도 간격도 1.2초로 넓혔다.
- **`popupTitles`는 스택이 비면 값 자체를 지운다**(`setDebugPopupTitles`). `toEqual([])`은 통과할
  수 없는 기대였다 — "없음"을 확인한다.
- 가방 두 번째 편에 옛 레일 좌표(1096)가 하나 더 남아 있어 배치표에서 읽도록 바꿨다.

## v0.55.16 — 2026-09-04

**원래 흔들리던 카드·조각 탭도 `tapUntil`로 옮겼다. 게임 코드는 바뀌지 않는다.**

v0.55.15를 확인하다 룬·급여 두 편이 그 전 회차엔 통과하고 이번엔 떨어지는 것을 봤다. 수정 탓이
아니라 **원래 흔들리던 자리**였다 — 두 편이 공유하는 진입 헬퍼가 도감 그리드가 카드를 세우기
전에 카드를 눌러, 회차에 따라 맞기도 빗나가기도 했다. 앞선 통과는 운이었다.

- 도감 카드 탭(급여·룬·에셋 회귀)은 정보창이 열릴 때까지, 룬 조각 탭은 쪽지가 뜰 때까지 다시
  누른다. 한 번 누르고 5초를 기다리는 방식은 "왜 안 눌렸는지"를 영영 말해 주지 않는다.

## v0.55.15 — 2026-09-04

**입력면 대기를 걷어 내자 그 뒤에 숨어 있던 좌표·전제 오류가 드러났다. 게임 코드는 바뀌지 않는다.**

- **레일 좌표를 스펙에 베껴 두지 않는다.** 가방은 `y 1096`, 우편은 `y 792`를 누르고 있었는데
  실제 오른쪽 레일은 우편 640 · 친구 792 · 가방 944다 — 가방은 허공을, 우편은 친구를 누르고
  있었다. 두 편 모두 화면이 소유한 `LOBBY_RAIL_BOUNDS`에서 읽는다. 줄이 옮겨져도 같이 따라간다.
- **에셋 회귀 편도 준비 화면이 빈 상태라고 가정하고 있었다.** 세 명이 이미 선 채로 셋을 더
  누르니 도디·메테·루카 대신 토리카·렉시아·루카가 섰다. 먼저 내리고 고르도록 고치고, 줄이
  바뀌는 자리(메테)의 y도 그리드 간격에 맞췄다(1358 → 1324).
- 급여 팝업과 룬 이름 입력도 정보창·쪽지가 원화와 조각을 마저 그린 뒤에야 입력면을 가지므로
  `tapUntil`로 옮겼다.

## v0.55.14 — 2026-09-04

**같은 자리에서 떨어지던 나머지 편들도 `tapUntil`로 옮겼다. 게임 코드는 바뀌지 않는다.**

v0.55.13에서 도감 진입에 먼저 쓴 방식이 실제로 통했다 — 세 편 중 하나만 통과하던 `relicScroll`이
셋 다 통과한다. 같은 실패(누른 뒤 5초를 기다렸다가 "상태가 안 바뀐다"로 떨어짐)를 내던 편들을
그 뒤에 오는 상태를 직접 기다리도록 옮겼다.

- 가방: 레일 버튼 → `inventoryCategory`, 상단 젬 칩 → `popupTitles`
- 우편: 레일 두 번째 행 → `mailPopup.open`
- 룬 쪽지·급여 팝업·에셋 회귀: 하단 탭 → `scene === "relics"`

## v0.55.13 — 2026-09-04

**입력면이 생기기 전에 누르고 지나가던 E2E를 고친다. 게임 코드는 바뀌지 않는다.**

- `tests/e2e/canvasInput.ts`에 **`tapUntil`**을 더했다. 씬이 이름을 바꾼 순간에도 그 화면의
  입력면은 아직 없을 수 있다 — 로비의 하단 탭처럼 원화·SD를 읽고 나서 만들어지는 것들이 그렇다.
  한 번만 누르면 허공을 치고, 뒤이은 5초 대기는 "왜 안 눌렸는지" 대신 "상태가 안 바뀐다"만
  말한다. 예전의 느린 `locator.click()`이 안정성 대기로 그 몫을 우연히 채우고 있었고, 그걸
  걷어 내면서 드러났다. 반응이 없으면 한 번 더 누르는 것은 사람이 실제로 하는 일과 같다.
- 도감 진입(`relicScroll`)이 먼저 이 방식을 쓴다. 같은 자리에서 떨어지던 다른 편들도 확인해
  차례로 옮긴다.

## v0.55.12 — 2026-09-04

**펼친 기여도 판도 전투를 멈춘다. 그리고 E2E가 배속을 믿지 않게 했다.**

- **기여도 판을 펼친 동안에도 전투 진행이 멈춘다.** 팝업·정보창과 같은 이유다 — 화면을 덮고
  읽는 판인데 뒤에서 전투가 계속 굴러가면, 다 읽고 접었을 때 돌아갈 전장이 이미 끝나 있다.
  실제로 캡처 한 장이 10초를 넘는 환경에서 판을 읽는 사이 전투가 끝나 결과 팝업이 떠 버렸다.
- **배속은 프레임이 촘촘할 때만 실제로 빨라진다.** 코어는 한 프레임에 진행할 시간에 상한을
  둔다(`SKIRMISH.maxCatchUp` 0.25초). 프레임이 드문 환경에서는 3배속을 걸어도 진행이 1배속
  언저리로 눌리므로, 배속을 걸었다는 이유로 짧게 잡아 둔 E2E의 대기(35초)가 충전이 끝나기 전에
  끊겼다 — 실측으로 `speed: 3`이 걸린 채 충전 속도가 그대로임을 확인했다. 그 바닥 속도에서도
  끝나도록 대기를 90초로 늘리고, 전투를 끝까지 보는 편이 들어갈 수 있게 편 전체 한도도 4분으로
  올렸다. 이유는 설정과 스펙 주석에 남겼다.
- **준비 화면이 빈 상태로 열린다고 가정하던 두 편을 고쳤다.** 화면은 직전 편성을 복원하고
  없으면 자동 편성으로 채우므로, 세 번만 누르면 이미 선 세 명을 도로 내려 0/3이 되고 출전
  버튼이 아무 일도 하지 않는다. 해제 후 다시 고르는 순서를 `pickParty` 한 곳으로 묶었다.

## v0.55.11 — 2026-09-04

**E2E가 오래 실패해 있던 셋을 고쳤다. 게임 코드는 바뀌지 않는다.**

- **전투를 거치는 편의 한도를 3분으로 올리고 한 곳에만 둔다**(`playwright.config.ts`). GPU 없이
  도는 환경에서는 타이틀 로딩부터 전투 진입까지만 1분 반이 들고 캡처 한 장이 다시 10초를 쓴다 —
  2분 한도로는 판을 열고 분류를 바꾸는 편이 조작을 다 마치고도 캡처 도중에 잘렸다. 편마다
  `test.setTimeout`으로 다시 적던 두 줄은 지웠다. 기여도 판·결과 기여도·패시브 회복 표시 세 편이
  같은 이유로 함께 떨어지고 있었고, 조작 자체는 따로 재현해 보면 처음부터 끝까지 정상이다.
- **확정 편성 저장 테스트의 기대값이 뒤집혀 있었다.** 편성은 고른 순서로 서는데 기대값은 그리드
  순서(렉시아 → 토리카)를 적어 두어 실제 고르는 순서(토리카 → 렉시아)와 어긋났다. 그리드 순서를
  기대값으로 적으면 정렬이 무너져도 통과하므로, 일부러 어긋나게 고른 순서를 그대로 확인한다.

## v0.55.10 — 2026-09-04

**톡 친 손이 꾹 누름으로 읽히던 것을 고쳤다.**

- 눌린 시간을 벽시계(`Date.now`)가 아니라 **입력 사건의 시각**(`pointer.downTime`·`upTime`)으로
  잰다. 벽시계로 재면 화면이 한 번 밀리는 동안 손가락은 이미 뗐는데도 그 사이가 눌린 시간으로
  잡혀, 짧은 탭이 꾹 누름이 되고 원래 조작(편성 토글·궁극기)이 통째로 사라진다. 사건 시각은
  브라우저가 그 입력을 실제로 받은 때라 메인 스레드가 막혀도 흔들리지 않는다. 저사양 환경의
  E2E에서 편성 여섯 번 탭 중 하나가 조용히 삼켜져 저장 순서가 갈리며 드러났다.
- 게이지가 다 차서 먼저 여는 길도 **손가락이 아직 눌려 있을 때만** 지난다
  (`activePointer.isDown`). 프레임이 밀리면 늦게 도착한 `pointerup`보다 게이지 한 바퀴가 먼저
  돌아, 이미 뗀 손으로 정보창이 열렸다.
- E2E: 궁극기 충전 캡처가 기다리던 "정확히 50%" 구간은 **존재하지 않는 프레임**이었다. 충전은
  한 방마다 약 24%씩 계단으로 올라 0.47 다음이 0.71이라, 좁은 창을 기다리면 영영 오지 않고
  60초를 태운 뒤 실패한다. 부채꼴이 반쯤 걷힌 것을 남기는 것이 목적이므로 실제로 지나가는
  중간 구간(0.35~0.8)을 잡도록 고치고 캡처 이름도 `-mid`로 바꿨다.

## v0.55.9 — 2026-09-03

**v0.55.8이 아예 부팅되지 않던 것을 고쳤다.**

- `PopupLayer` 생성자가 `scene.events.once(SHUTDOWN, ...)`을 걸었는데, **팝업 층을 필드 초기화로
  두는 씬**(상점·프리미엄·환경설정)에서는 그 생성자가 씬 생성자 안에서 돌고 그때 `scene.events`는
  아직 없다. Phaser는 게임을 만들 때 등록된 모든 씬을 한 번씩 생성하므로
  `Cannot read properties of undefined (reading 'once')`가 터져 타이틀이 `ready`에 닿지 못했다.
  씬 종료 정리는 **첫 팝업을 열 때** 늦게 걸고, 종료 처리에서 스스로 풀어 씬이 다시 서면 다시 건다.
- `tests/unit/popupLayerBoot.test.ts`가 그 생성자 본문에 `events`가 다시 섞이면 실패한다. 부팅
  자체가 죽는 종류의 회귀라 유닛 테스트로 막는다 — E2E는 한 편에 1~2분이 들어 늦게 알려 준다.
- 이 회귀는 E2E가 잡았다. 앞선 실패를 컨테이너의 Chromium 버전 탓으로 본 것이 잘못이었고,
  브라우저는 정상적으로 떴으며 실제로 앱이 죽어 있었다.

## v0.55.8 — 2026-09-03

**판이 떠 있는 동안 전투를 멈추고, 캐릭터 그리드를 꾹 눌러 상세를 열 수 있게 했다.**

- **팝업·정보창이 열려 있는 동안 전투 진행이 멈춘다.** 판을 읽는 사이에 뒤에서 전투가 굴러가면
  다 읽고 닫았을 때 판이 이미 갈려 있다. 멈추는 것은 **코어 시간뿐**이다 — 컷인·파티클 같은 씬
  tween과 게이지 추격은 그대로 돌아, 닫는 순간 화면이 얼어 있었던 것처럼 보이지 않고 값이
  점프하지도 않는다. `lastStepAt`은 멈춰 있는 동안에도 계속 지금으로 밀리므로 다시 흐를 때
  멈춰 있던 시간이 한꺼번에 들어가지 않는다.
- 열린 팝업 수는 `PopupLayer`가 센다(`anyPopupOpen`). 팝업 층은 화면마다 그때그때 새로
  만들어져 씬이 제 층을 전부 알지 못하므로, 세는 자리는 씬이 아니라 그 경계 한 곳이다.
- **캐릭터 그리드를 꾹 누르면 정보창이 열린다** — 원정 편성·원정 지도 HUD·발굴 배치·전투 프로필.
  스테이지 파티 편성에만 있던 조작이라 화면마다 되는 곳과 안 되는 곳이 갈렸다. 시간·취소·연출
  규칙은 `src/ui/longPressInfo.ts`의 `bindLongPress` 하나가 갖고, 각 화면은 "무엇을 열지"만 넘긴다.
- **누르는 동안 동그란 게이지가 12시부터 시계 방향으로 찬다.** 아무 반응 없이 기다리게 하면 길게
  누르는 조작이 있다는 것 자체를 모르고 손을 언제 떼야 하는지도 알 수 없다. 게이지가 한 바퀴 도는
  순간이 곧 열리는 순간이고, 열린 뒤에는 짧게 걷힌다. 시간·진행도 규칙은 Phaser를 부르지 않는
  `longPressGauge.ts`에 두어 화면과 테스트가 같은 값을 읽는다(`tests/unit/longPressGauge.test.ts`).
- 손가락이 18px 넘게 밀리면 스크롤로 보고 게이지를 접는다. 스크롤 그리드(원정 편성·발굴 배치)는
  끌린 거리로 짧은 탭도 함께 가른다 — 끌어 내리다 손을 뗀 곳의 카드가 편성되지 않는다.
- 정보창이 팝업 위에서 열릴 수 있도록 `InfoManager`에 `baseDepth`를 두었다. 발굴 배치처럼 팝업
  안의 그리드에서 여는 창은 팝업 층보다 위여야 한다 — 기본 층으로 두면 창이 판 뒤에 가려
  아무것도 열리지 않은 것처럼 보인다. 팝업은 열 때마다 새로 만들어지므로 창 자체는
  `sceneInfoManager`가 씬당 하나로 묶어 겹겹이 쌓이지 않게 한다.
- 원정 증강 대상 고르기는 이제 제 핸들러만 걷는다. `removeAllListeners("pointerup")`로 쓸면 같은
  입력면에 걸린 꾹 누름까지 사라져, 증강을 한 번 고른 뒤로는 지도 HUD에서 상세가 열리지 않았다.

## v0.55.7 — 2026-09-03

**상태 칩을 다시 작게 줄이고, 원형 게이지를 덮이는 검정으로 바꾸고, 체력 바 왼쪽에 붙였다.**

- **칩 30 → 22px.** 칩에서 읽어야 하는 것은 "무언가 묻어 있다"와 어느 계열인가(색·문양), 그리고
  겹 수뿐이다 — 정확한 수치가 필요한 손은 눌러서 쪽지를 연다. 키운 만큼 SD와 체력 바를 가렸다.
- **원형 게이지(고리) → 시계 방향으로 덮이는 반투명 검정**(`clockWedge.ts`). 작은 칩 안에 선을
  하나 더 그으면 그 선이 표식과 겹쳐 곧 그림이 된다. 덮는 쪽은 아무 그림도 더하지 않고 남은
  시간만 말하며, 다 덮이는 순간이 곧 풀리는 순간이다. 도형은 마스크가 아니라 **칩 사각형 안에서
  잘라** 만든다 — 기하 마스크는 컨테이너 이동을 물려받지 않아 매 프레임 움직이는 칩에서 어긋난다.
- **줄이 체력 바 왼쪽 끝부터 붙는다.** 가운데 정렬은 상태가 하나 붙을 때마다 줄 전체를 밀어,
  이미 읽고 있던 칩까지 좌우로 움직였다. 바 폭은 배치표 한 곳이 갖고 `UnitHealthBar`도 그 값을
  읽는다 — 두 값이 갈리면 줄이 바를 벗어난다.
- `clockWedge.test.ts`가 부채꼴이 칩 밖으로 나가지 않는지, 모서리를 빠뜨리지 않는지, 덮을수록
  넓어지는지를 지킨다.

## v0.55.6 — 2026-09-03

**머리 위 상태를 체력 바 옆 마름모에서 바 위의 칩 한 줄로 옮기고, 눌러서 읽게 했다.**

- **자리.** 작은 마름모가 체력 바 **옆**에 붙어 있었다 — 상태가 둘만 걸려도 바가 밀려 어디까지가
  체력인지 흐려졌고, 겹 수를 적을 자리도 없었다. 이제 **바 위**에 칩이 한 줄로 서고, 칩 하나가
  상태 하나다. 액자는 전투 버프 칩과 같은 규칙(깎인 네모 + 사방 외곽선 + 안쪽 비네트)을 작게
  줄여 쓴다(`UnitStatusChips`).
- **겹 수는 칩 우하단의 작은 수**다. 같은 표식을 여러 개 늘어놓지 않고, 가운데가 아니라 모서리에
  적어 표식 그림과 숫자가 겹치지 않게 했다.
- **남은 시간은 칩 둘레를 도는 시계**다. 12시에서 시계 방향으로 남은 만큼 돈다. 분모를 화면이
  스킬 정의에서 되짚지 않도록 코어의 상태가 `total`을 들고 오게 했다(기절·출혈·덧칠). 손질은
  시간이 흘러 사라지지 않으므로 시계를 그리지 않는다.
- **바나 칩을 누르면 쪽지가 열린다**(`UnitStatusPopup`). 칩은 작아 "무엇이 걸렸나"까지만 말하고,
  몇 겹이 얼마나 남았는지는 눌러서 읽는다. 칩과 쪽지는 **같은 순수 목록**(`unitStatusModel.ts`)을
  읽어 칩에는 뜨는데 쪽지에는 없는 상태가 생기지 않는다.
- **파치의 4타 카운터는 본인에게 붙는다.** 다음 한 방을 정하는 것은 그 개체의 타수이므로 적
  머리 위가 아니라 **자기 프로필의 버프 칩**이 그 수를 우하단에 든다(`ActiveCombatBuff.stacks`).
- `unitStatusModel.test.ts`가 순서·겹·시계 값과 쪽지 한 줄을, `battleStatusLayout.test.ts`가 칩
  줄의 가운데 정렬과 겹치지 않는 간격을 지킨다. pixel-7 캡처로 칩과 쪽지를 함께 확인했다.

## v0.55.5 — 2026-09-03

**파치 폭주 설명에서 튕기는 횟수를 뺐다 — "그 적을 날려버린다"로 끝난다.**

- 초든 횟수든 그 수는 **플레이어의 다음 조작을 바꾸지 않는다.** 날아가는 그림이 곧 그 답이고,
  그동안 무엇을 못 하는지는 `[[knockback|날려버림]]` 태그가 이미 말한다. 화면 문구는 행동을
  결정하는 정보만 남긴다는 규칙을 그대로 따른 것이다(v0.55.3에서 초 → 횟수로 바꿨던 것을
  한 걸음 더 줄였다).
- 전투 규칙(3번 튕김)은 그대로다. `bounces`는 데이터가 갖고 화면이 말하지 않을 뿐이다.

## v0.55.4 — 2026-09-03

**E2E 조작 하나가 6.3초 걸리던 것을 0.7초로 줄였다 — 전투 화면 테스트가 실패하던 근본 원인이다.**

- **원인은 게임이 아니라 헬퍼였다.** GPU 없는 컨테이너에서 실측하니 한 번 누르는 데
  `boundingBox()` 2.0초 + `locator.click()` 3.9초 = **6.3초**가 들었다. Phaser 캔버스는 매
  프레임 다시 그려지므로 Playwright의 "안정될 때까지" 대기가 좀처럼 끝나지 않는다. 전투는
  14초면 끝나므로 조작 두어 번이면 결과 팝업이 떠 버렸고, 그 다음 탭이 팝업을 닫아 지도로
  빠지면서 엉뚱한 줄에서 실패했다. 같은 자리를 `page.mouse.click`으로 누르면 0.37초,
  상자는 브라우저의 `getBoundingClientRect()`로 0.33초에 읽는다.
- **`tests/e2e/canvasInput.ts` 하나가 캔버스 입력을 소유한다.** 스펙 12개가 저마다 같은 헬퍼를
  복사해 두고 있었고 그중 절반이 느린 `locator.click`이었다 — 한 곳을 고쳐도 나머지가 그대로
  남는 구조였다. `tap`·`drag`·`longPress`·`captureGame`을 여기로 모았다.
- **캡처도 5~6초에서 1초대로 줄었다.** 기기 배율(pixel-7 2.625배)을 그대로 두면 1080×1920
  화면이 2835×5040으로 구워진다. `scale: "css"`로 CSS 픽셀 크기만 굽는다.
- **`__PF_DEBUG.battle`을 전투 씬 종료 때 비운다.** 남겨 두면 이미 지도로 나온 뒤에도 마지막
  전투 값이 그대로 읽혀 "전투 중"이라고 답한다 — E2E가 그 굳은 값을 몇 초씩 기다리다 실패
  이유를 엉뚱한 줄에 남겼다.
- **대기에 기대던 자리를 눈에 보이는 확인으로 바꿨다.** 출격 선택판·룬 쪽지처럼 비동기로
  그려지는 판은 열릴 때까지 기다린 뒤 누른다. 예전에는 `locator.click`의 4초 대기가 우연히
  그 몫을 하고 있었다.
- 결과: 문제였던 "전투 기여도 판" 편이 **2분 뒤 실패 → 28초 통과**로 바뀌었다.
- **남은 실패는 이번 변경 이전부터 있던 것이다.** flow.spec의 "스토리 전투 확정 편성"과
  "전투 시작의 빠른 연속 탭"은 **main 코드에서도 같은 값으로 실패한다**(편성 순서가
  `["rex","anky","spino"]`가 아니라 `["anky","rex","spino"]`로 저장된다). 편성 토글 규칙이
  바뀐 뒤 테스트의 기대값만 옛 값으로 남은 것으로 보이며, 무엇이 맞는지는 기획 판단이라
  이번에는 건드리지 않고 기록만 남긴다.

## v0.55.3 — 2026-09-03

**날려버림과 사망이 곡선으로 굴러가지 않고 맞은 방향으로 따악 튀어 나간다.**

- **방향은 시전자 → 피격자다.** 사망 사건에 `sourceId`(마지막 일격을 넣은 쪽)를 실어, 화면이
  그 두 점으로 날아가는 방향을 만든다. 예전에는 좌우를 **난수로 골라** 때린 쪽으로 되날아가는
  그림이 나왔다. 지속 피해처럼 가한 쪽이 없는 죽음만 바라보던 반대쪽으로 날린다.
- **맞는 순간 몸이 가로로 길고 세로로 꾹 눌린다**(`SQUASH`). 세게 맞은 그림은 날아가는 속도가
  아니라 이 한 프레임이 만든다. `placePuppet`이 매 프레임 배율을 다시 잡으므로 tween이 아니라
  시작 시각으로 두고 `syncViews`가 그 위에 곱한다 — tween으로 두면 다음 프레임에 덮인다.
- **끝을 시간이 아니라 부딪히는 횟수로 정한다.** 날려버림 3번, 사망 6번이다. 시간으로 끊으면
  전장 크기와 속도에 따라 어떤 판에서는 두 번, 어떤 판에서는 다섯 번 튕겨 같은 연출이 다른
  무게로 읽힌다. `FerocityTrait.bounces`와 `Fighter.knockback.bouncesLeft`가 그 수를 갖고,
  `seconds`는 벽에 닿지 못할 때를 위한 안전장치로만 남는다.
- **속도를 올렸다.** 날려버림 720 → 2400 px/s, 사망 260~520(난수) → 2600 px/s. 구간 보간도
  `Sine.InOut`에서 `Linear`로 바꿨다 — 구간마다 가속·감속하면 튕기는 것이 아니라 흐르는 것으로
  보인다. 사망은 마지막에 조용히 오므라들지 않고 **번쩍** 터지며 사라진다.
- **파치 폭주 설명이 초 대신 튕기는 횟수를 말한다.** 실제로 정해져 있는 것이 그 수이고, 날아가는
  시간은 전장과 부딪히는 자리에 따라 달라져 초를 적으면 화면과 갈린다.
- 사망 궤적(`knockbackFlightPath`)의 계약도 시간에서 횟수로 바뀌었다. 회귀 테스트는 시킨 횟수만큼만
  벽에 닿는지, 전장을 벗어나지 않는지, 튕길수록 느려지는지를 지킨다.

## v0.55.2 — 2026-09-03

**끼고 있는 룬을 벗을 길을 만들고, 기여도 판이 제 버튼에 가리지 않게 했다.**

- **이미 끼워져 있는 룬의 쪽지는 「세공 · 해제」다.** 예전에는 세공과 판매가 섰는데, 끼고
  있는 룬은 서버가 판매를 거부하므로 **눌리지 않는 버튼**만 자리를 차지했고 정작 벗는 길은
  룬 가방 안의 "비우기" 한 곳뿐이라 사실상 없었다. 그 자리에 지금 할 수 있는 조작을 세운다.
  해제는 룬 ID가 아니라 **자리**(`equippedRuneSlot` → `unequipRune(relicId, slotIndex)`)로
  보낸다 — 저장의 장착표가 렐릭별 칸 배열이라 비울 칸을 지목해야 한다.
- **전투 기여도 판을 펼치면 그래프 칩이 사라진다.** 칩은 판 **위에** 서 있어 첫 행을 가리고
  있었다. 감춘 칩을 다시 누르라고 할 수는 없으므로 **판 밖 아무 곳이나 누르면** 접히고 칩이
  돌아온다. 투명한 판을 깔지 않고 씬의 포인터를 직접 듣는다 — 전장 위에 입력 면을 한 겹 깔면
  그 아래의 SD와 조작 칩이 함께 막히고, 이 판은 전투를 멈추는 창이 아니다.
- **그 판의 행에도 얼굴 액자를 넣었다.** 결과 화면의 기여도 판에는 있는데 인게임 판에는 없어
  이름만 다섯 줄이 늘어섰다. 자리는 칩이 비운 판 왼쪽 여백이고, 같은 개체가 그대로면 액자를
  다시 만들지 않는다(원화 텍스처를 비동기로 읽으므로 매 갱신마다 새로 만들면 깜빡인다).
- **상점 구매 확인 창은 판 밖을 눌러도 닫힌다.** 아직 아무것도 쓰지 않은 자리라, 사지 않기로
  한 손이 오른쪽 위 X를 찾아 올라갈 이유가 없다. 실제 차감은 확정 버튼만 한다.
- 알려진 문제: `flow.spec.ts`의 "전투 기여도 판을 열고 세 분류를 바꾼 뒤…" E2E는 **이번 변경
  전부터** 실패한다. 전투가 14초 안팎으로 끝나는데 E2E 조작 하나가 2초쯤 걸려, 마지막 조작이
  이미 뜬 결과 팝업에 닿아 지도로 빠진다. 판 자체는 pixel-7 캡처로 확인했다.

## v0.55.1 — 2026-09-03

**로비 바닥선이 관절을 기준으로 삼지 않는지 확인하고, 관절 자체를 검사로 묶었다.**

- 바닥선은 처음부터 **alpha 경계(`content.bottom`)**를 쓰고 있었다 — 발 관절이 아니다.
  관절은 그림 밖에 박혀 있는 묶음이 있어 바닥선의 기준이 될 수 없다.
- 다만 **배율을 재는 눈 관절은 그림 안에 있어야만 뜻이 선다.** 밖에 있으면 그 개체만
  조용히 크거나 작아지고, 원인이 `lobbyZoom` 값이 아니라 관절에 남아 값을 고쳐도 다시
  어긋난다. 그래서 `tests/unit/lobbyPortrait.test.ts`에 눈·코어 관절이 alpha 상자 안에
  드는지 검사하는 계약을 더했다. 지금 11종은 모두 안에 있다(눈 높이 10.3~26.8%).
- 그 기준을 `CLAUDE.md`의 로비 항목에도 남겼다.

## v0.55.0 — 2026-09-03

**서버 시각과 멱등 수령을 기준으로 하는 도시 교류 파견을 추가했다.**

- 12~24시간 도시, 데이터 태그 기반 추천 속성·스쿼드, 보상 성향과 단서 일지를 정적 데이터로 분리했다.
- 편성·개방·시간·가중치·완료 계산을 순수 규칙으로 두고, FakeServer가 출발 시 종료 시각과 결과를 확정한다.
- 저장 v30은 진행 슬롯을 JSON 스냅샷으로 보존하고 v29 이하 저장은 빈 슬롯으로 마이그레이션한다.
- 푸른 홀로그램 교류 화면과 로비/설정 왕복을 연결했으며 전용 배경 전에는 로비 원화를 임시 사용한다.
- 병합하며 화면 문구가 내부 ID(`gold`·`water`·`gear`)를 그대로 적던 세 자리를 표시 이름으로
  고쳤다 — 재화는 `currencyGuide`, 속성은 `ELEMENT_LABEL`, 스쿼드는 `SQUADS` 표가 이름을 갖는다.

## v0.54.6 — 2026-09-03

**마키의 첫 도약에 틈을 두고, 궁극기를 위력 대신 순환으로 세웠다.**

- **패시브 「고품격 식재료는 어딨냥?」의 첫 도약을 1.2초 뒤로 미뤘다**
  (`Passive.huntOpeningSeconds`). 시계를 0에서 시작하니 전투 첫 프레임에 자리를 옮겨,
  플레이어가 전장을 눈에 담기도 전에 마키만 다른 곳에 서 있었다 — 어디에서 어디로 갔는지가
  보이지 않으면 도약이 아니라 그냥 다른 자리에서 시작한 것으로 읽힌다. 재발동 간격(10초)과
  처치 즉시 재도약은 그대로다.
- **궁극기 「오마카세」 400% → 250%, 처치 시 게이지 환급 100 → 200.** 한 방의 크기를 줄이고
  **처치했을 때만 돌아오는 몫**을 키웠다. 코스트가 220이라 잡으면 거의 그대로 다음 궁극기로
  이어지고, 못 잡으면 아무것도 없다 — 마무리를 노리는 개체라는 성격이 위력이 아니라 순환에서
  나온다.
- 두 테스트가 값을 손으로 적고 있어(`energyRefundOnKill 100`, `power 400`) 데이터에서 읽도록
  고쳤다. 수치를 조정할 때마다 테스트만 옛 값으로 남는 것을 막는다.

## v0.54.5 — 2026-09-03

**로비에 선 애착 렐릭의 크기를 상자가 아니라 키가 정하게 했다.**

- **문제.** 로비는 원화를 감상하는 화면인데, 크기를 `LOBBY_BOX` 하나에 맞춰 넣고 있었다.
  원화마다 캔버스 여백과 등신이 달라 **1.08 m 토리카가 1.76 m 메테보다 크게** 섰고,
  발끝 높이도 개체마다 제각각이라 같은 광장에 선 것으로 보이지 않았다.
- **모든 개체가 같은 바닥선(`LOBBY_PORTRAIT_SPOT.floor`)에 발을 세운다.** 예전에는 그림
  위쪽을 상자 천장에 붙였기 때문에, 작은 개체는 발이 공중에 떴다.
- **키가 크기를 정한다.** 눈 관절에서 발끝(alpha 경계)까지가 화면에서 `관찰 프로필의 키(m)
  × 912 px`가 되도록 원화마다 `lobbyZoom`을 실측해 붙였다. 기준은 메론(1.58 m) — 지금
  가장 보기 좋은 크기라 그 원화만 보정이 없고, 나머지가 그 크기를 향한다.
  토리카 0.774 · 도디 0.729 · 티아 0.851 · 파치 0.924 · 스테라 0.965 · 루카 0.999 ·
  렉시아 1.002 · 스피나 1.018 · 마키 1.048 · 메테 1.089.
- **자리 계산은 화면이 아니라 `portraitPlacement.ts`가 갖는다.** 씬이 좌표와 배율을 직접
  적으면 다음에 고칠 때 테스트와 화면이 다른 값을 읽는다.
- `tests/unit/lobbyPortrait.test.ts`가 실제 ZIP에서 잰 눈 관절로 그 비례(0.5% 이내)와
  같은 바닥선, 화면 위로 넘지 않는지를 지킨다. pixel-7 캡처로 가장 작은 도디부터 가장 큰
  메테까지 함께 확인했다.

## v0.54.4 — 2026-09-03

**스테라의 게이지 용어를 바로잡고, 규칙어 태그에서 중복 수치와 군더더기를 걷어 냈다.**

- **스테라 일반 공격의 아군 게이지 2 → 5.**
- **"궁극기 충전량"을 "궁극기 게이지"로 고쳤다.** 평타는 게이지를 **직접 채우는** 것이고
  충전량은 공격당 얼마씩 오르는가라는 다른 값인데, 두 문장이 같은 말을 쓰고 있었다. 폭주도
  "공격할 때마다 오르는 게이지가 늘어난다"로 바꿔 무엇이 커지는지 분명히 했다.
- **출혈 태그에서 시간과 비율을 뺐다.** 렉시아와 마키가 같은 태그를 쓰는데 태그가 "3초 동안
  2%"라고 못 박고 있어, 본문과 중복이면서 시간이 다른 개체가 생기면 곧바로 거짓말이 된다.
  이제 **시간·비율은 본문이 스킬마다 적고 태그는 그것이 무엇인지만** 말한다. 쓰는 개체가
  하나뿐인 덧칠·뇌진탕·손질은 그대로 태그가 수치를 갖는다 — 그 기준을 `CLAUDE.md`에 남겼다.
- **태그 본문의 굵게 표기와 도입부를 걷어 냈다**(손질·덧칠·돌진). 팝업은 규칙을 확인하러 여는
  자리라 효과·대상·수치만 남긴다.
- 테스트가 스테라의 충전량과 손질 문구를 숫자·문장으로 못 박고 있어 데이터에서 읽게 바꿨다.

## v0.54.3 — 2026-09-03

**전수 점검에서 드러난 네 수치를 조정하고, 무거운 전신 원화 셋을 다시 구웠다.**

60초 동안 방어력 100의 상대에게 넣는 총 피해를 개체마다 계산해 비교한 결과다(평타 + 부가
효과 + 자기 충전만으로 쓰는 궁극기).

- **마키 평타 110% → 80%, 궁극기 코스트 180 → 220.** 조정 전 9,873이 아니라 12,132로 렉시아의
  1.36배였다. 네 축이 모두 상위였던 탓이다 — 최고 공격력(190)에 평타 110%, 손질 폭발, 출혈,
  그리고 SSR 최저 코스트. 지금은 렉시아의 1.11배로, 같은 등급의 최상위라는 자리는 지키되
  혼자 두 배로 벌어지지는 않는다.
- **스피나 궁극기 코스트 300 → 200.** 300은 게이지 최대치라 60초에 네 번밖에 쓰지 못했고,
  SSR인데 코스트 90짜리 SR 루카(14회)와 총합이 거의 같았다. 이제 여섯 번 쓴다.
- **메테 궁극기 코스트 50 → 80, 잃은 체력 회복 20% → 15%.** 3.3초마다 팀 회복은 같은 지원
  궁인 스테라(코스트 200)와 견줄 수 없이 잦았다.
- **렉시아 일반 공격 95% → 120%.** 위 셋을 조정한 뒤 SSR 최상위가 마키 혼자 남아, 같은 등급의
  전사도 그 자리를 다툴 수 있게 올렸다. 60초 총합이 8,895 → 9,935로 마키(9,873)와 나란히 선다.
- **토리카·렉시아·루카의 전신 원화를 다시 구웠다**(작업은 아트 쪽). 메시 격자가 181×256·136×192
  에서 90×128로 줄어 정점이 46,336 → 11,520이 됐고, 이제 전 개체가 10,880~12,288로 고르다.
  로비 대표가 기본값 토리카라 그 한 장이 매 프레임 스프링 시뮬레이션 비용을 결정했다. **알파
  경계는 그대로**여서 `assetMetadata.ts`는 고치지 않았다.
- **티아의 표식은 그대로 뒀다.** 표식은 공격자가 들고 있어(`shimmerMarkTargetId`) **표적이
  바뀔 때만** 터진다 — 매 타격 터지는 것이 아니므로 R 자리에 맞는다.
- 테스트가 조정한 수치를 손으로 다시 적지 않도록, 메테 회복·스피나 코스트 검증을 **데이터에서
  읽어** 계산하게 바꿨다. 수치를 적어 두면 조정한 뒤 옛 값이 테스트에만 남는다.

## v0.54.2 — 2026-09-03

**무역 카탈로그 조회 실패가 팝업을 사용할 수 없는 상태로 남기던 오류를 고쳤다.**

- 조회를 `try/catch/finally` 수명주기로 묶어 성공과 실패 모두 닫힌 팝업이나 지난 세대의 늦은
  응답을 버린다. 요청 문지기는 재접속 연타를 한 요청으로 합치고 해당 요청의 정리만 잠금을 푼다.
- 실패하면 상품 영역만 비운 뒤 세계관 안의 짧은 `교신 두절` 상태와 실제 가능한 `재접속`을
  표시한다. `PopupLayer`가 소유한 무역 판·제목과 로비의 공용 뒤로가기는 그대로 유지한다.
- Phaser에 의존하지 않는 표시·요청 계약 테스트로 API 거절 뒤 chrome·닫기 경로와 중복 요청
  방지를 고정했다.

## v0.54.1 — 2026-09-03

**무역 상품 갱신이 팝업 제목까지 지우던 수명주기 오류를 고쳤다.**

- `TradePopup`에서 PopupLayer의 판·제목을 소유하는 본문과 서버 응답으로 다시 그리는 상품 목록
  컨테이너를 분리했다. 이제 카탈로그 응답은 상품명·가격·버튼·비활성 사유만 교체하므로 `무역`
  제목과 팝업 chrome이 닫힐 때까지 유지된다.
- 닫힌 뒤 도착한 카탈로그 응답은 기존 세대 번호와 함께 본문·상품 컨테이너의 활성 상태까지 확인해,
  Phaser가 이미 파괴한 컨테이너를 다시 사용하지 않는다.
- 모바일 E2E는 무역 진입 직후와 상품 행 렌더 뒤의 제목을 각각 확인하고, 상품 입력과 돌아가기
  입력이 같은 팝업에 함께 남는 계약을 고정한다.

## v0.54.0 — 2026-09-03

**11번 원화에 마키(SSR 땅 암살자)를 세우고, 파치를 SR로 올렸다.**

- **마키 / 126 / OMAKASE / 나이트 기어.** 마카이로두스 해츨링. 이터널 시티 최고의 정형외과
  의사이자 자칭 미식가로, 인양조가 다치면 그 자리에서 붙여 놓는 야전 담당의다. 연구원의 전속
  담당의를 자처하지만 매번 퇴짜를 맞고, 케어실 벽 식재료 순위표 1등 칸에는 연구원의 이름이
  적혀 있다. 태생 전투력 2367로 SSR 띠 안이며 **이동 속도 128로 전 개체 중 가장 빠르다** —
  가장 약해진 적을 골라 뛰어드는 개체라 발이 곧 정체성이다.
- **손질**(새 규칙어)은 덧칠과 반대 축이다. 쌓아 두었다가 나중에 쓰는 값이 아니라 **세 겹째가
  쌓이는 프레임에 스스로 터지고** 겹이 0으로 돌아간다. 시간이 흘러 사라지지도 않는다 — 세
  번째 칼질이 곧 결과이기 때문이다. 터지는 피해는 그 순간 칼을 댄 개체의 공격력에서 나온다.
- **고품격 식재료는 어딨냥?**(패시브)은 **쿨다운을 0에서 시작해** 첫 프레임에 곧바로 도약한다 —
  "전투 시작 시"를 따로 분기하지 않아도 같은 코드 하나가 시작과 재발동(처치 즉시 · 10초마다)을
  모두 맡는다. 최저 체력 적을 고르는 계산은 스피나의 잠행과 한 함수를 공유한다.
- **오마카세**(궁극기)는 처치했을 때만 게이지 100을 돌려받는다(`energyRefundOnKill`). 살아남으면
  아무것도 없다 — 연쇄 처형이 되려면 실제로 끝냈어야 한다.
- **폭주**는 터진 손질 피해의 50%를 **아군 전체**의 회복으로 돌린다. 흡혈이 아니라 팀 회복이라
  때리지 않은 아군도 함께 받는다.
- 손질도 덧칠과 같은 양식의 겹 뱃지를 갖는다(`statusBadgeOffsets`가 네 자리로 늘었다).
- **파치를 R → SR로 올렸다.** R 띠 상단(2194)에서도 세다는 판단이라, SR 띠(2210~2330) 안인
  2288로 옮겼다. 태생 능력치만 오르고 스킬은 그대로다.
- **메론·파치의 전신 원화가 정보창에서 아래로 내려앉던 것을 고쳤다.** 코어(`중심1`) 관절이
  가시 영역의 25%·19% 지점에 박혀 있어(다른 원화는 28~35%) 그 관절을 화면 기준선에 맞추면
  몸이 그만큼 아래로 밀린다. 규칙대로 화면이 아니라 `portraitOffsetY`로 되돌렸다.

## v0.53.0 — 2026-09-03

**10번 원화에 파치(R 땅 전사)를 세우고, 새 규칙어 셋(뇌진탕·돌진·날려버림)을 코어에 열었다.**

- **파치 / 093 / HARD HAT / 앱솔루트 팽.** 파키케팔로사우루스 **성체**. 결전 부대가 밀고
  들어갈 자리를 머리로 뚫는 돌파 담당이라 스쿼드 임무와 그대로 겹친다. 한참 사춘기인 비행
  청소년이라 체리맛 사탕을 물고 다니며 지시마다 툴툴거리지만, 연구원이 말한 자리에서는 한 발도
  물러서지 않는다. 태생 전투력 2194로 R 띠(2080~2200) 안이고, 머리로 받는 개체라 방어가
  두껍고 발이 빠르다(이속 116).
- **무면허 안전제일**(패시브)은 한 방에 들어오는 피해에 **상한**을 둔다. 최대 체력의 40% 이하인
  평범한 타격은 그대로 다 맞고, 넘는 한 방만 그 선까지 눌린다 — 즉사급 일격을 맞아도 체력이
  60% 남고, 두 번이면 20%, 세 번째에 쓰러진다. 아무리 센 공격이든 세 대는 버틴다.
- **뇌진탕**(새 규칙어)은 방어력·속성·경감을 모두 지나치는 즉발 고정 피해다. 최대 체력의 5%,
  치명타였다면 15%. 출혈처럼 시간을 두고 흐르지 않는 이유는 "네 번째 배트가 헬멧을 울린
  순간"이기 때문이다. 수치는 태그가 말하고 본문은 걸린다는 사실만 적는다.
- **`statusEffectEvery`**(새 계약)로 상태 효과의 주기를 정한다. 확정 치명타(`periodicCritical`)와
  다른 축이다 — 그쪽은 피해를 키우고 이쪽은 부가 효과가 걸리는 차례를 정한다. 파치의 배트는
  네 번째에만 기절과 뇌진탕을 건다.
- **돌진**(새 대상 계약 `chargeLine`)은 지금 보는 방향으로 뚫고 지나가며 통로 안의 적을 모두
  친다. 나아가는 거리를 스킬에 적지 않고 **이동 속도 × `SKIRMISH.chargeSeconds`**로 두어,
  "발이 빠른 개체가 더 깊이 파고든다"가 능력치 하나로 설명되게 했다.
- **날려버림은 뇌진탕의 일부가 아니라 파치의 폭주가 얹는 몫이다.** 폭주 중에만 뇌진탕이 확정
  치명타가 되고, 그 위에 적을 전장에서 튕겨 다니게 하는 효과가 더 붙는다. 코어도 그 순서로
  갈라 뒀다(`knockbackSlamOf`) — 다른 개체가 뇌진탕을 갖게 되어도 그 개체가 적을 날리지는
  않는다. 날린 뒤에는 가장 가까운 적으로 표적을 옮긴다.
- **사망 연출도 같은 물리로 바꿨다.** 조용히 떠올라 사라지는 대신 펑 하고 튀어올라 전장을
  튕겨 다니다 삥 하고 오므라들며 별이 된다. 궤적은 화면이 매 프레임 적분하지 않고
  `src/ui/knockbackFlight.ts`가 **구간으로 끊어** 미리 구한다 — 적분하면 배속과 프레임 시간에
  따라 부딪히는 자리가 달라진다. 다만 **좌표의 소유자는 코어**이며(`Fighter.knockback`), 씬은
  회전과 부딪히는 순간의 조각만 얹는다.

## v0.52.5 — 2026-09-03

**10·11번 원화(파치·마키)를 에셋 경계에 등록했다. 스킬과 렐릭 정의는 아직 없다.**

- ZIP 네 장이 `public/` 루트에 올라와 있어 `public/puppets`로 옮겼다. 로더는 그 폴더만 읽으므로
  루트에 두면 파일이 있어도 로딩 단계에서 찾지 못한다.
- 전신·SD 모두 alpha > 16 경계를 실측해 `assetMetadata.ts`에 적고, `assets.ts`의 전신·SD 표와
  `PortraitAssetId`에 더했다. 규칙대로 캔버스 크기를 그대로 적지 않는다 — 그러면 카드와 전신
  배율이 통째로 틀어진다.
- **카드 잘라내기 회귀 목록에 셋을 더했다.** 파치·마키뿐 아니라 **메론도 빠져 있었다** — 목록에
  없으면 정수리가 잘려도 테스트가 통과한다. ZIP의 `머리1`·`눈1`·`눈2` 관절을 그대로 넣었다.

## v0.52.4 — 2026-09-03

**아군 SD 등록이 두 벌로 갈라져 있어 메론이 전투 밖에서 토리카로 서 있었다.**

- `battleAssetFor`와 `sdAssetFor`가 저마다 `if` 사슬을 갖고 있었고, 메론은 전투 쪽에만 적혀
  있었다. 그래서 **원정 편성·승리 결과 MVP·발굴 현황**처럼 비전투 화면에서만 조용히 토리카 SD로
  되돌아갔다 — 폴백이 있는 실수라 오류가 나지 않고 다른 캐릭터가 대신 서는 모습으로만 드러난다.
- **표 하나로 합쳤다.** 아군은 `ALLY_SD_ASSETS`, 적은 `ENEMY_SD_ASSETS_BY_ID`를 쓰고, 전투는
  적 표에 없으면 아군 표를 그대로 통과한다. 새 개체를 한쪽에만 적을 자리가 없어진다.
- `assets.ts`는 Phaser를 들여와 Vitest에서 import할 수 없으므로, 회귀 테스트는 **소스 문자열**로
  표를 읽어 모든 아군 렐릭이 제 자리를 가졌는지 확인한다(토리카만 1번 SD를 제 것으로 쓴다).

## v0.52.3 — 2026-09-03

**덧칠의 수치를 태그로 옮기고, 메론 궁극기 라벨을 예상 최대 피해량으로 바꿨다.**

- **겹 상한·지속 시간·겹당 증가율을 키워드가 말한다.** 그 전의 덧칠 설명은 "겹칠수록 커진다"는
  말만 있어 정작 몇 겹까지 몇 초 동안 얼마나 커지는지를 어디서도 읽을 수 없었고, 수치는 기본
  공격 본문에만 괄호로 붙어 있었다. 경직이 이미 그렇듯 **값을 아는 쪽은 태그**이므로 옮기고,
  본문은 "덧칠을 한 겹 쌓는다"로 줄였다 — 태그가 말하는 것을 본문이 되풀이하지 않는다.
  키워드 문구와 실제 전투 계약이 갈리지 않도록 테스트가 세 값을 직접 대조한다.
- **궁극기 라벨은 겹당 값이 아니라 다 칠했을 때의 피해를 말한다**(`예상 최대 피해량`). 이
  궁극기만 위력이 겹당 값이라, 다른 스킬과 같은 자리에 겹당 수치를 세우면 혼자 4분의 1로 보인다.
  라벨이 "얼마나 세게 터지나", 본문이 "무엇에 비례하나"를 맡는다. 겹 상한은 궁극기가 아니라 그
  덧칠을 만드는 기본 공격이 갖고 있으므로 거기서 읽어 한 곳에만 둔다.
- 궁극기 이름을 「완성_진짜 마지막(4).png」로 바꿨다.

## v0.52.2 — 2026-09-03

**덧칠을 10초·최대 4겹으로 고치고, 메론이 다 칠한 적에게서 손을 떼게 했다.**

- 겹 상한을 5 → 4로, 지속 시간을 8 → 10초로 옮겼다. 궁극기 최대 배율은 주문력 300% → 240%가
  되어 렉시아의 단일 대상 궁극기 아래로 내려간다.
- **패시브에 표적 이동을 더했다** — 표적의 덧칠이 상한에 닿으면 가장 가까운 다른 적으로 옮겨
  간다. 이 절이 없으면 메론은 처음 고른 적만 계속 때리고, 상한을 넘는 타격은 지속 시간만
  갱신할 뿐 겹을 늘리지 않아 **나머지 적에게는 한 겹도 칠하지 못한다.** 전장 전체를 터뜨리는
  궁극기를 가진 개체라 밑그림도 전장 전체에 퍼져야 뜻이 선다.
- 표적을 옮기는 방법은 이크티오 다이브(티아 폭주)와 한 함수를 공유한다. 추적을 `null`로 푸는
  대신 **가장 가까운 다른 적을 직접 고르는** 이유도 같다 — 비워 두면 다음 프레임의
  `resolveTarget`이 방금 때린 그 적을 다시 뽑아 "바꿨다"가 되지 않는다.


## v0.52.1 — 2026-09-03

**메론 궁극기의 겹당 위력을 120% → 60%로 내렸다.**

- 겹당 120%면 다섯 겹에서 주문력의 600%가 되어, **단일 대상 300%인 렉시아의 궁극기를 전장
  전체에 두 배로 얹는** 값이었다. 지원가가 최상위 딜러의 한 방을 넘으면 등급도 역할도 뜻을
  잃는다.
- 60%면 다섯 겹을 다 칠했을 때 정확히 렉시아와 같은 300%에 닿는다. 그마저도 다섯 번 칠하는
  설정이 먼저 필요하므로, 전장 전체 광역이라는 이점의 대가가 남는다.
- 이 상한은 `tests/unit/skillArt.test.ts`가 렉시아의 궁극기 위력과 직접 비교해 지킨다 —
  숫자를 적어 두면 렉시아가 조정될 때 그 관계가 조용히 깨진다.

## v0.52.0 — 2026-09-03

**9번 원화에 메론(SSR 물 지원가)을 세우고, 새 디버프 「덧칠」을 코어에 열었다.**

- **메론 / 047 / QUIET SKETCH / 사일런트 룬.** 메갈로돈의 **유체** 화석에서 복원했다 — 성체
  표본의 파장은 리바이어던급이라 복원 승인이 나지 않았고, 연구소가 힘이 덜 여문 쪽을 골랐다는
  세계관 안의 판단으로 남겼다. 18살(E.C. 18년) 소심한 일러스트레이터라 발이 느리고(이속 86)
  화력 대신 체력·저항이 두껍다. 태생 전투력 2372로 SSR 띠(2340~2460) 안이다.
- **덧칠(`overpaint`)은 겹칠수록 그 적이 받는 모든 피해가 커지는 디버프다.** 기존 지원가 셋과
  축이 겹치지 않게 골랐다 — 메테는 추가타·경직, 도디는 평타 힐, 스테라는 충전량이라 **받는
  피해 증폭 + 그 피해를 아군의 생존으로 환류**하는 자리가 비어 있었다.
  - 일반 「러프 크로키」가 한 겹씩 칠한다(8초·겹당 +6%·최대 5겹).
  - 궁극 「완성_진짜 마지막(4)」는 **쌓인 겹을 터뜨린다.** 위력은 총량이 아니라 **겹당 값**
    (주문력 120%)이라 다섯 겹이면 600%가 되고, 한 겹도 없는 적은 **대상에서 빠진다** — 위력 0의
    최소 피해 1이 뜨면 "터졌다"와 "터뜨릴 게 없었다"가 같은 숫자로 읽힌다. 터뜨린 덧칠은 지워진다.
  - 패시브 「스케치 시작」은 덧칠된 적을 때린 **아군 본인**이 그 피해의 10%를 회복한다. 팀 힐이
    아니라 앞에 나선 사람의 몫이라, 표식 하나가 화력 증폭과 생존을 함께 묶는다.
  - 폭주 「네? 마음에 안 드신다고요...?」는 **아군 전원의 일반 공격**에 덧칠을 얹는다. 앞에 못
    나서는 아이라 제 화력이 아니라 남의 손을 빌리는 쪽으로 발현한다.
  - 증폭은 `resolveReceivedDamage` 한 곳에서 감산보다 **먼저** 곱해지고, 겹 수는 머리 위 뱃지
    하나와 그 옆의 작은 수로만 알린다.
- **티아의 발굴 생산량이 화석 18/h로 남아 있었다.** 발굴 재화 밴드를 20~26/h에서 0.25~0.30/h로
  낮출 때 누락된 값이라, 하루 458개를 뽑아 일일 목표(90)의 다섯 배를 혼자 공급하고 있었다.
  0.28/h로 내려 밴드 안에 넣었다(일일 7.12, 목표의 7.9%). 메론도 같은 밴드의 0.28/h다.
- 회귀 테스트는 덧칠의 겹침·상한·소멸, 증폭 배율, 회복 대상, 겹당 폭발 배율과 표식 소거,
  폭주 중 아군 타격의 덧칠까지 다섯 편으로 고정하고, 표시 계약 네 편을 함께 남겼다.

## v0.51.3 — 2026-09-03

**로비의 콘텐츠 진입점과 편의 기능을 역할별 레일로 정리했다.**

- 오른쪽 콘텐츠 레일을 임무 → 상점 → 무역 순서로 고정하고, 임무 보상 알림과 기존 팝업 연결을 유지했다.
- 우편·친구·가방은 왼쪽 편의 레일로 분리해 중앙 캐릭터와 하단 행동 버튼을 가리지 않게 했다.
- 역할별 고정 좌표와 상단 영역·하단 내비게이션 안전 간격을 순수 배치표와 단위 테스트가 함께 검증한다.

## v0.51.2 — 2026-09-03

**유료 상품 화면을 인게임 상점과 구분되는 프리미엄 진입점으로 명확히 했다.**

- 유료 상품 전용 화면과 하단 탭의 표시 이름을 **프리미엄**으로 바꾸고, 기존 홀로그램 배치와
  쇼핑백 아이콘은 유지해 기능의 위치와 조작 감각은 달라지지 않게 했다.
- Phaser 씬 키를 `shop`에서 `premium`으로 이관하고 설정 복귀 데이터, 디버그 표시 계약,
  단위·E2E 테스트도 함께 옮겨 새 인게임 상점인 로비의 **무역**과 이름이 겹치지 않게 했다.
- 상품 ID와 구매 이력은 화면 명칭과 별개의 영속 계약이므로 변경하지 않았다.
## v0.51.1 — 2026-09-02

**두 암살자의 치명타 원천을 패시브로 옮겼다.**

- v0.51.0에서 태생 치명타를 전 개체 공통 10%로 맞추면서 루카(24)·스피나(15)의 치명타형
  정체성이 사라졌다. 렉시아는 패시브가 받쳐 주지만 두 암살자는 원천이 없었다.
- 규칙이 말하는 방식 그대로 **패시브에 붙였다** — 루카 "무리 사냥" +15%, 스피나 "전투의 환희"
  +10%. 스피나가 낮은 이유는 연격이 한 행동에 **두 번** 판정해 같은 값도 더 자주 터지기 때문이다.
  실전 확률은 루카 25% · 스피나 20%로, 태생에 적혀 있던 v0.50.2 이전(24 · 15)과 비슷하거나 조금 높다.
- **개체 이름이 아니라 필드 하나로 읽게 고쳤다.** 전투가 `passive.kind === "battleMaidMastery"`를
  직접 물어 치명타 가산을 꺼내고 있어, 다른 개체가 같은 값을 적어도 무시됐다. 이제
  `Passive.criticalChancePercent`가 적힌 개체면 종류를 가리지 않고 더한다. 공격력·치명타 피해
  가산도 같은 방식으로 바꿔, 새 개체가 값만 적으면 전투가 그대로 읽는다.
- 설명문도 종류별 분기에 손으로 적지 않고 **공통 절 하나**가 뒤에 붙는다. 그 사실을 이미 제
  문장에서 말하는 렉시아만 예외다.

## v0.51.0 — 2026-09-02

**등급이 태생 능력치와 성장을 정하도록 규칙을 세우고, 여덟 개체를 그 띠 안으로 옮겼다.**

- **등급 순서가 실제로 뒤집혀 있었다.** 태생 전투력 평균이 SR 2302 > R 2179 > SSR 2156이었고,
  전체 최하위가 SSR 스피나(2062), 최상위가 SR 스테라(2391)였다. 레벨 성장도 전 등급 공통
  +2%/레벨이라 키워도 격차가 영원히 생기지 않았다.
- **`src/core/rarityScaling.ts` 한 표가 규칙을 소유한다.**
  - `RARITY_STAT_BAND` — 태생 전투력 띠. R 2080~2200 · SR 2210~2330 · SSR 2340~2460. 겹치지
    않고 등급당 약 6% 차이다. 총량을 전투력으로 재는 이유는 그 값이 곧 오각형의 넓이라
    "넓이가 등급을 따른다"가 그대로 규칙이 되기 때문이다.
  - `RARITY_LEVEL_GROWTH` — 레벨당 R 1.8% · SR 2.0% · SSR 2.2%. `applyLevelGrowth`는 등급을
    **필수 인자**로 받는다. 기본값을 두면 새 호출부가 등급을 빠뜨린 채 모두 같은 속도로 자란다.
  - `COMMON_SECONDARY_STATS` — 치명타 확률 10 · 치명타 피해 150 · 충전량 26 · 흡혈 0 · 야성
    보정 0을 **전 개체가 공유**한다. 오각형에 보이지 않는 곳에서 총량이 갈리면 같은 등급끼리도
    세기가 어긋난다. 그 수치가 필요한 개체는 렉시아처럼 패시브·폭주로 끌어다 쓴다.
- **공격 속도·이동 속도는 손대지 않았다.** 난전에서 눈에 보이는 움직임이라 개체마다 다른 것이
  곧 정체성이고, 지금 배합이 좋다는 판단이었다(78~122 · 72~124 그대로).
- 실제 변화 — 띠 안에 이미 있던 토리카·루카·도디·티아는 성장 능력치를 **한 숫자도 바꾸지 않았다.**

  | 개체 | 등급 | 태생 전투력 | 주요 변화 |
  | --- | --- | --- | --- |
  | 스피나 | SSR | 2062 → **2358** | 체력 760→960 · 공격 124→156 · 방어 38→48 · 저항 46→58 · 주문 58→74 |
  | 렉시아 | SSR | 2162 → **2363** | 체력 820→980 · 공격 132→158 · 방어 42→50 · 저항 36→44 · 주문 76→92 |
  | 메테 | SSR | 2243 → **2360** | 체력 1180→1260 · 공격 116→124 · 방어 104→110 · 저항 72→76 · 주문 48→52 |
  | 스테라 | SR | 2391 → **2270** | 주문 132→**62** · 체력 880→980 · 공격 108→120 · 저항 96→106 |
  | 토리카 | SR | 2271 → 2319 | 부가 능력치 공통화만 |
  | 루카 | SR | 2246 → 2233 | 부가 능력치 공통화만 |
  | 티아 | R | 2187 → 2195 | 부가 능력치 공통화만 |
  | 도디 | R | 2170 → 2162 | 부가 능력치 공통화만 |

- **스테라의 주문력 132는 사표였다.** 일반 공격은 물리(공격력)이고 궁극기는 순풍 부여라 주문력을
  쓰는 스킬이 하나도 없는데 최고 수치였고, 그 없는 힘이 전투력만 부풀려 SR 1위를 만들었다.
  62로 낮추고 그 몫을 실제로 쓰는 체력·저항·공격력에 돌려줬다 — 표시 총량은 줄었지만 실전에서는
  더 세다.
- 부가 능력치 공통화로 렉시아(치명타 20/160)·루카(24/165)의 태생 치명타가 사라졌다. 렉시아는
  패시브가 치명타 확률·피해를 각각 25퍼센트포인트 올려 정체성이 그대로 남지만, **루카와 스피나는
  지금 치명타 원천이 없다** — 필요하면 폭주에 붙이는 것이 이 규칙의 방식이다.
- 등급 성장률이 적에게도 적용돼 SSR 보스 폰토스가 레벨 25에서 48% → 52.8% 성장한다(체력 4144 →
  4278). 원정 보스 회귀 구간을 그 값으로 다시 고정했다.
- `tests/unit/rarityScaling.test.ts`가 규칙을 강제한다 — 띠의 순서·겹침, 모든 개체의 띠 소속,
  등급 간 최저/최고 역전, 부가 능력치 공통값, 성장률 순서. 새 개체를 띠 밖 수치로 넣으면 실패한다.

## v0.50.2 — 2026-09-02

**스테라·티아의 전용 스킬 일러스트를 구워 붙였다.**

- 올려 준 원본 여덟 장을 파이프라인 이름 규칙으로 바꿔(`char007skill_00N` · `char008skill_00N`)
  `scripts/prepare_skill_icons.py`가 굽게 하고, 표에 두 줄(`char007: stella`, `char008: tia`)만
  더했다. 스크립트는 손대지 않았다 — 이름 규칙만 지키면 표 한 줄이 전부라는 계약 그대로다.
- 구운 뒤 **원본 PNG 여덟 장(약 11MB)은 저장소에서 지웠다.** 색은 파일에 굽지 않으므로 두
  개체 모두 속성·직군에서 나온 tint를 화면이 입힌다(티아 물·전사, 스테라 바람·지원가).
- `RELICS_WITH_ART`에 두 id를 더했다. 그림 파일만 두고 이 목록을 잊으면 화면은 계속 공용 아이콘을
  쓰므로, 네 칸이 모두 전용 키를 돌려주는지 검사하는 테스트를 함께 남겼다.
- pixel-7 캡처로 두 정보창의 패시브·일반 공격·궁극기 세 액자와 폭주 뱃지가 모두 새 그림으로
  바뀐 것을 확인했다.

## v0.50.1 — 2026-09-02

**정보창 인물을 조금 올리고, 순풍에 수치와 스테라 몫의 지속 회복을 붙였다.**

- 정보창 전신 원화가 살짝 가라앉아 보였다. 코어 관절 자리를 980 → **950**으로 올려 얼굴이
  화면 절반 위쪽에 남게 했다. 값은 화면이 아니라 Phaser 없는 `INFO_PORTRAIT_FOCUS`가 소유하며,
  폰토스 실루엣 회귀 테스트도 그 상수를 읽어 자리를 옮겨도 계약이 함께 움직인다.
- **순풍 키워드가 수치를 말한다** — "공격 속도와 이동 속도가 **각각 20%** 오른다". 무엇인지만
  말하고 얼마인지는 빼 두면, 정작 이 버프를 받을지 판단할 수 없다. 데이터와 문장이 갈리지
  않도록 `skillArt.test.ts`가 순풍을 거는 모든 스킬의 수치와 키워드 문장을 대조한다.
- **스테라 궁극기에 매초 최대 체력 2% 회복을 더했다.** 이 값은 **순풍 태그가 아니라 궁극기가
  얹는다**(`TeamBuff.maxHpRegenPercentPerSecond`) — 태그에 넣으면 누가 걸어 준 순풍이든 회복이
  따라와 지원가 한 명이 팀 회복까지 겸하게 된다. 스테라가 건 순풍만 회복을 데려온다.
  - 회복은 긴급 회복과 같은 1초 틱·같은 회복 경계를 쓰고, 순풍이 끝나는 순간 함께 멈춘다.
  - **겹쳐 걸어도 즉시 터지지 않는다** — 갱신할 때마다 한 틱이 나가면 궁극기를 연달아 써서
    회복을 뽑을 수 있으므로, 새로 걸린 순간부터 시계를 다시 센다.
  - 남은 시간을 깎기 **전에** 틱을 돌려, 지속 시간이 0이 되는 프레임의 마지막 한 틱을 잃지 않는다.

## v0.50.0 — 2026-09-02

**스테미나 창의 판이 창 밖으로 삐져나오던 것을 고치고, 충전을 세 칸으로 세웠다.**

- **위쪽 전용 판이 팝업 몸판을 뚫고 나와 있었다.** 몸판은 `chipPoints`로 왼쪽 위·오른쪽 아래를
  폭의 14%만큼 깎는데, 그 안의 판만 `slantedRect`(거의 네모)로 그려 두 모양이 섞였다. 판의 왼쪽
  위 귀퉁이가 몸판의 빗변을 넘어 밖에 떠 보였고, 한 창 안에 두 종류의 판때기가 보였다. 창 안의
  판과 칸이 **모두 몸판과 같은 비율로 깎이도록** 한 함수로 모았다.
- **액자가 판 천장에 붙어 보이던 것도 같은 자리에서 왔다.** 액자·수치·시간의 y를 판 위쪽부터
  고정값으로 적어 두어, 시간 줄이 사라지는 순간(가득 찼을 때) 남은 둘이 위로 쏠렸다. 이제
  쌓인 높이를 먼저 재고 그 덩어리를 판 가운데에 놓는다.
- **충전이 줄 하나에서 같은 크기의 칸 셋으로 바뀌었다** — 활력 토닉 · 긴급 보급(다이아) ·
  보급 요청(광고). 셋은 서로 대체재라 어느 하나를 크게 세우면 값을 비교하기 전에 크기가 먼저
  답을 정한다. 폭과 간격이 정말 균등한지, 칸이 몸판의 깎인 모서리 안에 드는지는 순수 배치표
  (`src/ui/staminaPopupLayout.ts`)와 테스트가 지킨다.
- **세 칸 모두 실제 서버 경계를 지난다.** 다이아 충전은 새 경계 `GameApi.rechargeStamina`가
  값 차감과 회복을 한 처리 단위로 확정한다 — 값과 회복량은 화면이 아니라
  `STAMINA_RECHARGE_SOURCES` 표가 소유하고, 서버는 수단 ID만 받아 표와 대조한다. 이미 가득 찬
  상태는 **차감 전에** 거절해 헛돈을 쓰지 않게 하고, 상한을 넘는 몫만 버린다. 광고는 이미 있던
  보상 슬롯(`daily-stamina`)과 `claimAdReward`를 그대로 쓰므로 회복량·일일 한도를 새로 적지
  않는다. 취소·SDK 미준비는 성공을 흉내 내지 않고 다른 두 칸을 그대로 남긴다.
- 창 높이는 손으로 적지 않고 **쌓인 내용에서 거꾸로 구한다** — 사용처 줄이 늘거나 칸 높이를
  고치면 창이 저절로 그만큼 자라, 마지막 줄이 판 밖으로 밀려나지 않는다.

## v0.49.1 — 2026-09-02

**없어진 "진압" 조작과 "관제탑" 역할 서술을 코드·문구·문서에서 걷어 냈다.**

- 야성 키워드 팝업이 아직 **"관제탑은 필요할 때 진압으로 게이지를 비워 되돌릴 수 있다"**고
  말하고 있었다. 진압은 오래전에 없앤 조작인데(v0.4.0에서 자동 감소로 바꿨다) 플레이어가
  실제로 읽는 자리에만 살아남아, 있지도 않은 버튼을 찾게 만들었다. 대신 실제로 일어나는 일
  — 폭주가 시간이 지나면 스스로 가라앉는다 — 을 적었다.
- **주인공이 "관제탑" 역할을 한다는 서술도 지웠다.** 야성은 차고 터지고 가라앉는 것을 전부
  전투가 스스로 하며 플레이어는 그 시점을 고르지 않는다. `bond.ts`의 유대 배율 주석이
  "언제 터뜨릴지 고르는 관제탑"이라 적어 두어, 코드를 읽는 쪽에 없는 조작을 다시 만들 여지를
  남기고 있었다. 플레이어가 바꾸는 것은 편성과 함께 싸운 시간(유대)뿐이라고 고쳐 적었다.
- 같은 이유로 `BattleScene`·`asyncArenaContracts`·`skirmish.test.ts`의 주석과
  `docs/lore.md`·`docs/design-gaps.md`·`docs/combat-affinities.md`·`docs/asynchronous-arena.md`의
  진압 전제를 함께 정리했다. CLAUDE.md에는 **"야성에는 플레이어가 누르는 조작이 없다"**를
  규칙으로 남겨 같은 문장이 되살아나지 않게 했다.
- 세계관 쪽 **관제탑은 그대로 둔다** — 시그널 아이가 모여 지상을 내려다보는 장소이자
  제1구역 다섯 번째 스테이지 이름이다. 지운 것은 "플레이어의 역할" 쪽 서술뿐이다.

## v0.49.0 — 2026-09-02

**7번 원화에 스테라(게오스테른베르기아)를 붙이고, 아군을 밀어 주는 지원 계약을 새로 열었다.**

- **스테라** — 시그널 아이 소속 SR 바람 속성 지원가. 쁘띠 로그에서 이적한 최연소 관측 담당이라
  연구원도 선임처럼 "선배"라 부르고, 그 나이대의 어린 개체들에게는 선망 받는 선배로 남는다.
  7번 원화는 이름·전투 데이터가 없어 `PORTRAIT_ASSETS`에 등록조차 못 하던 자리였다
  (`TAPEJARA_ASSET`). 이미 실측해 둔 알파 경계를 그대로 쓰고 상수 이름만 확정 명칭으로 바꿨다.
- **도디와 역할·속성이 겹쳐서 힐러가 아니라 버퍼로 짰다.** 같은 바람 지원가를 둘 두면서 둘 다
  회복을 맡기면 뒤에 나온 쪽이 상위 호환이거나 하위 호환이 된다. 스테라는 체력이 아니라 **아군의
  스킬 회전**을 앞당긴다.
  - 패시브 **"선망 받는 루키의 일상"**(`lowHpVanish`) — 체력이 절반 이하가 되면 전투당 한 번
    3초 동안 [[stealth|은신]]해 표적에서 벗어난다. 발동 경계·단일 발동권은 긴급 회복과 같은
    자리·같은 방식이고, 은신에 들어가는 순간 이미 이 개체를 노리던 추적도 함께 푼다 —
    그러지 않으면 "표적에서 벗어난다"가 말뿐이다.
  - 폭주 **"자, 역풍의 시간이다!"**(`tailwindRally`) — 아군 전체의 공격당 야성·궁극기 충전량을
    각각 5씩 올린다. 여러 제공자가 겹쳐도 가장 높은 하나만 쓴다(아다지오·무리 사냥과 같은 정책).
  - 일반 공격 **"산뜻한 바람"** — 공격력 50% 물리 피해에 더해 생존 아군 전체의 궁극기 게이지를
    2 채운다(`allyEnergyGain`). 시전자 자신도 포함하며 한 공격 행동에 한 번만 나눠 준다.
  - 궁극기 **"상승 기류"** — 피해도 회복도 없이 생존 아군 전체에 10초 동안 **순풍**을 건다.
- 그래서 **피해도 회복도 없는 지원 스킬 계약(`SupportSkill` + `TeamBuff`)을 새로 만들었다.**
  회복 슬롯에 밀어 넣으면 "회복량"이라는 이름의 값이 공격 속도 비율을 뜻하게 되어 화면과 전투가
  서로 다른 단위를 읽는다. 순풍은 공격 속도·이동 속도에 함께 곱하고 은신과 같은 공용 전투 시계로
  흐르며, 다 흐르면 수치까지 비워 남은 값이 다음 판정으로 새지 않는다. 새 키워드 `tailwind`가
  "무엇인지"를 말하므로 스킬 본문은 **무엇을 얼마나 오래 거는지**만 적는다.
- 회귀 테스트: 아군 전체 충전·순풍 부여와 만료·폭주 팀 보정·패시브 단일 발동을 전투 계약으로
  고정하고, 표시 계약(대상·태그·중복 여부)을 `skillArt.test.ts`에 함께 남겼다. 카드 얼굴 크기는
  보정 없이 중앙값 범위에 들어와 `cardZoom`을 두지 않았다.

## v0.48.0 — 2026-09-02

**임시 캐릭터와 턴제 시절의 잔재를 걷어 내고, 티아의 스킬을 표식형으로 다시 짰다.**

- **스밀라·케찰을 삭제했다.** 둘은 전용 원화가 없어 토리카의 전신·SD를 색만 바꿔 쓰던 임시
  캐릭터라, 도감·연구소·편성 어디에 나와도 같은 얼굴이 세 번 서는 화면이 됐다. 나머지 렐릭이
  모두 제 원화를 갖게 된 지금은 대체할 정식 캐릭터를 기다리는 자리가 아니라 그냥 중복이다.
- 그래서 **공유 임시 원화 체계 자체를 걷어 냈다** — `torika-placeholder` 키, 렐릭별 구분 색 표
  (`tints.ts`의 `TINTS`/`tintFor`), `portraitUsesRelicTint`, 카드용 `relicCardTint`가 모두
  사라지고 `PORTRAIT_ASSETS`는 키 하나에 원화 하나만 갖는다. 남긴 것은 카드 뒷배경이 여전히
  쓰는 `mixWhite` 하나다. 색 필터로 다른 개체를 대신 세우는 길이 없어야 새 개체가 원화 없이
  들어오지 않는다.
- **턴제 시절의 죽은 규칙도 함께 지웠다.** `swapMomentum`("교대 직후 첫 공격 강화")과
  `rearMend`("후방에 있는 동안 매 턴 회복")는 실시간 난전으로 옮긴 뒤 `skirmish.ts`에 구현이
  없었는데, 설명문만 화면에 남아 존재하지 않는 교대·턴을 플레이어에게 말하고 있었다. 두 종류를
  `PassiveKind`에서 없애고 그 패시브를 쓰던 적 둘을 실제로 도는 규칙으로 옮겼다 — 토비의
  "무리 본능"은 `bleedStreak`(같은 적 3연타 시 출혈), 리파의 "잔존 신호"는
  `emergencyRecovery`(체력 절반 이하에서 지속 회복)다.
- 야성 효과 `criticalChanceBonus`도 스밀라가 유일한 사용처였어서 함께 지웠다. 쓰는 개체가
  없는 분기는 전투 계산 안에서 조용히 낡는다.
- **티아**(구 "티아라")로 이름을 줄이고, 스킬을 **표식형**으로 다시 짰다. 앞에 서서 버티는
  `frontGuard` + `damageReduction` 조합은 물가를 뛰어다니는 아이의 그림과 맞지 않았다.
  - 패시브 **"반짝반짝 첨벙첨벙!"**(`shimmerMark`) — 타격한 적에게 표식을 남기고, 표식이 없는
    적을 때리면 표식이 그 적에게 옮겨가며 주문력 100%의 마법 피해를 추가로 준다. 표식은
    **공격자가 소유**한다 — 대상에 붙여 두면 티아가 둘일 때 서로의 표식을 지운다.
  - 폭주 **"이크티오 다이브!"**(`ichthyoDive`) — 자기 이동 속도 +100%, 일반 공격 뒤 표적을
    다른 적으로 바꾼다. 새 표적은 여기서 고르지 않고 `resolveTarget`에 맡기되, 방금 때린
    상대가 다시 뽑히지 않도록 가장 가까운 **다른** 적으로 옮겨 붙인다.
  - 둘이 한 덩어리라 **일반 공격 "물장구"를 광역에서 단일 대상으로** 되돌렸다. 범위로 여럿을
    함께 때리면 한 번 휘두를 때마다 표식이 여러 번 옮겨 가 무엇이 표식인지 읽히지 않는다.
    궁극기는 그대로 광역이다.
- 삭제로 비는 자리는 남은 개체로 옮겨 테스트를 유지했다(자동 편성·미보유 조회·저장 마이그레이션
  손상 케이스). 도감 개체번호 정렬은 이제 일곱 개다.

## v0.47.0 — 2026-09-02

**8번 원화에 티아라(이크티오사우루스)를 붙여 열세 번째 렐릭으로 공개했다.**

- **티아라** — 쁘띠 로그 소속 R등급 물 속성 전사. 물가를 제 놀이터로 삼는 유체 어룡이고,
  강가에서 반짝이는 것을 주워 와 "쁘띠 로그의 일"이라 우기지만 정작 칭찬은 베일 뒤에 숨어
  듣는다. 관찰 일지·소속 근거는 `docs/factions.md` 배정표에 함께 남겼다.
- 스킬은 **주문력 비례 마법**으로 짰다 — 앞에 서서 물살을 퍼뜨리는 전사라 계수는 공격력이
  아니라 주문력에서 나온다. 일반 공격 "물장구"(주위 반경 190), 궁극기 "반짝이는 건 다 내
  거야!"(반경 300 + [[stagger|경직]]), 패시브는 전방 피해 감소, 폭주는 받는 피해 30% 감소다.
  새 코어 규칙을 만들지 않고 이미 있는 계약(`targeting: nearbyEnemies`·`frontGuard`·
  `damageReduction`)만 조합했다.
- 전신·SD 원화의 **알파 경계를 실측**해 `assetMetadata.ts`에 적었다(전신 1086×1448, SD 1254²).
  반투명 지느러미 베일이 실루엣 폭을 캔버스 끝까지 넓혀 카드에서 얼굴만 작아지므로
  (중앙값의 0.89배), 렉시아와 같은 방식으로 `cardZoom` 1.12를 두어 중앙값에 맞췄다.
- 회귀 테스트: 카드 잘라내기·얼굴 크기 목록에 티아라를 더하고(머리·눈 관절), 도감 등록과
  실측 경계, 스킬 표시 계약(광역·주문력 계수·경직 문장)을 함께 고정했다.

## v0.46.2 — 2026-09-02

**각인이 가방에 곧바로 반영되지 않던 버그를 고치고, 액자 속 조각을 더 줄였다.**

- 세공 화면이 `GameApi`를 직접 불러 룬은 바뀌는데 **가방이 그 사실을 몰랐다** — 각인을 마치고
  돌아와도 카드의 금색 테두리가 가방을 다시 열 때까지 붙지 않았다. 세공·각인·이름 바꾸기를
  `InventoryManager` 경계로 옮겨 확정 결과를 Session에 반영하고 `inventory` 신호를 함께 낸다
  (판매·표식이 이미 쓰던 그 경계다). `tests/unit/inventory.test.ts`가 신호 수로 회귀를 막는다.
- 액자 안 조각 채움 비율을 0.72 → 0.6으로 낮췄다. 조각이 액자를 거의 채워 부담스러웠다.

## v0.46.1 — 2026-09-02

**원정 기록·편성·활성 런의 배경 역할을 분리하고 순위 팝업의 두 원화를 가볍게 합성했다.**

- 첫 기록은 인물이 없는 침수 도시 원경, 편성은 전투 필드, 활성 런은 장축 지도를 사용한다.
  원화 확인 결과 기록 배경에 폰토스가 없어 전신 Puppet은 유지하되, 원경 비네트와 단색 막을
  낮춰 실측 alpha bounds를 따르는 Puppet만 주 피사체로 읽히게 했다.
- 순위 팝업은 도시 원경을 배경으로 두고 전투 필드를 12% 질감층으로 합성한다. 공용 팝업 배경에
  이미지 alpha와 오버레이 강도 옵션을 더해 순위 행의 대비를 해치지 않도록 했다.
- 상태별 키 단위 테스트와 첫 기록·순위 팝업·편성·활성 지도의 모바일 캡처 흐름을 고정했다.

## v0.46.0 — 2026-09-02

**모든 개체의 스킬 설명문을 데이터에서 조립하는 한 양식으로 합치고, 스테미나에 전용 창을 만들었다.**

- **`skillDescription()`이 이제 공격·정형 회복 스킬의 문장을 전부 짓는다.** 양식은
  `대상 → 피해 → 부가 효과` 한 줄이고, 대상은 전투 엔진이 읽는 `targeting` 계약에서, 피해는
  스킬 아이콘 위 라벨과 같은 값에서, 부가 효과는 구조화 필드에서 나온다. 스피나·렉시아·도디만
  고쳤던 지난 판과 달리 **12개체 전부**가 같은 순서로 읽힌다.
- 그래서 `relics.ts`의 `basic`·`ultimate`에서 **`desc`를 모두 지웠다**(`desc`는 문장을 지을 수
  없는 패시브·야성 특성만 쓰는 선택 필드가 됐다). 문장이 데이터와 함수 두 곳에 있으면 수치를
  조정한 뒤 옛 문장이 그대로 남는다 — "긴 송곳니로 벤다"·"하늘에서 내리꽂는다" 같은 묘사문도
  함께 사라졌다.
- 주어가 바뀌는 절(주기 치명타·전이·충전 조건)은 "주고"로 잇지 않고 제 문장으로 세운다.
  한 문장 안에서 말하는 대상이 바뀌면 읽다가 걸린다.
- `tests/unit/skillArt.test.ts`에 **"스킬 설명문 양식 계약"**을 더했다 — 모든 개체의 두 슬롯이
  대상으로 시작하고 피해 태그를 갖는지, 데이터에 원문이 되살아나지 않았는지, 묘사문이 다시
  섞이지 않았는지 한꺼번에 검사한다. 개체가 늘어도 이 계약이 양식을 지킨다.
- **상단 줄의 스테미나는 `64/122` 한 덩어리·한 색이다.** 현재와 최대를 다른 색·다른 크기로
  가르면 한 칸에 두 정보가 서고, 회복 시간까지 넣으면 글자가 칸을 넘었다.
- **스테미나 전용 창을 만들었다**(`src/ui/StaminaPopup.ts`). 위에서부터 액자 → 현재/최대 →
  회복 시간 → 충전 → 사용처다. 충전 줄은 `src/data/staminaRecharge.ts` 표에 있는 것만 세우며,
  지금은 활력 토닉 하나다 — 젬 충전·광고 보급은 서버 경계가 없어 넣지 않았다. 눌러도 아무 일이
  없는 버튼은 준비 상태를 과장한다.
- 그림 한 장을 담는 액자를 `src/ui/itemFrame.ts` 하나로 모았다(가방 칸·스테미나 창).
- **각인이 난수 등급을 굴리지 않는다.** 세공 성공 한 번과 같은 값을 확정으로 더하고, 기록만
  남기던 것을 고쳐 **그 옵션의 수치도 함께 올린다** — 전투 계산은 이미 각인을 성공 한 번으로
  세고 있어 화면의 값과 실제 능력치가 갈려 있었다. `grade`는 예전 저장을 읽기 위한 선택 필드로
  남고 새로 쓰지 않는다.
- 각인 표식을 별에서 **세공 칸과 같은 다이아(노란 발광)**로 바꾸고 크기를 세공 표식에 맞췄다.
  같은 표식을 쪽지의 옵션 이름 옆에도 작게 붙여 어느 줄이 완성됐는지 그 자리에서 읽게 했다.
- 세공 화면의 안내 문구를 다음에 할 일로 바꿨다("각인을 진행해 룬을 완성해 주세요." ·
  "모든 세공이 끝났습니다.").
- 액자 안 조각이 부담스럽게 커서 채움 비율을 0.8 → 0.72로 낮췄다.

## v0.45.0 — 2026-09-02

**룬 액자를 다시 다듬고, 가방 네 탭을 같은 액자 격자로 맞추고, 상단 재화 칸의 눌림 버그를 고쳤다.**

- **액자 안에서는 캔버스가 아니라 그림을 가운데에 맞춘다**(`src/ui/runePieceContent.ts`).
  조각 셋은 하트를 부채꼴로 가른 것이라 캔버스의 다른 자리를 쓰는데, 그대로 넣으니 1번은
  왼쪽 위·2번은 오른쪽 위·3번은 아래로 쏠려 작게 앉았다. 알파 경계를 실측해 조각을 액자
  한가운데에 가장 크게 세운다. 하트를 이루는 젬 슬롯은 그대로다 — 거기서는 그 쏠림이 곧 하트다.
- 뒷배경에 **그냥 칠하는 바닥 한 겹**을 더했다. 겹쳐 밝아지는 합성만으로는 어두운 능력치 색
  (방어·저항)이 검은 판에 묻혀 두 주 옵션이 갈리지 않았다. 안쪽 비네트는 0.55 → 0.3으로 얕게
  눌러 가장자리에서도 색이 읽히게 했다.
- 잠금·즐겨찾기 표식이 카드의 깎인 모서리 밖으로 삐져나오던 것을 고쳤다. 두 변에서 같은 만큼만
  들여 놓으면 빗변을 넘으므로, 깎인 깊이의 절반부터 시작해 **액자 안쪽**에 앉힌다.
- **판매 버튼을 작고 붉게 오른쪽 끝으로 뺐다.** 세공과 같은 폭(200)으로 나란히 서서 서로 겹쳤고,
  되돌릴 수 없는 조작이 주 조작과 같은 무게로 읽혔다.
- **가방 네 탭이 모두 액자 격자다.** 재화·소비품·재료 칸에서 오른쪽 이름·설명 열을 걷어 내고
  액자와 수량만 남겼다 — 그 글은 눌러서 여는 쪽지가 이미 말하고, 늘어놓으면 한 줄에 두 칸밖에
  서지 못했다.
- **상단 재화 칸을 누르면 아이콘이 거대해진 채 굳던 버그를 고쳤다.** 조각마다 `setScale`을
  불렀는데, `setDisplaySize`로 크기를 정한 아이콘에는 그것이 곧 "원본 크기의 1.08배"라 배율이
  통째로 날아갔다. 칸 조각들을 컨테이너의 국소 좌표에 세우고 눌림은 컨테이너에만 준다.
- **스테미나 표기를 두 줄로 나눴다.** 현재·최대·회복 시간을 한 줄에 몰아넣어 글자가 칸을
  넘었다. 윗줄은 지금 값(제 색)과 상한(작고 흐리게), 아랫줄은 회복 시간(작고 얇은 회색)이다.
- **스킬 본문을 "대상 → 피해 → 부가 효과" 한 양식으로 맞췄다.** 스피나 궁극기는 대상과 기절
  3초를, 렉시아 궁극기는 포효 묘사 대신 피해와 회복 50%를, 도디 일반 공격은 부리로 쪼는 묘사
  대신 마법 피해와 회복 비율을 말한다. 본문의 피해 수치는 스킬 아이콘 위 라벨과 **같은 값**을
  받아 쓴다 — 따로 계산하면 위아래가 갈린다.
- **밸런스: 도디 일반 공격 위력 70% → 50%, 피해 비례 아군 회복 100% → 50%.** 유일한 상시
  회복원이 일반 공격만으로 파티를 계속 채워, 최종 보스 관문의 생존 구간이 지나치게 헐거웠다.
  그만큼 전투가 길어져 `skirmish.test.ts`의 폰토스 구간 상한도 30초 → 34초로 함께 넓혔다.

## v0.44.0 — 2026-09-01

**룬 액자의 뒷배경을 실제 능력치 색의 발광으로 바꾸고, 룬에 잠금·즐겨찾기를 더했다.**

- 액자 뒷배경이 자체 색표(`tones`) 대신 능력치 레이더와 같은 `STAT_TONE`(`src/ui/statTones.ts`)을
  읽는다. 정보창의 능력치 칩·축 이름과 같은 색이라 "무엇이 오르는 룬인가"가 색 하나로 이어진다.
- 그 색을 평평한 삼각형 두 장으로 깔던 것을 **가운데로 갈수록 밝아지는 발광**으로 바꿨다.
  진한 한 겹은 조각까지 그 색으로 덮어 탁했고, 겹이 적으면 줄어드는 윤곽이 계단으로 보여
  아주 옅은 겹을 여럿 깐다(다 더해도 0.5 미만).
- 칠이 액자 밖으로 새던 것을 고쳤다. 삼각형이 네모 기준이라 깎인 모서리로 색 조각이 삐져
  나왔다. 기하 마스크는 컨테이너 이동을 물려받지 않아 스크롤하는 가방에서 어긋나므로,
  마스크 대신 **칠할 도형을 액자 도형으로 잘라**(`src/ui/runeBackdrop.ts`) 안에 가뒀다.
  가방·장착용 가방·세공·조각 요약이 모두 같은 `addRuneFrame` 하나를 지나므로 함께 고쳐졌다.
- 정보창에서 조각을 눌러 여는 **장착용 룬 가방을 가방과 같은 카드 한 장**(`addRuneCard`)으로
  합쳤다. 이름·등급·옵션을 늘어놓던 가로 카드가 사라지고 액자 격자가 됐다 — 두 화면이 카드를
  따로 갖고 있어 한쪽만 옛 모습으로 남았다.
- **각인까지 마친 룬의 표시를 금빛 발광 비네트 한 겹으로 바꿨다**(`drawShapeInnerGlow`).
  숨 쉬는 발광과 반짝이는 별 tween을 걷어 낸 이유는, 목록에 여럿 서면 그 반짝임이 액자보다
  먼저 읽히고 카드마다 tween이 매 프레임 도는 비용이 되기 때문이다.
- **룬 쪽지에 잠금·즐겨찾기 칩 두 장**을 왼쪽 위에 더했다(`src/ui/MarkChip.ts`). 정보창 헤더의
  즐겨찾기·애착 뱃지가 쓰던 모양을 공용 프리팹으로 꺼내 둘이 같은 한 장을 쓴다. 켜진 표식만
  카드 왼쪽 위에 작게 선다.
- 표식은 화면이 아니라 서버가 확정한다 — `GameApi.markRune`을 더하고 주지 않은 값은 그대로
  두어 별을 켜는 요청이 자물쇠를 끄지 않게 했다. **잠근 룬의 판매는 `RUNE_LOCKED`로 거부**한다.
  자물쇠는 판매를 실제로 막는 값이라 표시와 거부가 한 곳에서 갈려야 한다.
- 저장 호환: `RuneInstance.locked`·`bookmarked`는 **선택 필드**라 예전 저장은 그대로 읽히고
  둘 다 꺼진 것으로 다뤄진다. 마이그레이션이 필요 없다.

## v0.43.1 — 2026-09-01

**세 편성 화면의 드래그 표현을 하나로 합쳤다.**

- 파티에만 있던 감광·확대 추적·교체 고스트·깎인 드롭 칸·취소 복구를 공용 Phaser 표현
  컨트롤러(`src/ui/formationDragVisualController.ts`)로 옮겼다. 미리보기와 확정이 갈리지
  않도록 배열 결과는 계속 `moveFormationSlot`만 쓴다.
- 원정과 발굴은 Puppet이 컨테이너 변환을 물려받지 않는 제약을 유지하며, 각 화면의 기존 화면
  좌표 배치 콜백을 공용 표현기에 주입한다.
- 모바일 회귀 검증에 원정·발굴의 드래그 중 대상 칸과 반투명 교체 미리보기 상태를 추가했다.

## v0.43.0 — 2026-09-01

관찰 일지의 색과 분량을 손보고, 스피나 궁극기의 남은 `%` 표기를 다른 스킬과 같은 규칙으로
맞췄다.

- **관찰 일지의 이름표와 본문을 색으로 갈랐다.** 개체번호·프로젝트 같은 라벨은 회색
  (`COLOR.inkDim`), 그 옆 실제 값과 표본 기록·문답 내용은 명시적으로 흰색(`COLOR.ink`)을
  쓴다. 상단 식별 정보를 하나의 여러 줄 문자열 대신 `{ label, value }` 목록으로 바꿔 각 줄을
  라벨/값 두 개의 텍스트 객체로 그린다. 소속 이야기(`squadNote`)처럼 부가 설명 문단만 예전처럼
  회색을 유지한다.
- **완료한 관찰 기록을 별도 레이어로 옮겼다.** 그동안은 최근 기록 한 건만 보여 주고 나머지는
  저장된 채로 화면 어디서도 다시 볼 방법이 없었다 — 매일 답하는 인터뷰가 쌓일수록 그만큼이
  그냥 사라지는 셈이었다. 이제 관찰 일지에 "전체 기록 보기 (N건)" 링크가 뜨고, 누르면 화면
  가운데에 따로 뜨는 "관찰 기록" 팝업이 날짜순으로 한 건씩 넘겨 보여 준다(좌우 화살표 +
  "N / 전체" 카운터). 최신 순 정렬과 페이지 경계는 `src/ui/observationHistory.ts`의 순수
  함수(`sortedObservationHistory`, `clampObservationPage`) 하나가 정한다. 관찰 일지와 같은
  자리에 겹쳐 열면 두 판의 닫기 X가 거의 포개지므로, 이 레이어는 특정 버튼에 붙는 쪽지가
  아니라 화면 가운데 뜨는 독립된 기록판으로 뒀다.
- **스피나 궁극기의 두 %를 하나의 실제 수치 태그로 합쳤다.** 위력(공격력 200%)과 공격 속도
  (150%) 두 축을 합산하는 궁극기라, 예전에는 "전투 중에도 계속 바뀌는 값"이라는 이유로 예외로
  두고 `%` 그대로 남겼다. 하지만 이 정보창의 다른 모든 수치도 이미 "전투 중 버프는 빼고 지금
  성장한 대로"만 보여 주므로, 스피나만 실시간 값을 반영하는 척 `%`로 남겨 두면 오히려 기준이
  갈렸다. `attackSpeedCompositeDamageKeyword`가 두 축을 하나의 배율로 합쳐 실제 피해 수치를
  계산하고, 상단 `[[damage-value]]` 라벨과 본문이 이제 같은 값을 쓴다(예전에는 상단 라벨이
  위력 축만 계산해 실제보다 작은 수를 보여 주던 것도 함께 고쳤다). 능력치를 모르는 자리(대상
  없는 도감 미리보기)에서는 여전히 옛 `%` 표기로 되돌아간다.
- `PopupLayer`가 지금 열린 팝업 제목을 `window.__PF_DEBUG.popupTitles`에 쌓아 두게 했다.
  Canvas 안 팝업은 DOM에서 무엇이 열려 있는지 알 방법이 없어, 중첩 팝업을 다루는 E2E가 좌표를
  눈대중으로 맞추기 전에 먼저 실제로 열린 팝업을 확인할 수 있게 하려는 진단용 필드다.
- 규칙은 `tests/unit/observationHistory.test.ts`(정렬·페이지 경계)와
  `tests/unit/skillArt.test.ts`(스피나 합산 수치, 능력치 없을 때의 되돌림)가 고정한다.

## v0.42.3 — 2026-09-01

- **불사 폰토스의 HP 센티널을 제거했다.** 전투 생존은 기존 `SkirmishState.boss`와
  `Fighter.immortal` 계약만 사용하고, 지도·전투·상세창은 같은 유한한 고정 스탯을 유지한다.
- **폰토스 정보 표기를 1등급 LV.20으로 통일했다.** 표기 레벨로 스탯을 다시 계산하지 않아 기존의
  높은 보스 수치는 보존하며, 전투 중 상세창도 런타임 HP 대신 지도 표시 스냅샷을 사용한다.

## v0.42.2 — 2026-09-01

- **관찰 일지 스쿼드 엠블럼을 기존 104px의 정확한 130%인 135.2px로 키웠다.** 확대율을 별도
  상수로 남기고, 복제 그림자의 성장·아래쪽 오프셋까지 포함한 실제 외곽으로 x·y·이름 위치와
  메타데이터 폭을 다시 계산했다. 덕분에 커진 표식이 팝업 안쪽 여백이나 제목 영역을 침범하지 않는다.
- **엠블럼 원화의 가로세로 비율을 보존한다.** 긴 변만 지정 크기에 맞추므로 가로형·세로형 표식도
  정사각형으로 찌그러지지 않으며, 대표 비율 1:2·1:1·2:1에서 그림자와 이름/본문 안전 영역을
  순수 레이아웃 테스트로 고정했다.

## v0.42.1 — 2026-08-31

- **도디의 동경 1순위를 주인공으로 못 박았다.** 시그널 아이를 올려다보는 것도 결국 "대장님 옆에서
  더 쓸모 있어지려고"라서, 두 마음의 순서가 흐려지면 안 된다. `squadNote`에 **연구원의 1호 팬을
  자처한다**는 문장을 넣고(일지 첫 장부터 그 이름이 적혀 있다), 일지가 그리는 스쿼드 줄의 라벨도
  `동경` → `스쿼드 동경`으로 바꿨다 — 그냥 "동경"이라고만 적으면 본문이 말한 순서와 어긋난
  두 문장이 나란히 선다.
- **타이틀 로고를 30% 키웠다**(680 → 884px).
- **로고 자리를 그림 상자가 아니라 보이는 글자 기준으로 다시 잡았다.** 로고타입 파일은 사방에
  넓은 투명 여백이 있어(글자가 세로로 31.4%~69.6%만 차지한다), 상자 기준으로 두면 30% 확대와
  함께 부제가 190px 아래로 밀려 **배경 원화의 얼굴을 덮었다.** `titlename.webp`의 alpha 경계를
  실측해 `TITLE_LOGOTYPE_CONTENT` 표로 두고, 부제 자리와 섬광 폭을 모두 거기서 계산한다.
  덕분에 로고는 커지고 부제는 글자 바로 아래에 붙으면서도 얼굴을 가리지 않는다.
- **관찰 일지의 스쿼드 이름을 두껍게 세웠다.** 설명이 아니라 이름표라 `emphasis`(20px)에서
  `display`(24px)로 올렸다. 뒤에 판이나 복제 그림자는 두지 않는다 — 엠블럼이 이미 제 어둠을
  데리고 있어 겹치면 표식이 두 겹으로 보인다. 대신 엠블럼과 같은 강조색·같은 중심선에 맞춰
  한 덩어리로 읽히게 했다.

## v0.42.0 — 2026-08-31

소속 스쿼드를 배정표 속 한 칸에서 **화면에 보이는 설정**으로 끌어냈다. 관찰 일지에 엠블럼이
찍히고, 그 무리에서 무엇을 하고 누구를 어떻게 부르는지가 함께 읽힌다.

- **자치 스쿼드를 코드가 아는 값으로 만들었다.** `docs/factions.md`의 배정표만 있고 코드에는
  아무것도 없어서, 소속은 문서를 열어야만 알 수 있는 정보였다. `SquadId`는 코어 모델 타입으로,
  이름·라틴 표기·역할·**연구원 호칭**은 `src/data/factions.ts` 한 표로 두고, 12종 전원의
  `RelicDef.squad`를 배정표 그대로 채웠다. 코어가 데이터 표를 알 필요는 없으므로 id만 코어에 둔다.
- **관찰 일지 우상단에 소속 엠블럼을 찍었다.** 판 **안쪽**에 앉힌다 — 밖으로 내밀면 홀로그램
  판의 실루엣이 깨져 붙인 스티커처럼 보인다. 일지 원화 클립은 좌상단·우하단만 깎으므로 우상단은
  직각이라 표식이 모서리에 바짝 붙어도 사선에 잘리지 않는다. 엠블럼만으로는 이름을 말하지
  못하므로 바로 아래에 스쿼드 이름을 짧게 붙였다.
- **엠블럼 양식을 `src/ui/FactionMark.ts` 하나로 통일했다.** 스쿼드가 늘어도 같은 모양으로
  따라오게 하려는 것이다. 판때기를 받치지 않고 **같은 그림을 검게 세 겹 복제해** 크기를 조금씩
  키우며 겹쳐 어둠이 번지게 만든다 — 도형으로 그림자를 그리면 엠블럼과 무관한 네모가 뒤에
  남고, 캔버스 흐리기(`shadowBlur`)는 팝업을 열 때마다 CPU가 다시 계산해 프레임이 튄다.
  엠블럼 그림이 아직 없는 스쿼드는 **아무것도 세우지 않는다** — 빈 네모나 물음표를 두면 그것이
  무슨 표식인지 되묻게 만든다.
- **도디에게 쁘띠 로그의 성격을 입혔다.** 막내 기록병이라는 자리, 일지 절반이 주운 반짝이
  목록이라는 사고뭉치 결, 주인공을 `대장님`으로 부르는 호칭까지 `squadNote`·`researcherTitle`에
  남겼다. **시그널 아이를 동경한다**는 설정도 `admiredSquad`로 넣어, 관제탑에서 다 내려다보는
  "다 아는 어른들"을 우러러보는 관계가 일지에 한 줄로 뜬다. 배정표에도 같은 근거를 적었다.
- **소속 이야기가 있는 개체는 일지 판이 150px 더 길어진다.** 같은 높이에 밀어 넣으면 기록
  본문이 인터뷰 줄과 겹쳐 두 문단이 한 덩어리로 읽힌다. 문단 자리는 눈대중이 아니라 실제로
  그려진 기록 본문의 높이에서 잰다 — 그러려고 `KeywordManager.layout`이 채운 줄 수를 컨테이너
  크기로 남기게 했다(컨테이너는 자식이 늘어도 크기를 스스로 알지 못한다).
- 규칙은 `tests/unit/factions.test.ts`가 고정한다(스쿼드 표의 이름·호칭 중복 없음, 엠블럼 목록과
  키 규칙, 전원 배정, 동경 스쿼드가 자기 소속과 다른 실재 스쿼드인지, 개체가 쓰는 호칭이 소속
  스쿼드가 실제로 쓰는 말인지).

## v0.41.0 — 2026-08-31

타이틀 진입과 편성 조작을 손봤다. 판때기로 가리던 것을 **복제 그림자**로 바꾸고, 편성에서
자리를 끌어 옮기는 동안 무슨 일이 일어나는지를 그림으로 보여 준다.

- **타이틀의 제목·부제 뒤 판때기를 걷어 내고 복제 그림자를 넣었다.** 검은 판 두 장이 화면
  위쪽을 통째로 어두운 상자로 만들어 배경 원화가 잘려 보였다. 같은 그림·같은 글자를 검게 한 겹
  복제해 깔고, 글자 복제본에는 획 둘레로 번지는 검은 띠(글자 크기의 0.26배)를 둘렀다. 띠가 실제
  배경이 되므로 대비가 배경 원화와 무관해진다 — 부제 자리(노란 후드)는 밝은 곳 휘도가 0.758까지
  올라 강조색이 **1.45:1**, 설명 줄이 1.85:1까지 떨어지지만 검은 띠 위에서는 각각 **10.8:1**·
  7.9:1로 AA(4.5:1)를 넉넉히 넘는다(`background_011.webp` 부제 띠 실측 평균 0.132·최대 0.758).
- **제목과 부제를 바짝 붙였다.** 판이 없어진 만큼 사이가 벌어지면 한 덩어리가 아니라 서로 다른
  두 정보처럼 보인다. 로고 아래 40px·90px이던 두 줄을 6px·54px로 당겼다.
- **최소 리소스가 도착하기 전에는 검은 화면만 둔다.** 예전에는 배경이 없는 동안 회색 판 위로
  부제·버전·마름모가 먼저 떠 화면이 조립되는 과정이 그대로 보였다. 이제 배경과 로고가 준비된
  순간 한꺼번에 들어온다. 파일 하나가 없어도 검은 화면에 갇히지 않도록 `loaderror`도 같은
  진입을 부르고, 진입은 정확히 한 번만 통과한다.
- **제목이 가운데에서 좌우로 열린다.** 로고를 가로로 늘였다 줄이면 그림이 찌그러지므로 잘라내는
  창을 넓혀 연다 — 비율은 처음부터 끝까지 그대로다. 벌어지는 틈을 따라 흰 섬광이 함께 퍼졌다
  사라지고, 다 열린 뒤에는 마스크를 폐기해 이후 프레임 비용으로 남기지 않는다.
- **오프닝 회상 버튼을 좌상단에 작게 올렸다.** 이 화면의 주된 조작이 아니라 이미 본 사람을 위한
  곁길이라 가운데를 비웠다(360×96 → 220×64).
- **화면을 누른 자리를 푸른빛으로 바꿨다**(`EFFECT_TAP_COLOR`). 금색은 재화·보상·강조를 뜻하므로
  아무 데나 눌러도 뜨는 반응에 같은 색을 쓰면 "눌렀다"와 "받았다"가 섞여 읽힌다.
- **편성 자리를 끌어 옮길 때 무슨 일이 일어나는지 보여 준다.** 그동안 드래그 판정은 있었지만
  화면에는 아무 표시도 없어서 놓기 전까지 결과를 알 수 없었다. 이제 판이 어두워지고 **든
  캐릭터 자체가 1.25배로 커져 손끝에 떠오르며**, 자리를 내주는 캐릭터가 바뀔 자리로 반투명하게
  미리 옮겨 선다. 미리보기는 확정 규칙(`moveFormationSlot`)을 그대로 통과시켜 보여 준 것과
  놓은 결과가 갈리지 않는다. 이름·자리 글자는 확정된 편성을 말하므로 끄는 동안 눌러 둔다.
- **놓을 자리를 네모칸으로 알린다.** 섬광 한 번은 어디까지가 그 자리인지 말해 주지 못한다.
  네 모서리를 어긋나게 깎은 칸을 끄는 동안 계속 띄우고, 지금 가리키는 칸만 진해지고 1.06배
  커진다. 배경이 비칠 만큼만(평소 0.14, 가리킨 칸 0.3) 채워 판이 세 조각으로 갈라져 보이지
  않게 했다.
- 규칙은 `tests/unit/formationDragVisual.test.ts`가 고정한다(미리보기와 확정 규칙의 일치, 빈
  자리 교환, 가리킨 칸과 나머지 칸의 차이, 진하기 상한).

## v0.40.1 — 2026-08-31

- **편성 제스처를 입력 의도만 전달하는 공용 Phaser 컨트롤러로 좁혔다.** 탭·드래그 시작·이동·드롭·취소만 콜백으로 전달하고, 스토리·원정·발굴의 배열과 저장 경계는 기존 소유자가 유지한다.
- **장기 누름·이동 거리·경계 드롭 판정을 순수 함수와 단일 상수로 통합했다.** 슬롯 밖 종료, 씬 종료, 팝업 소유자 종료, 두 번째 포인터를 명시적으로 취소하고 숫자키 슬롯 선택 뒤 방향키 이동도 같은 드롭 경로를 쓴다.
- **탭·긴 탭·드래그·포함 경계·취소 사유를 단위 테스트로 고정했다.** Phaser 렌더링 없이 임계값 회귀를 검출한다.
- **타이틀 로고 판과 부제 판을 분리했다.** 로고 판은 로고 그림만 딱 감싸는 자체 레이어로 alpha를
  따로 계산했다(흰 로고가 그 자리 배경 위에서 AA 4.5:1을 넘기는 최소값 0.0933 → 0.1). 부제·설명
  두 줄은 별도의 얕은(낮은) 레이어로 두고 기존 계산값(0.46)을 그대로 쓴다.
- **로딩 마름모를 화면 완전 최하단으로 내렸다**(0.7 → 0.94). TAP TO ENTER 문구보다도 아래로
  내려가 로딩 표시가 화면에서 가장 낮은 자리에 선다.

## v0.39.4 — 2026-08-31

- **타이틀 유리판 alpha를 눈대중이 아니라 실측으로 정했다.** 부제·설명 줄 자리의 배경 원화
  픽셀 평균을 재서 COLOR.void와 섞었을 때 가장 옅은 글자색(inkDim)이 WCAG AA 일반 텍스트
  기준(4.5:1)을 넘기는 최소 alpha가 0.4508임을 계산했고, 0.46으로 살짝 여유를 둬 적용했다.

## v0.39.3 — 2026-08-31

- **타이틀 로고를 훨씬 키우고 뒤에 옅은 유리판 + 검은 그림자를 받쳤다.** 로고 그림 하나만
  배경 원화 위에 얹으면 밝은 부분에 묻혀서, `drawLayer`로 기울어진 유리판(alpha 0.4)을
  로고·부제 뒤에 깔아 대비를 만들었다(그림자는 `HOLO.shadow` 기본값). 로딩 마름모와
  "TAP TO ENTER"·오프닝 회상 버튼은 캐릭터 얼굴에서 더 떨어지도록 화면 하단으로 밀었고,
  탭 문구는 배경과 겹칠 수 있어 검은 그림자를 얹었다.

## v0.39.2 — 2026-08-31

- **타이틀(로딩) 화면이 텍스트 제목 대신 로고 그림과 캐릭터 원화 배경을 쓴다.** "For - Garden"
  텍스트를 `sprites/ui/titlename.webp` 로고 이미지로 바꾸고, 단색 배경 대신
  `sprites/background/background_011.webp` 캐릭터 원화를 `drawVignette`로 눌러 깐다. 이 화면
  자체가 로딩 화면이라 두 자산은 `scenes/loadingSteps.ts`의 공용 로딩 단계를 기다리지 않고
  `TitleScene`이 진입 직후 곧바로 읽는다. 로딩 마름모 칸도 얼굴과 겹치지 않도록 조금 더
  아래로 내렸다.
- **5대 자치 스쿼드 엠블럼 원화를 구워 `sprites/factions/{fang,gear,eye,rune,rogue}.webp`로
  정리했다.** `docs/factions.md`의 1~5번 순서(앱솔루트 팽·나이트 기어·시그널 아이·사일런트
  룬·쁘띠 로그)에 맞춰 매칭했다. 원본은 저장소에 남기지 않고
  `scripts/prepare_title.py`가 굽는다.

## v0.40.0 — 2026-08-31

- **스토리·원정·발굴의 상단 SD 슬롯에 공용 장기 누름 드래그 편성을 추가했다.** 원본 Puppet 대신
  확대 번호 고스트와 얇은 강조면만 움직이며, 드롭 뒤 각 화면의 기존 비동기 SD 렌더 경로로
  재구성해 컨테이너 좌표 상속 제약을 피한다.
- **슬롯 이동 규칙을 Phaser 없는 순수 함수로 분리했다.** 채운 칸 교환과 빈 칸 이동을 같은 교환
  규칙으로 처리하고, 동일·범위 밖 입력에서도 원본을 보존한 새 배열을 반환한다.
- **발굴 순서 이동은 편집 사본에서만 허용한다.** 완료 전에는 서버 확정 편성과 Session을 바꾸지
  않아 취소·닫기가 기존 생산 편성을 안전하게 유지한다.

## v0.39.1 — 2026-08-31

- **스토리·원정·발굴 편성 슬롯을 직접 탭해 해당 자리를 해제할 수 있게 했다.** SD 로딩과 분리된
  투명 입력면이 기존 확대 피드백과 드래그 판정을 소유하며, 자리 인덱스로 제거해 보이는 칸과
  결과가 어긋나지 않는다.

## v0.39.0 — 2026-08-31

- **스토리와 원정 준비 화면이 마지막으로 확정한 콘텐츠별 편성을 복원한다.** 원정 저장 v27은
  미보유·중복 렐릭을 마이그레이션에서 제거하며, 기존 스토리 파티와 독립 발굴 배치를 서로
  복사하지 않아 콘텐츠를 오갈 때 사용자의 선택이 덮어써지지 않는다.

## v0.38.0 — 2026-08-31

- **전투 프로필 위에 활성 버프 액자 행을 추가했다.** 코어 셀렉터가 확정한 효과만 프리팹에
  전달하고, 프리팹이 안정 키 기반 재사용·입력 차단·스킬 아트 폴백까지 소유해 HUD 갱신 때
  기존 아이콘이 흔들리거나 버프 탭이 궁극기 입력으로 이어지지 않게 했다.
- **1080×1920 프로필 배치표에 버프 슬롯 규격과 실제 외곽을 포함했다.** 한 개와 최대 네 개의
  액자가 화면 밖으로 나가지 않고 카드·체력·야성 게이지와 겹치지 않는 계약을 정적 테스트로 고정했다.

## v0.37.0 — 2026-08-31

- **전투 UI 움직임 강도를 `기본`·`감소`·`끔`으로 선택하는 저장 설정을 추가했다.** 머리 위 체력 바,
  하단 프로필 체력 게이지와 EffectManager 및 궁극기의 카메라 흔들림이 같은 강도 배율을 소비한다.
  `끔`에서도 체력 색, 붉은 피해 잔상과 피해 숫자는 유지해 접근성 선택이 전투 정보를 지우지 않는다.
- **저장 규격을 v26으로 올렸다.** v25 이하와 버전 없는 저장에는 기존 연출과 같은 `default`를
  부여하며, 손상되거나 알 수 없는 선택값도 같은 값으로 정규화한다.

## v0.36.2 — 2026-08-31

전투 화면이 이펙트와 수치에 가려 정작 캐릭터가 안 보인다는 지적을 반영해 **표시를 전부 뒤로
물렸다.** 판단 기준 하나를 세웠다 — **예쁜 캐릭터를 가리는 순간 이펙트와 수치는 타격감이
아니라 방해다.**

- **피해 수치를 절반 가까이 줄이고 반투명하게 깔았다.** 등급별 크기를 34~92px에서 22~52px로
  낮추고, 캐릭터 위에 머무는 동안은 진하기 0.3으로 거의 비쳐 보이게 뒀다. 위로 떠오르며
  또렷해졌다가(`risingAlpha`) 끝에서 사라진다. 떠오르는 거리도 SD 키(210px)를 넘기도록
  170~280px로 늘려, 얼굴 위에 머물며 가리는 구간 자체를 없앴다. 진하기는 tween이 아니라 상승
  tween의 `onUpdate`에서 계산한다 — 프레임당 곱셈 몇 번이라 tween을 하나 더 거는 것보다 싸다.
- **속성 상성 화살표(▲·▼)를 걷어 냈다.** 유리하면 숫자 자체가 커지므로 같은 말을 두 번 하면서
  글자 폭만 넓혔다. 코어 `attack` 사건의 `effectiveness` 필드와 `elementEffectiveness`도 함께
  지웠다 — 쓰는 곳이 없는 필드는 남겨 두면 썩는다.
- **"주는 피해"와 "받는 피해"를 색으로 갈랐다.** 아군이 받은 피해는 물리·마법·고정·치명타를
  가리지 않고 **붉은 계열 하나로** 묶이고, 세기에 따라 옅은 살구빛에서 진한 다홍으로 짙어진다
  (`INCOMING_DAMAGE_TONE`). 여섯이 뒤엉킨 난전에서 "지금 누가 크게 맞았나"를 숫자를 읽지 않고도
  알 수 있다. 적에게 입힌 피해만 종류별 색을 갖는다 — 보통 피해(물리·마법)는 흰색, 고정 피해는
  보랏빛, 치명타는 노란색이다. 회복(연두)·보호막(푸른빛)·디버프는 받는 쪽에서도 제 색을
  지킨다: 그 셋은 "맞았다"가 아니라 다른 사건이기 때문이다.
- **경감은 종류를 가리지 않고 회색 하나로 통일했다.** "덜 들어갔다"는 무엇으로 맞았는지보다
  먼저 읽혀야 하는 정보라 색이 갈리면 오히려 흐려진다. 색 우선순위를 **경감 → 아군 피격 →
  치명타 → 궁극기 → 종류**로 고정했다.
- **디버프 색을 `DEBUFF_TONE` 표로 분리했다.** 출혈은 다크체리, 중독은 어두운 보랏빛이고 전부
  어둡게 눌러 둔다 — 지속 피해는 잔타로 자주 뜨므로 밝게 두면 큰 한 방과 같은 무게로 읽힌다.
  **새 디버프는 그 표와 `DebuffId`에만 더하면 되도록** 화면에서 색을 고르지 않게 했다(중독은
  전투 규칙보다 색표가 먼저 준비된 상태다).
- **광역 공격의 범위를 바닥에 그린다.** 코어에 `areaImpact` 사건을 더해 실제로 터진 자리와
  반경을 싣고(지정 원형·주위 광역·폭주 광역), 씬은 `groundArea`로 **눌린 마름모**를 SD보다
  뒤에 깐다. 정원을 그리면 바닥에 누운 것이 아니라 캐릭터 앞에 세워 둔 고리처럼 보인다.
  숫자만 셋이 한꺼번에 뜨면 왜 함께 맞았는지 읽히지 않는다.
- **폭주를 발광에서 필터로 바꿨다.** 몸 안팎에 두르던 흰 발광 두 겹(`FEVER_GLOW_TEXTURE`)을
  통째로 걷어 냈다 — 밝은 배경 원화 위에서 하얗게 떠 정작 봐야 할 캐릭터가 그 속에 묻혔다.
  대신 일러스트 자체에 그 개체의 스킬 아이콘 색을 입히고, 섞는 비율이 0.28~0.62를 오가며
  울그락불그락 끓는다. 매 프레임 칠하면 Puppet 조각 전부에 tint를 다시 먹여 그만큼이 프레임
  비용이 되므로 **10단계로 끊어** 단계가 바뀐 프레임에만 칠하고, 피격 섬광이 도는 동안에는
  아예 칠하지 않는다(`isHitFlashing`을 새로 노출했다).
- **섬광 진하기 상한을 0.6으로 낮추고 잦은 것일수록 더 옅게 했다**(일반 공격 0.5→0.3,
  궁극기 0.72→0.55). 실제 전장 캡처에서 일반 공격 섬광이 SD 위에 하얀 덩어리로 뭉쳐 보였다.
- 규칙은 `tests/unit/damageNumbers.test.ts`(20개)와 `tests/unit/effectPresets.test.ts`가
  고정한다 — 받는 피해의 붉은 계열 통일, 회복·보호막·디버프의 색 유지, 경감 회색 통일,
  화살표 부재, 진하기 곡선, 크기 상한, 바닥 범위의 눌림·진하기까지 포함한다.

## v0.36.1 — 2026-08-31

- **전투 입력을 명시적 판별 DTO로 통일하고 Phaser 비의존 정규화 경계를 추가했다.** 원정 뒤 스토리
  출격에서 직전 `runId`·노드·편성·증강이 재사용될 수 있던 경로를 새 `{ mode: "stage" }` 객체로
  차단했다. 전투 상단 문구도 모드별로 나눠 원정 중 선택된 스토리 이름이 보이지 않게 했다.
- **일반 원정·보스에서 스토리로, 스토리에서 원정으로 전환하는 순서 회귀 테스트를 추가했다.**
  스토리 입력에 원정 전용 필드가 남지 않는지와 모드별 헤더 문구까지 순수 단위 테스트로 고정했다.


## v0.36.0 — 2026-08-31

- **이펙트 매니저(`src/managers/EffectManager.ts`)를 세워 화면에 터지는 모든 것의 소유자를 하나로 모았다.**
  폭주·패시브·일반 공격·궁극기와 회복·보호막·사망·화면 조작이 전부 이 경계를 지난다. 씬은
  "무슨 일이 일어났는지"만 넘기고 파편 수·색·크기를 직접 고르지 않는다 — 씬마다 값을 정하면
  같은 기술이 화면마다 다른 무게로 터진다. `BattleScene`의 사망 별(`add.star`)과 손으로 만든
  피해 글자도 이 경계로 옮겼다.
- **동그란 파티클을 마름모 파편으로 바꾸고 중력을 걷어 냈다.** 참고한 데모는 원형 파티클이
  중력 0.15로 발밑에 쏟아지는 불꽃놀이였는데, 위에서 내려다보는 난전에서는 "터졌다"가 아니라
  "흘렸다"로 보인다. 파편은 좌우 꼭짓점 높이를 어긋나게 깎은 **마름모**이고 대부분의 종류가
  **위로** 뜬다(`gravity` 음수). 파문도 원이 아니라 납작한 마름모다.
- **렉이 걸리지 않는 방식으로 다시 짰다.** 데모의 `shadowBlur` 빛 번짐은 파티클 하나마다
  CPU가 그림자를 다시 계산해 난전에서 곧바로 프레임이 떨어진다. 대신 ① 흰 그림 두 장
  (`fx-shard`·`fx-glow`)을 한 번만 굽고 `tint`로 색만 갈아 끼우며(`src/ui/effectTextures.ts`),
  ② 종류마다 emitter 하나를 재사용하고 파문·수치 글자는 풀에서 꺼내 쓰고, ③ 한 프레임에 여는
  수(3)와 살아 있는 파문(14)·글자(26) 상한을 두어 넘치면 조용히 버린다. 폭주 발광도 같은
  `fx-glow` 한 장을 쓰도록 합쳐, 같은 발광이 두 곳에서 따로 구워지지 않게 했다.
- **인게임과 메뉴의 조작 이펙트를 다른 결로 나눴다.** 메뉴는 근미래 홀로그램 장비를 누른
  손맛(얇은 마름모 파문 한 겹), 전장은 단순한 SD가 뚜따시하는 화면에 맞춘 작은 조각 셋이다.
  씬마다 붙이면 화면이 늘 때마다 빠뜨리므로 모든 씬 위에 늘 떠 있는
  `EffectOverlayScene` 한 겹이 포인터를 듣는다(부트가 `launch`로 한 번 띄운다).
- **피해 수치를 세기에 따라 커지게 했다**(`src/ui/damageNumbers.ts`). 등급은 절대 수치가 아니라
  **대상 최대 체력 대비 비율**로 매긴다 — 절대값으로 가르면 레벨이 오를수록 모든 타격이 최대
  등급에 붙어 크기가 뜻을 잃는다. 잔타 34px에서 한 방 92px까지 다섯 등급이고, 등급이 오를수록
  외곽선·튀는 배율·머무는 시간이 함께 커진다(예전에는 30/40px 두 가지에 750ms 고정이라 너무
  빨리 사라졌다). 최고 등급의 큰 한 방만 화면을 흔들고, 조작이 어긋나지 않도록 아군이 맞을
  때는 흔들지 않는다.
- **피해 종류를 색으로 갈랐다.** 물리(흰빛)·마법(하늘빛)·고정(보랏빛)·출혈(다크체리)·회복(연두)·
  보호막(에너지 푸른빛)·무효(흐린 잉크)이며, 궁극기는 종류색보다 강조색이 앞선다. 치명타·
  궁극기는 한 등급 크고 숫자 뒤로 파편이 튀며, 경감된 타격은 한 등급 작다. 속성 상성은 문장이
  아니라 방향 표식(▲·▼) 하나로만 붙인다.
- **씬이 def를 다시 읽어 색을 되짚지 않도록 코어가 표시 정보를 함께 싣는다.** `SkirmishEvent`의
  `attack`에 `damageType`·`effectiveness`·`mitigated`를 더했다. 피해 전이는 확정 값을 그대로
  옮기므로 `"true"`(고정 피해)다. 상성 방향은 `src/core/element.ts`의 `elementEffectiveness`
  한 곳에서만 나온다.
- 규칙은 `tests/unit/damageNumbers.test.ts`·`tests/unit/effectPresets.test.ts`가 고정한다
  (등급 단조 증가, 종류별 색 분리, 섬광 진하기 상한, 프레임 예산, 파편 수 상한, 중력 상한).

## v0.35.1 — 2026-08-30

- **렐릭 카드에서 렉시아만 얼굴이 작아 보이던 문제를 고쳤다.** 카드 배율은 `content` **폭**으로
  정해지는데, 렉시아는 낫 무기가 좌우로 크게 뻗어 그 폭이 캔버스를 거의 다 차지한다
  (1023 / 1054). 몸은 그만큼 넓지 않으므로 혼자 축소됐다 — 두 눈 사이 거리로 재면 전신 원화
  여섯 종 중앙값의 **72%**로, 다음으로 작은 루카(94%)와도 큰 차이였다. 게다가 배율을 정한 그
  낫은 **정작 잘라내기에서 버려진다**: 카드 크롭은 머리 관절 기준 327~899이고 낫은 15~327에
  있어 화면에 나오지도 않는다. 화면 밖으로 나가는 만큼만 되돌리는 `cardZoom: 1.38`을 붙여
  100%로 맞췄다(값은 눈대중이 아니라 중앙값 49.0 ÷ 측정값 35.4로 계산했다).
  `content` 자체는 ZIP 안 WebP를 alpha > 16으로 다시 측정해 기존 값이 정확함을 확인했다 —
  경계가 틀린 게 아니라 폭 기준 배율이 무기까지 세고 있었던 것이다.
- **"작아 보인다"를 검사 가능한 계약으로 바꿨다.** `tests/unit/puppetAnchors.test.ts`에
  "실제 원화의 카드 얼굴 크기"를 더해, 모든 전신 원화의 얼굴이 중앙값의 0.8~1.35배 안에
  들어오는지 검사한다. 크기 대리 지표로 **두 눈 관절 사이 거리**를 쓴다 — 실루엣 폭은 무기·
  소매·들어 올린 손에 휘둘리지만 눈 간격은 얼굴 크기만 따라간다. 렉시아의 `cardZoom`을 떼면
  실패하는 테스트도 함께 두어, 보정이 사라지면 바로 드러나게 했다.
- **남겨 둔 것:** 도디(129%)·메테(120%)는 한계 위쪽에 붙어 있다. 둘은 원화 자체가 머리가 큰
  디자인이라 측정 오차가 아니라 의도로 보고 이번에는 손대지 않았다. 아트 방향이 더 촘촘한
  정렬을 원하면 그 둘의 `cardZoom`을 낮추면서 테스트의 상한도 함께 좁힌다.

## v0.35.0 — 2026-08-30

- **플레이어 정보창에 애착 렐릭·결투장 티어·최대 클리어·원정 역대 최고 기록을 같은 위계의 2×2 기록 영역으로 추가했다.** 애착은 공개 렐릭/초상 ID를 함께 전달해 공용 `PortraitCard`를 사용하고, 최대 스테이지는 저장 Set 순서가 아닌 정적 스테이지 순서를 따르는 순수 규칙으로 결정한다. 원정은 주간 기록과 혼동하지 않도록 `allTimeBestScore`와 “역대 최고” 라벨을 한 쌍으로 고정했다.
- **결투장 프로필 조회용 manager/API 경계를 먼저 마련했다.** 서버 확정 `seasonTierId`가 없는 현재 FakeServer 및 미배치 계정에서는 티어를 만들어 내지 않고 기록 칩 자체를 숨기며, 티어 표식은 공용 glyph 로더에서 그린다.

## v0.34.5 — 2026-08-30

- **렐릭 탭 그리드에서 토리카·도디의 정수리가 수평으로 잘리던 버그를 고쳤다.** 여러 번 손댔는데도
  낫지 않았던 이유까지 함께 남긴다 — 원인은 카드가 아니라 `src/puppets/anchors.ts`의
  `MAX_HEAD_DROP_RATIO`(0.34)였다. 이 한계가 걸리면 자르기 시작점이 `content.top`보다 **아래로**
  내려가 원화의 맨 윗부분이 통째로 사라지고, 그 단면이 수평선으로 남는다. 원래는 들어 올린 손처럼
  머리가 아닌 부위가 머리보다 높이 솟은 포즈만 걸리라고 둔 값인데, 등신이 낮아 머리가 큰
  토리카(머리 위 여백 ÷ 자르기 높이 = 0.386)·도디(0.383)가 함께 걸렸다. 실제 원화 여섯 종을
  전부 재서 가장 큰 값이 0.39 언저리인 것을 확인하고 한계를 **0.46**으로 올렸다 — 카드 절반(0.5)
  보다는 아래라 진짜 들어 올린 손 포즈에서는 여전히 얼굴을 상단 절반 안에 붙잡아 둔다.
- **되돌린 변경:** 토리카의 `cardHeadDropY: 14`를 지웠다. 이미 잘린 그림을 아래로 옮기는 보정이라
  잘린 단면을 되돌리지 못했고, 잘림이 사라진 지금은 공통 상단 여백(`HEAD_TIP_MARGIN_RATIO`)만으로
  정수리 위에 충분한 틈이 남는다. 원인을 덮는 개체별 보정 손잡이가 남아 있으면 다음에도 같은
  자리를 다시 덮게 되므로 필드 자체를 없앴다.
- **회귀 테스트를 지어낸 값이 아니라 실제 에셋으로 바꿨다.** 지금까지의 카드 잘라내기 테스트는
  전부 손으로 만든 프레임을 써서, 실제 원화가 잘리는 동안에도 한 번도 실패하지 않았다.
  `tests/unit/puppetAnchors.test.ts`에 전신 원화 일곱 종의 실제 메타데이터와 ZIP `머리1` 관절로
  "정수리를 자르지 않는다 / 정수리 위에 틈이 남는다 / 얼굴이 위쪽 절반 안에 선다"를 검사하는
  회귀 테스트를 더했다. 그러려면 메타데이터가 Phaser 없이 읽혀야 해서, 토리카·렉시아·스피나·
  루카·타페자라의 크기·알파 경계도 도디·메테·폰토스처럼 `src/puppets/assetMetadata.ts`로 옮기고
  `assets.ts`는 URL만 붙이게 했다(경계값은 ZIP 안 WebP를 alpha > 16으로 다시 측정해 확인했다).
- **그리드 왼쪽의 `/` 깎임이 머리 옆을 세로로 베던 문제를 구조로 풀었다.** 머리가 빠져나오는 홈이
  위아래 같은 폭의 직사각형이라, 잘린 모서리를 피하느라 좁아진 아랫폭이 정수리까지 그대로 따라
  올라가 머리 옆을 잘랐다. 홈을 **위로 갈수록 벌어지는 사다리꼴**로 바꿨다(`chipPoints`의
  `openTopWidth`·`openTopOffsetX`). 모서리를 피해야 하는 것은 홈이 칩 윗변과 만나는 한 줄뿐이고
  그보다 위에는 칩이 없어 아무것도 침범하지 않기 때문이다. 아울러 `portraitCardHeadWindow`를
  모든 카드가 쓰게 해, 깊게 깎인 왼쪽에 맞춰 오른쪽까지 함께 좁히던 예전 대칭 공식
  (`portraitCardNotchWidth`, 삭제)을 대신하게 했다. 도감 카드 기준으로 홈이 180 → 203(칩 윗변)·
  271(꼭대기)로 넓어졌고, 캐릭터별 `cardHeadEscape` 실측 없이도 대부분 해결된다 — 그 필드는
  그래도 모자란 원화를 위한 미세 조정으로 남긴다.
- **`Session`에 `playerResearch`가 들어오며 깨져 있던 단위 테스트 세 개의 세션 픽스처를 고쳤다.**
  `collectionManager`·`fakeServer`·`relicProgression` 테스트가 손으로 만든 세션 리터럴을 쓰는데
  새 필수 필드가 빠져 `npm run typecheck`가 실패하고 있었다. `createInitialPlayerResearchProgress()`를
  넣어 되살렸다 — 이 변경 자체와는 무관하지만 커밋 전 품질 게이트를 막고 있었다.

## v0.34.4 — 2026-08-30

- **`public/` 바로 아래에 흩어져 있던 콘텐츠 원화 11장을 `public/sprites/content/`로 옮겼다.**
  `Content1~5_001*.webp`·`journal_001.webp`가 배경(`sprites/background`)·재화
  (`sprites/currency`) 등 다른 모든 원화와 달리 `public/` 루트에 그대로 남아 있어 정리
  기준이 어긋나 있었다. `src/ui/backgrounds.ts`·`src/scenes/loadingSteps.ts`의 로딩 경로와
  `scripts/prepare_backgrounds.py`의 굽는 대상(콘텐츠 원화는 이제 `sprites/content`로 굽는다)을
  함께 옮겨 `public/` 루트에는 `fonts`·`puppets`·`sprites`만 남는다.

## v0.34.3 — 2026-08-30

- **환경설정 "지원" 탭(계정 정보·계정 탈퇴 등)이 위쪽으로 뭉쳐 겹쳐 보이던 버그를 고쳤다.**
  `SettingsScene.buildRows`의 `section()` 클로저는 자기 스코프의 `y`만 갱신하는데,
  `buildSupportRows(y, section)`는 그 갱신을 반영하지 못하는 별도의 매개변수 `y`를 계속
  들고 있어 — 패널 제목은 올바른 위치에 그려지지만 그 아래 행들은 제목이 그려지기 **전**
  위치(패널 시작점 바로 아래)에서부터 다시 쌓여 제목·본문이 겹쳤다. `section()`이 갱신된
  `y`를 반환하도록 고치고 `buildSupportRows`가 그 값을 이어받게 해 각 섹션의 제목과 본문이
  올바른 순서로 쌓이게 했다. 겹침이 사라진 김에 두 패널의 높이(420→330, 742→660)도 실제
  내용 길이에 맞춰 줄여 아래쪽 빈 공백을 없앴다.
- **병합 과정에서 남은 `InventoryPopup`의 죽은 `onWalletChanged` 호출을 지웠다.**
  `InventoryManager.useConsumable`이 이미 managerEvents로 지갑 갱신을 발행하고
  TopBar가 이를 구독하므로(v0.34.2 이전에 들어온 "매니저 이벤트로 UI 동기화" 변경), 더는
  존재하지 않는 생성자 콜백을 여기서 다시 부를 필요가 없다.

## v0.34.2 — 2026-08-30

- **퍼블릭에 있었지만 어디에도 연결되지 않았던 재화·Puppet 원화를 정리했다.**
  `sprites/currency/dna.webp`는 이미 구워져 있었는데도 `currency-dna` 키가 계속
  `heart.webp`를 임시로 빌려 쓰고 있었다 — `src/ui/currencyIcons.ts`가 제 파일을 가리키게
  고쳤다. `puppets/char_007.zip`·`charSD_007.zip`(가칭 "타페자라", 프테라 모티브 신규
  캐릭터)과 `puppets/enemy_004.zip`·`enemySD_004.zip`(puppet.json 원제 "비늘 후드의 신비한
  꼬마 탐험가")은 렐릭 데이터·전투 수치·소속 팩션이 아직 정해지지 않아 `src/puppets/assets.ts`에
  측정된 `PuppetAsset`(`TAPEJARA_ASSET`/`TAPEJARA_SD_ASSET`, `EXPLORER_ASSET`/
  `EXPLORER_SD_ASSET`)로만 등록해 두고, 화면이 실제로 쓰는 표(`PORTRAIT_ASSETS`·
  `ENEMY_SD_ASSETS`)에는 아직 넣지 않았다. 콘텐츠가 정해지면 이 네 상수를 그 표에 연결하기만
  하면 된다.

## v0.34.1 — 2026-08-30

- **퍼블릭에 원본 그대로 남아 있던 콘텐츠 배경 4장을 구웠다.** `Content2~5_001background.png`
  (장당 9MB 안팎)를 `scripts/prepare_backgrounds.py`로 WebP(장당 500KB 이하)로 굽고 원본은
  저장소에서 지웠다. `Content2`(원정)는 이미 원정 화면이 지도·필드 배경을 따로 갖고 있어,
  대신 원정 순위 팝업(`ExpeditionRankingPopup`)의 전용 배경으로 붙였다. `Content3`(케이크
  대작전)·`Content4`(현상수배)·`Content5`(레이드)는 각 콘텐츠 진입 화면(`SortiePreviewScene`)이
  지금까지 임시로 빌려 쓰던 스테이지 지도 배경 대신 자기 배경으로 갈아입는다. 새 키 네 개
  (`expeditionRanking`·`sortieCake`·`sortieBounty`·`sortieRaid`)는 `src/ui/backgrounds.ts`
  한 곳에서만 소유하며 `BACKGROUND_ASSETS`를 통해 타이틀 로딩 단계에서 함께 적재된다.

## v0.34.0 — 2026-08-30

- **전투 기여도 그래프 팝업을 "공격 · 방어 · 회복" 버튼과 짝지었다.** 결과 팝업에서 그래프를
  열면 그 버튼은 사라지고, 그래프 판 우측(원래 닫기 X가 서던 자리)에 작은 "돌아가기" 라벨
  버튼이 대신 선다. 그래프를 닫으면 숨겼던 버튼이 다시 보인다
  (`StageCompletePopup.onOpenContribution`이 `onClosed` 콜백을 받고,
  `BattleContributionPopup.open`이 그 콜백을 실제 팝업 종료 시점에 정확히 한 번 불러 준다).
- **기여도 그래프의 이름 목록에 얼굴 액자를 더했다.** 이름만으로는 다섯 줄에서 누구인지 한눈에
  읽히지 않아, 재화 액자·룬 조각과 같은 "액자" 예외(불투명 사각 + 사방 테두리 + 안쪽 비네팅)를
  얼굴에도 적용하는 `FaceFrame`을 새로 만들어 이름 앞에 붙였다. 카드(`PortraitCard`)와 달리
  머리가 빠져나오는 홈을 두지 않고 사각 안에 얼굴을 그대로 채운다.
- **스토리 승리 팝업의 편성이 그리드 카드 대신 SD가 idle로 선 모습으로 바뀌었다.** Puppet은
  컨테이너 변환을 물려받지 않으므로 팝업 본문 밖의 전용 레이어(화면 좌표)에 세우고, 이름표와
  "MVP!"만 팝업 본문에 얹는다. "MVP!"는 승리 표제처럼 튀어 오르며 살짝 기울어 작아도 먼저
  눈에 든다.
- **토리카의 그리드 카드 뿔 잘림을 마저 고쳤다.** 모든 카드가 공유하는 상단 여백
  (`HEAD_TIP_MARGIN_RATIO`, v0.33.0)만으로는 뭉툭한 뿔이 여전히 살짝 넘쳐 보였다. 카드
  전용(`portraitOffsetY`와 달리 전신 정보창에는 영향을 주지 않는) 새 보정값
  `PuppetAsset.cardHeadDropY`를 토리카에만 얹어 그림을 카드 안에서 살짝 더 내렸다.

## v0.33.0 — 2026-08-30

- **머리 카드 잘라내기에 공통 상단 여백을 더했다(`computeHeadCardFrame`).** `headroom: 0`은
  내용 상자 맨 위에 자르기를 딱 붙여, 뾰족하거나 갈래진 장식(깃털·머리카락)은 자연스럽게
  가늘어지며 사라져 눈에 띄지 않았지만 토리카의 뿔처럼 끝이 뭉툭·넓은 장식은 잘린 단면이
  수평선으로 그대로 남아 그리드 허용 범위 밖으로 넘친 것처럼 보였다. 캐릭터별 예외 없이
  `HEAD_TIP_MARGIN_RATIO`(카드 높이의 3.5%) 하나만 자르기 시작점 위에 더해 모든 캐릭터의
  정수리가 살짝 낮아지고 숨 쉴 틈이 생기게 했다.
- **일반 노드 클리어 보상도 주간 누적 점수(랭킹 팝업의 단계 보상 트랙)에 더한다.** 순위
  기준(`bossWeek.bestScore`)은 여전히 불사 보스에게 입힌 피해량만 반영하지만(v0.32.0 참고),
  누적 점수는 보스전만이 아니라 노드를 도는 노력도 조금씩 보태 주간 보상 단계에 기여하게
  했다(`expeditionNodeRewardScore`, `completeExpeditionNode`). 활성 원정 화면의 "획득 전리품"
  판 아래에는 이번 런이 보탠 몫을 판 없이 흰 글자+검은 테두리로만 보여준다.
- **원정 "포기하기" 버튼을 작게 줄이고 화면 우상단 구석으로 옮겼다.** 전리품 판이 점수 한
  줄만큼 커지며 버튼과 가까워졌던 것을, 겹치지 않는 화면 위쪽 빈 자리로 올려 실수로 누르기
  어렵게 했다.
- **원정 랭킹 팝업에서 기여도 보상 목록을 지웠다.** "랭킹"과 "주간 보상" 두 팝업이 같은 누적
  보상 단계 목록을 중복해서 보여주고 있었다 — 이제 보상 수령은 "주간 보상"
  팝업(`ExpeditionRewardPopup`)의 몫으로 단일화하고, 랭킹 팝업은 순위표만 크고 또렷하게
  보여준다(그래서 이제 쓰이지 않는 `src/ui/expeditionRewardStage.ts`는 지웠다). 아직 다인원
  서버가 없어 순위표에 항상 내 기록 하나만 뜨던 것도, 친구 화면의 임시 인물(`PREVIEW_FRIENDS`)
  셋을 빌려 순위표가 늘 여럿 있는 것처럼 채웠다 — 실제 서버가 다수 이용자 기록을 돌려주면
  이 보정은 지운다.

## v0.32.0 — 2026-08-30

- **주간 랭킹 반영 대상을 바로잡았다.** 불사 보스는 애초에 처치가 불가능해 "완료"라는 결과가
  없다 — 성과는 승패가 아니라 그 안에서 입힌 피해량 자체다. v0.31.0에서 일반 노드 진행 점수를
  런 종료 시 함께 반영하도록 고쳤던 것을 되돌리고, 대신 `submitExpeditionBossScore`(승패와
  무관하게 항상 제출된다)만이 주간 랭킹·누적 보상·소탕이 참조하는 역대 최고점을 갱신하도록
  단일화했다.
- **스토리 스테이지 승리 결과를 `StageCompletePopup`(보상 팝업의 연장선)으로 다시 만들었다.**
  "확인 및 저장" 버튼을 눌러야 저장되던 계약을 버리고, 승리 즉시 `completeStage`로 확정한 뒤
  이미 지급이 끝난 결과만 보여준다 — 그래서 보상 팝업과 같은 계약대로 화면 아무 곳이나 눌러도
  다음 화면(지도)으로 넘어간다. 위에서부터 귀여운 별 장식의 "Victory!" 표제, 분류별 몫의 합으로
  고른 MVP를 가운데 크게·나머지 둘을 옆에 작게 세운 편성, 공격·방어·회복 기여도 그래프를 여는
  버튼, 보상 액자 순으로 쌓는다. MVP 판정(`battleContributionMvp`)은 원시 수치 대신 분류별
  몫(ratio)의 합을 쓰므로, 공격을 몰아 받은 딜러와 회복을 몰아 받은 힐러가 규모 차이 없이
  공정하게 경쟁한다. 패배 결과 화면은 기존 그대로 둔다.

## v0.31.0 — 2026-08-30

- **주간 원정 실제 점수를 랭킹에 연동.** 지금까지 주간 랭킹(`getExpeditionWeeklyBest`/
  `getExpeditionLeaderboard`)은 20층 보스를 잡아 점수를 제출한 런만 반영했고, 일반 진행만 하고
  포기하거나 전멸한 런의 실제 도달 점수는 어디에도 남지 않았다. `settleExpeditionRun`이 결과와
  무관하게 `run.bestScore`(실제 도달 점수)를 같은 주간 누적/최고 집계에 합류시키도록 고쳤다 —
  단, 그 런이 이미 보스 점수를 제출했다면(그 값이 `run.bestScore`에도 흡수돼 있으므로) 중복
  반영하지 않는다.
- **원정 기회를 주 2회로 제한.** `playsThisWeek`는 있었지만 실제로 막는 곳이 없어 무제한이었다.
  `EXPEDITION_WEEKLY_POLICY.maxPlaysPerWeek`(2)를 두고 `ExpeditionManager.start`에서 강제한다.
- **소탕 추가.** 원정 기회를 하나 소비해 직접 플레이하지 않고도 지금까지 달성한 역대 최고
  점수(`allTimeBestScore`, 새 저장 필드)의 80%를 주간 기록에, 노드 클리어 전리품 상한의 50%를
  즉시 지급한다(`GameApi.sweepExpedition`). 주간 달성도(누적 점수 단계 보상)는 문턱을 처음
  넘는 순간에만 지급되므로 같은 주에 반복해도 이미 넘은 문턱에는 의미가 없지만, 다음 문턱을
  향해 누적치를 계속 쌓을 수는 있어 막지 않았다. 출격 버튼 옆에 나란히 두었다.
- **원정 첫 화면(주간 기록)을 다시 짰다.** 폰토스 전신을 화면 가득 채우도록 크게, 얼굴이 제목
  아래에서 시작하도록 높이 세웠다(이전에는 애매한 높이로 잘려 작아 보였다). 상시 펼쳐 두던
  큰 순위표 판을 없애고, 화면 중하단에 내 주간 최고/순위/누적을 보여주는 간결한 점수 패널
  하나만 남겼다. 순위표는 "랭킹" 버튼을 눌러야 여는 팝업(`ExpeditionRankingPopup`, 이미
  있었지만 보스 결과 화면에서만 쓰이던 것을 재사용)으로 옮겼고, "주간 보상" 버튼도 같은 줄에
  단순한 크기로 추가해 기록 보상 팝업을 바로 연다.

## v0.30.5 — 2026-08-30

- 토리카(`TORIKA_ASSET.cardZoom`)가 0.62로 지나치게 확대를 줄여, 도감·편성·발굴 등 모든
  카드 그리드에서 혼자만 작고 왼쪽으로 치우쳐 보였다. 0.82로 다시 맞춰 다른 카드와 비슷한
  크기로 채운다.
- 방치 발굴 팝업의 "보유 렐릭" 편집 그리드(`IdleExcavationPopup`의 `GRID_VIEW`) 카드
  비율(215×235, 세로/가로 1.09)이 도감 그리드(300×400, 1.55)와 달라, 같은 캐릭터라도
  머리 관절 기준 잘라내기(`computeHeadCardFrame`)가 카드 가로세로비를 그대로 잘라내는
  비율로 쓰는 탓에 두 화면에서 서로 다른 구도로 잘려 보였다(도디의 모자·책 노출 범위가
  달랐던 사례). 카드 높이를 268로 늘려 도감과 같은 비율로 맞췄다 — 그리드는 이미
  스크롤 가능하게 설계돼 있어 안전하게 늘어난다.

## v0.30.4 — 2026-08-29

- 편성 화면 상성 화살표를 흰 테두리로 두른 홑겹 삼각형 대신, 사방 테두리 없이 그림자 ·
  같은 색 발광 · 뾰족한 끝 두 변에만 거는 밝은 강조선으로 이루어진 V자 홈 화살촉으로 다시
  그렸다(`AffinityDirection`). 기존 모양은 이 화면의 "테두리를 두르지 않는다" 홀로그램
  규칙을 어기고 있었다.
- 자동 배치 버튼을 화면 위쪽(제목 옆)에서 보유 렐릭 그리드 위 우측으로 옮겼다
  (`PartyScene`). 그리드와 같은 배치표(`ROSTER_GRID`)를 공유해 그리드 오른쪽 경계에
  맞추고, 셋째 아군 자리 이름표와 겹치지 않도록 버튼 폭을 좁혔다.
- `refresh()`가 매번 자동 배치 버튼 좌표를 `{ x: 180, y: 190 }`으로 하드코딩해 덮어써
  E2E가 실제 이동한 위치를 읽지 못하던 버그를 고쳤다 — 이 때문에 위치를 옮긴 뒤에도
  자동 배치 버튼을 실제로 누르는 테스트가 옛 좌표만 눌러 왔다.
- 자동 배치로 이미 셋을 고른 상태에서 새 카드를 누르면 아무 반응이 없어 "편성을 고칠 수
  없다"로 보이던 것을 고쳤다 — 가득 찬 상태에서 새 카드를 고르면 마지막 자리를 그 카드로
  바로 바꾼다(`PartyScene.toggle`).

## v0.30.3 — 2026-08-29

- 루카의 관찰 프로필에 벨로키랍토르 기반 성체의 복원 이력과 신장 1.62m·체중 59kg을 추가하고,
  도감에도 같은 수치와 단거리 질주에 적합한 하체 근육·가벼운 골격을 명시해 두 화면의 설정이
  어긋나지 않게 했다. 해금 기록은 유일한 연구원인 주인공의 1인칭 관찰로 고쳐, 집과 휴식을
  좋아하는 단거리 선수라는 정체성과 육식 렐릭들과의 관계, 연구소 소파 및 치즈케이크 습관을
  체중 조롱이나 의학적 단정 없이 담았다.

## v0.30.2 — 2026-08-29

- 스피나의 `stealthLeap`가 이동 속도에 따른 도약처럼 읽히던 설명을 `순간이동` 규칙 키워드로
  바로잡았다. 저장·전투 호환성을 위해 기존 내부 식별자는 유지하되, 실제 전투처럼 경로와 이동
  시간을 거치지 않고 같은 발동 프레임에 착지 좌표가 확정된다는 계약을 타입 주석과 단위 테스트에
  명시했다.

## v0.30.1 — 2026-08-29

- 렉시아(`LEXIA_ASSET`, char_002.zip)의 `content`(alpha 실제 경계)가 1번 토리카
  (char_001.zip) 값을 그대로 옮겨 온 것이었다. 렉시아가 왼쪽으로 크게 뻗어 쥔 낫 무기가
  이 잘못된 경계 밖으로 잘려 있어, 카드·정보창 배율이 무기 없는 좁은 폭을 기준으로 계산되며
  실제보다 확대되어 도감 그리드에서 다른 카드보다 얼굴이 눈에 띄게 크고 상반신이 덜 보였다.
  ZIP 안 WebP의 alpha > 16 실제 경계로 다시 측정해 `{ left: 15, top: 43, right: 1038,
  bottom: 1455 }`로 고쳤다. 스크린샷으로 스밀라·케찰·토리카와 같은 비율의 상반신이 보이는지
  확인했다.

## v0.29.11 — 2026-08-29

- v0.29.10의 대각선-홈 겹침 수정 이후에도 스피나(뒷머리 오른쪽)·메테(후드 왼쪽)·도디(머리
  깃털)의 머리 장식이 한쪽으로 쏠려 대칭 홈의 그 쪽 대각선 모서리 안쪽에서 여전히 애매하게
  잘려 보였다. 관절은 항상 카드 가운데를 기준으로 잡지만 장식은 포즈에 따라 한쪽으로 쏠려
  그려지기 때문이다. `portraitGrid.ts`에 `portraitCardHeadWindow`를 추가해 `PuppetAsset`의
  새 필드 `cardHeadEscape: { left?, right? }`(칩 폭 대비 비율)로 그 쪽 홈만 넓히도록 했다 —
  대각선 안쪽 안전 여유(v0.29.10에서 고친 자기 교차 문제)는 항상 지킨다. 세 캐릭터의 실측값을
  채우고 스크린샷으로 확인했다.
- 루카처럼 들어 올린 손·망토가 얼굴보다 높이 솟은 포즈는 `computeHeadCardFrame`의
  `headroom: 0`이 "내용 상자 맨 위"에서 그대로 자르기를 시작해, 얼굴이 카드 아래로 밀려나거나
  잘려 나갔다(발굴·도감 카드에서 루카 얼굴이 거의 보이지 않던 문제). `MAX_HEAD_DROP_RATIO`
  안전장치를 추가해 머리 관절이 항상 카드 상단 34% 안에 들어오도록 자르기 시작점의 하한을
  잡았다 — 새 캐릭터를 추가해도 자동으로 적용되는 일반 안전망이라 손볼 필요가 없다. 다만
  루카의 들어 올린 손 자체(머리 폭 기준 잘라내기 범위 밖으로 나가는 부위)는 이 두 안전장치로
  담을 수 있는 범위를 넘어서 카드에 여전히 나오지 않는다 — 카드 잘라내기 폭 자체를 늘리는
  시도(`cardZoom` 계열 배율 조정)가 루카 원화에서 일관되지 않게 동작해(비율에 따라 캐릭터가
  아예 화면 밖으로 밀려나는 등) 이번에는 되돌리고 남겨 둔다. 같은 시도를 반복하지 않기 위한
  기록이다 — 다음에는 실제 렌더링된 Puppet 텍스처의 실측 크기부터 확인해야 한다.
- 회귀 테스트: `portraitCardHeadWindow`의 비대칭 확장·안전 클램프(`tests/unit/portraitGrid.test.ts`),
  `computeHeadCardFrame`의 상단 안전장치(`tests/unit/puppetAnchors.test.ts`).

## v0.30.0 — 2026-08-29

- 런타임 전투원 ID별 공격·방어·회복 기여도 시스템을 추가했다. 공격은 경감과 과잉 피해를
  제외한 실제 HP 손실만 공격력/주문력으로 나누고, 방어는 방어력/저항력 경감과 보호막 제공자
  흡수를 나누며, 회복은 시전자가 만든 실제 HP 증가만 기록한다. 결과 UI와 향후 서버 검증기가
  같은 순수 누적 정책과 불변 스냅샷을 사용해 중복 렐릭 편성과 프레임 분할에도 안정적으로
  재현할 수 있게 했다.

## v0.29.10 — 2026-08-29

- 모든 캐릭터 카드(`PortraitCard`)의 좌측 상단 대각선 깎임이 머리가 빠져나오는 윗변 홈과
  겹쳐, 튀어나온 머리·모자·장식이 그 `/` 대각선에 애매하게 잘려 보이던 문제를 고쳤다.
  출격 팝업의 SD 자리(`ExpeditionEntryButton.sdMask`)가 이미 같은 문제를 "띠를 깎인
  깊이만큼 내린다"는 방법으로 풀어 둔 걸 그대로 가져와, 홈 폭 자체를 위쪽 두 모서리의
  깎인 깊이만큼 줄이는 순수 함수 `portraitCardNotchWidth`로 만들었다(`portraitGrid.ts`).
  도감·발굴·원정 등 이 카드를 쓰는 모든 화면에 한 번에 적용된다. 실제 화면 스크린샷으로
  전/후 차이(도디 모자 장식이 인접 카드 배경에 걸쳐 잘리던 부분)를 직접 확인했다.

## v0.29.9 — 2026-08-29

- 발굴 편성 그리드의 하단 그라데이션(비네트)을 지웠다. 스크롤 없이도 항상 떠 있어서
  다 스크롤한 마지막 줄까지 가려 버렸다 — 마스크로 자연스러운 컷을 만드는 대신, 도감
  그리드(RelicsScene)와 같은 28px 여유를 스크롤 하한에 더해 마지막 줄 밑변이 마스크
  경계에 정확히 겹쳐 앤티에일리어싱으로 깎이지 않게 했다.
- 원정 편성판(보유 렐릭 그리드)이 보유 렐릭 3줄부터 출격 버튼과 겹치던 문제를 고쳤다.
  그리드가 고정 좌표에 그대로 쌓이기만 하고 스크롤·마스크가 아예 없었다 — 도감·발굴과
  같은 스크롤 그리드 패턴(뷰포트 마스크, 드래그·휠, 카드별 syncMask 틱커)을 새로 달아
  편성판 아래·힌트/출격 버튼 위 사이에서만 스크롤하게 했다. 실제 화면 스크린샷으로
  겹침이 사라졌는지 확인했다.
- 스피나 기본 공격 설명에서 상단 라벨과 겹치는 "공격력의 80%"를 지우고 토리카 일반
  공격과 같은 "적 한 명에게 물리 피해를 준다" 형태로 줄였다. 궁극기 설명의 "피해를
  준다고 기절시킨다"는 인용형 어미("~라고")로 읽히는 오타였다 — 어간에 연결어미를
  붙인 "주고 기절시킨다"로 고쳤다.

## v0.29.8 — 2026-08-29

- 발굴 편성 그리드에서 둘째 줄부터 카드가 뷰포트 경계에서 갑자기 잘려 취소·배치 버튼을
  침범하는 것처럼 보이던 문제를 고쳤다. 스크린샷으로 직접 확인해 원인을 잡았다 — 뷰포트가
  한 줄 반 정도만 담을 높이라 스크롤 없이도 둘째 줄이 이름표 없이 얼굴만 걸친 채 끊겨
  버튼 바로 위에서 뜬금없이 잘려 보였다. 다른 화면 가장자리 바와 같은 `drawGlassFade`
  검정 그라데이션과 얇은 경계선을 마스크 밖(항상 보이는 층)에 얹어, 스크롤할 카드가 있을
  때만 카드가 어둠 속으로 자연스럽게 사라지고 버튼 자리와 분명히 갈라지게 했다.

## v0.29.7 — 2026-08-29

- 룬 세공을 한 번 성공/실패한 뒤 버튼이 꺼진 채로 남아 바로 이어서 누를 수 없던 버그를
  고쳤다. `render()`가 요청 완료 시 세공 버튼을 새로 만드는데, 그 시점에 `pending` 플래그를
  `finally`에서야 늦게 풀어 방금 만든 새 버튼도 꺼진 채로 나왔다 — `render()` 호출 전에
  먼저 풀도록 순서를 바꿨다. 이름 변경 뒤에도 같은 이유로 세공 버튼이 꺼져 있던 문제를
  같은 방식으로 고쳤다.
- 스피나(전신 원화) 카드가 실제보다 작고 왼쪽으로 치우쳐 보이던 문제를 고쳤다. `content`
  경계가 잘리지 않은 원본 캔버스 그대로였던 탓이며, ZIP 안 WebP의 alpha > 16 실제 경계로
  다시 측정해 넣었다. 카드 확대 배율은 이 경계의 폭에 반비례하므로 자동으로 커지고, 머리
  관절 중심 크롭도 더 좁아진 창으로 다시 잡혀 자연히 중앙으로 당겨온다.

## v0.29.6 — 2026-08-29

- 발굴 보관량 게이지가 경과 시간이 아니라 실제 쌓인 재화량을 보관 한도(시간당 생산량 ×
  보관 한도 초)와 비교해 채운 비율을 보여주도록 고쳤다. 기존 경과 시간 기준 계산은 조회
  (`getIdleExcavation`)마다 서버가 정산을 다시 돌려 기준 시각(`lastSettledAt`)이 지금으로
  밀리므로, 창을 열 때마다 게이지가 늘 0%로 보이는 버그가 있었다. `excavationStorageFillRatio`
  시그니처를 실제 재화량 기반으로 바꾸고 `excavationStorageLimitSeconds`를 분리했다.
- 발굴 편집 그리드를 두 줄 이상 스크롤하면 둘째 줄부터 캐릭터 원화가 카드를 따라 내려오지
  않고 예전 자리에 남아 그리드와 어긋나던 현상을 고쳤다. `PortraitCard`의 원화 자체 기하
  마스크는 부모 컨테이너의 이동을 상속하지 않아 스크롤마다 `card.syncMask()`를 다시 불러야
  하는데(도감 그리드가 이미 쓰는 패턴), 발굴 그리드는 뷰포트 마스크만 갱신하고 카드별 마스크
  갱신을 빠뜨렸다.
- 1~3번 칸에 이미 배치된 렐릭 카드에 원정 편성판과 같은 어두운 반투명 오버레이를 추가했다.
  칩 밖으로 튀어나온 머리 몫까지 원화 알파 그대로 검게 겹치는 기존 `selectedOverlayAlpha`
  옵션을 그대로 켰을 뿐이라 별도 마스크 계산은 새로 만들지 않았다.

## v0.29.5 — 2026-08-29

- 원정 최종층 보스의 영문 스펠링을 `pontus`에서 `pontos`로, 한글 표기를 "폰투스"에서
  "폰토스"로 바로잡았다. 렐릭 id·스킬 id·Puppet 에셋 상수·타입·경로·테스트를 전부 함께
  고쳐 이름이 코드 전역에서 하나로 일관되게 했다(Puppet ZIP `enemy_Pontos.zip`·
  `enemySD_Pontos.zip`, 스킬 아트 `public/sprites/skills/pontos/` 포함). 저장 데이터에는
  등장하지 않는 보스 정의라 마이그레이션은 필요 없다.

## v0.29.4 — 2026-08-29

- 원정 최종층 보스 폰투스의 전용 스킬 일러스트를 구워 연결했다. 보스는 번호 대신 렐릭 id를
  원본 이름 머리말로 그대로 쓰므로 `prepare_skill_icons.py`의 원본 표에 `pontus: pontus`를
  추가했다. 흰 실루엣 WebP 네 장을 `public/sprites/skills/pontus/`에 구웠고, `skillArt.ts`의
  `RELICS_WITH_ART`에 `pontus`를 더해 정보창이 전용 그림을 쓰게 했다. 원본 PNG 4장은 굽고 나서
  저장소에서 지웠다.

## v0.29.3 — 2026-08-29

- 도디(5번)·메테(6번) 전용 스킬 일러스트를 구워 연결했다. `prepare_skill_icons.py`의 원본 표에
  `char005: dodo`, `char006: mette`를 추가해 흰 실루엣 WebP 네 장씩(패시브·일반 공격·궁극기·
  폭주)을 `public/sprites/skills/{dodo,mette}/`에 구웠고, `skillArt.ts`의 `RELICS_WITH_ART`에
  두 id를 더해 정보창 스킬 아이콘이 공용 대체 아이콘 대신 전용 그림을 쓰게 했다. 원본 PNG
  8장은 굽고 나서 저장소에서 지웠다.

## v0.29.2 — 2026-08-29

- `CLAUDE.md`에 야성·패시브·일반 공격·궁극기 설명문의 공통 규칙을 문서화했다("스킬 설명문
  규칙" 절). 고정 % → 실제 능력치 환산 태그, 상단과 겹치는 수치 생략, 반복되는 같은 값은 한
  번만 말하기, 정의된 규칙어는 태그로만 연결, `%p` 금지(회귀 테스트로 고정)를 캐릭터마다
  다시 판단하지 않도록 한곳에 모았다.
- 폰투스 패시브 "심해의 압력"이 "매초 주문력이 상승한다"처럼 막연하게만 말하던 것을 실제
  수치("12 상승")로 명시하도록 `passiveDescription()`에 `abyssalPressure` 분기를 추가했다.
  키워드 사전에 `ap`(주문력)를 추가해 atk·def와 같은 방식으로 태그를 걸 수 있게 했다.

## v0.29.1 — 2026-08-29

- 메테 궁극기(순수 회복형)를 정보창에서 누르면 스킬 팝업이 열리지 않던 버그를 고쳤다. `previewSkillDamage`는
  damageType/power가 없는 스킬에 호출하면 예외를 던지는데, 궁극기는 패시브와 달리 이 조건 없이
  항상 미리보기를 시도했다. 판별을 `canPreviewSkillDamage()`로 뽑아 궁극기도 패시브와 같은 기준으로
  가드한다.
- 설명문에서 스킬마다 다시 적던 % 수치를 실제 능력치로 환산해 태그로 보여 주는 쪽으로 정리했다.
  메테 크레센도(스타카토 추가타), 아다지오의 무게(보호막), 도디 궁극기(아군 회복)가 각각
  `[[damage-value]]`, `[[shield-value]]`, `[[heal-value]]` 태그로 실제 수치를 보여 준다.
  전투 중 계속 바뀌는 수치(스피나 궁극기의 "현재 공격 속도"처럼)는 여전히 %로 남긴다 — 고정값이
  아니라서 미리 환산해 둘 수 없다.
- 메테 기본 공격 설명에서 "0.1초간 경직"의 "0.1초" 중복을 지웠다. 경직 키워드 자체가 이미 약
  0.1초라고 설명하므로 스킬 문구에서는 `[[stagger|경직]]` 태그만 남긴다.
- 크레센도·아다지오의 무게 설명을 각각 "50%의 피해량을 가진 스타카토(태그) 추가 발동", "아군이
  군중제어(태그)에 걸리면 정화" 형태로 줄였다. "군중제어"·"마법 피해" 키워드를 새로 추가했다.
- 렉시아 패시브 설명을 "전투 시작 시, 네 능력이 각각 25%, 25%, 25%, 25%"에서 "전투 시작 시,
  네 능력이 모두 25% 오른다"로 줄였다 — 네 수치가 항상 같은 값이라 한 번만 말해도 충분하다.
- 도디 야성 발현("인비저블 썸띵?") 설명에서 "(속도 ×2)" 괄호를 지웠다. 배속 환산은 개발 참고용
  주석에만 남긴다.
- (참고) "%p"(퍼센트포인트) 표기는 이전에 한 번 시도했다가 플레이어에게 낯선 단위라 되돌리고
  회귀 테스트(`사용자 노출 퍼센트 표시 계약`)까지 남긴 결정이라, 이번에도 치명타 확률류 수치는
  일반 `%`로 유지했다.

## v0.29.0 — 2026-08-29

- 발굴 현황의 SD 발밑과 누적 액자 사이에 보관 한도가 찬 비율을 임무 연구도와 같은 HoloBar 게이지로 보여 준다. 정산 로직과 같은 확장 배율을 공유하는 순수 함수 `excavationStorageFillRatio`를 추가했다.
- 설정 > 고객지원·데이터 탭에 "모든 캐릭터 획득" 임시 디버그 진입점을 추가했다. 스타터 렐릭 추가처럼 저장 마이그레이션이 옛 계정까지 소급하지 않는 변경을 재설치 없이 확인할 수 있다. (참고: 기존 "저장 데이터 초기화" 역시 로컬 저장을 지우고 재부팅해 최신 스타터 구성을 다시 받는 방법으로 이미 동작한다.)

## v0.28.1 — 2026-08-28

- 도디와 메테의 전신·SD를 각각 5번과 6번 Puppet 묶음에 연결하고, 실제 알파 경계로 카드 확대와 전투 바닥선을 맞췄다. 전용 원화가 연결됨에 따라 토리카를 재사용하던 임시 메테 계약을 제거했다.
- 도디 스킬명을 `인비저블 썸띵?`, `연구원님, 이것 좀 보세요!`, `깃펜 톡톡`, `세기의 대발견... 맞죠?!`로 확정하고 일반 공격 계수를 기획안의 주문력 70%로 바로잡았다.

## v0.28.0 — 2026-08-28

- 저장 호환 ID `dodo`를 유지하면서 표시 이름을 도디로 확정하고, 토리카와 같은 일러스트 비율의 신장 1.08m 및 가벼운 조류형 체중 20kg을 관찰·도감·해금 기록에 통일했다.
- 도디의 폭주를 공격 속도 +100%(속도 x2, 공격 간격 50%)로 교체하고, 생존 중 아군 방어력·저항력 +15% 및 적 받는 회복량 -30% 오라를 최댓값 비중첩 규칙으로 추가했다.
- 도디 일반 공격에 실제 감소 HP 기반 최저 현재 HP 아군 회복을 추가하고 자신 포함·편성 순서 동률 규칙을 확정했다. 궁극기는 250 게이지로 지정 원 안 적에게 주문력 200% 마법 피해, 생존 아군에게 주문력 200% 회복을 함께 적용한다.
- 도디의 임시 전신과 SD를 같은 토리카 기반 에셋으로 명시했으며, 폰투스 정보창 전신은 조금 키우고 위로 올려 하단 치우침을 완화했다.

## v0.27.10 — 2026-08-28

- 스피나 궁극기 `범람의 포식자`의 현재 공격 속도 계수를 300%에서 150%로 낮춰 공격 속도 누적의 피해 기여도를 조정했다. 공격력 200% 부분과 3초 기절, 궁극기 비용 300은 그대로 유지한다.
- 레벨 1의 공격력 124와 기본 공격 속도 122에서 복합 원피해가 431이고, 기본 공격 실제 적중마다 공격 속도 +3에 의해 다음 궁극기 원피해가 4.5씩 증가하는지 고정했다. 방어 적용 뒤 정수 반올림되는 실제 피해는 원피해와 분리해 검증한다.

## v0.27.9 — 2026-08-28

- 플레이어에게 노출되는 스피나 관찰 기록을 유일한 연구원인 주인공의 1인칭 기록으로 고쳤다. 별도의 `연구원 한 명`이 있는 듯한 표현을 없애면서 수중경비대 근무, 정확한 순찰·수문 점검, 숨겨 둔 어항 취미, 주인공의 조용한 청소와 다음 날 책상에 놓인 조개는 유지했다.
- 스피나 기록에 제3의 연구원이 다시 등장하지 않고 주인공의 직접 관찰과 책상 위 답례가 유지되는지 정적 데이터 테스트로 고정했다.

## v0.27.8 — 2026-08-28

- 렉시아 전용 관찰 질문에 체격 경쟁, 강자 도전, 작은 렐릭을 대하는 예절 상황을 추가했다. 선택별 직접 답변과 눈에 보이는 습성을 분리해, 예의를 갖춘 경쟁심과 약자를 위압하지 않는 태도가 연구 기록에 남는다.
- 관찰 질문 순환 테스트를 렐릭 ID 기준으로 일반화하고 렉시아 전용 질문의 등장·격리·고유 저장을 검증한다. 완료 뒤 연구 일지의 인터뷰 기록 글자도 공용 본문 크기로 키워 모바일 가독성을 높였다.

- 기록 보상 길을 **얇은 실선**으로 다시 그렸다. 굵은 막대 대신 한 줄짜리 선이 위로 뻗고, 지나온 구간은 **흰 선분** 한 겹으로 덧그어 표시한다. 마디는 선 위에 찍히는 작은 마름모(지나온 마디만 속이 참)이고 가지도 같은 굵기의 실선이다. 현재 누적은 흰 선이 끝나는 자리에 붙는 짧은 칩이 말한다.
- 순위 화면의 보스 전신을 더 크게(1000 → 1150) 세우고 더 위로 올려(발끝 1300 → 1400) 얼굴과 상반신이 판 위에서 넉넉히 보이게 했다.
- 편성 카드의 선택 표시가 **머리 픽셀만** 누른다. 칩 몸통은 도형으로 덮되 윗변 밖 머리는 원화를 검게 복제해 홈 마스크 안에서만 겹친다 — 홈을 도형으로 칠하던 직전 방식은 머리 옆의 투명한 빈자리까지 네모나게 어둡게 만들었다.
## v0.27.7 — 2026-08-28

- 스킬 팝업의 요약 줄에만 전달되던 문맥형 키워드 사전을 본문에도 연결했다. 이제 토리카 `다들 그만해!` 본문의 추가 피해 수치에도 밑줄과 입력 영역이 생기며, 누르면 방어력 15%에서 계산했다는 상세 팝업이 열린다.
- 요약과 본문이 같은 레이아웃 옵션 생성기를 사용하게 해, 이후 캐릭터의 동적 피해 수치가 어느 위치에 놓여도 링크 동작이 빠지지 않도록 회귀 테스트로 고정했다.

## v0.27.6 — 2026-08-28

- 토리카 폭주 설명에서 `방어력의 15% (19)`처럼 산식과 결과를 한꺼번에 보여 주던 문장을 **피해 수치 19**만 보이도록 정리했다. 수치를 누르면 현재 방어력에서 15%를 받은 값이라는 근거가 추가 팝업에 열린다.
- 일반 공격과 궁극기도 같은 문맥형 피해 수치 정의를 사용한다. 렉시아를 비롯해 렐릭 정의가 늘어나도 공격력·주문력·방어력과 배율을 공용 양식으로 설명하며, 캐릭터별 팝업을 복제하지 않는다.
- 플레이어에게 보이는 야성 설명과 렉시아 기록에서 `피버`를 실제 버프 명칭인 **폭주**로 통일했다.

## v0.27.5 — 2026-08-28

- 기록 보상을 게이지 세 줄에서 **위로 뻗는 길 한 줄**로 바꿨다. 마디마다 임계값이 서고 보상은 우 → 좌를 번갈아 가지처럼 내민다. 지나온 길만 강조색으로 차고 현재 누적은 그 끝에 표식으로 붙는다. 길이 창보다 길면 판을 키우지 않고 그 안에서 드래그·휠로 흐른다. 자리 계산은 순수 모듈 `expeditionRewardTrack.ts`가 맡고 Vitest가 마디 간격·좌우 번갈이·채움 보간을 고정한다.
- 원정 편성 화면의 "빠른 원정 확인 중…", "기준 점수를 먼저 기록하세요", "빠른 원정을 불러오지 못했습니다" 같은 상태 문구를 없앴다. 서버가 슬롯을 열어 두었을 때만 버튼이 생기고, 그 버튼이 보상과 남은 횟수를 직접 말한다. 지급 결과처럼 행동을 바꾸는 짧은 알림만 편성 안내 줄에 남는다.
- 규칙(`CLAUDE.md`)에도 조회 중·준비 안 됨·실패 상태를 화면에 쓰지 않고 그 자리를 비운다는 조항을 더했다.

## v0.27.4 — 2026-08-28

- 원정 기록 화면의 순위판을 **불투명하게**(0.98) 바꾸고 위로 200px 더 길게 늘렸다(680 → 880). 반투명한 판 위에서는 보스의 옷자락과 순위 글자가 섞여 읽혔다.
- 보스 전신도 한 뼘 키우고(900 → 1000) 그만큼 위로 올려 판이 길어져도 얼굴과 상반신이 그대로 보인다.
- 판 안의 기둥과 목록 자리는 이제 판 윗변에서 재므로, 판 높이를 바꾸면 함께 따라온다. 순위 줄도 여섯에서 여덟까지 선다.

## v0.27.3 — 2026-08-28

**원정의 첫 화면을 주간 기록으로 바꾸고 편성을 그 다음 단계로 내렸다.**

- 원정에 들어가면 편성판이 아니라 **기록 화면**이 먼저 뜬다. 위에는 20층 보스 폰투스가 서고 순위판이 그 허리 아래를 덮으며 내려온다. 왼쪽 기둥은 내 최고 순위·최고 점수·누적 점수와 그 아래 **기록 보상** 입구이고, 오른쪽은 주간 순위 목록이다.
- 하단 **출격** 버튼이 편성 단계를 연다(`scene.restart({ stage })`). 편성에서 뒤로가기는 로비가 아니라 한 단계 앞인 기록 화면으로 돌아간다. 준비 화면의 옛 "주간 기록" 버튼은 첫 화면과 중복이라 없앴다.
- 누적 보상은 순위와 분리해 **기록 보상 팝업**(`ExpeditionRewardPopup`)이 맡는다. 단계 수만큼만 판을 열고 바깥을 눌러 닫는다. 단계 한 줄을 그리는 규칙은 결과 화면의 기록판과 `expeditionRewardStage.ts` 하나를 공유한다.
- 진행 중 런이 있으면 지금처럼 곧바로 지도로 들어간다. 기록 화면은 새 원정을 시작할 때만 거친다.
- E2E: 저장 검증이 실제로 저장되지 않는 파생값(`expedition.active`)을 보고 있어 늘 통과할 수 없었다. 진행 중 런(`expedition.run.relics`)을 보도록 고쳤다.

## v0.26.13 — 2026-08-28

- **궁극기 충전의 시계 방향 가림막이 다시 보인다.** 카드를 통째로 `BitmapMask`로 쓰면 카드 안의 기하 마스크(원화·배경)와 겹쳐 가림막이 통째로 사라졌다. 카드 몸통(윗변이 닫힌 칩) 기하 마스크(`PortraitCard.createBodyMask`)로 바꿔 같은 결과를 안전하게 얻는다.
- **궁극기 연출이 전투 배속을 20%만 받는다**(`ultimatePresentationSpeed`, 3배속에서 1.4배). 컷인 기본 길이도 읽을 수 있게 늘렸다(진입 190·퇴장 140ms, 머무름 380~440ms). 결정타 동작도 고정 2배속 대신 같은 몫을 쓴다.
- 전투 조작 칩을 전장 위쪽에서 **우하단 프로필 줄 바로 위**로 옮기고, 궁극기 **연출 on/off 칩**을 자동 궁극기 위에 새로 세웠다. 연출 칩은 환경설정의 `ultimateCutIn`과 같은 값을 켜고 끈다.
- 카드 선택 반투명 표시가 칩 몸통만 덮어 빠져나온 머리만 밝게 남던 것을 고쳤다. 머리 홈까지 같은 실루엣으로 덮는다(원정 준비 화면의 선택 표시).
- 출격 선택판 개편으로 어긋났던 E2E 진입 좌표(스토리·원정·전투 조작 칩)를 새 자리로 맞췄다.
## v0.27.2 — 2026-08-28

- 궁극기 결정타의 공격·사망·종료 사건은 원래 순서대로 즉시 재생하되, 종료 사건 뒤에는 공격 모션과 확대 복귀를 기다리지 않아 결과 UI와 다음 장면 진행이 바로 이어진다.
- 사망 SD의 760ms 비행은 결과 정산과 분리된 배경 시각 효과로 유지하고, 궁극기 취소는 현재 시전자의 확대·공격만 정리해 사망 연출이나 정산을 중복 실행하지 않는다.

## v0.27.1 — 2026-08-28

- 궁극기 컷인·확대·공격 Puppet·복귀를 하나의 연출 시간축으로 묶었다. 1배속도 빠르게 시작하되 2·3배속은 관절 보간이 순간이동처럼 보이지 않는 상한을 공유한다.
- 공격 전 확대와 Puppet 공격을 동시에 시작하고 복귀는 더 짧게 만들어, 확대를 별도 턴처럼 기다리거나 공격 완료가 늦어지지 않게 했다.
- 스킵은 컷인과 확대만 생략하고 코어의 발사와 사건 소비는 기존 단일 경계를 그대로 거친다. 연출 배율은 코어 시간에 재적용하지 않는다.

## v0.27.0 — 2026-08-28

- 궁극기 연출을 전투 중 즉시 건너뛰는 세 번째 HUD 칩을 추가했다. 배속·자동 궁극기와 같은 홀로그램 테마와 저장 경계를 사용하며, 스킵해도 코어가 만든 공격·피해·사망·종료 사건은 빠짐없이 재생한다.
- 저장 모델의 `presentation.ultimateCutIn`을 제거하고 전투 조작인 `game.skipUltimatePresentation`으로 옮겼다. 과거 저장에서 컷인을 꺼 둔 경우 새 스킵 값을 켜며, 새 필드가 이미 있으면 그 값을 우선하는 마이그레이션으로 하위 호환한다.

## v0.26.12 — 2026-08-28

- 출격 칸의 **검은 테두리 잔상**을 없앴다. 같은 도형을 조금씩 줄여 여러 번 두르는 옛 비네트(`drawInnerVignette`)는 가로로 긴 판에서 좌우가 세로보다 훨씬 많이 줄어 검은 줄이 여러 겹 어긋나 남는다. 네 변에서 안쪽으로 빠지는 그라데이션 네 장(`drawFrameVignette`)으로 바꿔 어느 비율에서도 고르게 어두워지고 선이 남지 않는다. 원화와 같은 칩 마스크를 나눠 쓴다.
- 케이크 대작전과 현상수배는 맞닿는 대신 16px 여백으로 갈랐다. 마주 보는 변을 곧게 두는 규칙은 그대로라 여전히 한 판을 나눈 것처럼 읽힌다.
- 스토리 칸의 SD를 10% 줄였다(`sdScale`). 개체마다 원화 크기가 달라 혼자 커 보이는 SD만 이 배율로 맞춘다.

## v0.26.11 — 2026-08-28

- 출격 칸의 SD가 캐릭터 카드와 같은 규칙으로 선다. 몸은 칸 안에 갇히고 머리만 윗변 밖으로 빠져나오며(`sdMask`), 발끝을 아래 변 밖에 두어 **상반신 위주**로 보인다. 크기도 1.5배 이상(칸 높이의 1.68배)으로 키웠다.
- SD마다 **복제 그림자**를 한 겹 깔았다. 칸 가운데를 향해 밀고 본체와 같은 동작을 함께 재생한다 — 한쪽만 움직이면 그림자가 딴 자세로 남는다.
- 칸 사이를 76px로 띄우고 전체를 위로 올려 판 상단의 빈 공백을 줄였다.
- 제목을 25% 키우고(46 → 58, 일일 던전 30 → 38), 부제목은 잘린 오른쪽 아래 모서리만큼 안쪽으로 밀어 빗변 밖으로 삐져나오지 않게 했다.
- 두 일일 던전의 SD는 뺐다. 칸이 좁아 SD가 이름 위로 올라와 "케이크 대작전"이 가려졌다 — 지정된 스토리(적1)와 원정(폰투스)만 세운다.
- 머리 띠가 윗변 아래로 깎인 모서리 깊이만큼 더 내려간다. 그러지 않으면 `/`로 잘린 모서리가 머리 옆을 대각선으로 베어 낸다.
- 케이크 대작전과 현상수배는 한 판을 둘로 나눈 것처럼 맞닿는다. 마주 보는 변은 깎지 않고 곧게 두고(`split`), 그 사이만 곧은 선 하나(`|`)가 끊는다.

## v0.26.10 — 2026-08-28

- 출격 선택판을 870×1250 → 980×1240으로 넓히고 다섯 콘텐츠 칸도 함께 키웠다(스토리 800×220, 일일 던전 390×200, 레이드 800×200, 원정 800×230).
- 칸의 글자를 이름 46 · 상태 22로 키우고 **진한 그림자**를 깔아 원화 위에서도 먼저 읽히게 했다. 자리는 SD 반대쪽 아래 구석이다 — 스토리·던전·레이드는 우하단, 원정만 좌하단이다.
- 칸마다 SD가 버튼 윗변 밖으로 빠져나와 선다(카드 그리드의 머리와 같은 결). 스토리는 적1, 케이크 대작전은 적2, 현상수배는 적3, 원정은 오른쪽에 폰투스다. 준비 중인 레이드에는 세우지 않는다. 자리 계산은 `sortieEntrySdSpot` 하나가 맡는다.
- 세워 둔 SD가 2.6초마다 한 기씩만 드문드문 움직인다. 다섯이 동시에 뛰면 무엇을 고르는 화면인지 흐려진다.
- Puppet은 컨테이너 변환을 물려받지 않으므로 SD는 원점에 선 전용 레이어에 화면 좌표로 세우고, 판이 닫힐 때 타이머·Puppet·돌아가기를 함께 거둔다.
- 출격 판의 돌아가기를 판 안쪽 우하단에서 **다른 팝업과 같은 화면 우하단 슬롯**(`BACK_SLOT`)으로 옮겼다.

## v0.26.9 — 2026-08-28

**대부분의 카드 그리드에서 맨 위 머리가 잘리던 원인 세 가지를 모두 고쳤다.** 카드가 머리 모양이 아니라 사각형 구멍에 맞춰 잘리고 있었다.

- 칩 윗변의 머리 구멍이 칩 폭의 0.58뿐이라 뿔·모자·묶은 머리가 구멍의 좌우 변에 깎여 네모난 덩어리로 보였다. 폭을 0.8로 넓히고 높이도 카드 높이의 0.22(최대 54) → 0.26(최대 64)으로 키웠다.
- 잘라내기 시작점(`headroom`)이 0.04이라 원화의 정수리를 늘 몇 px씩 잘라 냈다. 카드에서는 머리 끝이 곧 목록의 윗선이라 0으로 두고 한 줄도 자르지 않는다.
- 전신 정보창의 세로 보정(`portraitOffsetY`, 루카 -34)을 카드에서도 그대로 올려 정수리가 구멍 위로 넘어가 잘렸다. 카드는 위로 올리는 보정을 받지 않는다.
- 직전 버전에서 넣었던 `headOverhang` 잘라내기 보정은 되돌렸다. 머리 관절 위 원화가 구멍보다 크다는 이유로 시작점을 내려, 오히려 머리를 절반 가까이 잘라 냈다.
- 마스크가 있는 목록(도감·발굴)은 첫 줄과 마스크 사이에 `PORTRAIT_GRID_MASK_GAP`(8px)을 둔다. 정확히 맞닿으면 가장자리 한 줄이 깎인다.

## v0.26.8 — 2026-08-28

- 치즈케이크 대작전·현상수배 원본 PNG(각 2MB대)를 `scripts/prepare_backgrounds.py`로 구워 WebP(130KB·217KB)만 남기고 원본을 지웠다. 스크립트가 배경(`background_00N`)뿐 아니라 콘텐츠 원화(`ContentN_00M`)도 함께 굽는다.
- 출격 선택판의 두 일일 던전이 각자의 전용 원화를 칩 실루엣에 클리핑해 세운다. 같은 그림을 나눠 쓰면 나란히 선 두 버튼이 한 콘텐츠의 두 갈래처럼 읽힌다.
- 카드 머리가 칩 구멍의 좌우 변까지 잘려 네모난 덩어리로 보이던 것을 고쳤다. 구멍을 칩 폭의 0.58 → 0.8로 넓히고, 구멍보다 큰 머리는 `headCardFrame`의 `headOverhang`이 잘라내기 시작점을 내려 구멍 안에 담는다. 머리 구멍 자체도 카드 높이의 0.22(최대 54) → 0.26(최대 64)으로 키워 뿔·모자가 들어간다. 그리드 안전 영역은 이 값에서 유도되므로 배치도 함께 따라간다.

## v0.26.7 — 2026-08-28

- 캐릭터 카드 그리드의 **머리 안전 영역**을 공용 순수 규칙으로 세웠다(`src/ui/portraitGrid.ts`). 머리는 칩 윗변 밖으로 빠져나오므로 첫 줄을 마스크나 판 윗변에 붙이면 정수리가 잘린다. 첫 줄 자리와 스크롤 길이는 이제 화면이 눈대중으로 정하지 않고 `portraitGridFirstRowY`·`portraitGridContentHeight`만 쓴다.
- 도감(렐릭 탭)의 첫 줄이 상단 마스크에 4px 물려 머리가 잘리던 것을 고쳤다. 카드 규격도 한 곳으로 모아 미보유 구역과 같은 값을 읽는다.
- 발굴 배치 그리드가 현황의 누적 액자 판과 **같은 윗변**에서 시작한다. 첫 줄은 그보다 머리 돌출만큼 내려 서므로, 눈에 보이는 그리드 윗선(머리 끝)이 액자 판 윗선과 맞아 두 화면이 한 자리를 공유한다.

## v0.26.6 — 2026-08-28

- 발굴 팝업의 `/ 발굴` 머리글이 배경 원화 뒤로 숨던 문제를 고쳤다. Phaser Container는 자식의 depth로 순서를 바꾸지 않고 넣은 순서대로만 그리므로, `build` 뒤에 원화를 얹는 판은 머리글을 다시 올려야 한다. `PopupLayer.raiseChrome`을 공용 경계로 두고 발굴이 본문·원화를 갈아 끼울 때마다 부른다.
- 임무 카드의 달성도 게이지가 흐리게 읽히던 것을 고쳤다. 레이어 순서 문제는 아니었고(카드 면을 먼저 넣어 게이지가 그 위에 있다) 옅은 홈과 채움만으로는 카드 면에서 떨어져 나오지 않는 문제였다. 연구도 게이지와 같이 빈 자리를 짙은 검정으로 누르고 최대치를 흰 선으로 둘렀다.
- 게이지 규칙(`CLAUDE.md`)도 함께 고쳤다. 읽어야 하는 진행도는 판때기 위에서도 검은 홈과 흰 외곽을 켜고, 설정 슬라이더처럼 조작 자체가 값을 말하는 줄만 켜지 않는다.

## v0.26.5 — 2026-08-28

- 발굴 배치가 새 화면을 열지 않는다. 슬롯을 누르면 팝업이 통째로 갈리는 대신 **아래 칸만** 현황 요약(누적 액자·광고·수확)에서 보유 렐릭 그리드로 바뀌고, 위 칸의 슬롯과 SD는 그대로 서 있다. 수확이 서 있던 자리를 그대로 **배치**가 물려받고 **취소**는 배치 중에만 그 왼쪽에 나타났다 사라진다.
- 한 칸을 채우면 선택이 저절로 다음 빈 칸으로 넘어간다(`nextExcavationSlot`). 세 번의 배치가 카드→칸→카드로 끊기지 않는다. 방금 비운 칸에서는 그대로 머문다.
- 슬롯 줄을 배경 원화의 발굴 벽면 가운데로 조금 내리고, SD가 서는 바닥도 슬롯 자리에서 유도해 둘이 따로 놀지 않게 했다.
- 발굴 SD가 땅으로 꺼져 보이던 모션을 통통 뛰는 도약으로 바꿨다. 원인은 두 가지였다 — Puppet(Mesh)의 원점은 이미지 한가운데인데 발끝 좌표(`groundY`)로 y를 움직여 제 키의 절반만큼 내려갔고, 같은 이징을 되감는 yoyo라 내려올 때도 감속했다. 이제 실제로 선 자리를 기준으로 포물선을 그린다.
- 편성이 바뀔 때 이전 SD를 치우지 않아 옛 자리에 SD가 남던 문제도 함께 고쳤다.

## v0.26.4 — 2026-08-28

- 원정 지도에서 연 적 정보창의 전신 원화가 스킬 아이콘·수치 판 앞으로 튀어나오던 문제를 고쳤다. 원화 깊이를 스테이지와 같은 1001로 낮춰 정보창의 판 아래에 서게 했다(3001은 정보창의 chrome·스킬 층보다 위였다).
- 증강 선택 팝업이 아군 얼굴을 다시 그리지 않는다. 지도 하단의 생존 HUD가 곧 대상 선택지이므로 판을 그 위로 올리고, 고르는 동안만 HUD를 암전 위로 띄워 입력을 연다. 같은 아군이 한 화면에 두 벌로 보이던 중복과, 두 곳에서 따로 늙던 카드 규격을 함께 없앴다.
- 증강 후보 세 장의 글자를 키우고 범위·효과를 두 줄로 갈라 각각 제 색과 본문 밝기로 읽히게 했다. 판도 조금 더 불투명하다.
- 협업 규칙: E2E는 기본으로 돌리지 않고 필요한 한 편만 지정해 돌리며, 전체 E2E는 사람에게 먼저 물어본 뒤에 시작한다는 규칙을 `CLAUDE.md`에 명시했다. 한 편이 1~2분이라 확인 없이 돌리면 다른 작업이 그만큼 멈춘다.
- 원정 지도의 생존 HUD를 인게임 전투와 같은 크기·가로 기준선으로 맞추고, 생존을 알리던 노란 발광을 뺐다 — 그 발광은 전투에서 "궁극기가 찼다"는 뜻이라 지도에서 같은 색을 쓰면 두 뜻이 섞인다. 배치표(`EXPEDITION_LAYOUT`)의 `relics` 구역과 증강 요약 칩 자리도 실제 bounds에 맞춰 옮기고, 쓰이지 않던 `sortie` 구역은 하단 행동선으로 합쳤다. 저장 마이그레이션은 필요 없다.

### v0.26.3 — 2026-08-28

**개발 빌드에서 실제 원정 보스 진입 흐름을 빠르게 검수한다.**

- 준비 화면에 개발 전용 20층 바로가기를 추가했다. 바로가기는 실제 지도 노드의 적 미리보기와
  출격 버튼을 거쳐 기존 보스 전투 DTO로만 진입한다.
- 매니저가 유효한 지도·3기 편성·19층 도달점·보스 멱등 ID를 한 저장으로 준비하며 production
  경계에서는 호출 자체를 거부한다. 정식 주간 점수 제출과 정산 규칙은 그대로 둔다.

## v0.26.2 — 2026-08-28

- 전투 프로필 생성이 `Cannot read properties of undefined (reading 'syncMask')`로 씬 진입을 통째로 막던 문제를 고쳤다. Phaser Container 생성자가 내부에서 `setPosition`을 부르는데, 그 시점에는 재정의한 `syncMask`가 참조하는 `card`가 아직 만들어지기 전이었다. 카드가 없으면 조용히 넘어가도록 해 생성자 안의 첫 호출을 무해하게 만든다.

## v0.26.1 — 2026-08-28

- 전투·원정 지도·20층 보스의 카드, 체력, 야성, 활성·사망 표현을 공용 전투 프로필 프리팹으로 통합해 화면마다 달랐던 글자 기준선과 게이지 간격을 하나로 고정했다.
- 원정 지도는 프리팹 내부 치수를 재정의하지 않고 전체 배율만 조절하며, 세 칸의 실제 bounds와 하단 출격 버튼 비겹침을 순수 배치 테스트로 보호한다. 저장 마이그레이션은 필요 없다.

## v0.26.0 — 2026-08-27

**원정 20층에 전용 보스 폰투스를 배치한다.**

- 리바이어던 멜빌레이 모티브의 폰투스 정적 정의와 네 스킬 표시 정보를 추가하고, 플레이어 획득 풀과는 분리했다.
- 전신·SD ZIP의 원본 크기와 알파 경계를 측정해 전용 레지스트리 및 중앙 프리로드에 연결했다.
- 20층 boss만 폰투스 단독 편성을 반환하며 다른 boss 층은 기존 3인 fallback을 유지한다. 저장 마이그레이션은 필요 없다.

## v0.25.0 — 2026-08-27

**원정 불사 보스를 단일 실시간 난전과 서버 재현 점수 흐름으로 통합.**

- 보스도 공용 `skirmish`의 이동·피격·사망·궁극기·배속 흐름을 사용하며, 옵션 상태가 생존 시간, 누적 실제 피해, 단계와 리미트를 함께 계산한다. 적 HP 소진은 승리가 아니고 아군 전멸만 종료다.
- 별도 보스 씬 진행기를 제거하고 공용 SD/HUD 위에 실시간 점수와 서버 최고 기록만 고정했다.
- 제출에는 피해량 대신 실제 전투에서 발생한 동작 시각·종류만 담아 서버가 편성과 성장 상태로 점수를 다시 계산하며, 요청 ID 멱등 처리를 유지한다. 저장 마이그레이션은 필요 없다.

## v0.24.5 — 2026-08-27

- 관찰 일지의 표본 설명·일기 내용·문답·선택 버튼 사이 여백을 넓히고 팝업과 원화 클립을 함께 늘려, 확장된 세로 흐름에서도 배경 모서리가 튀어나오지 않게 했다.
- 문답 버튼을 두껍게 만들고 답변을 가운데 정렬했으며 글자 크기와 강조 두께를 높여 선택지를 빠르게 비교할 수 있게 했다.

## v0.24.4 — 2026-08-27

- 토리카의 지각 붕괴 방어력 계수를 180%에서 300%로 상향하고 전투 회귀 기대값도 같은 단일 스킬 정의를 기준으로 갱신했다.
- 원정 지도 하단 편성을 인게임 프로필 카드와 체력·야성 게이지 문법으로 통일했다. 체력 문구는 성장 후 최대 체력과 저장된 잔여 비율을 결합한 실제 수치이며, 전투 전 야성은 0으로 표시한다.
- 노드 정보창을 스토리 적 편성 판처럼 간결하게 바꾸고 적 카드를 공용 적 상태창에 연결했다. 출격은 정보창에서 분리해 캐릭터 그리드 아래, 돌아가기와 같은 하단 행동선 중앙에 고정했다.

## v0.24.3 — 2026-08-27

- 토리카 폭주의 2초 기절을 제거하고 기절 저항과 별개인 0.1초 경직으로 교체했다. 3명을 맞혀 궁극기보다 효율이 높아지던 방어력 추가 피해는 60%에서 15%로 낮추고 공격 속도 20% 증가를 추가했다.
- 폭주 설명은 현재 방어력을 15% 피해로 환산한 값을 동적으로 보여 준다. 일반 공격·궁극기의 피해량 요약도 기본 능력치가 아니라 레벨·돌파·장착 룬이 반영된 최종 능력치로 계산하도록 수정했다.

## v0.24.2 — 2026-08-27

- 토리카의 폭주 기본 공격에 방어력 60% 물리 추가 피해를 적용하고, 주 대상과 주변 적 모두에게 2초 경직을 적용하도록 전투 규칙을 구체화했다. 방어력 계수는 방어형 성장을 보상하되 방어력 180% 계수인 궁극기보다 효율을 낮게 유지하려고 60%로 책정했다.
- 모든 폭주 설명을 특성의 구조화된 수치에서 동적으로 생성해, 밸런스 수치를 바꾸어도 도감 문구와 실제 전투가 함께 갱신되게 했다.

## v0.24.1 — 2026-08-27

- 관찰 질문을 공용 풀과 렐릭별 풀로 분리하고, 토리카의 식욕·몸무게·보호 행동을 설정에 맞춘 전용 질문과 반응으로 추가했다.
- 렐릭과 UTC 날짜를 함께 쓰는 단일 결정 함수로 화면 표시와 저장 검증을 통일했다. 기존 기록은 원문·답변 스냅샷과 공용 질문 ID를 그대로 유지하므로 저장 마이그레이션은 추가하지 않았다.

## v0.24.0 — 2026-08-27

- 토리카를 유치원생 또래의 트리케라톱스 해츨링으로 구체화하고, 기원 연대·복원 연차·성장 단계·키·몸무게와 성격 관찰 기록을 일지에 추가했다.
- 폭주를 다른 스킬과 같은 아이콘 중심 상세창으로 통일하고, 야성·경직처럼 별도 규칙이 있는 말은 눌러 설명을 열도록 했다.
- 토리카의 패시브·일반 공격·궁극기 명칭과 설명을 새 설정에 맞추고, 도감 기준 실제 피해량과 산출 능력치를 함께 표시하도록 피해 미리보기를 확장했다.

## v0.23.0 — 2026-08-26

- 원정 노드별 적 ID·레벨·속성 정의를 정보창과 실제 전투가 함께 읽도록 연결해 임시 스토리 편성 의존을 제거했다. 새 적 데이터는 중앙 편성표만 교체하면 두 화면에 동시에 반영된다.
- 원정 진입 일러스트의 마스크를 팝업 내부 월드 좌표에 동기화하고, 스토리 입구에는 Content1, 원정 편성에는 Content2 필드 원화를 적용했다.
- 증강 후보의 선택 연출을 단일 선택으로 정리하고 전체 증강도 하단 확정 전에는 저장되지 않도록 통일했다.
- 원정 승리·패배·포기·보스 완료의 서버 확정 전리품을 공용 보상 팝업으로 표시하며, 이전 원정 DTO가 스토리 전투에 재사용되지 않도록 전투 진입 입력을 초기화했다.

## v0.22.1 — 2026-08-26

- 임무 연구도 게이지의 중심/왼쪽 기준 혼용을 바로잡아 팝업 좌측 돌출을 막고, 기울어진 발굴 버튼의 알림점에 같은 좌표 변환을 적용했다.
- 원정 증강 후보·대상 안내·초상·확정 조작을 독립 세로 구역으로 재배치해 세 번째 후보와 대상 UI가 겹치지 않게 했다. 이후 UI 작업은 공용 프리팹과 다른 화면의 안전 영역을 함께 분석하고 실제 bounds와 모바일 캡처로 검수하도록 협업 규칙을 보강했다.

## v0.22.0 — 2026-08-26

- 20층 보스 노드를 일반 난전과 분리된 불사 보스 모드에 연결했다. 런의 현재 HP와 전체 증강을 시작 스냅샷에 적용하고, 단계별 광역 피해로 전멸할 때까지 클라이언트에는 행동만 기록한다.
- 서버가 런 편성·성장·증강·현재 HP로 행동열을 재현해 피해 점수를 확정한 뒤, 같은 점수를 노드에 반영하고 `completed` 정산까지 직렬화한다. 제출 ID와 정산 ID는 전투 진입 전에 저장해 중간 종료 재시도에도 중복 점수와 지급을 막았다.
- 결과 영수증에 이번 점수, 주간 최고 갱신, 누적 점수, 순위 변화와 실제 지급된 런 재화를 함께 표시한다. v23 저장은 보스 멱등 키를 빈 값으로 보충해 v24로 마이그레이션한다.

## v0.21.0 — 2026-08-26

- 원정 노드 완료를 서버 API 경계로 옮기고, 노드 종류·런 누적량·주입 난수로만 보상을 계산해 클라이언트 재화 조작과 재요청 중복 지급을 막았다.
- 전투 배율과 보석 보장 보물 표를 분리하고, HUD에 방금 증가분과 런 상한을 다른 색·문구로 표시해 경로 선택 결과를 즉시 읽게 했다.

## v0.20.2 — 2026-08-26

- 원정 증강 선택을 승리 보상에서 전투 진입 준비로 옮기고, 저장된 후보가 남아 있으면 지도 입력보다 먼저 복원해 후보 재추첨과 건너뛰기를 막았다.
- 증강 후보에 이름·등급·전체/개인 범위·효과 수치를 함께 표시하고, 개인 증강은 서버가 지정한 세 렐릭 중 PortraitCard 발광으로 대상을 고른 뒤 매니저에서 확정하도록 바꿨다. 무리 전투의 두 번째 선택은 같은 작업판으로 이어진다.
- 하단 축약 증강 행을 누르면 전체 확정 목록과 개인 대상을 펼쳐 볼 수 있는 상세 쪽지를 추가했다.

## v0.20.1 — 2026-08-26

- 원정 경로와 입력면을 같은 스크롤 지도 프리팹으로 분리하고, 큰 층 간격·드래그/휠 경계·현재 도달 층 자동 포커스를 순수 배치 계산과 함께 추가했다. 노드 상태는 테두리 대신 크기·발광·명도로 구분해 홀로그램 테마를 유지한다.

## v0.20.0 — 2026-08-25

- 원정 노드 보상을 런 임시 보상으로 보관하고, 정상 종료와 포기를 동일한 서버 원자 정산으로 지갑 상한까지 이전하도록 바꿨다. 런 ID와 정산 ID를 함께 저장해 재전송과 다른 ID를 이용한 중복 지급을 막는다.
- 기존 광고 검증 슬롯에 빠른 원정을 추가했다. 서버가 현재 또는 직전 주 최고 점수의 25%만 계산하며 기준 점수가 없거나 광고가 실패하면 지급하지 않고, UTC 일일 2회·주간 5회로 제한한다.
- 저장 식별자 도입 전 원정 런은 맵 시드 기반 런 ID와 빈 정산 ID로 마이그레이션한다.


### v0.25.0 — 2026-08-27

**원정 불사 보스를 단일 실시간 난전과 서버 재현 점수 흐름으로 통합.**

- 보스도 공용 `skirmish`의 이동·피격·사망·궁극기·배속 흐름을 사용하며, 옵션 상태가 생존 시간,
  누적 실제 피해, 단계와 리미트를 함께 계산한다. 적 HP 소진은 승리가 아니고 아군 전멸만 종료다.
- 별도 보스 씬 진행기를 제거하고 공용 SD/HUD 위에 실시간 점수와 서버 최고 기록만 고정했다.
- 제출에는 피해량 대신 실제 전투에서 발생한 동작 시각·종류만 담아 서버가 편성과 성장 상태로
  점수를 다시 계산하며, 요청 ID 멱등 처리를 유지한다. 저장 마이그레이션은 필요 없다.

### v0.19.0 — 2026-08-25

**불사 원정 보스의 검증 가능한 주간 피해 경쟁과 누적 보상을 추가한다.**

- 보스는 HP 승리 없이 받은 피해를 점수로 누적하고, 시간 단계별 광역 공격과 종말 단계로 반드시 아군 전멸 시 종료하도록 순수 규칙과 정적 밸런스를 분리했다.
- 클라이언트는 피해 숫자 대신 공격 동작열만 제출하며 API가 서버 편성 능력치로 재현해 전멸 순간 점수, 최고 기록, 주간 누적을 확정한다.
- 월요일 00:00 UTC 초기화, 동점 최초 달성 우선, 비정상 점수 거부 정책과 최고 점수·제출·누적 보상·리더보드 계약을 추가했다.
- 누적 보상은 단일 단계 표와 서버 수령 ID로 중복 지급을 막고, 가방 팝업에 붙어 있던 중복 돌아가기를 없애 로비 우하단 버튼 하나로 닫기 수명을 연결했다.

### v0.18.0 — 2026-08-25

**전투 종류별 원정 증강 제안과 재접속 고정 선택을 추가한다.**

- 증강의 ID·이름·등급·개인/전체 범위·효과 수치·중첩 상한을 정적 데이터로 모아 보상 규칙과 전투 효과가 같은 정의를 읽게 했다.
- 일반은 일반 풀 1회, 무리는 일반 풀 2회 연속, 정예는 고급 풀 1회로 제안하며 휴식·보물은 증강을 포기하는 경로 고점 손실을 그대로 유지한다.
- 제안 안의 중복, 적용 불가능한 개인 대상, 중첩 상한을 순수 규칙에서 제거하고 개인 효과는 생존/부활 가능 렐릭을 명시적으로 고르게 했다.
- 제안 seed·후보·선택 결과·개인 대상을 런 저장에 함께 보존해 재실행 재추첨을 막고, 원정 화면 하단에는 선택 결과를 비대칭 칩과 `+N` 축약으로 표시한다.

### v0.17.0 — 2026-08-25

**원정 전투의 가변 편성, 지속 체력과 범위형 증강 규칙을 추가한다.**

- 난전 생성기를 팀별 1~5기로 일반화하면서 기존 3대3 시작 좌표는 보존하고, 원정 저장 HP·생존 상태를 시작값으로 받아 종료 결과 DTO로 되돌리도록 했다.
- 일반 3기·강화된 대형 정예 1기·무리 5기의 정적 밸런스와 휴식 회복/조건부 부활 규칙을 데이터에 모았다.
- 전체/지정 렐릭 공격력 및 매 공격 출혈을 순수 증강 모델로 분리하고 기존 피해 공식과 단일 출혈 슬롯에 연결해 계산과 상태이상을 복제하지 않았다.
- 전멸 시 전투와 런을 종료하며 배치, 지속 HP, 전멸, 효과 범위, 출혈 중첩 및 휴식 부활 회귀 테스트를 보강했다.

### v0.16.0 — 2026-08-25

**저장 seed로 복원 가능한 20층 원정 갈림길 생성 규칙을 추가한다.**

- Phaser 비의존 원정 맵 생성기가 외부 RNG를 받아 1~19층의 여러 갈래와 모든 경로가 합류하는
  20층 단일 보스를 만들며, 각 노드는 고유 ID·층·열·선행·후행 ID를 함께 보존한다.
- 전투·휴식·보물 가중치, 연속 제한과 모든 경로의 최소 전투 수를 보장하는 필수 전투 층을
  `src/data/expedition.ts` 한 표로 모아 이후 밸런스 조정이 생성 규칙에 흩어지지 않게 했다.
- 도달 불가, 끊긴 경로, 역행·건너뛴 층과 연결 역참조를 찾는 검증 함수 및 고정 seed 회귀 테스트를
  추가해 앱 재실행 복원과 20층 보스 불변식을 보호한다.

### v0.15.0 — 2026-08-25

**출격 선택판과 주간 원정 준비·이어하기 저장 경계를 추가한다.**

- 로비의 기존 원정 버튼을 추후 PvP용 결투 입구로 바꾸고, 출격을 누르면 로비가 은은하게 남는
  선택판에서 스토리와 원정을 고르도록 분리했다.
- 신규 원정 화면에서 보유 렐릭 정확히 3기를 선택하며, 선택 검증과 진행 중 스냅샷 저장은
  `ExpeditionManager` 한 곳만 수행한다. 진행 중 원정은 새 편성으로 덮지 않고 이어하기로 연다.
- 저장 버전을 v22로 올렸다. v21 이하 저장은 원정 주차·횟수·최고 점수·진행을 빈 상태로 보충하고,
  첫 조회에서 현재 UTC 주차로 정규화한다.
- 로비 원정 항목에는 주간 플레이 횟수, 진행 여부, 최고 점수, 빠른 원정 가능 여부만 짧게 표시한다.

### v0.14.6 — 2026-08-25

**임무 진입점을 오른쪽 편의 레일에서 왼쪽 콘텐츠 가장자리로 분리한다.**

- 임무 버튼을 프로필·홍보와 하단 교류·발굴 사이의 왼쪽 가장자리로 옮겨 중앙 캐릭터 무대와
  오른쪽 무역·우편·친구·가방 레일의 역할을 분리했다.
- 임무 전용 각진 체크리스트 클립보드 글리프를 추가해 가방의 두루마리 표식과 구분했다.
- 임무·원정·출격·하단 내비게이션 입력 bounds와 최소 간격을 단위 테스트로 고정하고 E2E 클릭
  좌표를 새 진입점에 맞췄다.

### v0.14.5 — 2026-08-25

**임무 팝업의 탭·연구도·목록 영역을 충돌 없는 한 레이아웃으로 정리한다.**

- 흩어진 Y 좌표를 영역별 상수로 모으고 연구도 액자와 게이지를 탭 아래로 내려 일정한 안전 여백을
  확보했다. 첫 임무 카드도 연구도 라벨 아래에서 시작하도록 맞췄다.
- 어두운 팝업에서도 게이지 최대 범위가 읽히도록 기존 HoloBar의 얇은 흰 외곽선을 적용했다.
- 보상 액자 외곽선의 반지름까지 포함한 탭 충돌 단위 테스트와 일일·주간 탭별 E2E 스크린샷을 추가했다.

### v0.14.4 — 2026-08-25

**도감의 보유·미보유 카드 전체를 경계가 있는 관성 스크롤로 묶는다.**

- 카드·구분선·미보유 제목을 하나의 콘텐츠 컨테이너로 옮기고 제목/정렬 아래부터 BottomNav 위까지만
  보이는 고정 기하 마스크를 적용해 목록이 조작 영역을 침범하지 않게 했다.
- 포인터 드래그·휠·모바일 관성을 하나의 높이 기반 clamp 경로로 통합하고, 정렬/씬 종료 시 위치와
  입력 핸들러를 초기화한다. 부모 이동을 상속하지 않는 PortraitCard 내부 마스크도 계속 동기화한다.
- 상단에는 기존 홀로그램 유리 페이드만 추가하고 하단은 BottomNav 페이드를 그대로 사용해 중복 판을
  만들지 않았다. E2E는 상·하단 제한과 드래그/휠, 탭 입력 분리를 검증한다.

### v0.14.3 — 2026-08-25

**적 1~3번 정보창 일러스트를 중심 관절 기준으로 다시 정렬한다.**

- ZIP 안 WebP 헤더를 직접 확인해 1번은 2000×2828, 2번은 1086×1449, 3번은 2000×2833으로
  실제 픽셀 크기를 바로잡고 기존 알파 경계도 같은 좌표계로 환산했다.
- 잘못된 캔버스 크기 때문에 1번이 과도하게 확대되고 2번이 우측 하단으로 밀리던 원인을 제거했다.
  개체별 관절 배율이나 임의 축소값을 덧대지 않고 다른 캐릭터와 동일하게 `중심1`을 기준으로 둔다.

### v0.14.2 — 2026-08-25

**도감 즐겨찾기를 유대 순서로 모으고 카드에 전용 표식을 더한다.**

- 보유 즐겨찾기를 선택 정렬보다 앞에 합성하고, 여러 즐겨찾기는 유대 레벨·경험치 내림차순으로
  정렬하되 동률에는 현재 도감 기준을 유지하는 순수 비교 함수를 추가했다.
- 미보유 구역은 즐겨찾기 대상에서 제외해 기존 하단 구역과 선택 정렬을 보존했다.
- 희귀도 로마자 아래에 각진 그림자의 즐겨찾기 전용 별표를 추가하고, 정보창을 닫을 때 카드 표식과
  위치를 함께 다시 조립하도록 바꿨다.
- 즐겨찾기 우선·유대 순서·동률 fallback·해제 복귀를 단위 테스트로 고정했다.

### v0.14.1 — 2026-08-25

**핵심 콘텐츠에서 설정을 열고 원래 위치로 안전하게 돌아간다.**

- 고고학·렐릭·연구소·상점 TopBar에 설정 진입을 연결하고, 콜백 없는 설정 아이콘은 입력면을
  만들지 않도록 바꿨다.
- 설정 반환 씬을 허용 목록으로 검증하며 상점 반환 데이터는 등록된 섹션만 복사한다. 설정 탭을
  바꿔 씬이 재시작되어도 검증된 반환 경로를 유지한다.
- 네 핵심 콘텐츠의 설정 왕복과 상점 섹션 복원을 모바일 E2E로 고정했다.

### v0.14.0 — 2026-08-25

**알림 판정과 붉은 점 프리팹을 공용 계층으로 모은다.**

- 임무 보상·발굴 상한·친구 요청·신규 이벤트·우편 키와 순수 표시 조건을 추가했다.
- API 상태 합성과 변경 이벤트를 `NotificationManager` 한곳에 두어 씬의 중복 판정을 없앴다.
- 숫자 배지를 붉은 원·흰 외곽선·짙은 그림자의 공용 점으로 교체하고 씬 종료 가드를 뒀다.
- 받은 편지함 데이터가 아직 없는 항목은 임의로 켜지 않고 0을 반환하는 API 계약부터 마련했다.

### v0.13.0 — 2026-08-25

**룬·지갑·중첩 아이템을 한 가방에서 보는 인벤토리 시스템을 추가한다.**

- 정적 아이템 카탈로그와 판별 가능한 소비 효과를 도입하고, 룬 인스턴스·지갑·중첩 스택의
  저장 소유권은 분리한 채 표시 모델에서만 합성했다.
- 저장 버전을 v20으로 올렸다. v19 이하 저장은 기존 지갑과 룬을 그대로 보존하며 빈 중첩
  인벤토리를 보충하고, 신규 스택은 ID·양·최대 중첩을 검증한다.
- 소비품 사용은 FakeServer가 보유량·양의 정수 수량·스테미나 상한을 검증하고 효과 적용·차감·
  저장을 한 처리로 확정하도록 했다.
- 로비 오른쪽 레일에 가방을 추가하고 기존 홀로그램 팝업 위에 네 카테고리 탭과 실제 행 수 기반
  마스크 세로 스크롤 격자를 배치했다. 룬은 기존 정보 쪽지, 재화는 읽기 전용 상세, 소비품은
  확인과 서버 결과로 이어진다.

### v0.12.6 — 2026-08-25

**개발 메모처럼 읽히는 화면 문구를 걷어 내고 설정의 좁은 화면 배치를 보완한다.**

- 무역·상점 제목 아래의 기능 풀이 부제와 연구소의 재화 희소도·복원 방식 설명을 제거했다.
  픽업 대상, 비용, 확률처럼 플레이어의 선택에 필요한 정보는 그대로 유지한다.
- 기능명 반복, BM·운영 의도, 구현 상태 같은 개발 설명은 인게임이 아니라 주석과 기획 문서에만
  두도록 UI 문구 검수 원칙을 `CLAUDE.md`에 명시했다.
- 환경 설정의 마지막 탭 이름을 짧게 다듬어 텍스트 확대 시 탭끼리 겹치지 않게 하고, 지원 화면의
  계정 패널과 고객지원·데이터 패널 사이에 안전 여백을 보장했다.

### v0.12.5 — 2026-08-25

**획득 확인창과 발굴 현황을 한 양식으로 묶고, 보이지 않던 발굴 SD를 화면에 세운다.**

- 공용 보상창의 암전을 짙은 검정으로 올려 뒤 발굴 현황과 확실히 갈랐다. 얕은 암전은 두 화면이
  한 겹으로 읽혀 무엇을 확인하는 창인지 흐려졌다.
- 확인 안내를 팝업 안이 아니라 **화면 하단**의 반투명 굵은 글자로 빼고 바깥 누름으로도 닫히게
  했다. 닫을 수 있는 곳이 화면 전체라는 사실은 팝업 밖에서 말해야 읽힌다. 같은 이유로 오른쪽
  위 X를 없앴다(중복 조작).
- 액자 수량에서 `+`를 빼고 검은 테두리를 둘렀다. 증가량인 것은 제목이 이미 말하고, 테두리가
  없으면 수가 액자 선에 붙어 읽힌다.
- 발굴 현황의 재화 칸(`ExcavationCurrencyFrame`)을 보상창 액자와 같은 실루엣·외곽선·비네트로
  다시 만들고 누적량을 액자 오른쪽 아래에 같은 양식으로 얹었다. 시간당 생산량은 `1.2K/H`처럼
  시간 단위를 약자로만 적고, 시간당 1을 못 채우는 재화만 `/D`로 바꿔 0으로 죽지 않게 했다.
- **발굴 SD가 화면에 나오지 않던 버그를 고쳤다.** Puppet은 자기 화면 좌표로 직접 그리는 GPU
  개체라 컨테이너의 이동·배율을 물려받지 않는다. 팝업 본문 안에 넣으면 국소 좌표 그대로 화면
  바깥에 그려진다. SD만 원점에 선 전용 레이어(팝업 판 바로 위 depth)로 옮기고 좌표도 화면
  좌표로 넘긴다. 함께 들어가 있던 표시 크기·가시성 재검증은 원인이 아니었으므로 텍스처 확인만
  남겼다.
- 발굴 뒤로가기를 좌하단 예외에서 공용 우하단 자리(`BACK_SLOT`)로 되돌렸다. 화면을 벗어나는
  버튼의 자리는 화면마다 다르지 않아야 한다.
- 배경 원화를 까는 팝업이 제목을 덮던 문제를 `PopupLayer`에서 고쳤다 — 내용을 채운 뒤 머리글을
  한 번 더 맨 위로 올린다.

### v0.12.4 — 2026-08-25

**발굴 슬롯 입력과 SD 비동기 fallback을 모바일에서 안전하게 분리한다.**

- 세 슬롯에 카드·Puppet과 독립된 210×245 최상단 입력면을 두어 현황과 편집이 같은 누름·확대·
  드래그 취소 규칙을 사용하고, 발굴 닫기는 팝업 X 대신 화면 좌하단 뒤로가기 아이콘으로 옮겼다.
- SD는 새 render마다 독립 generation을 발급하고 실제 루트·텍스처·표시 크기를 검증한 뒤에만
  fallback 카드를 숨긴다. 실패·늦은 완료·닫힘 직후 완료는 카드를 유지하며 Puppet을 한 번만 폐기한다.
- 주입 가능한 로더 경계의 성공·실패·지연 수명 단위 테스트와, SD 완료 뒤 세 슬롯 선택 및
  1.15 텍스트 확대 모바일 안전 영역을 확인하는 E2E를 추가했다.

### v0.12.3 — 2026-08-25

**발굴 수확 보상을 모바일 한 줄 결과창으로 또렷하게 확인한다.**

- 보상 액자를 넓고 낮은 한 줄로 바꾸고 1~4개는 가운데 정렬, 그 이상은 가로 드래그하도록 해
  보상 수가 달라도 화면 안전 여백과 아이콘 크기를 함께 지킨다.
- 수량을 액자 오른쪽 아래 안으로 옮기고 비네트와 짧은 그림자를 더해 아이콘을 가리지 않으면서
  밝은 원화 위에서도 읽히게 했다.
- 공용 팝업에 제목 크기와 암전 강도를 추가하고 발굴 결과에는 큰 제목과 0.18의 얕은 암전을 써,
  뒤 현황의 맥락은 남기되 확인 중 입력은 차단한다.
- 모바일 E2E가 보상창 열림, 뒤 로비 입력 차단, 본문 확인 뒤 발굴 현황 복귀를 연달아 검증한다.

### v0.12.2 — 2026-08-25

**발굴 수확 확인창이 현황 연출을 가리던 병합 회귀를 바로잡는다.**

- 획득 재화는 작은 액자와 수량만 남기도록 확인창 높이와 여백을 줄였다. 발굴 현황창 위에
  둘째 암전을 깔지 않아 발굴장 배경과 확정 편성 SD가 수확 확인 중에도 그대로 보인다.
- 현황 UI 재배치에서 수확 버튼이 하단 중앙으로 옮겨졌지만 과거 좌표를 누르던 E2E를 실제 배치와
  맞춰, 병합 후 확인창 회귀 검증이 다른 영역을 누르지 않게 했다.

### v0.12.1 — 2026-08-25

**연구소 획득 연출과 배치형 자원 발굴의 이름을 분리한다.**

- 연구소 전용 상태 기계와 첫 단계를 `researchPresentation`·`research`로 바꾸고 연구소 균열음의
  의미 키도 같은 계열로 맞췄다. 저장 데이터와 서버 DTO에는 이 연출 이름이 없어 마이그레이션은
  필요하지 않으며, 직렬화 계약인 `idleExcavation`은 배치형 자원 발굴 이름으로 그대로 유지한다.
- 확률 안내의 과거 각성 문구를 개체별 렐릭 파편, 파편을 소비하는 한계 돌파, 별 V 이후 공용
  DNA 조각 전환이라는 현재 규칙으로 고쳤다.

### v0.12.0 — 2026-08-24

**로비를 유지하는 방치 발굴 현황 팝업을 연결한다.**

- `PopupLayer` 한 장에서 서버 정산 중·실패 재시도·누적 자원·시간당 생산·보관 한도 시간을
  보여 주며, 닫을 때 로비와 애착 렐릭을 다시 만들지 않는다.
- 배치 슬롯의 SD Puppet과 실제 누적 비율로 걷히는 자원 더미 마스크를 연결하고, 저사양 및
  모션 감소 설정에서는 SD를 정지시킨다.
- 좁은 모바일에서 팝업 안전 영역, 중복 열기 방지, 뒤 로비 입력 차단을 E2E로 고정했다.

### v0.11.0 — 2026-08-24

**버전 문서를 도입하고 로딩 화면에 버전을 표기.**

- 개발 로그를 `VERSION.md` 하나로 모았다. 그동안 "무엇을 했다"가 커밋 제목에만 남아 있어
  왜 그렇게 바꿨는지가 남지 않았다 — 규칙은 `CLAUDE.md`, 이력은 이 문서로 갈랐다.
- 타이틀(로딩) 화면 좌측 하단에 `v0.11.0`을 표기한다. 로딩 화면은 어느 빌드에서나 반드시
  지나는 화면이라, 스크린샷 한 장으로 어느 빌드인지 알 수 있다.
- 버전 문자열의 단일 출처는 `package.json`이다. 화면은 그 값을 읽기만 한다.
- `v1.0.0`을 **정식 런칭 번호로 예약**했다. 개발 중에는 저장 비호환 변경이라도 부 자리만
  올린다 — 그때마다 주 자리를 올리면 정작 런칭에서 쓸 번호가 남지 않는다.

### v0.10.0 — 2026-08-24

**전투 화면을 읽히게 다시 짠다 — 게이지 둘, 카드가 말하는 궁극기.**

- 전투 프로필 게이지를 체력·폭주 둘로 줄였다. 궁극기 충전은 바 대신 **카드 그림 자체**가
  12시부터 시계 방향으로 밝아지며 말한다. 바가 셋이면 어느 것이 급한지 읽히지 않았다.
  가림막은 도형이 아니라 BitmapMask다 — 도형으로 자르면 칩 밖으로 나온 머리까지 검게 찬다.
- 게이지와 수치를 스르륵 따라오게 했다(`stepMeters`). 깎이는 순간이 보이지 않으면 얼마나
  아팠는지 알 수 없고, 궁극기 연출이 끝나는 순간 값이 점프해 보였다.
- 머리 위 체력 바를 `UnitHealthBar` 한 곳으로 모았다. 밝은 배경 원화 위에서 사각형 두 장은
  묻혀서, 그림자·깎인 몸통·칸 구분선·왼쪽 끝 빗금 네 겹을 함께 쓴다.
- 체력은 능력치와 같은 연두, 야성은 붉은 계열로 묶고 게이지 옆 수치도 같은 색을 쓴다.
  어느 수가 무엇인지 색으로 먼저 읽히게 하려는 것이다.
- 게이지에 최대치 흰 테두리와 빗금 칸을 넣었다(`HoloBar`의 `outline`·`ticks`). 채움만으로는
  "어디까지가 이 게이지인가"가 배경 원화 위에서 보이지 않았다. 판때기 위 게이지는 켜지 않는다.
- 배경 원화 7·8·9를 WebP로 굽고 원본 PNG는 저장소에서 뺐다. 한 장이 10MB에 가까워 그대로
  두면 첫 로딩이 통째로 그만큼 늘어난다.
- 적 셋의 스킬 일러스트를 아군과 같은 파이프라인으로 구워 넣었다.

### v0.9.0 — 2026-08-23 ~ 2026-08-24

**등급·별·전투력 표기를 정리 — 별은 돌파, 등급은 바탕색.**

- 별 다섯 칸을 **로마자 한 글자**(`I`~`V`)로 바꿨다. 칸을 세어야 알 수 있었고, 카드마다
  다섯 개가 반짝여 얼굴보다 먼저 읽혔다. 희귀도는 대신 **카드 바탕색**이 맡는다.
- 별의 뜻을 성급에서 **한계 돌파 단계**로 바꾸고 중복 획득에 파편을 도입했다. 중복을
  "각성"과 "돌파" 두 축으로 나누면 무엇을 키우는 중인지 읽히지 않아 하나로 합쳤다.
  돌파 버튼은 레벨 칸이 아니라 별 옆에 세우고, 드는 파편을 버튼이 직접 말한다.
- 룬을 **자리별 조각**으로 갈랐다(`RuneInstance.part`). 아무 룬이나 아무 칸에 끼우면 세 칸이
  사실상 한 칸이라, 어느 자리를 파밍할지가 목표가 되지 않았다. 자리 불변식은 서버가 지킨다.
- 전투력을 별도 칸에서 빼내 **오각형 면 안에 눕혔다**. 전투력이 곧 그 면의 넓이이므로 판 위에
  숫자 칸을 따로 세울 이유가 없다. 축마다 실제 수치를 축 이름과 같은 색으로 쌓았다.
- 도감을 보유·미보유로 가르고 정렬을 버튼 하나(개체번호→희귀도→전투력)로 돌게 했다.
  기준마다 버튼을 세우면 지금 어느 기준인지 색으로 읽어야 한다.
- 세공은 고른 줄이 계속 고른 채로 남게 했다. 한 줄에 세 번씩 반복하는 일이라 누를 때마다
  다시 고르게 하면 그 손이 그대로 낭비다. 각인만 남으면 선택을 푼다 — 되돌릴 수 없는
  한 번의 선택이라 사람이 다시 고르게 한다.
- 칸 제목과 팝업 머리글을 `SectionTitle` 한 모양으로 모았다. 화면마다 제 나름의 제목을
  만들면 같은 위계가 어디서는 판, 어디서는 맨 글자가 된다.
- 재화 수를 세로로만 늘려 굵게 키우고 아이콘 쪽으로 붙였다. 가로까지 키우면 자릿수가
  늘 때 칸이 함께 넓어지고, 멀리 떼면 그림과 숫자가 두 정보로 읽힌다.

### v0.8.0 — 2026-08-23

**운영·플랫폼 경계를 코드 밖으로 밀어낸다.**

- 계정/인증과 원격 저장 메타데이터를 `AccountApi`가 소유하게 했다. 인증 토큰은 `Session`·
  `SaveData`·`localStorage`에 절대 넣지 않고, SDK가 없는 빌드는 성공을 흉내 내지 않고
  `unsupported`를 돌려준다. 로컬/서버 해시가 다르면 자동으로 덮지 않고 고르게 한다.
- 광고 보상을 서버 검증형으로 만들고 슬롯 구성 계약과 정책 문서를 함께 뒀다. 클라이언트가
  보상을 확정하면 어떤 검증도 의미가 없다.
- 연구 후원 패스(월정액) 권리 정의를 추가했다.
- 오디오 버스를 `AudioManager` 한 곳으로 모으고, 진동·공유 같은 플랫폼 기능은 어댑터 뒤로
  숨겼다. 씬이 플랫폼 API를 직접 부르면 웹·앱 빌드가 갈라진다.
- 설정 저장 모델(`GameSettings`)과 탭형 모바일 설정 화면을 추가했다.

### v0.7.0 — 2026-08-23

**스킬 일러스트 파이프라인 — 흰 실루엣 한 장에 속성·직군 색을 입힌다.**

- 스킬 아이콘을 흰 실루엣 WebP 한 장으로 굽고(`prepare_skill_icons.py`), 색은 파일에 굽지
  않고 `skillArtTint`가 속성·직군을 섞어 정한다. 색이 개체에서 나오기 때문이다.
- 그림 한 장을 담는 칸에만 **액자 예외**를 뒀다(불투명 채움 + 사방 외곽선 + 안쪽 비네트).
  배경 원화 위에서 어디까지가 그림인지 또렷해야 한다. 판때기에는 쓰지 않는다.
- 아트가 없는 개체는 공용 효과 아이콘으로 되돌아가되 색판은 그대로 깔아, 아군 창과 같은
  체계로 남게 했다. 적도 같은 파이프라인을 쓴다.
- 로비 왼쪽에 발굴 입구를 세우고, 고고학(장기 탐사)과 발굴이 서로 다른 콘텐츠임을 문구에서
  갈라 놓았다.

### v0.6.0 — 2026-08-23

**전투 연출과 적 표시를 정리.**

- 궁극기 컷인을 직렬화하고 방향·배율·머무는 시간·흔들림을 `ultimatePresentations.ts` 한 표로
  뺐다. 전투 중 여러 번 반복되는 연출이라 길이가 곧 기다림이다 — 포효를 따로 기다리지 않는다.
- 궁극기 트윈이 중간에 끊겨도 입력 잠금이 풀리게 했다. 씬 전환과 겹치면 전투가 굳었다.
- **적도 아군과 같은 정보창을 쓴다.** 전용 화면을 따로 세우면 정보창이 좋아질 때 그 화면만
  옛 모습으로 남는다. 적은 문맥으로 현재 전투 줄(체력·궁극·야성·상태이상)만 덧붙인다.
- 파티 미리보기의 상성을 힌트 문구 대신 방향 화살표로 바꿨다.
- 난전에서 적이 확실히 눌리도록 히트영역을 고쳤다.
- 개체 2·3·4와 트리케라톱스 스킬셋 데이터를 채웠다.

### v0.5.0 — 2026-08-23

**룬(하트 젬) 시스템 — 순수 규칙 + 원자적 서버 경계.**

- 룬 도메인 타입과 강화 규칙을 Phaser 없는 순수 모듈로 만들고 난수를 인자로 주입했다.
- 룬 인벤토리 DTO와 원자적 API를 추가하고, 보유 목록을 ID 나열에서 **인스턴스**로 옮겼다.
  같은 등급이라도 옵션이 다른 물건이라 ID만으로는 표현되지 않는다.
- 룬 파생 능력치를 전투 계산에 일관되게 반영하고, 세공 팝업을 공용 프리팹 하나로 모았다.
- 세공 화면을 가운데로 옮겨 주·보조 옵션을 갈라 놓고, 가방을 목록에서 격자로 바꿨다.
- 확률을 막대 두 개가 아니라 한 줄을 성공·실패가 나눠 갖게 하고 결과를 별로 박았다.
- 룬을 누르면 곧바로 세공이 열리지 않고 간소한 쪽지가 먼저 열리게 했다. 골드를 쓰고
  되돌릴 수 없는 조작이라 손이 한 번 더 들어가야 한다.

### v0.4.0 — 2026-08-22

**야성을 벌에서 상으로 뒤집고, 전투 코어를 하나로 남긴다.**

- 야성을 억제 대상에서 **피버 보상**으로 바꿨다. 최대치에 닿으면 폭주가 시작되고 게이지는
  스스로 줄어든다 — 별도 진압 조작은 없고, 피버 중에도 궁극기를 쓰고 적만 공격한다.
- 개체별 발현(`ferocityTrait`)을 난전에 적용하되 공식은 공용 야성 규칙 하나로 뒀다.
- 레거시 턴제 전투 코어를 제거했다. 실시간 난전(`skirmish.ts`)과 둘이 남아 있으면 어느 쪽이
  진짜 규칙인지 알 수 없다.
- 경제 재화를 잡초에서 **치즈케이크**로 개명하고 상단 재화 줄에 렐릭 문맥을 연결했다.
- 발굴을 연구소에서 분리하면서 저장 불변식을 함께 지켰다.
- 루카(랩터)와 적 1~3의 전신·SD 에셋을 붙이고, 룬 아이콘을 하트 원화 세 조각으로 갈라
  등급색 넷을 정했다.
- 파티 진입 가드와 모바일 탭 오작동을 고쳤다.

### v0.3.0 — 2026-08-22

**서버 경계를 넓힌다 — 가챠·상점·경제·이벤트.**

- 가챠의 천장과 픽업 보장을 갈라 놓았다. 한 카운터가 둘을 겸하면 어느 쪽이 찼는지 말할 수 없다.
- 공용 상품 카탈로그 기반 상점, 화석 경제와 DNA 교환, 서버 시각 기준 이벤트 정의,
  일일 관찰 인터뷰, 일일·주간 미션 진행도를 각각 정적 데이터 + API 경계로 추가했다.
- 서버 권위 비동기 아레나를 설계 문서로 먼저 고정하고(`docs/asynchronous-arena.md`),
  로비에서 친구 흐름을 연결했다.
- 프로젝트 가이드 문서를 현재 플레이에 맞춰 정렬했다.

### v0.2.0 — 2026-08-22

**아이콘·재화 표기를 굽기 파이프라인으로 통일.**

- 속성·직군·재화 아이콘을 `prepare_icons.py`가 굽는 WebP로 옮기고 원본은 저장소에서 뺐다.
  속성·직군은 단색 실루엣이라 **색도 그 스크립트 표에서 정한다** — 화면이 tint로 다시
  칠하지 않는다.
- 상단 재화를 보석·골드·스테미나로 바꾸고 모집 화면에는 다이아·화석·호박석을 세웠다.
  화면마다 다른 조합을 `TopBar`의 표 하나에 뒀다.
- 재화 칸만 기울이지 않고 둥근 면으로 바꿨다. 여러 칸이 나란히 설 때 칸마다 기울면
  숫자와 아이콘까지 비뚤어 보여 줄이 어수선해진다.
- 화석 색은 한 번 채도를 올렸다가 **되돌렸다** — 호박석과 구분은 됐지만 돌빛이라는 인상이
  사라져, 채도를 덜어 회색 쪽으로 맞추는 편이 재료로 읽혔다.
- 카드 위 속성·직군이 원화에 묻히지 않게 그늘을 넓혔다.

### v0.1.0 — 2026-08-22

**기록 시작 — 정보창과 성장 흐름의 기본형.**

- 정보창에 감상 모드와 좌우 넘김을 넣고, 상세를 여는 돋보기를 판 오른쪽 끝 안쪽에 작고
  흐리게 붙였다. 화면에 늘 떠 있어야 하는 것은 균형이고 숫자는 물어볼 때다.
- 돌파가 레벨 상한을 열도록 하고, 성장을 레벨업 버튼이 아니라 **급여**(치즈케이크)로 짰다.
- 일일·주간 미션 진행도를 추가했다.
- 이 항목 이전의 프로토타입 단계(씬 골격·저장 모델·홀로그램 UI 토큰 도입)는 별도 이력을
  남기지 않았다.
