/**
 * 편성 칸 밑판의 공용 생김새 값.
 *
 * Phaser 없는 순수 모듈에 두는 이유는 회귀 테스트가 그리기 구현이 아니라 이 값을 읽어야 하기
 * 때문이다 — 그리는 것은 `formationSlotChrome.ts`의 `addFormationSlotPlate` 한 곳이다.
 *
 * 네 화면(스토리·원정·발굴·교류 파견)이 같은 세 자리를 고르므로 칸도 같은 판이어야 한다.
 * 기준은 이미 잘 읽히던 **발굴의 칸**이다 — 반투명 유리면 한 장을 늘 깔고, 누군가 서 있으면
 * 발밑에 얇은 투영 그림자만 두며, 비었으면 자리 번호가 그 자리를 지킨다.
 *
 * 판을 **서 있을 때만 감추지 않는다**. 원정이 그랬는데, 세 자리 중 둘만 차면 남은 한 칸만
 * 판이 보여 빈 자리가 "못 쓰는 자리"처럼 읽혔다. 세 칸이 늘 같은 판 위에 서야 무엇을 더
 * 고를 수 있는지가 한눈에 보인다.
 */
export const FORMATION_SLOT_PLATE = {
  /** 판 윗변 강조선의 진하기. */
  edgeAlpha: 0.55,
  /** 발밑 투영 그림자 — 칸 폭 대비 너비, 높이, 진하기. */
  groundWidthRatio: 0.82,
  groundHeight: 25,
  groundAlpha: 0.16,
  /** 빈 칸에 서는 자리 번호의 크기. */
  emptyFontSize: 22,
} as const;

/**
 * 전투 편성 칸의 **추천 직군**. 빈 칸 가운데에 「추천 직군」 한 줄과 직군 아이콘 둘이 은은하게 선다.
 *
 * **빈 칸에만 선다.** 누군가 서면 그 자리가 무엇을 원했는지는 더 이상 고를 거리가 아니고, 그때부터는
 * 서 있는 개체의 속성·직군 뱃지가 같은 자리를 말한다 — 둘이 함께 서면 칸 위가 표식으로 붐빈다.
 * 판 윗변에 걸친 이름표로 늘 세웠을 때는 SD 머리와 겹쳐 앞 층으로 올려야 했고, 이름까지 적어
 * 칸마다 한 줄이 더 붙었다.
 *
 * 전투 편성(공용 편성 화면·원정)에만 넘긴다 — 발굴·교류 파견은 직군이 자리를 정하지 않는다.
 * **은은하게** 둔다(`alpha`) — 빈 칸의 주인공은 「여기에 세우라」는 판 자체이고, 추천은 거드는 말이다.
 */
export const FORMATION_ROLE_HINT = {
  /** 글줄 크기와 아이콘 한 변. */
  fontSize: 20,
  iconSize: 40,
  /** 아이콘끼리의 틈, 글줄과 아이콘 줄 사이. */
  gap: 10,
  lineGap: 12,
  /** 전체 진하기. */
  alpha: 0.62,
} as const;
