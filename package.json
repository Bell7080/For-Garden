{
  "name": "eternal-city",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "이터널 시티 — RELIC 관리 프로젝트. 세로형 모바일 수집형 RPG.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "phaser": "^3.90.0",
    "puppetforge": "github:bell7080/webgle"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.3",
    "vitest": "^3.2.4"
  }
}
