# Claude Code 협업 안내

이 문서는 Codex와 Claude Code가 같은 구조와 의존 방향을 유지하기 위한 저장소 진입점이다.

## 필수 구조 규칙

- `src/core`: Phaser를 import하지 않는 순수 게임 규칙만 둔다. 실시간 전투의 단일 소유자는
  `skirmish.ts`이며, 좌표와 시간까지 전부 계산하고 씬은 그 결과를 그리기만 한다. 피해·야성·
  궁극기 같은 작은 공용 공식은 순수 모듈로만 분리하고 별도 전투 진행기를 만들지 않는다.
  난수는 인자로 주입한다.
- `src/api`: 씬이 의존하는 서버 인터페이스와 임시 FakeServer 구현을 둔다. 재화 차감과 가챠
  난수 결과는 씬에서 계산하지 말고 반드시 이 경계를 통과시킨다.
  계정/인증과 원격 저장 메타데이터는 `AccountApi`가 소유한다. **보안상 인증 토큰은 절대로
  `Session`·`SaveData`·`localStorage`에 넣지 않고** 플랫폼 보안 저장소 또는 HttpOnly 서버
  세션에만 둔다. SDK가 없는 빌드는 성공을 흉내 내지 않고 `unsupported`를 반환한다.
- `src/data`: 캐릭터, 배너, 스테이지처럼 운영 중 늘어나는 정적 정의를 둔다.
  대사 원문과 분기는 `src/data/dialogues`에 `DialogueStory` 정적 데이터로 두고 씬에 하드코딩하지
  않는다. 노드는 ID·화자·본문·스탠딩·표정/모션·다음 ID·선택지를 표현한다.
- `src/managers`: 여러 씬이 공유하는 수집 상태와 재사용 UI의 공개 진입점을 둔다.
  `StoryManager`는 대사 선택 효과와 완료 저장의 유일한 경계다. 선택 효과를 콜백으로 받지 않고
  `bondXp` 같은 판별 가능한 데이터 명령으로 제한한다. 그래야 정적 콘텐츠가 임의 코드를 실행하지
  못하고, 대상·범위·재관람 중복 보상을 저장 직전에 일관되게 검증할 수 있다.
- `src/state`: 직렬화 가능한 진행 데이터 모양과 현재 메모리 세션을 둔다. 씬에서 직접 변경하지
  말고 해당 manager를 통해 변경한다.
  런타임 `Session`의 `Set`은 `SaveManager`만 JSON 배열로 바꾼다. 상태 변경은 manager/API
  경계에서 검증을 모두 마친 뒤 `saveManager.save`를 호출하며, 씬은 `localStorage`를 직접
  참조하지 않는다. 부트만 저장 로드·마이그레이션·복구를 조율하고 저장 버전은 로컬 모델의
  책임이므로 서버 `PlayerStateDto`에 섞지 않는다.
- `src/ui`: Phaser 프리팹의 실제 구현과 테마 토큰을 둔다. 화면을 벗어나는 뒤로가기는
  `IconButton`의 `addBackButton`만 쓰고, 자리(우하단)와 생김새를 씬마다 다시 정하지 않는다.
  글꼴은 `src/ui/fonts.ts`가 유일한 소유자다 — 아래 "글꼴 규칙"을 따른다.
- `src/scenes`: 화면 배치와 입력 연결만 담당한다. 게임 규칙과 수집 검증을 복제하지 않는다.
  계정 전환 성공 뒤에는 `BootScene`으로 돌아가 저장 검증·마이그레이션을 다시 통과하며 서버
  DTO를 `Session`에 직접 대입하지 않는다. 로컬/서버 해시가 다르면 `SaveConflictPopup`에서
  양쪽 레벨·재화·최근 플레이를 보여 주고 고르게 하며 자동 덮어쓰지 않는다.
  에셋 로딩은 씬마다 흩지 말고 `scenes/loadingSteps.ts`의 단계 목록에만 더한다 — 아래
  "로딩 화면" 참고.
- `src/puppets`: PuppetForge 로딩, 배치, 동작 재생을 전담한다.

## 캐릭터 기능을 추가할 때

> **용어 구분:** 애착(`Session.favorite`)은 로비에 표시할 대표 렐릭 선택이고, 유대
> (`RelicProgress.bond*`)는 획득·승리·일일 상호작용으로 성장하는 렐릭별 관계 수치다.
> 애착 변경은 유대 경험치나 레벨을 직접 변경하지 않는다.

1. 캐릭터 정의는 `src/data/relics.ts`에 추가한다.
2. 획득·보유·편성·애착 설정은 `RelicCollectionManager`를 사용한다.
3. 상세 정보는 `CharacterInfoManager`를 생성해 `showRelic`으로 연다.
4. 새 화면만의 상세 팝업을 복사해 만들지 말고 `src/ui/info.ts`의 공용 프리팹을 확장한다.
5. 캐릭터를 화면에 크게 세울 때는 이미지 상자가 아니라 `src/puppets/anchors.ts`가 찾아 주는
   `중심1`(코어)·`머리1` 관절을 기준으로 배치한다. 그리드·프로필의 얼굴 카드는
   `src/ui/PortraitCard.ts`를 쓰고 화면마다 잘라내기 값을 다시 계산하지 않는다.
6. 전투 수치 규칙은 `src/core`에서 구현하고 Vitest로 고정한다. 공속·이속처럼 난전 진행에
   영향을 주는 수치는 `src/core/skirmish.ts`의 상수와 함께 테스트로 묶는다.
7. 속성 상성과 직업의 기준은 `docs/combat-affinities.md`를 따른다. 직업은 데이터 태그일 뿐
   숨은 보정치를 만들지 않고, 속성 배율은 `src/core/element.ts` 한곳에서만 정의한다.
8. 캐릭터 프로필(성격·소속 서사)을 새로 만들거나 확정할 때는 `docs/factions.md`의 5대
   자치 스쿼드 중 하나를 반드시 함께 정한다. role·종(origin)의 생태·희귀도/스토리상 위치·
   야성 특성이 말하는 성격을 종합해서 고르고, 그 문서의 배정표에 근거와 함께 추가한다.
   소속은 `RelicDef.squad`(필수)에 남기고, 스쿼드의 이름·라틴 표기·역할·**연구원 호칭**은
   `src/data/factions.ts` 한 표가 소유한다. 화면이 스쿼드 이름이나 호칭을 직접 적지 않는다.
   **소속을 이름표로만 두지 않는다** — 그 무리에서 무엇을 하고 누구를 어떻게 부르는지를
   `squadNote`·`researcherTitle`(그 스쿼드가 실제로 쓰는 호칭 중 하나)로 남긴다. 다른 스쿼드를
   동경하는 설정은 독립 필드나 화면 라벨로 분리하지 말고, 그 관제·판단을 흉내 내는 것처럼 직접
   관찰된 행동으로 `unlockRecord.text`의 복원 후 관찰 문장 안에 자연스럽게 표현한다. `squadNote`는
   관찰 일지가 네 줄로 그리므로 그 안에 끝낸다.
   스쿼드 이름은 설명이 아니라 **이름표**라 `display`로 두껍게 세우고, 뒤에 판이나 복제
   그림자를 두지 않는다 — 엠블럼이 이미 제 어둠을 데리고 있어 겹치면 표식이 두 겹으로 보인다.
   **스쿼드 엠블럼은 `src/ui/FactionMark.ts` 하나가 그린다.** 판때기를 받치지 않고 **같은 그림을
   검게 여러 겹 복제해** 크기를 조금씩 키우며 겹쳐 어둠이 번지게 만든다 — 도형으로 그림자를
   그리면 엠블럼과 무관한 네모가 뒤에 남고, 캔버스 흐리기는 열 때마다 프레임이 튄다. 표식은
   **판 안쪽**에 앉힌다. 밖으로 내밀면 홀로그램 판의 실루엣이 깨져 붙인 스티커처럼 보인다.
9. 자동 편성은 `src/core/partyAffinity.ts`, 배속 순환은 `src/core/battleControls.ts`의 순수 규칙을
   사용한다. 씬에서 상성 점수나 허용 배속을 다시 만들지 않는다.
   **자리를 끌어 옮길 때는 숫자나 글자로 알리지 않는다**(`src/ui/formationDragVisual.ts`) — 뒤가
   어두워지고 든 캐릭터 자체가 커져 손끝에 떠오르며, 자리를 내주는 캐릭터가 바뀔 자리로
   반투명하게 미리 옮겨 선다. 미리보기는 확정 규칙(`moveFormationSlot`)을 그대로 통과시킨다.
   따로 계산하면 보여 준 것과 놓은 결과가 갈린다. 놓을 자리는 **섬광이 아니라 네모칸**으로
   알린다 — 번쩍하고 마는 표시는 어디까지가 그 자리인지 말해 주지 못하므로, 모서리를 어긋나게
   깎은 칸을 끄는 동안 계속 띄우고 지금 가리키는 칸만 진해지고 커진다.
10. 렐릭의 관찰 프로필 데이터를 작성할 때는 `docs/lore.md`의 **E.C. 표기 규칙**을 따른다.
    E.C.가 0~20 범위인지, 외형·말투·정서·서사적 역할을 아우르는 캐릭터 콘셉트와 일치하는지
    함께 검수하며, 실제 나이나 서사 경과 연도처럼 증가시키지 않는다.

## 로딩 화면

- **최소 리소스(배경 원화·로고)가 도착하기 전에는 검은 화면만 둔다.** 배경 없이 글자와 마름모부터
  뜨면 화면이 한 번 조립되는 과정이 그대로 보인다. 준비된 순간 한꺼번에 들어오고, 그때 제목이
  **가운데에서 좌우로 열리며** 섬광이 함께 퍼진다 — 로고를 가로로 늘였다 줄이면 그림이
  찌그러지므로 **잘라내는 창을 넓혀** 연다. 파일 하나가 없어도 검은 화면에 갇히지 않도록
  `loaderror`도 같은 진입을 부른다.
- **로고 자리는 그림 상자가 아니라 보이는 글자를 기준으로 잡는다.** 로고타입 파일은 사방에
  넉넉한 투명 여백을 두고 있어, 상자로 배치하면 글자와 부제 사이에 큰 빈틈이 생기고 부제는
  그만큼 내려가 배경 원화의 얼굴을 덮는다. alpha 경계를 실측한 `TITLE_LOGOTYPE_CONTENT` 하나가
  위·아래·좌·우 비율을 갖고, 부제와 섬광 폭이 모두 그 값에서 나온다. 아트를 바꾸면 같은
  방법으로 다시 재서 그 표만 고친다.
- **제목·부제 뒤에 판때기를 받치지 않는다.** 검은 판을 깔면 화면 위쪽이 통째로 어두운 상자가
  되어 배경 원화가 잘려 보인다. 대신 같은 그림·같은 글자를 **검게 한 겹 복제해** 깔고, 글자
  복제본에는 획 둘레로 번지는 검은 띠를 두른다. 그 띠가 실제 배경이 되므로 대비가 배경 원화와
  무관해진다 — 부제 자리(노란 후드)는 밝은 곳 휘도가 0.758까지 올라 강조색이 1.45:1까지
  떨어지지만, 검은 띠 위에서는 10.8:1이라 AA를 넉넉히 넘는다.
- 부트(`BootScene`)는 저장 로드·마이그레이션·복구만 조율하고 곧바로 타이틀을 띄운다.
  글꼴·배경 원화·아이콘·Puppet 묶음처럼 무거운 것은 **타이틀이 로딩 화면 노릇을 하며** 읽는다.
  검은 화면으로 기다리게 하지 않기 위해서다.
- 읽을 것이 늘면 씬에서 `this.load`를 직접 부르지 말고 `scenes/loadingSteps.ts`의
  `LOADING_STEPS`에 단계를 더한다. 진행 칸(마름모) 수가 목록 길이라 저절로 맞는다.
- 한 단계가 실패해도 다음 단계로 넘어간다. 아트 파일 하나가 UI와 전투 규칙까지 막지 않는다.
- 다섯 칸이 다 차기 전에는 다음 화면으로 넘어가지 못하고, 다 찬 뒤에는 화면 아무 곳이나 눌러
  진행한다. E2E가 기다리는 `__PF_DEBUG.ready`는 로딩이 끝나는 순간에만 true가 된다.
- Phaser Text는 그린 순간의 글꼴로 텍스처를 굳히므로, 글꼴 단계가 끝나면 타이틀은
  `refreshTextTextures`로 이미 그린 글자를 한 번 다시 굳힌다.

## UI 스타일 규칙 — 근미래 홀로그램

화면 전체가 하나의 투영 장비처럼 보여야 한다. 생김새 토큰과 도형 그리기는 `src/ui/holo.ts`가
독점하고, 씬은 거기서 꺼내 쓴다.

- **개발 설명을 화면에 쓰지 않는다.** 기능을 제목 그대로 다시 풀이하는 부제, 재화의 흔함·귀함,
  BM 종류, 구현 상태나 운영 의도를 설명하는 문장은 플레이어의 선택에 필요하지 않으므로 코드
  주석이나 기획 문서에만 둔다. 화면 문구는 세계관 안의 명칭, 현재 상태, 비용·보상·확률·기간처럼
  행동을 결정하는 정보만 남긴다. 새 문구를 추가할 때는 "이 문장이 없어도 조작 결과가 같은가"를
  검수하고, 같다면 인게임에 노출하지 않는다. **조회 중·준비 안 됨·불러오기 실패 같은 상태도
  마찬가지다** — 그 자리를 비우고 조작만 감춘다. "기준 점수를 먼저 기록하세요" 같은 문장은
  개발자에게나 의미가 있고 플레이어가 지금 할 일을 바꾸지 않는다.

- **테두리를 두르지 않는다.** 사방 외곽선 대신 ① 아래로 깔리는 그림자, ② 윗변 한 줄의 얇은
  강조선, ③ 얇은 구분선만 쓴다. `drawLayer`의 `edge`는 일부러 윗변만 긋는다.
  예외는 **액자 하나뿐이다** — 스킬 아이콘처럼 그림 한 장을 담는 칸은 불투명하게 채우고
  `drawShapeOutline`으로 사방을 두른 뒤 `drawInnerVignette`로 테두리 안쪽만 어둡게 눌러,
  배경 원화 위에서 어디까지가 그림인지 또렷하게 만든다. 판때기에는 이 둘을 쓰지 않는다.
- **면은 단색 플랫 + 살짝 기울임.** 패널·버튼은 `slantedRect`, 카드는 `chipPoints`로 만든다.
  광택·그라데이션 입체는 쓰지 않고, 깊이는 그림자와 겹침으로만 만든다.
- **바탕은 유리처럼 반투명하게.** `HOLO.glass`를 기본으로 하고, 배경 원화가 비쳐야 한다.
- **화면 가장자리 바(상단 재화·하단 탭·전투 프로필)는 판때기를 깔지 않는다.** `drawGlassFade`로
  검정→투명 그라데이션만 두고 `drawHairline`으로 경계를 알린다.
- **누르면 커진다.** 눌린 상태를 색이 아니라 크기(1.05~1.16배)로 알린다. 지금 화면인 탭도
  밑줄 상자가 아니라 크기와 강조색으로만 구분한다.
- **게이지는 전부 `HoloBar`.** 체력·각성·야성이 같은 기울기와 같은 두께 체계를 공유한다.
  배경 판 없이 옅은 홈과 채움 두 겹으로만 그린다.

### 화면별 자리 규칙

- **UI를 새로 만들거나 고칠 때 한 화면만 보고 좌표를 정하지 않는다.** 먼저 같은 역할의 공용
  프리팹, 환경설정의 행·슬라이더, 다른 팝업의 제목·탭·본문·하단 조작, 인게임 HUD의 안전 영역을
  폭넓게 대조한다. 기울임·마스크·컨테이너 변환까지 자식 장식에 전파되는지 확인하고, 각 요소의
  실제 외곽 bounds와 인접 영역 간격을 순수 배치표/테스트로 고정한다. 마지막에는 1080×1920
  모바일 캡처로 팝업 가장자리 돌출과 지도·증강·초상·행동 UI의 겹침을 함께 검수한다.

- **기록 보상 길은 얇은 실선 한 줄이다.** 판이나 굵은 막대를 세우면 보상 액자와 무게가 같아져
  어느 쪽이 길인지 흐려진다. 지나온 구간만 **흰 선분**을 덧그어 "여기까지 왔다"를 선 하나로
  말하고, 마디는 선 위에 찍히는 작은 마름모다.
- **기록 보상은 게이지가 아니라 위로 뻗는 길이다**(`ExpeditionRewardPopup` + 순수 규칙
  `expeditionRewardTrack.ts`). 마디마다 임계값이 서고 보상은 **우 → 좌**를 번갈아 가지처럼
  내밀며, 지나온 길만 강조색으로 차고 현재 누적은 그 끝에 표식으로 붙는다. 길이 창보다 길면
  판을 키우지 않고 그 안에서 흐른다. 게이지 한 줄은 "지금 얼마나 찼나"만 말하고 "다음에 무엇이
  오나"는 못 말한다.
- **원정은 기록 → 편성 → 지도 순서다.** 들어가면 주간 기록 화면(보스 전신 + 순위판)이 먼저 뜨고
  하단 출격 버튼이 편성을 연다. 순위와 누적 보상은 한 판에 겹치지 않는다 — 보상은
  `ExpeditionRewardPopup`이 맡고, 단계 한 줄은 결과 화면과 `expeditionRewardStage.ts`를 공유한다.
  진행 중 런이 있으면 기록 화면을 건너뛰고 곧바로 지도로 들어간다.
- **로비**는 캐릭터를 보는 화면이다. 조작은 가장자리로 밀고 가운데는 비운다.
  왼쪽 위 = 플레이어 프로필(`TopBar`)과 홍보 칸, 오른쪽 위 = 재화와 설정,
  오른쪽 중단 = 상점·우편·친구 아이콘 줄(`RailButton`),
  아래 = 왼쪽 교류·발굴(푸른색) ↔ 오른쪽 원정·출격(주황). 좌우 버튼은 `perspective`로 화면
  가운데를 향해 쏠린 사다리꼴이 되어 서로 마주 본다. 왼쪽은 서브 콘텐츠 자리라 출격과 같은
  줄에 서더라도 크기는 교류와 같게 두고, 위계는 크기와 색으로만 가른다.
  하단 탭 첫 슬롯은 **고고학**(장기 탐사)이고, 로비 왼쪽의 **발굴**과 서로 다른 콘텐츠다.
- **공식 용어:** **연구소**는 화석·호박석을 소비해 캐릭터를 획득하는 확률형 **화석 연구**
  기능이다. **발굴**은 로비에서 렐릭을 배치하고 시간이 지나 생산된 재화를 수확하는 **자원 수집**
  기능이다. 연구소의 캐릭터 획득을 발굴이라 부르지 않으며, 발굴의 편성·생산·수확을 연구라
  부르지 않는다. 첫 안내에서는 반드시 "연구소의 캐릭터 획득 연구"와 "배치형 자원 발굴"을
  한 문장 안에서 대조해 두 기능이 같은 행동으로 읽히지 않게 한다.
- **출격 선택판의 칸에 SD를 세울 때는 캐릭터 카드와 같은 규칙을 쓴다.** 몸은 칸 안에 갇히고
  머리만 윗변 밖으로 빠져나오며(`ExpeditionEntryButton.sdMask`), 발끝은 아래 변 밖에 두어
  상반신 위주로 보이게 한다. 자리·크기·머리 띠·그림자 방향은 `sortieEntrySdSpot` 한 곳이
  정한다. 복제 그림자는 칸 **가운데를 향해** 밀고, 본체와 같은 동작을 함께 재생한다 —
  한쪽만 움직이면 그림자가 딴 자세로 남는다. 머리 띠는 윗변 아래로 **깎인 모서리 깊이만큼**
  더 내려가야 한다 — 그러지 않으면 `/`로 잘린 모서리가 머리 옆을 대각선으로 베어 낸다.
  Puppet은 컨테이너 변환을 물려받지 않으므로 SD와 그 마스크는 화면 좌표로 세운다.
- **나란히 선 두 칸은 한 판을 둘로 나눈 것처럼 세운다.** 마주 보는 변까지 빗변으로 깎으면
  서로 마주 본 두 조각으로 읽히므로, 맞닿는 변만 곧게 두고(`split`) 그 사이는 좁은 여백으로
  가른다.
- **판 안쪽 비네팅은 `drawFrameVignette`을 쓴다.** 같은 도형을 줄여 가며 두르는
  `drawInnerVignette`은 가로로 긴 판에서 좌우가 세로보다 많이 줄어 검은 줄이 여러 겹
  어긋난 잔상으로 남는다. 네 변 그라데이션은 비율이 어떻든 고르게 눌러 준다.
- 로비 대사는 이름줄과 대사줄만 덮는 얇은 띠를 쓴다. 판을 키우는 대신 불투명하게 하고,
  경계는 위아래 선 두 줄과 이름 옆으로 이어지는 선이 잡는다. 이름은 금색, 대사는 흰색이다.
- 배경 원화와 출격 진입 버튼 일러스트(`ContentN_00M`)는 `scripts/prepare_backgrounds.py`가 구운
  WebP만 둔다 — 배경은 `public/sprites/background`, 버튼 원화는 `public/sprites/content`다.
  원본 PNG는 한 장에 10MB에 가까워 그대로 두면 첫 로딩이 통째로 그만큼 늘어난다. 굽고 나면
  원본은 저장소에서 지운다. 어느 화면이 몇 번을 쓰는지는 `src/ui/backgrounds.ts`의 표 하나에만 있다.
- 배경 원화를 쓰는 화면은 `drawVignette`로 가장자리를 눌러 가운데로 눈이 가게 한다.
- **그림 한 장을 담는 액자는 `src/ui/itemFrame.ts`의 `addItemFrame` 하나가 그린다**(가방 칸,
  스테미나 창의 큰 아이콘). 룬만 등급 색과 주 옵션 뒷배경까지 함께 그려야 해서 `addRuneFrame`이
  따로 있고, 두 액자의 깎임·외곽선·안쪽 비네트는 같은 값을 쓴다.
- 아이콘은 `src/ui/glyphs.ts`의 `drawGlyph`만 쓴다. 씬에서 도형을 직접 조립하지 않는다.
- 속성·직군·재화 아이콘은 그림 파일이다. 원본은 저장소에 남기지 않고 `scripts/prepare_icons.py`가
  구운 WebP만 `public/sprites/{elements,roles,currency}`에 둔다. 속성·직군은 단색 실루엣이라
  **색도 그 스크립트의 표에서 정한다** — 화면이 tint로 다시 칠하지 않는다.
- 스킬 일러스트는 **흰 실루엣 WebP 한 장**이다. 원본(검은 판 위 흰 그림)은 저장소에 남기지
  않고 `scripts/prepare_skill_icons.py`가 밝기를 알파로 옮겨 구운 것만
  `public/sprites/skills/<렐릭id>/{passive,basic,ultimate,ferocity}.webp`에 둔다. 파일에 색을
  굽지 않는 이유는 색이 개체의 속성·직군에서 나오기 때문이다 — 속성과 직군을 섞은 은은한
  파스텔은 `src/ui/skillArt.ts`의 `skillArtTint` 하나가 정하고 화면은 tint로 입히기만 한다.
  아트가 없는 개체는 공용 효과 아이콘(`skillIcons.ts`)으로 자동으로 되돌아가되, 액자의 색판은
  그대로 깔아 아군 창과 같은 체계로 남는다. 적도 같은 파이프라인을 쓴다 — 원본 이름의 머리말만
  `enemy00N`이고 나머지 규칙은 같다.
- **재화 칸은 눌릴 때 칸 하나를 통째로 키운다.** 조각마다 `setScale`을 부르면 `setDisplaySize`로
  크기를 정한 아이콘과 세로로 늘인 값 글자가 제 배율을 잃고 원본 크기로 튀어 그대로 굳는다
  (v0.44.0까지 상단 재화를 누르면 아이콘이 거대해진 채 남았다). 조각은 칸 컨테이너의 국소
  좌표에 세우고 눌림은 그 컨테이너에만 준다.
- **상단 줄의 칸은 지금 얼마인지만 말한다.** 스테미나는 상한과 함께 읽어야 뜻이 서므로
  `64/122`를 **한 덩어리·같은 색**으로 적되(두 수를 다른 색이나 크기로 가르면 한 칸에 두 정보가
  선다), 자릿수가 늘어난 만큼 글자만 한 뼘 줄인다(`valueRatio`). 회복 시간처럼 **지금 당장
  조작을 바꾸지 않는 수는 칸에 넣지 않고** 눌러서 여는 창이 맡는다 — 한 줄에 다 넣으면 글자가
  칸을 넘고 정작 자주 보는 두 수가 작아진다.
- **스테미나만 전용 창이다**(`src/ui/StaminaPopup.ts`). 다른 재화는 "무엇으로 얻고 어디에 쓰는가"만
  읽으면 되지만(`CurrencyGuidePopup`), 스테미나는 남은 양·회복 시간·지금 채우는 수단이 곧 다음
  조작을 정한다. 창은 위에서부터 **액자 → 현재/최대 → 회복 시간 → 충전 → 사용처** 순이고,
  충전 줄은 `src/data/staminaRecharge.ts` 표에 있는 것만 세운다 — 아직 서버 경계가 없는 젬
  충전·광고 보급은 넣지 않는다. 눌러도 아무 일이 없는 버튼은 준비 상태를 과장한다. 경계가 생기면
  그 표에 한 줄을 더한다.
- 재화 칸은 `src/ui/CurrencyChip.ts` 하나가 그린다(상단 줄·팝업 보유량 모두). 값 글자는
  화면에서 가장 자주 훑는 수라 **역할이 `display`이고 세로로만 늘인다** — 가로까지 키우면
  자릿수가 늘 때 칸이 함께 넓어진다. 아이콘과 수는 바짝 붙인다: 멀리 떼면 그림과 숫자가
  두 정보로 읽힌다. 같은 규칙을 급여 줄과 버튼의 비용 표기도 따른다.
- 상단 재화 줄의 칸만 기울이지 않고 둥근 면(`drawRoundedLayer`)을 쓴다. 여러 칸이 나란히
  설 때 칸마다 기울면 숫자와 아이콘까지 비뚤어 보여 줄이 어수선해지기 때문이다.
- 상단 줄의 재화는 화면마다 다른 조합을 세운다 — 로비·도감은 보석·골드·스테미나, 모집은
  다이아·화석·호박석이다. 조합은 `src/ui/TopBar.ts`의 `SLOTS` 한 표에만 있고, 자릿수가 큰
  재화는 `formatCurrency`로 K·M으로 줄여 칸 폭이 흔들리지 않게 한다.
- 속성·직군을 화면에 세울 때는 `src/ui/AffinityBadge.ts`를 쓴다. 두꺼운 그림자와 같은 색
  발광을 겹치는 규칙이 거기 한 곳에 있고, 속성이 크고 직군이 조금 작다는 크기 차이도 지킨다.
- SVG 아이콘(`public/sprites`)도 같은 결을 지킨다. 둥근 끝(`stroke-linecap="round"`)과 둥근
  모서리(`rx`)를 쓰지 않고 `miter`로 각지게 맞춘다. 아이콘 바탕은 SVG에 넣지 않는다 —
  칩은 UI 쪽(`iconCard`)이 그리므로 파일에 넣으면 칩이 두 겹으로 겹친다.

### 애착과 즐겨찾기

> 셋을 섞지 않는다. **애착**(`Session.favorite`)은 로비에 서는 한 명, **즐겨찾기**
> (`Session.bookmarked`)는 목록을 추리는 여러 명, **유대**(`RelicProgress.bond*`)는 렐릭별
> 관계 수치다. 하나를 바꿔도 나머지는 그대로 둔다.

- 둘 다 `RelicCollectionManager`를 통해서만 바꾼다(`setFavorite`, `toggleBookmark`).
- 도감은 **보유와 미보유를 갈라** 세운다. 가진 것이 위에 모이고 아직 없는 것은 "미보유 렐릭"
  아래로 내려간다. 정렬은 버튼 **하나**가 개체번호순 → 희귀도순 → 전투력순을 돌아가며 맡는다 —
  기준마다 버튼을 세우면 지금 어느 기준인지 색으로 읽어야 하고 기준이 늘 때마다 줄이 좁아진다.
  목록만 훑는 화면이라 상단 줄에는 프로필도 재화도 세우지 않는다(`TopBar`의 `profile: false`).
- 도감에서 카드를 누르면 곧바로 정보창이 열린다. 목록 화면에 요약 칸이나 애착 버튼을 다시
  만들지 않는다 — 같은 정보를 두 곳에 두면 어느 쪽을 봐야 하는지 흐려진다.
- 정보창 헤더 왼쪽의 뱃지 두 개가 이 둘을 맡는다. 1번 즐겨찾기(책갈피), 2번 애착(하트).
  꺼짐은 회색 선, 켜짐은 강조색이다.

### 캐릭터 카드

`src/ui/PortraitCard.ts` 한 장을 도감·편성·전투 궁극기 프로필·연구 결과가 모두 공유한다.
새 화면이 생겨도 카드를 다시 만들지 않는다.

- 모서리를 **서로 다르게 깎은 칩** 한 장에 원화를 머리(`머리1`) 관절 기준으로 크게 확대해
  넣는다. 뒷배경이 거의 남지 않을 만큼 채우는 것이 기준이다(`fillRatio` 0.56). 네 모서리를
  같은 길이로 깎으면 반듯한 팔각형이 되어 정면 판때기처럼 보이므로 어긋나게 깎는다.
- 그림 영역이 작아 정보창에서 혼자 커 보이는 원화(적 전신 등)는 화면이 아니라
  `PuppetAsset.portraitZoom`으로 맞춘다. 화면은 개체마다 다른 높이를 정하지 않는다.
  다만 **먼저 `content`가 실제 알파 경계와 맞는지 확인한다** — 캔버스 크기나 가시 영역을
  잘못 적어 두면 배율이 통째로 틀어지고, 그걸 `portraitZoom`으로 덮으면 원인이 숨는다.
  코어 관절 높이가 달라 혼자 내려앉아 보이는 원화는 `portraitOffsetY`로 올린다(루카 -34).
  **이 보정은 전신 정보창의 것이라 카드는 위로 올리는 값을 받지 않는다** — 카드는 머리 끝을
  구멍 위에 맞춰 세우므로, 거기서 더 올리면 정수리가 구멍 윗변에 잘린다.
- 등신이 낮아 혼자 커 보이는 원화는 카드 규격이 아니라 `PuppetAsset.cardZoom`으로 맞춘다.
  (토리카 0.62. 카드 크기를 캐릭터마다 다르게 만들지 않는다.)
- 칩 윗변 가운데는 열려 있어 **머리가 칩 밖으로 빠져나온다.** 어깨 아래는 칩 안에 갇힌다.
  구멍은 **머리가 통째로 지나갈 만큼** 넓고 높아야 한다 — 좁으면 원화가 잘리는 것이 아니라
  구멍의 좌우 변이 머리를 네모로 깎아 사각형 덩어리처럼 보인다. 크기는
  `portraitCardOverhang`(높이)과 `CHIP_NOTCH_WIDTH`(폭) 두 값이 정하고 화면은 건드리지 않는다.
- **구멍은 직사각형이 아니라 위로 갈수록 벌어지는 사다리꼴이다**(`portraitCardHeadWindow` →
  `chipPoints`의 `openTopWidth`). 잘린 모서리(`/`)를 피해야 하는 것은 구멍이 칩 윗변과 만나는
  **그 한 줄뿐**이고 그보다 위에는 칩이 없어 아무것도 침범하지 않는다. 위아래를 같은 폭으로
  뚫으면 모서리를 피하느라 좁아진 아랫폭이 정수리까지 따라 올라가 머리 옆을 세로로 벤다.
  좌우 모서리는 깎인 깊이가 서로 다르므로(왼쪽이 훨씬 깊다) **깊은 쪽만** 줄인다 — 깊은 쪽에
  맞춰 양쪽을 함께 줄이면 얕은 쪽이 이유 없이 좁아진다.
- **모자·깃털·후드가 한쪽으로 쏠린 원화는 `PuppetAsset.cardHeadEscape`로 그 쪽만 더 연다.**
  관절은 항상 카드 가운데를 기준으로 잡는데, 장식이 한쪽으로 쏠려 그려지면 그 쪽이 잘린다
  (스피나의 뒷머리 오른쪽, 메테의 후드 왼쪽, 도디의 머리 깃털). 사다리꼴 홈이 생긴 뒤로는
  대부분 필요 없으니 **먼저 보정 없이 확인하고** 그래도 남을 때만 `{ left }`·`{ right }`를
  채운다. 칩 윗변에서는 그 쪽 대각선 깎임 안쪽까지만 열 수 있고(그 이상은 v0.29.10에서 고친
  자기 교차 문제가 재발한다), 꼭대기는 칩 폭까지 열린다. 장식이 머리 폭 자체를 벗어나면
  (들어 올린 손처럼) 홈을 넓히는 것만으로는 못 고치고 잘라내는 범위 자체를 넓혀야 한다 —
  아직 일반적인 해법이 없어 캐릭터마다 직접 확인한다(루카의 들어 올린 손이 남아 있다).
- **정수리가 수평으로 평평하게 잘려 보이면 원인은 카드가 아니라 `MAX_HEAD_DROP_RATIO`다.**
  `computeHeadCardFrame`은 머리보다 높이 솟은 부위(들어 올린 손·망토)가 있어도 얼굴이 카드
  상단 범위 안에 들어오도록 자르기 시작점의 하한을 잡는데, **그 한계가 걸리는 순간 시작점이
  `content.top`보다 아래로 내려가 원화의 맨 윗부분이 통째로 사라진다.** 값이 좁으면 등신이 낮아
  머리가 큰 원화(토리카·도디)가 함께 걸려 정수리가 잘린다 — v0.34.3까지 그랬다. 이럴 때
  개체별 보정(`cardHeadDropY` 같은 것)을 새로 만들지 않는다. 이미 잘린 그림을 옮길 뿐이라
  단면이 그대로 남고 원인이 숨는다. 대신 `tests/unit/puppetAnchors.test.ts`의 "실제 원화의 카드
  잘라내기"에 그 원화를 더해 `clipsContentTop`을 확인하고, 걸린다면 한계값 자체를 조정한다.
- **카드 잘라내기 회귀 테스트는 지어낸 프레임이 아니라 실제 에셋으로 쓴다.** 손으로 만든 값만
  검사하면 실제 원화가 잘리는 동안에도 테스트는 계속 통과한다(그래서 위 버그가 여러 번 살아남았다).
  새 전신 원화를 넣으면 크기·알파 경계를 `src/puppets/assetMetadata.ts`에 적고(Phaser 없이 읽혀야
  한다) `assets.ts`는 URL만 붙인 뒤, 그 회귀 테스트 목록에 ZIP의 `머리1` 관절과 함께 더한다.
- **그리드에 세울 때는 머리가 잘리지 않는 안전 영역을 먼저 잡는다.** 첫 줄을 스크롤 마스크나
  판의 윗변에 붙이면 빠져나온 정수리가 잘린다. 첫 줄 중심은 `src/ui/portraitGrid.ts`의
  `portraitGridFirstRowY(뷰포트 상단, 카드 높이)`로만 정하고 스크롤 길이는
  `portraitGridContentHeight`를 쓴다 — 화면마다 눈대중으로 몇 픽셀 내리면 카드 높이가 바뀔 때
  기준이 갈라진다. 그리드의 **보이는 윗선은 카드 몸체가 아니라 머리 끝**이므로, 같은 화면의 다른
  판과 나란히 맞출 때도 그 윗선을 기준으로 맞춘다(발굴의 배치 그리드와 누적 액자 판이 같은
  윗변을 쓰는 이유다). 규칙은 `tests/unit/portraitGrid.test.ts`가 지킨다.
- 이름줄은 **별도 레이어가 아니다.** 칩 안쪽 아래에 투명→검정 그라데이션을 깔고 그 위에
  `LV.n`(강조색)과 이름을 얹는다. 레이어를 하나 더 겹치면 그림자가 둘로 갈라져 카드가 두
  장으로 보인다. 카드 한 장에 그림자도 하나다.
- **선택 표시의 어두운 면은 그린 픽셀만 누른다.** 몸통은 칩 도형으로 덮되, 윗변 밖으로 빠져나온
  머리는 원화를 검게 복제해 홈 마스크 안에서만 겹친다 — 홈을 도형으로 칠하면 머리 옆의 투명한
  빈자리까지 네모나게 어두워진다.
- **카드 명도 효과의 합성 순서는 `원본 픽셀 → 개별 효과 → 카드 전체`다.** 순수 계산은
  `src/core/portraitAlpha.ts`가 소유한다. 잠금은 원화 tint만 바꾸고 알파 면을 더하지 않으며,
  선택과 궁극기 충전은 각각 몸통 면과 실제 원화 알파를 가진 머리 복제를 서로 겹치지 않게 한 번씩
  그린다. 사망 명도는 마지막 카드 전체 알파만 맡는다. 머리 복제에 사망 알파를 미리 곱한 뒤 카드
  전체에 다시 곱하거나, 몸통 면을 머리 홈까지 늘려 같은 픽셀에 효과 알파를 두 번 적용하지 않는다.
- 고른 카드·애착 렐릭은 **테두리를 두르지 않는다.** 카드 실루엣을 조금씩 키워 겹친 발광
  (`paintGlow`)과 1.06배 확대로만 알린다.
- **별은 로마자 한 글자다**(`src/ui/rarityMark.ts`의 `addStarMark`). 별 다섯 칸은 칸을 세어야
  알 수 있고 카드마다 다섯 개가 반짝여 얼굴보다 먼저 읽혔다. 카드 오른쪽 위, 예전에 개체번호가
  섰던 자리에 `I`~`V`가 서고, 그림자는 흐리게 번지지 않고 **각지게 계단으로** 진다. 표식의
  글자·색·발광은 그 함수 하나가 정하고 화면은 크기만 고른다.
- 인물 뒤에는 배경 원화가 한 겹 깔린다(`BACKGROUND.cardBackdrop`). 이 원화는 **윗변이 닫힌**
  칩 마스크를 쓴다 — 인물과 같은 마스크를 쓰면 머리가 빠져나오라고 열어 둔 홈까지 채워져
  카드 밖에 배경 조각이 떠 보인다. 빈 색면만 두면 카드가
  심심하고, 원화를 그대로 두면 인물보다 먼저 읽힌다 — 그래서 **등급색으로 물들여** 눌러 두고
  그림은 그 색 아래에서 결만 남긴다. 색은 여전히 등급이 정한다.
- **별과 희귀도는 다른 축이다.** 별은 한계 돌파 단계(아래 "별과 파편")이고, 희귀도는 **카드
  바탕색**이 맡는다 — SSR 황금 호박, SR **분홍에 가까운 보라**, R **하늘빛**(`RARITY_TONE`).
  셋 다 어둡게 누르지 않는다 — 누르면 세 등급이 같은 잿빛 판으로 보인다. 원화가 칩을 거의
  채우므로 가장자리에만 남지만, 그 한 줄로 희귀도가 먼저 읽힌다. 정보창의 등급 글자
  (`RARITY_GEM`)와 같은 계열을 쓴다.
- 등급·포지션 같은 분류 문구는 카드에 넣지 않는다. 카드에서 읽어야 하는 것은 얼굴·레벨·이름·
  등급뿐이고, 나머지는 상세 정보의 몫이다.
- `src/ui/stars.ts`의 별은 이제 성급이 아니라 룬 세공 결과·각인처럼 "박히는 표식"만 그린다.
- 기하 마스크는 컨테이너 이동을 물려받지 않으므로, 카드가 자리를 잡은 뒤 `syncMask`로 월드
  좌표에 맞춘다. 카드를 옮기거나 배율을 바꾸면 다시 불러야 한다.

## 별과 파편

> 별(`RelicProgress.breakthrough` + 1)은 **같은 개체를 몇 번 더 만났나**이고, 레벨은 **얼마나
> 먹였나**다. 희귀도(`RelicDef.rarity`)는 개체가 원래 얼마나 귀한지이며 성장으로 바뀌지 않는다.

- 모든 개체는 별 하나(`I`)로 시작한다. 연구소에서 같은 개체를 다시 획득하면 **그 개체의 파편**
  (`Session.relicFragments`)이 쌓이고, 파편으로 한계를 돌파할 때마다 별이 하나 올라 다섯에서
  멈춘다. 별 다섯에 닿은 뒤의 중복만 공용 DNA 조각(마일리지)이 된다.
- 단계별 파편·치즈케이크·레벨 상한·해금 효과는 `BREAKTHROUGH_STEPS` 한 표뿐이다. 화면이 문구나
  수치를 따로 적지 않고, 상한을 묻는 곳은 전부 `relicLevelCap(breakthrough)`을 쓴다.
- 중복 획득을 "각성"과 "돌파" 두 축으로 나누지 않는다. 같은 행동이 두 숫자를 올리면 무엇을
  키우는 중인지 읽히지 않아 하나로 합쳤다.
- 파편 차감과 단계 확정은 `GameApi.breakThroughRelic`이 한 처리 단위로 맡는다. 정보창의
  **한계 돌파 버튼은 레벨 칸이 아니라 별 옆**에 서고, 가진 파편과 다음 별에 드는 파편을
  버튼이 직접 말한다.

## 정보창과 팝업

- 등급 글자는 보석처럼 칠한다(`RARITY_GEM`). SSR 황금 호박, SR 보랏빛~분홍, R 청량한
  푸른빛이고 색이 위아래로 흐른다. 화면마다 등급 색을 새로 정하지 않는다.
- 오른쪽 칸 제목(유대·능력치·룬)은 판이 기울어도 **기울지 않는다.** 셋이 같은 x에 서고
  왼쪽의 짧은 막대가 판에 묶는다. 제목이 저마다 다른 각도로 누우면 같은 위계로 읽히지 않는다.
- **전투력은 오각형 면 안에 옅게 눕는다**(`src/core/combatPower.ts` + `StatRadar`의 `power`).
  판 위에 따로 칸을 세우지 않는 이유는 전투력이 곧 그 면의 넓이이기 때문이다 — 면이 넓은
  개체일수록 숫자도 함께 커진다. 표시와 정렬에만 쓰고 전투 계산에는 절대 쓰지 않으며,
  가중치 표는 그 파일 하나뿐이다.
- 축마다 실제 수치를 **축 이름과 같은 색으로** 이름 위아래에 쌓는다(`StatRadar`의 `values`).
  옆으로 밀면 이름과 수가 한 줄로 붙어 읽히고, 안쪽으로 밀면 그래프 선과 겹친다.
- 능력치 칸에는 숫자가 아니라 **오각형 그래프**가 선다(`StatRadar`). 축 이름은 굵게 키우고
  능력치 색을 입혀 "무엇이 센 개체인가"를 먼저 읽게 한다. 정확한 수치와 기본값 대비 상승분은
  돋보기로 여는 상세가 맡는다 — 화면에 늘 떠 있어야 하는 것은 균형이고, 숫자는 물어볼 때다.
- 캐릭터 정보창은 `src/ui/info.ts` 한 장이다. 왼쪽은 인물(원화·스킬 아이콘·SD), 오른쪽은
  수치(등급·레벨·급여·유대·능력치·하트 젬)다. 화면에 늘 떠 있는 것은 "지금 얼마나 컸는가"
  까지고, 그 밖의 자세한 것(관찰 일지·각성·추가 능력치·스킬·용어)은 전부 팝업이 맡는다.
- **관찰 일지는 세 영역을 섞지 않는다.** 상단 표본 메타데이터와 중단의 화석 발굴 기록은
  `COLOR.inkDim`의 회색, 복원 후 관찰 기록은 `COLOR.ink`의 흰색으로 읽힌다. 키·몸무게 같은
  관찰 프로필 수치는 상단 상세정보에 한 번만 두고 발굴 기록에서 되풀이하지 않는다. 영역의 y를
  고정하지 말고 실제 텍스트 높이와 규정 간격을 누적하며, 안전 높이를 넘으면 판을 키우지 않고
  내부를 스크롤한다(`observationJournalLayout.ts`).
- **적과 친구도 같은 정보창을 쓴다.** 화면을 새로 만들지 않고 문맥(`InfoContext`)으로 빼기만
  한다 — 친구는 유대·급여·룬이 빠지고, 적은 거기에 현재 전투 줄(체력·궁극·야성·상태이상)이
  붙는다. 전용 화면을 따로 세우면 정보창이 좋아질 때 그 화면만 옛 모습으로 남는다.
  적 수치는 `getStageEnemies`가 만든 레벨 보정 정의를 그대로 읽고 창이 다시 보정하지 않으며,
  적 전신 원화도 `PORTRAIT_ASSETS`에 함께 등록해 아군과 같은 경로(`portraitAssetFor`)로 찾는다.
- **제목표는 한 모양뿐이다**(`src/ui/SectionTitle.ts`). 칸 제목(유대·능력치·룬)도 팝업 머리글도
  판의 **윗변에 걸터앉고**, 왼쪽의 짧은 빗금이 제목을 판에 묶는다. 화면마다 제 나름의 제목을
  만들면 같은 위계가 어디서는 판, 어디서는 맨 글자가 된다.
- 팝업은 글자가 들어가는 만큼만 넓다. 남는 여백은 읽는 데 도움이 되지 않고 뒤 화면만 가린다.
- **팝업은 새 화면이 아니라 누른 것 위에 얹히는 쪽지다.** `PopupLayer.open`에 `anchor`와
  `onClose`를 주고, 부른 버튼은 팝업이 떠 있는 동안 눌린 크기를 유지한다. 뒤를 어둡게 덮는
  `dim`은 젬 고르기처럼 그 순간 다른 조작을 막아야 하는 팝업에만 켠다.
- **이미 지급이 끝난 결과는 `src/ui/RewardPopup.ts` 한 장이 알린다**(상자·임무·발굴 수확).
  고를 것이 없는 영수증이라 **화면 아무 곳이나 눌러 닫히고**, 그래서 오른쪽 위 X를 두지 않는다.
  안내 문구("화면을 눌러 확인")는 팝업 안이 아니라 **화면 하단**에 반투명 굵은 글자로 선다 —
  누를 수 있는 곳이 화면 전체라는 말은 팝업 밖에서 해야 읽힌다. 뒤를 짙은 검정으로 덮어 뒤
  화면과 갈라 두고, 액자 수량에는 `+`를 붙이지 않고 검은 테두리로 액자 선에서 떼어 놓는다.
- 발굴 현황의 재화 칸(`src/ui/ExcavationCurrencyFrame.ts`)은 그 보상 액자와 **같은 양식**이다.
  같은 실루엣·사방 외곽선·안쪽 비네트를 쓰고 누적량은 액자 오른쪽 아래에, 시간당 생산량은 그
  아래 한 줄에 `1.2K/H`처럼 **시간 단위를 약자로만** 적는다(시간당 1을 못 채우는 재화만 `/D`).
- 설명문 안의 용어는 `[[keyword-id|표기]]`로 적고 `KeywordManager`만 그린다. 강조색·밑줄·
  중첩 팝업 규칙을 화면마다 새로 짜지 않는다. 용어 원문은 `src/data/keywords.ts`에 둔다.
- **관찰 일지는 이름표(회색)와 본문(흰색)을 색으로 가른다.** 개체번호·프로젝트 같은 라벨은
  `COLOR.inkDim`, 그 옆의 실제 값과 관찰 기록 본문·문답 내용은 `COLOR.ink`로 명시한다 —
  "잘 보여야 하는 관찰 내용"과 "그것이 무엇인지 말해 주는 이름표"가 같은 무게로 읽히면 본문이
  라벨 사이에 묻힌다. 소속 이야기(`squadNote`)처럼 부가 설명인 문단만 예외로 회색을 쓴다.
- **완료한 관찰 기록은 별도 레이어에서 한 건씩 넘겨 본다**(`openObservationHistory` +
  `src/ui/observationHistory.ts`의 순수 규칙). 관찰 일지 쪽지에는 가장 최근 한 건만 두고,
  두 번째 기록부터는 "전체 기록 보기 (N건)" 링크가 여는 별도 팝업이 페이지 단위로 넘긴다 —
  매일 쌓이는 인터뷰를 전부 한 쪽지에 밀어 넣으면 캐릭터 소개보다 로그가 더 길어진다. 이
  레이어는 특정 버튼 위에 붙는 쪽지가 아니라 **화면 가운데 따로 뜨는 기록판**이다 — 관찰
  일지와 같은 자리에 겹쳐 열면 두 판의 닫기 X가 거의 포개져 헷갈린다. 페이지를 넘길 때마다
  팝업을 닫고 다시 여는 것은 룬 세공 갱신과 같은 경계(닫고 다시 연다)를 그대로 쓴 것이다.
- `PopupLayer`는 지금 열린 팝업 제목을 `setDebugPopupTitles`로 `window.__PF_DEBUG.popupTitles`에
  쌓아 둔다. Canvas 안에서 어떤 팝업이 실제로 열려 있는지 DOM에서 알 방법이 없어, 정확한 탭
  좌표를 계산하기 어려운 중첩 팝업 E2E는 이 값으로 먼저 확인한 뒤 좌표를 맞춘다.

### 스킬 설명문 작성 규칙 — 야성·패시브·일반 공격·궁극기

캐릭터마다 문장을 새로 지으면 같은 뜻이 화면마다 다른 무게·다른 단위로 읽히고, 수치를
조정한 뒤 옛 문장이 그대로 남는다. 그래서 **화면에 보이는 본문은 문장을 적어 두는 것이 아니라
데이터에서 조립한다.** 네 슬롯 모두 `src/ui/skillPresentation.ts`의
`ferocityTraitDescription`·`passiveDescription`·`skillDescription` 세 함수만 본문을 만든다.

- **공격 스킬과 정형 회복 스킬(`basic`·`ultimate`)에는 `desc`를 적지 않는다.** `skillDescription`이
  구조화 필드에서 문장을 짓는다. 새 스킬이 새로운 효과를 가지면 문장을 데이터에 적는 대신 그
  함수에 **효과의 절 하나**를 더한다. `tests/unit/skillArt.test.ts`의 "스킬 설명문 양식 계약"이
  모든 개체의 두 슬롯을 한꺼번에 검사하므로, 원문을 되살리거나 양식을 벗어나면 실패한다.
- `desc`가 남아 있는 곳은 **문장을 지을 수 없는 것들뿐**이다 — 패시브·야성 특성 중 전용 분기가
  없는 것. 전용 분기가 있는 패시브·야성 특성(`kind`/`effectId`)의 `desc`는 표시되지 않는 데이터
  문서용 사본이라, 그런 스킬을 고칠 때는 반드시 함수 쪽 분기도 함께 고친다.

#### 한 문장의 뼈대

```
{대상}에게 {피해}를 준다.            ← 부가 효과가 없으면 여기서 끝
{대상}에게 {피해}를 주고[,] {효과}.  ← 주어가 같은 첫 효과 하나만 이어 붙인다
{그 밖의 효과}.                      ← 주어가 바뀌는 절은 제 문장으로 선다
```

- **대상은 전투 엔진이 읽는 `targeting` 계약에서 만든다**(`적 한 명`·`자신의 주위 모든 적`·
  `전장의 모든 적`·`지정한 원 안의 모든 적`). 설명문이 대상을 따로 적으면 실제로 맞는 범위와
  갈린다. 지정 원은 아군도 함께 판정하지만 **피해를 받는 것은 적뿐**이라 본문은 적만 말한다.
- **효과 절의 순서는 때린 결과에 가까운 것부터다** — 추가 타격(연격) → 그 피해로 생기는 회복 →
  상태이상 → 그 밖의 규칙(전이·주기 치명타·충전 조건). 새 효과를 넣을 자리를 이 순서로 정하면
  개체가 늘어도 문장 모양이 갈리지 않는다.
- **주어가 바뀌는 절은 "주고"로 잇지 않는다.** 주기 치명타·전이처럼 말하는 대상이 달라지는 절을
  한 문장에 이으면 읽다가 걸린다. 쉼표는 회복처럼 절이 길 때만 두고, 상태이상은 쉼표 없이 잇는다
  ("주고 3초 동안 기절시킨다").
- **묘사문을 쓰지 않는다.** 포효하거나 부리로 쪼거나 하늘에서 내리꽂는 문장은 전투 결과를 바꾸지
  않는다. 이름(스킬명)이 이미 그 그림을 말한다.

- **고정 수치는 실제 값으로 환산해 태그로 보여준다.** "공격력 200%"처럼 정적인 배율은 지금
  능력치로 계산한 실제 숫자를 `[[damage-value|123]]`·`[[shield-value|123]]`·`[[heal-value|123]]`
  같은 조회 가능한 태그로 만든다(레벨·룬으로 능력치가 변하면 숫자도 함께 바뀐다). **명중 시점의
  상대값만 예외로 `%`를 그대로 남긴다**(잃은 체력 비례 회복처럼 대상마다 달라지는 값) — 대상이
  없으면 계산할 수 없는 값이기 때문이다.
- **위력(%)과 다른 능력치(%)를 하나의 배율로 합쳐 계산하는 스킬**(스피나 궁극기처럼 공격력과
  공격 속도를 함께 쓰는 경우)도 예외가 아니다. 이 패널의 모든 수치가 이미 "전투 중 버프는 빼고
  지금 성장한 대로"만 보여 주므로, 스킬 하나만 실시간 값을 반영하는 척 `%`로 남기면 오히려
  기준이 갈린다. 두 축을 하나로 합친 실제 수치 태그 하나로 보여 준다
  (`attackSpeedCompositeDamageKeyword`, 상단 라벨과 본문이 같은 값을 쓴다).
- **본문의 피해 수치는 스스로 계산하지 않는다.** 스킬 아이콘 위 `valueLabel`이 쓰는 그 값을
  그대로 받아 쓴다(`skillDescription`의 `stats.damage`) — 두 곳이 따로 계산하면 같은 스킬의
  피해가 위아래에서 다른 수로 보인다. 능력치를 모르는 자리(도감)에서는 위력 %로 되돌아가되
  **어느 능력치에서 나오는 배율인지 함께 말한다**(`주문력의 50%`) — 마법 피해가 늘 주문력에서
  나오는 것은 아니다(메테는 공격력이다).
- **스킬마다 시간이 다른 효과는 본문에 초를 적는다.** 기절이 그렇다 — 키워드 설명은 그 상태가
  무엇인지만 말한다. 경직은 반대로 키워드가 "약 0.1초"를 명시하므로 적지 않는다.
- **같은 값이 여러 곳에 반복되면 한 번만 말한다.** 렉시아 패시브처럼 네 능력치가 전부 같은
  값으로 오르면 "공격 속도·공격력·치명타 확률·치명타 피해가 모두 25% 오른다"로 줄인다. 값이
  서로 달라지는 순간 다시 나열해야 한다.
- **정의된 규칙어는 새로 설명하지 않고 태그로 연결한다.** 기절·경직·출혈·은신·연격·군중제어·
  물리 피해·마법 피해·잃은 체력처럼 `src/data/keywords.ts`에 이미 있는 개념은 `[[id|표기]]`로만
  가리킨다. 캐릭터 전용 용어(예: 메테의 "스타카토")는 전역에 넣지 않고 그 스킬을 여는 곳에서만
  `contextualKeywords`로 주입한다.
- **태그가 이미 말하는 내용을 본문에서 중복하지 않는다.** 경직은 키워드 설명 자체가 "약 0.1초"를
  명시하므로 스킬 문구에서 "0.1초간 경직"처럼 다시 적지 않는다.
- **`%`만 쓰고 `%p`(퍼센트포인트)는 쓰지 않는다.** 덧셈형 확률(치명타 확률 +25 같은)도 플레이어
  에게는 그냥 `%`로 보여 준다. `%p` 표기는 한 번 시도했다가 "플레이어에게 낯선 개발자 단위"라는
  이유로 되돌렸고, `tests/unit/skillArt.test.ts`의 "사용자 노출 퍼센트 표시 계약" 테스트가 소스
  문자열에 `%p`가 다시 섞이면 실패한다 — 같은 시도를 두 번 하지 않기 위한 회귀 테스트다.
- **개발 의도·구현 상태를 설명하는 문장을 넣지 않는다.** 위 "개발 설명을 화면에 쓰지 않는다"
  규칙의 스킬판이다. 효과·대상·수치처럼 전투 결과를 바꾸는 정보만 남긴다.
- 새 캐릭터·보스를 추가하거나 스킬 문구를 고치면 `tests/unit/skillArt.test.ts`에 그 개체의
  표시 계약(태그·환산 수치·중복 여부)을 검사하는 테스트를 함께 남긴다.

- 성장은 레벨업 버튼이 아니라 **급여**다. 치즈케이크를 먹여 경험치를 쌓고 넘치면 레벨이 오른다.
  차감과 확정은 `GameApi.feedRelic`이 한 처리 단위로 맡는다.
- 각성은 0~5단계이고 단계별 효과표는 `src/core/relicProgression.ts`의 `AWAKENING_STEPS`
  하나뿐이다. 화면이 문구를 따로 적지 않는다.
- 레벨 상한은 **돌파**가 연다. 상한을 묻는 곳은 전부 `relicLevelCap(breakthrough)`을 쓰고
  숫자를 직접 적지 않는다. 단계별 상한과 재료는 `BREAKTHROUGH_STEPS` 한 표에만 있으며,
  재료 차감과 단계 확정은 `GameApi.breakThroughRelic`이 한 처리 단위로 맡는다.
- **룬을 누르면 세공 화면이 곧바로 열리지 않는다.** 먼저 간소한 쪽지(`openRuneInfoPopup`)가
  열려 등급·이름·옵션·장착처를 보여 주고, 거기서 **세공**을 한 번 더 골라야 강화 화면이 뜬다.
  골드를 쓰고 되돌릴 수 없는 조작이라 손이 한 번 더 들어간다. 정보창에서 연 룬에만 **장착**
  버튼이 함께 선다 — 어디서 열었는지에 따라 할 수 있는 일이 다르다.
- 세공은 **고른 줄이 계속 고른 채로 남는다.** 한 줄에 세 번씩 반복하는 일이라 누를 때마다
  다시 고르게 하면 그 손이 그대로 낭비다. 그 줄이 다 차면 다음 줄로 저절로 넘어가고, 모든
  세공이 끝나 각인만 남으면 선택을 푼다 — 각인은 되돌릴 수 없는 한 번의 선택이라 무엇에
  새길지는 반드시 사람이 다시 고른다. 고른 줄은 색만이 아니라 왼쪽의 빗금으로도 알린다.
- 세공 화면의 확률은 막대 두 개가 아니라 **한 줄을 성공과 실패가 나눠 가진다**(`addChanceLine`).
  결과는 별로 박힌다 — 성공은 푸른 별, 실패는 다크체리, 각인은 줄 맨 뒤에 노란 별 하나로
  크게. 별의 모양과 겹은 `stars.ts` 규칙(`addGlowStar`) 하나를 그대로 쓰고 색만 바꾼다.
- **가방은 목록이 아니라 격자이고, 칸 하나는 액자 한 장이다**(룬은 `addRuneCard`). 룬·재화·
  소비품·재료 **네 탭이 모두 같은 양식**이라 이름과 설명을 오른쪽에 늘어놓지 않는다 — 그 글은
  눌러서 여는 쪽지가 이미 말하고, 늘어놓으면 한 줄에 두 칸밖에 서지 못한다. 액자에 겹치는
  것은 수량 하나뿐이다. 가방과 정보창에서 조각을 눌러 여는 **장착용 가방이 같은 카드 한 장**을
  쓴다 — 한쪽만 고치면 다른 쪽이 옛 모습으로 남는다.
- **판매는 세공·장착과 같은 크기로 나란히 세우지 않는다.** 되돌릴 수 없는 다른 성격의
  조작이라 작고 붉게 오른쪽 끝으로 물러난다(`RUNE_NOTE_BUTTONS`). 셋을 같은 폭으로 두면 줄이
  넘쳐 서로 겹치고, 무엇이 이 쪽지의 주 조작인지도 읽히지 않는다.
- **액자 뒷배경은 두 주 옵션의 실제 능력치 색이다**(`src/ui/statTones.ts`의 `STAT_TONE` —
  능력치 레이더 축과 같은 표). 대각선으로 갈라 위 주 옵션과 아래 주 옵션이 나눠 갖되, 평평한
  색면이 아니라 **가운데로 갈수록 밝아지는 발광**이다(겹쳐 밝아지는 합성, 다 더해도 0.5 미만).
  한 겹을 진하게 깔면 색이 조각까지 덮어 탁해지고, 겹이 적으면 줄어드는 윤곽이 계단으로 보인다.
  **칠은 마스크가 아니라 도형을 잘라 액자 안에 가둔다**(`src/ui/runeBackdrop.ts`) — 기하 마스크는
  컨테이너 이동을 물려받지 않아 스크롤하는 가방에서 어긋나고, 네모로 칠하면 깎인 모서리로 색이
  새어 액자 밖에 조각이 남는다.
- **각인까지 마친 룬은 금빛 발광 비네트 한 겹으로만 알린다**(`drawShapeInnerGlow`). 조각 위로
  액자 안쪽에서 번지고, 카드 테두리도 금색으로 바뀐다. 숨 쉬는 발광이나 날아다니는 별 tween을
  쓰지 않는다 — 목록에 여럿 서면 그 반짝임이 액자보다 먼저 읽히고 매 프레임 비용이 된다.
- **룬은 자기 자리를 갖고 나온다**(`RuneInstance.part`). 1번 조각은 1번 칸에만 들어간다 —
  아무 룬이나 아무 칸에 끼울 수 있으면 세 칸이 사실상 한 칸이고, 어느 자리를 파밍할지가
  목표가 되지 않는다. 가방은 그 칸의 조각만 보여 주고, 자리 불변식은 화면이 아니라 서버
  (`RUNE_SLOT_MISMATCH`)가 지킨다.
- 룬 아이콘은 하트 한 장이 아니라 **자기 조각 하나**이며, 그림 한 장을 담는 칸이므로 위의
  **액자 예외**를 쓴다(`addRuneFrame`). 조각만 덩그러니 놓으면 어디까지가 그림인지 알 수 없다.
- **액자 안에서는 캔버스가 아니라 그림을 가운데에 맞춘다**(`src/ui/runePieceContent.ts`).
  조각 셋은 하트를 부채꼴로 가른 것이라 저마다 캔버스의 다른 자리(1번 왼쪽 위, 2번 오른쪽 위,
  3번 아래)를 쓴다 — 하트를 세우는 젬 슬롯은 그 자리를 그대로 둬야 하지만, 조각 하나만 서는
  액자에서 그대로 넣으면 그림이 한쪽으로 쏠려 작게 앉는다. 알파 경계를 실측한 표 하나가
  자리와 배율을 정하고, 아트를 다시 구우면 같은 방법으로 재서 그 표만 고친다.
- **표식(잠금·즐겨찾기)은 액자 안쪽에 앉힌다.** 카드 모서리는 대각선으로 깎여 있어 두 변에서
  같은 만큼만 들어가면 표식이 그 빗변을 넘어 붙인 스티커처럼 삐져나온다.
- 이미 누군가 끼고 있는 룬은 목록에서 **옅어진다**. 같은 룬을 다시 고르러 들어가는 헛걸음을
  만들지 않는다. 누가 끼고 있는지는 쪽지가 한 줄로 말한다.
- **잠금과 즐겨찾기는 쪽지 왼쪽 위의 작은 칩 두 장이다**(`src/ui/MarkChip.ts` — 정보창 헤더의
  즐겨찾기·애착 뱃지와 같은 한 장). 꺼짐은 회색, 켜짐은 제 색이고 켜진 것만 카드 왼쪽 위에
  작게 선다 — 회색 표식이 카드마다 늘어서면 액자보다 먼저 읽힌다. 뒤집는 것은 화면이 아니라
  `GameApi.markRune`이며, 잠근 룬의 판매는 서버(`RUNE_LOCKED`)가 거부한다 — 자물쇠는 판매를
  실제로 막는 값이라 표시와 거부가 한 곳에서 갈려야 한다.
- 룬(하트 젬) 슬롯은 하트 원화 한 장을 셋으로 자른 조각이다. `scripts/prepare_icons.py`가
  같은 중심점 기준으로 미리 오려 등급별로 다시 칠해 두므로, 세 조각을 같은 자리에 겹치면
  이음매 없는 하트 한 장이 된다. 등급 색(`HeartGemRarity`)은 초록 고급 → **하늘빛** 희귀 →
  **분홍에 가까운 보라** 영웅 → 빨강 전설이며, 넷 다 그림자를 띄워 스스로 빛나게 잡는다 —
  검정까지 내리면 보석이 아니라 칙칙한 돌이 된다. 색은 `src/ui/runeIcons.ts`의 `RUNE_ACCENT`와
  파이썬 스크립트의 `RUNE_TINTS` 두 곳이 이름까지 반드시 같아야 한다. 빈 자리는 같은 조각을 검게 눌러 파인
  것처럼 반투명하게 남긴다.

## 이펙트와 전투 수치

화면에 터지는 모든 것은 `src/managers/EffectManager.ts` 하나가 소유한다. 폭주·패시브·일반
공격·궁극기와 회복·보호막·사망·화면 조작이 전부 이 경계를 지난다. 씬은 **무슨 일이 일어났는지**
만 넘기고 파편 수·색·크기·수명을 직접 고르지 않는다 — 씬마다 값을 정하면 같은 기술이 화면마다
다른 무게로 터진다.

- **동그라미를 쓰지 않는다.** 파편은 좌우 꼭짓점 높이를 어긋나게 깎은 **마름모**이고, 충격파도
  원이 아니라 납작한 마름모다. 정확한 대칭은 반듯한 보석처럼 보이므로 일부러 어긋나게 깎는다.
- **중력으로 쏟지 않는다.** 위에서 내려다보는 난전이라 파편이 발밑으로 떨어지면 "터졌다"가
  아니라 "흘렸다"로 보인다. 대부분의 종류는 오히려 **위로** 뜬다(`gravity` 음수).
- **잔뜩 흩뿌리지 않는다.** 파편은 한 자리 수로 끊고, 궁극기만 파문 두 겹에 큰 섬광을 쓴다.
  셋 이상이 같은 무게로 터지면 어느 것이 큰 기술인지 읽히지 않는다.
- **섬광은 옅게 깐다.** 겹쳐 밝아지는 합성이라 진하게 두면 밝은 배경 원화 위에서 하얗게
  뭉개져 정작 봐야 할 SD와 피해 숫자가 그 속에 묻힌다(상한 0.6). 잦은 일반 공격일수록 더
  옅다 — **예쁜 캐릭터를 가리는 순간 이펙트는 타격감이 아니라 방해다.**
- **광역은 바닥에 범위를 그린다.** 코어의 `areaImpact` 사건이 실제로 터진 자리와 반경을
  싣고, 씬은 `groundArea`로 **눌린 마름모**를 SD보다 뒤에 깐다 — 정원을 그리면 바닥에 누운
  것이 아니라 캐릭터 앞에 세워 둔 고리처럼 보인다. 숫자만 여럿 뜨면 왜 셋이 함께 맞았는지
  읽히지 않는다.
- **렉이 걸리지 않는 방법은 셋뿐이다.** ① 흰 그림 한 장을 한 번만 굽고 `tint`로 색만 갈아
  끼운다(`src/ui/effectTextures.ts`) — 캔버스 그림자(`shadowBlur`)로 빛을 번지게 하거나 도형을
  매 프레임 다시 그리지 않는다. ② 종류마다 emitter 하나를 재사용하고 파문·수치 글자는 풀에서
  꺼내 쓴다. ③ 한 프레임에 여는 수와 살아 있는 파문·글자 수에 상한을 두고 넘치면 **조용히
  버린다**(`EFFECT_BUDGET`) — 놓친 한 방보다 끊긴 프레임이 훨씬 크게 보인다.
- **누른 자리는 금색이 아니라 푸른빛이다**(`EFFECT_TAP_COLOR`). 금색은 재화·보상·강조를 뜻하므로
  아무 데나 눌러도 뜨는 반응에 같은 색을 쓰면 "눌렀다"와 "받았다"가 섞여 읽힌다.
- **인게임과 메뉴는 결이 다르다.** 메뉴의 조작은 근미래 홀로그램 장비를 누른 손맛(얇은 마름모
  파문 한 겹)이고, 전장의 조작은 단순한 SD가 뚜따시하는 화면에 맞춘 작은 조각 셋이다. 씬마다
  붙이면 화면이 늘 때마다 빠뜨리므로 모든 씬 위에 늘 떠 있는 `EffectOverlayScene` 한 겹이
  포인터를 듣는다.
- 값은 전부 `src/ui/effectPresets.ts` 한 표에만 있다. 새 이펙트가 필요하면 씬에서 파티클을
  만들지 말고 이 표에 종류를 더한다.

### 피해 수치

색·크기·머무는 시간·화면 흔들림·진하기는 `src/ui/damageNumbers.ts`의 순수 규칙이 정하고 씬은
사건의 성격만 넘긴다.

- **화면을 가리지 않는다.** 글자는 작고(잔타 22px~한 방 52px) 반투명하며, 캐릭터 위에 머무는
  동안 가장 옅고(`nearAlpha`) 위로 멀리 떠오르며 또렷해졌다 사라진다(`risingAlpha`). 떠오르는
  거리는 SD 키를 넘긴다 — **예쁜 캐릭터를 가리는 순간 수치는 정보가 아니라 방해다.**
- **세기는 크기로 말한다.** 등급은 절대 수치가 아니라 **대상 최대 체력 대비 비율**로 매긴다 —
  절대값으로 가르면 레벨이 오를수록 모든 타격이 최대 등급에 붙어 크기가 뜻을 잃는다.
- **누가 맞았나를 색이 먼저 말한다.** 아군이 받은 피해는 물리·마법·고정·치명타를 가리지 않고
  **붉은 계열 하나로** 묶이며, 세기에 따라 옅은 살구빛에서 진한 다홍으로 짙어진다
  (`INCOMING_DAMAGE_TONE`). 적에게 입힌 피해만 종류별 색을 갖는다 — 보통 피해 흰색, 고정 피해
  보랏빛, 치명타 노란색, 궁극기 강조색이다. 회복(연두)·보호막(푸른빛)·디버프는 **받는 쪽에서도
  제 색을 지킨다** — 그 셋은 "맞았다"가 아니라 다른 사건이기 때문이다.
- **경감은 종류를 가리지 않고 회색 하나다.** "덜 들어갔다"는 무엇으로 맞았는지보다 먼저 읽혀야
  하므로 색이 갈리면 오히려 흐려진다. 색 우선순위는 **경감 → 아군 피격 → 치명타 → 궁극기 →
  종류** 순이다.
- **디버프 색은 `DEBUFF_TONE` 한 표에만 있다.** 출혈은 다크체리, 중독은 어두운 보랏빛이며
  전부 어둡게 눌러 둔다 — 지속 피해는 잔타로 자주 뜨므로 밝게 두면 큰 한 방과 같은 무게로
  읽힌다. **새 디버프는 그 표와 `DebuffId`에만 더하고** 화면이 상태마다 색을 새로 고르지 않는다.
- **속성 상성은 표식으로 붙이지 않는다.** 유리하면 숫자 자체가 커지므로 화살표는 같은 말을 두 번
  하면서 글자 폭만 넓힌다. (▲·▼를 한 번 붙였다가 이 이유로 걷어 냈다.)
- **치명타·궁극기는 한 등급 크고, 경감된 타격은 한 등급 작다.** 무효는 세기와 무관하게 늘 같은
  흐린 표식 하나다.
- **큰 한 방만 화면을 흔든다.** 잔타까지 흔들면 난전 내내 화면이 떨리고, 아군이 맞을 때
  흔들면 조작이 어긋난다.
- **씬이 `def`를 다시 읽어 색을 되짚지 않는다.** 코어의 `attack` 사건이 `damageType`·
  `mitigated`를 함께 싣는다. 새 피해 경로를 만들면 그 사건에도 채운다.
- **흡수는 숫자를 띄우지 않는다.** 같은 타격의 피해 숫자와 겹쳐 두 번 읽히기 때문이다. 새로
  덮인 보호막만 얻은 양을 알린다.

## 야성은 메리트다

야성(`ferocity`)은 벌이 아니라 상이다. 게이지가 차면 피해가 오르고, 그 상태에서도 궁극기를
쓸 수 있다. 유대가 높을수록 **더 빨리** 찬다(`amplifyFerocityGain`). 관제탑인 주인공은
최대치에 닿으면 **폭주 피버**가 시작되고 게이지는 시간에 따라 스스로 0까지 줄어든다. 별도 진압
조작은 없으며, 피버 중에도 궁극기를 쓰고 적만 공격한다.

- 궁극기 연출은 **전신 컷인 한 장 + 짧은 타격**이다. 컷인이 옆에서 들어와 이름을 잠깐 보여
  주고 빠지면, 전장의 SD가 한 뼘 커졌다가 세게 치고 곧바로 전투가 이어진다. 포효를 따로
  기다리지 않는다 — 전투 중 여러 번 반복되는 연출이라 길이가 곧 기다림이다. 방향·배율·머무는
  시간·흔들림은 `src/data/ultimatePresentations.ts` 한 표에만 있고 씬은 읽기만 한다.
- **연출은 전투 배속을 그대로 받지 않는다.** 진행은 배속만큼 빨라지지만 컷인까지 같은 배율로
  당기면 눈에 남지 않고 지나간다. 몫은 `ultimatePresentationSpeed`(배속의 20%) 하나가 정하고
  컷인·발돋움·결정타가 모두 그 값을 쓴다. 컷인 표시 여부는 환경설정의 `ultimateCutIn`이며
  전투 화면의 연출 칩이 같은 값을 켜고 끈다.
- **전투 조작 칩(배속·자동 궁극기·연출)은 우하단 프로필 줄 바로 위에 모인다.** 전장을 가리지
  않으면서 엄지가 닿는 자리이고, 궁극기와 관련된 두 조작(자동·연출)은 세로로 붙여 한 덩어리로
  읽힌다. 자리는 `BATTLE_CONTROLS` 한 곳이 정한다.
- 개체별 발현은 `RelicDef.ferocityTrait`에 두고 정보창은 패시브 아이콘 위 작은 뱃지로만
  알린다. 전투 공식은 여전히 공용 야성 규칙 하나다.
- **전투 중 프로필에는 게이지가 둘뿐이다** — 체력과 폭주. 궁극기 충전은 바가 아니라 **카드
  그림 자체**가 말한다: 아직이면 카드가 반투명하고, 12시부터 시계 방향으로 어둠이 걷히다가
  다 차면 그림이 온전히 선다. 가림막은 카드 **몸통**(윗변이 닫힌 칩) 마스크만 쓴다
  (`PortraitCard.createPortraitAlphaOverlay`) — 카드를 통째로 BitmapMask로 쓰면 카드 안의 기하 마스크와
  겹쳐 가림막이 통째로 사라진다. 몸통 실루엣은 그림이 채워진 자리와 같아 머리가 빠져나온
  투명한 자리를 검게 칠하지도 않는다.
- **체력은 능력치 색과 같은 연두, 야성은 붉은 계열이다.** 게이지와 그 옆의 수치가 같은 색을
  쓰므로 어느 수가 무엇인지 색으로 먼저 읽힌다. 야성은 차오를수록 짙어져 피버에서 가장 붉다.
- **머리 위 체력 바는 `src/ui/UnitHealthBar.ts` 하나가 그린다.** 배경 원화가 밝고 복잡해서
  사각형 두 장으로는 묻힌다 — 아래로 한 겹 복제해 깔고, 몸통을 `/`로 깎고, 흰 선으로 칸을
  나누고, 왼쪽 끝에 두꺼운 빗금을 하나 세운다. 네 겹이 함께 있어야 배경에서 떨어져 나온다.
  끝 빗금은 몸통보다 **크고 훨씬 짙다** — 같은 색·같은 높이면 체력 바가 거기서 잘린 것처럼 보인다.
- **게이지는 최대치를 흰 선으로 두르고 빗금으로 칸을 나눈다**(`HoloBar`의 `outline`·`ticks`,
  `UnitHealthBar`). 밝은 배경 원화 위에서든 반투명 카드 면 위에서든 채움만으로는 "어디까지가
  이 게이지인가"가 보이지 않는다. **읽어야 하는 진행도**(전투 체력·야성, 임무 달성도, 연구도)는
  빈 자리를 짙은 검정(`trackAlpha` 0.8 이상)으로 눌러 두고 외곽을 흰 선으로 두른다. 설정
  슬라이더처럼 조작 자체가 값을 말하는 줄만 켜지 않는다.
- **게이지와 수치는 스르륵 따라온다**(`stepMeters`). 깎이는 순간이 보이지 않으면 얼마나
  아팠는지 알 수 없다. 궁극기 연출 중에도 계속 따라붙어야 연출이 끝난 순간 값이 점프하지 않는다.
- 전투 프로필의 수치는 **제 게이지와 같은 색으로 굵게**, 아래로 한 겹 복제한 그림자를 달고
  적는다. 흐린 회색 글자는 게이지 바로 옆에 있어도 배경 원화 위에서 읽히지 않는다.
- 피격 섬광은 아주 옅게만 물들인다. 진하면 난전에서 여섯이 번갈아 빨개져 화면이 깜빡이고
  정작 봐야 할 체력 바와 피해 숫자가 뒤로 밀린다.
- **폭주는 빛이 아니라 필터다.** SD가 한 뼘 커지고 **일러스트 자체에 스킬 아이콘과 같은
  속성·직군 색**이 입혀지며, 섞는 비율이 옅은 쪽과 짙은 쪽을 오가며 울그락불그락 끓는다.
  **주위에 발광을 두르지 않는다** — 겹쳐 두면 밝은 배경 원화 위에서 하얗게 떠 정작 봐야 할
  캐릭터가 그 속에 묻힌다(그래서 v0.36.0에서 걷어 냈다). 색은 **단계로 끊어** 바뀐 프레임에만
  칠한다 — 매 프레임 칠하면 Puppet 조각 전부에 tint를 다시 먹여 그만큼이 프레임 비용이 되고,
  피격 섬광이 도는 동안에는 아예 칠하지 않는다(`isHitFlashing`).
- 상태이상은 숫자가 아니라 표식으로 알린다. 출혈(`BLEED`)처럼 전투 규칙이 있는 것은
  `src/core/skirmish.ts`가 계산하고 씬은 뱃지와 피해 숫자만 그린다.

## 글꼴 규칙

글꼴 파일·가족 이름·굵기 숫자는 `src/ui/fonts.ts`에만 있다. 씬·프리팹은 굵기를 고르지 않고
**글의 역할**만 고른다. 굵기를 눈대중으로 고르면 같은 위계의 글이 화면마다 다른 무게로 보인다.

| 역할 | 글꼴 | 쓰는 곳 |
| --- | --- | --- |
| `display` | NEXON Kart **ExtraBold** (800) | 화면 제목, 캐릭터·스테이지·스킬 이름, 화자 이름, 결과 문구, 버튼 라벨, 전투 피해 수치 |
| `emphasis` | NEXON Kart Gothic **Bold** (700) | 설명 속에서 튀어야 하는 소제목·라벨·태그·재화 수치, 하단 탭, 강조된 한 문장 |
| `body` | NEXON Kart Gothic **Medium** (500) | 스킬 설명, 대사 본문, 아카이브, 안내 문구, 통계 나열 — 문장으로 읽는 모든 글 |

- 텍스트는 반드시 `textStyle({ role, size, ... })`로 만든다. `role`은 선택이 아니라 필수다.
  기본값을 두지 않는 이유는, 기본값이 있으면 아무도 고르지 않기 때문이다.
- 애매하면 **문장이면 `body`**다. `display`는 "이름표"에만 쓴다. 한 화면에서 `display`가 여러
  개 보이면 위계가 무너진 것이다.
- `fontFamily`, `fontStyle`, `font-weight`, `font-family`를 `fonts.ts`/`theme.ts` 밖에서 직접
  쓰지 않는다. 굵기가 필요하면 역할을 고르고, 역할이 모자라면 역할을 늘리는 게 아니라 먼저
  기존 셋으로 표현되는지 검토한다 — 파일이 세 개뿐이라 다른 굵기는 브라우저가 가짜로 흉내 내
  한글 획이 뭉갠다. 예외는 캔버스 바깥의 회전 안내뿐이라 `src/style.css`의 `body` 한 곳에서만
  같은 가족·미디움을 지정한다.
- 글꼴 파일은 `public/fonts`에 두고 `FONT_FACES`에 등록한다. CSS에 `@font-face`를 따로 적지
  않는다 — 부트에서 `loadGameFonts()`가 그 목록으로 등록한다. `index.html`의 preload 줄도
  같은 목록이어야 한다.
- 글꼴은 타이틀 로딩의 첫 단계(`loadGameFonts()`)다. 씬에서 따로 부르지 않는다.
- 위 규칙은 `tests/unit/fonts.test.ts`가 강제한다(역할 누락, 직접 굵기 지정, 파일·preload
  불일치). 규칙을 바꾸려면 이 문서와 테스트를 함께 고친다.

## 애니메이션 주의사항

- Puppet ZIP은 `BootScene`에서 미리 파싱하며 `src/puppets/assets.ts`의 캐시를 우회하지 않는다.
- `puppetforge`는 GitHub 저장소(`github:Bell7080/WebGLE#<커밋 SHA>`)에서 직접 받는다. 반드시
  `Bell7080/WebGLE`의 `main`에 실제로 남아 있는 전체 SHA로 고정한다. 병합 후 삭제된 작업
  브랜치의 커밋을 가리키면 그 커밋이 사라져 `npm install` 자체가 실패한다.
- 현재 고정 커밋은 `b5af06a`(v0.41.0)이며 `pinnedSoft` 반경 감쇠가 게임 Runtime에도 들어 있다.
  범위 없는 GitHub 의존성으로 되돌리면 lockfile이 예전 Runtime을 계속 가리켜 발 고정 영역이
  통째로 딱딱하게 붙고 외곽 정점이 찌그러질 수 있으므로, 버전을 바꿀 때 ZIP과 Runtime을 함께
  회귀 검증한다.
- 최신 PuppetForge를 반영하려면 `main`의 최신 커밋 SHA로 `package.json`을 바꾸고
  `npm install`로 lockfile을 갱신한다. SHA를 그대로 두면 npm은 절대 새 커밋을 가져오지 않는다.
- **Puppet은 자기 화면 좌표로 직접 그린다.** 컨테이너의 이동·배율·마스크를 물려받지 않으므로
  팝업 본문처럼 옮겨진 컨테이너 안에 넣으면 국소 좌표 그대로 화면 바깥에 그려져 아무것도 보이지
  않는다. 팝업 위에 SD를 세울 때는 원점(0,0)에 선 전용 레이어에 담고 좌표도 화면 좌표로 넘긴다.
- 일회성 동작은 반드시 `playMotion`을 사용한다. 직접 `delayedCall`로 idle 복귀를 만들면 이전
  타이머가 최신 피격·공격 모션을 잘라 재생 끊김처럼 보일 수 있다.
- `public/puppets`에는 전신 3개, 아군 SD 3개, 적 SD 3개가 있다. 도도·스밀라·케찰만 토리카
  전신/SD와 `tints.ts`를 placeholder로 공유하며, 전용 에셋이 연결된 나머지 여섯 개체에는 tint를
  적용하지 않는다.

## 버전과 개발 로그

- 변경 이력은 `VERSION.md`에만 쌓는다. 이 문서(`CLAUDE.md`)에는 **지금 지켜야 하는 규칙**만
  남기고 완료 보고를 적지 않는다.
- 버전 문자열의 단일 출처는 `package.json`이며 `VERSION.md`와 항상 같은 값을 쓴다.
  타이틀(로딩) 화면 좌측 하단 표기는 그 값을 읽기만 한다 — 화면에 손으로 적지 않는다.
- 푸시할 때마다 `VERSION.md`에 그 버전 항목을 남기고, 보고에도 올라간 버전을 명시한다.
  번호 규칙(주.부.패치)과 기록 규칙은 `VERSION.md` 머리에 있다.
- **`v1.0.0`은 정식 런칭 번호다.** 출시 전에는 주 자리를 올리지 않는다 — 저장 데이터
  비호환 변경도 부 자리를 올리고 마이그레이션 방법을 그 항목에 함께 적는다.

## 변경 전 확인

GitHub Actions는 두지 않는다 — 배포는 Vercel이 맡고 품질 게이트는 로컬에서 돌린다. 커밋 전에
`npm run typecheck`, `npm test`, `npm run build`를 반드시 실행한다. 이 셋은 몇 초면 끝나므로
언제나 돌린다.

**E2E는 기본으로 돌리지 않는다.** Playwright 한 편이 브라우저를 띄우고 타이틀 로딩과 씬 전환을
실제로 기다리므로 한 번에 1~2분이 든다. 그래서 필요할 때, 필요한 만큼만 돌린다.

- 순수 배치표·규칙·데이터만 바꿨다면 Vitest로 충분하다. E2E를 돌리지 않는다.
- 화면 배치를 바꿔 눈으로 봐야 한다면 **그 화면을 여는 한 편만** `-g "<테스트 이름>"`과
  `--project=pixel-7`로 지정해 돌린다. 전체 스펙을 돌려 다른 화면까지 확인하지 않는다.
- **전체 E2E(`npx playwright test`)는 사람에게 먼저 물어보고 돌린다.** 시간이 길어 그동안
  다른 작업이 멈추므로, 필요하다고 판단해도 스스로 시작하지 않는다.
- 실패한 E2E가 이번 변경 때문인지 확실하지 않으면, 같은 한 편을 변경 전 상태에서 한 번만 더
  돌려 비교한다. 원인 없이 여러 번 반복 실행하지 않는다.

E2E는 `__PF_DEBUG.ready`(타이틀 로딩 완료)를 기다린 뒤에 조작한다. 로비 이후를 확인하는
테스트는 `page.goto` 대신 `tests/e2e/openingSave.ts`의 `startAfterOpening`으로 연다 —
저장이 없으면 타이틀에서 오프닝 스토리로 들어가기 때문이다. 저장 JSON을 테스트가 손으로
적지 말고 이 헬퍼가 `SaveManager`를 통과시켜 만든 것을 쓴다.

## 입력 및 임시 서버

### 획득 상태의 단일 소유자

- 연구소 획득으로 바뀌는 `owned`, `relicProgress`, 공용 DNA 조각은 `GameApi` 구현(FakeServer 및
  향후 실제 서버)만 한 처리 단위로 확정한다. `core/gacha.ts`는 순수 계산만 하며 상태를 변경하지 않는다.
- `RelicCollectionManager`는 조회·애착·편성만 담당한다. 획득 메서드를 다시 추가해 API 응답을 이중
  반영하지 않는다. 씬은 슬롯별 `PullResponse.results`를 표시만 한다.

- Phaser의 `pointerdown`/`pointerup`은 터치와 마우스를 함께 지원한다. 모바일 우선 입력은
  `pointerup`(브라우저의 `touchend`)에서 확정하되 PC 테스트용 마우스 동작도 제거하지 않는다.
- 씬은 `GameApi`만 참조하고 `FakeServer`의 내부 상태를 전제로 삼지 않는다. 실제 백엔드가 생기면
  `src/api/FakeServer.ts`의 공유 인스턴스를 HTTP 구현으로 바꾸고 DTO 규격을 유지한다.
