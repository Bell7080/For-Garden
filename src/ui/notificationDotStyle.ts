/** 모든 버튼이 공유하는 붉은 점의 픽셀 규격이며 Phaser와 무관한 시각 테스트 기준이다. */
export const NOTIFICATION_DOT_STYLE = {
  radius: 13,
  fill: 0xd92f45,
  outline: 0xffffff,
  outlineWidth: 4,
  shadow: 0x090b10,
  shadowAlpha: 0.82,
  shadowOffsetY: 7,
} as const;
