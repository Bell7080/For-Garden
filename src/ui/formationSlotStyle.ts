/**
 * 편성 칸 밑판의 공용 생김새 값.
 *
 * Phaser 없는 순수 모듈에 두는 이유는 회귀 테스트가 그리기 구현이 아니라 이 값을 읽어야 하기
 * 때문이다 — 그리는 것은 `formationSlotChrome.ts`의 `addFormationSlotPlate` 한 곳이다.
 *
 * 네 화면(스토리·원정·발굴·교류 파견)이 같은 세 자리를 고르므로 칸도 같은 판이어야 한다.
 * 기준은 이미 잘 읽히던 **발굴의 칸**이다 — 반투명 유리면 한 장을 늘 깔고, 누군가 서 있으면
 * 발밑에 얇은 투영 그림자만 두며, 비었으면 자리 번호가 그 자리를 지킨다.
 *
 * 판을 **서 있을 때만 감추지 않는다**. 원정이 그랬는데, 세 자리 중 둘만 차면 남은 한 칸만
 * 판이 보여 빈 자리가 "못 쓰는 자리"처럼 읽혔다. 세 칸이 늘 같은 판 위에 서야 무엇을 더
 * 고를 수 있는지가 한눈에 보인다.
 */
export const FORMATION_SLOT_PLATE = {
  /** 판 윗변 강조선의 진하기. */
  edgeAlpha: 0.55,
  /** 발밑 투영 그림자 — 칸 폭 대비 너비, 높이, 진하기. */
  groundWidthRatio: 0.82,
  groundHeight: 25,
  groundAlpha: 0.16,
  /** 빈 칸에 서는 자리 번호의 크기. */
  emptyFontSize: 22,
} as const;

/**
 * 전투 편성 칸의 **추천 직군 표**. 칸 판의 윗변에 걸터앉는 작은 이름표다(제목표와 같은 자리 문법).
 *
 * 전투 편성(스토리·레이드·현상수배·대작전·원정)에만 선다 — 발굴·교류 파견은 직군이 자리를
 * 정하지 않으므로 넘기지 않는다. 판 **안**에 두면 SD의 발이나 몸에 가리고, 판 밑에 두면
 * 이름줄과 겹친다. 윗변에 걸치면 칸 판에 딸린 것으로 읽히면서 SD의 머리 위 빈자리를 쓴다.
 * 층은 SD보다 앞이다(`recommendedRolesLayer`) — 판과 같은 층이면 머리가 솟은 원화가 가운데 표를 덮는다.
 */
export const FORMATION_ROLE_HINT = {
  height: 38,
  /**
   * 판 윗변에서 아래로 얼마나 내려 앉는가. 스토리 편성은 판 바로 위에 두 편의 전투력 판이
   * 걸쳐 있어(`PARTY_POWER_PLATE`), 가운데에 걸치면 그 판과 맞닿는다 — 한 뼘 내려 틈을 둔다.
   */
  offsetY: 5,
  iconSize: 28,
  fontSize: 21,
  /** 아이콘끼리, 아이콘과 글자 사이. */
  gap: 5,
  /** 좌우 안쪽 여백. */
  pad: 12,
  /** 판의 진하기. 반투명 유리 위에서도 글자가 읽히도록 칸 판보다 짙다. */
  alpha: 0.82,
} as const;
